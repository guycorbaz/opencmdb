# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project status

`opencmdb` is a self-hosted, single-binary **Rust** network reconciliation engine (IPAM + light app CMDB + topology) for home-lab/SMB. Core thesis: continuously compare the **observed** state (auto-discovered) against the **declared** state (documented); the gap is the product. **Planning is COMPLETE** (product brief, PRD, UX spec, architecture — all in `_bmad-output/planning-artifacts/`, decision register D1–D66). **As of 2026-07-22 the code RUNS**: `v0.1.1` is tagged and published to Docker Hub as `gcorbaz/opencmdb`, and it scans a CIDR and shows a real observed-vs-declared gap on one page. **Epics 1–4 of 23 are done** (the walking skeleton shipped as v0.1). **Epic 4 closed on 2026-07-25**: the fixture corpus, the metrics harness and the trap corpus — all written BEFORE the identity engine — are committed and locked (25 artefacts, 24 traps across nine families, plus the wire-format spec — **those are Epic 4's closing figures and they are HISTORY**; the living numbers are **28 artefacts, 26 traps across TEN families** since story 5.13b added the blinded-source pair). Its story 4.19 was SPLIT at closure: 4.19a shipped, **4.19b (the mutation generator) moved to Epic 11** — recorded in `epic-4-correct-course-2026-07-25.md` and GitHub issue #34, not silently. **Epic 5 — the identity engine — is IN PROGRESS since 2026-07-27** (**20 stories**, **19 done** _(5.14b INSERTED 2026-08-11 at 5.14's VALIDATION)_ _(5.13b INSERTED 2026-08-10 at story 5.13's contexting)_: 5.11b was INSERTED 2026-08-05 at story 5.11's contexting, splitting the seeded arrival-order fuzz — a MEASUREMENT of a property that already holds by construction, `join` returning a `BTreeMap` and the witness being a `min` over a `BTreeSet` — from the idempotence, which is new production code; 5.9b was INSERTED 2026-08-03 at story 5.9's contexting, splitting the schema from the resolver that fills it; 5.4b was INSERTED 2026-07-29 at story 5.4's contexting, splitting the verdict algebra — a function that must be TOTAL over a table D13 leaves one input class short — and its anti-float `xtask` gate out of 5.4). Its **three inherited debt stories come first and are all `done`** — 5.1, 5.2 and 5.2b, PRs #41, #44 and #46, all merged 2026-07-28. The engine proper starts at **5.3, `done`** — PR #48, merged 2026-07-29 — which ships the identity engine's own abstention vocabulary (`IdentityAbstentionCause`, in the new `crates/opencmdb-core/src/identity/`) and **no engine**: no cascade, no rule, no `Decision`, no join. **5.4 follows and is `done`** — PR #52, squash-merged 2026-07-29 as `da87b62`: it adds the engine's RETURN type in the same file — `Verdict` (D13's five verdicts), `RuleVerdict { rule, verdict, evidence }`, `RulesetVersion`, `Conclusion` and `Decision` — and still **no algebra**: nothing combines a verdict set, no rule produces a verdict, nothing produces a decision. **5.4b follows and is `done`** — PR #55 squash-merged 2026-07-30 as `4f4e774` after a green CI run, code-reviewed first (281 tests): it ships the ALGEBRA — `decide(Vec<RuleVerdict>, RulesetVersion) -> Decision`, a TOTAL pure function over D13's six rows plus the one input class the table leaves uncovered (`≥1 Opposes` alone → `Abstained { AbsenceOfProof }`, Guy's arbitration; the D13 correction itself is **GitHub issue #54**, a milestone act) — and a **sixth `cargo xtask ci` gate, `float-free`**, which reds on a float type or a float literal in code under `identity/`, comments stripped so the architecture may be quoted. The review applied 22 patches and registered 5 deferrals: `decide` now matches on `Option<RuleId>` rather than booleans, so the presence test and the rule selection are one act and no unreachable arm has to be justified; and the gate's matcher is a **numeric-literal tokeniser** rather than a digit-dot-digit search, because the search form was measured to pass `1e-3` and `1.` (both `f64`) while reddening `"192.168.0.1"`, `t.0.1` and any identifier containing `f64` — a shape story 5.5 would have tripped on its first MAC/IP rule — although in the event **5.5 wrote no IP literal at all**, and that mis-prediction was itself corrected as a finding. **5.5 follows and is `done`** — PR #57 squash-merged 2026-07-31 as `0ebd50f` after a green CI run, code-reviewed first (three layers): it ships `crates/opencmdb-core/src/identity/l1.rs` — the L1 join `join(&[Observation]) -> BTreeMap<(L2DomainId, MacAddr), BTreeSet<ObsId>>` (the value is a `BTreeSet` so order-independence holds by CONSTRUCTION, not by a `sort()` a refactor can drop), the two rules the committed corpus names (`l1-exact-mac -> Decisive` on **at least one shared key**, `l1-distinct-mac -> Disqualifying` when they share none, `Neutral` when either side carries no MAC), and `decide_pair` — **the first caller of `decide` outside its own tests** — with `CURRENT_RULESET_VERSION = RulesetVersion(1)` and still **no `Default`**. **281 → 309 tests.** Its AC3 was discharged as a NEGATIVE requirement: D13 calls the U/L bit and the IANA prefixes `Disqualifying` *"as GROUPING anchors"* and grouping is L2, so **no IANA predicate was added** and two tests assert `l1-exact-mac` still fires on a locally-administered MAC and on a VRRP MAC — implementing D13's label at L1 would have reddened two committed traps. Prove-to-red: six mutations, **every red assertion-carried, none compiler-carried**. **5.6 follows and is `done`** — PR #59 squash-merged 2026-08-01 as `020c706` after a green CI run, code-reviewed first (three layers): `crates/opencmdb-core/src/identity/blocking.rs` ships `CandidatePair` (private fields ordered by its constructor, `new(a, a) -> None` — which closes the register's self-pair entry **in the type**), `candidates(&[Observation]) -> BTreeSet<CandidatePair>` — **TOTAL by decision**, every unordered pair of distinct `obs_id`s, calling neither `join` nor `decide_pair` because a blocker that consults a rule is that rule's echo — and the floor. **The float was resolved by TYPING it, not avoiding it**: D13's `blocking_recall >= 0.999` ships as `BLOCKING_RECALL_FLOOR_PER_MILLE: u32 = 999` on D13's own milli-units corollary, and the `float-free` gate was **not** weakened — it walks **4** files under `identity/` where it walked 3. The corpus assertion lives in `opencmdb-bin`'s `fixtures.rs` test module because D47 forbids the domain crate to read files: **24 traps, 23 pairs, 10 `must-merge`, recall 1000‰, all asserted rather than quoted**. `blocking_recall_above_999` computes only the recall and the per-trap containment is a **separate** test — measured to be load-bearing: in one test a missing pair panics before any recall exists. **309 → 332 tests**, and **333** after the code review. Six mutations: **M1 (blocking on an L1 key) scores 700‰ exactly**, and **M2 (blocking on `l2_domain`) leaves the entire corpus GREEN** — only a synthetic cross-domain test reds it, which is why that test was written first. Zero compiler-carried reds. **At 5.6's close** nothing fed the corpus harness, the blocker had **no production caller**, and `Verdict::Supports`/`Opposes` had **no producer** (Epic 6) — story 5.7 changed the FIRST of those three and only the first. _(This sentence stood in the present tense until 5.7's code review on 2026-08-02, where it was falsified 400 words below by this same file; `docs/project-context.md` had corrected the identical sentence in 5.7's own commit and this one was missed.)_ **5.6 has now been code-reviewed** (three layers, 2026-08-01): 9 patches applied, 2 deferrals registered, 11 findings dismissed, **332 → 333 tests**, six gates green. What it caught was again mostly CLAIMS rather than behaviour — 6 of the 9 were sentences — and the HIGH one was a doc asserting that blocking on the MAC, the hostname *or* the `l2_domain` would each pass the whole corpus, **refuted by a measurement inside the same commit**: measured over the ten `must-merge` pairs, a MAC-blocked universe scores **700‰** (the story's own M1), a hostname-blocked one **400‰**, and only `l2_domain` reaches 1000‰. The one behavioural gap: no test fed `candidates` an observation with EMPTY `facts`, so a fact-presence narrowing was measured green across all 332 tests — the fifth consecutive story whose review found a claim outrunning its measurement. **5.7 follows and is `done`** — PR #61 squash-merged 2026-08-02 as `d47631b` after a green CI run, code-reviewed first (contexted and validated by two fresh agents before that — 13 findings applied, and for the THIRD consecutive story all 3 HIGH came from the agent that COMPILED the story, none from the fact-checker). **The seam is crossed and the trap corpus is a gate that RUNS**: `score_corpus` then reported **24 discovered, 13 scored, 0 failures, 0 wrong-rule, passed** _(story 5.8 turned that last word to FALSE — see below)_ where `scored` had read **0** since story 4.6b — nine stories. Two pieces: `score::outcome_of(&Decision) -> Outcome` (exhaustive, no `_` arm; **still no `From` impl** — the refusal was KEPT because a `From` makes the conversion free at every call site, which is the invisibility the refusal was about), and a new `crates/opencmdb-bin/src/l1_runner.rs` that walks the corpus, asks `decide_pair` about the pair each trap names and maps the result. **It is NOT in `trap_gate.rs`**: that file's *"the harness never calls a producer"* is a FILE-level property, and it would have degraded to a per-function promise on the day it first had something to promise about; `score_corpus`'s signature and body were **unchanged by 5.7** and the seam was a `BTreeMap<TrapId, Outcome>`, which is data _(5.8 widened that value to `Answer`)_. The selector is the EXPECTED RULE's LEVEL — a `l1-` **prefix**, never a whitelist — so a trap expecting an unimplemented `l1-*` rule is answered anyway and reds; `scored_in` = 7 / 6 / **0**, and that zero is asserted with its reason (the `must-abstain` column is measured by nothing). **333 → 350 tests.** Six mutations, every red assertion- or panic-carried, zero compiler-carried; **M4 was run on BOTH sides and the difference IS the finding** — dropped from `answer_trap` it panics, dropped from `l1_answers` the **entire suite stays green**, because the level selector removes the corpus's only pairless trap first. 🔴 It corrects a premise story 5.8 was given: **eleven** committed traps are unanswerable at L1, not eight — the three `must-abstain` ones are invisible to an `l2-*` selector because `Expectation::MustAbstain` carries a cause and no rule. `epics.md` was deliberately NOT edited; the correction is registered with 5.8 as owner. The `VerdictVectorEntry` unification is **re-owned, not done** — 11 of 11 trap-named replay streams carry no `capability` control record, so a `ScoredRecord` would mean inventing a snapshot for all 24 traps — and **the blocker still has no production caller**, by decision: a trap NAMES its pair. **5.7 has now been code-reviewed** (three layers, 2026-08-02, commit `6f958dd`): 11 patches applied, 2 deferrals registered, 7 findings dismissed, **350 → 352 tests**, six gates green — and it STAYS at `review`, because `done` is the MERGE's business. AC1–AC10 were re-verified MET by independent measurement, and **6 of the 11 patches were sentences rather than behaviour — the sixth consecutive story with that shape**. The two behavioural ones were both measured INVISIBLE before their tests existed: `answer_trap` had **re-opened the self-pair** story 5.6 closed in the TYPE — a hand-built `Trap { observations: vec![x, x] }` merges on `l1-exact-mac` because an observation shares every key with itself, and `answer_trap` never calls `read_traps`, so `Trap::validate`'s `DuplicateObservation` did not hold the precondition there (Guy's call: close it in `named_pair`, not in a doc sentence); and nothing covered a trap naming THREE observations, where relaxing the pattern to `[a, b, ..]` left all 350 tests green — story 5.6's M2 idiom, on the upper half of the guard AC2 closed for the lower. The HIGH claim was again a doc **falsified inside its own file**: this very file still said *"Nothing feeds the corpus harness (5.7)"* in the present tense 400 words above *"the seam is crossed"*, while `docs/project-context.md` had corrected the identical sentence in 5.7's own commit — one twin updated, the other missed. Also caught: a Debug Log figure of **311** code lines for `trap_gate.rs` where it is **405**, *below* the story's own 384 baseline and therefore impossible; and *"all seven ids"*, quoted in three documents and guarded by `checked > 0` alone, now asserted as `checked == 21` / `distinct == 7` — placed in the committed-corpus test and deliberately **not** in the root-parameterised helper, where it would have reddened M5c on the count and masked the both-occur guard M5c proves load-bearing. Two sub-agent findings were **refuted by re-measurement** and dismissed with it. **5.8 follows and is `done`** — PR #63 squash-merged 2026-08-03 as `bbd3b3a` after a green CI run (56s), code-reviewed first (three layers) — the bucket for the traps L1 cannot answer. 🔴 **It turns the committed gate RED on purpose**: `24 discovered, 13 scored, 0 failures, 0 wrong-rule, **11 unanswerable**, passed = FALSE`, and it stays false until Epic 6 implements `l2-*` — which is what `epics.md:416` always said (*"NFR4 stays RED and is closed by Epic 6"*) and what D18 demands (*"a gate that cannot fall is decoration"*). Three pieces: `score::Answer` (`Answered(Outcome)` | `Unanswerable { cause }`) and `UnanswerableCause` (three variants, the measured classes **8 / 2 / 1**) in `opencmdb-core`; `Report`'s fourth bucket `unanswered: Vec<Unanswered>` with `unanswered_in(column)` and a `passed()` that it blocks; and `l1_answers` made **TOTAL over the corpus** — 24 entries, not 13. **The seam widened in VALUE, not in arity**: `score_corpus` takes `&BTreeMap<TrapId, Answer>`, still no engine, no callback, no closure, so 4.6b's AC1 (*"it must not require an engine to exist"*) survives literally and an EMPTY map is still green — asserted in one line. 🔴 **`Unanswerable` is NOT an abstention, and that is the whole story**: map it to `Outcome::Abstained` and `example-must-abstain` PASSES because nothing was asked — D18's cowardice moved from the engine up to the harness. Mutation M3 measures it. The classification is **PAIR-FIRST** (*cannot be asked* outranks *cannot be routed*), which is what gives 8 / 2 / 1 rather than 8 / 3 / 0; the ANSWERED set is invariant under that order and a test says so. The `NFR4 NOT MET` line is rendered **only while the bucket is non-empty**, so it deletes itself the day Epic 6 empties it rather than depending on someone remembering. **352 → 364 tests**, and **367** after the code review. It also **edits `epics.md:1545`** — the one lifting of this project's verify-only rule, taken because 5.7 registered the 8→11 correction with 5.8 as owner and a false premise left standing in the epic file is the defect six consecutive reviews have caught — and closes the second inherited entry: `l1_answers` now refuses a cross-file duplicate `TrapId`, **tested through `l1_answers` directly**, because `score_corpus` refuses the same corpus for its own reasons and a test written through the harness stays GREEN with the runner's guard deleted. **5.8 has now been code-reviewed** (three layers, 2026-08-03, commits `e4591e6` and `eac17ae`): 19 patches applied, 2 decisions taken by Guy, **364 → 367 tests**, six gates green. 🔑 **Four of the defects violated the story's OWN AC8** — the doc twins that AC exists to keep in step — and two guards were measured to assert NOTHING before their patch: `unaccounted()`'s arithmetic, and the partition assertion, which now READS THE MAP and reds at `23 != 24` under M5b where the literal-oracle form stays green. Same family as M5: an oracle that restates the expectation instead of measuring the code. The two decisions: the `NFR4 NOT MET` line is **ventilated by cause** rather than naming one closer, because Epic 6 takes the bucket 11 → 3 and a single-closer sentence would have gone on naming as its closer the epic that had just shipped; and `TrapError::RuleMalformed` now mirrors `FamilyMalformed`, because a padded rule id was silently mis-routed into the unanswerable bucket and explained there with a false sentence. **5.9 follows and is `done`** — PR #65 squash-merged 2026-08-03 as `57db541` after a green CI run (58s), code-reviewed first — — the first story to add DDL since story 3.2, and the one whose central deliverable is **invisible without a running MariaDB**: `DATABASE_URL` is unset locally, every DB-backed test passes by `return`ing, and the suite reports the same counts either way — so the whole story was built and mutated against a real `mariadb:10.11.11` (host port **13306**; 3306 is held by an unrelated container, a trap the validation caught before it could migrate another project's database). It ships `0002_interface_and_identity_link.sql` — `interface`, `identity_link`, `link_candidate`, and **no `device`, no `entity` supertype, no `state` column** (all Epic 6 / the lifecycle epic, all registered) — plus `InterfaceId`/`LinkId` in `observation/mod.rs` and the adapter in `repo.rs`. **367 → 383 tests.** 🔴 **The story was SPLIT at contexting: 5.9b was INSERTED** (Epic 5 → 17 stories) — 5.9 is the schema and its adapter, 5.9b the resolver that fills it, so **the blocker STILL has no production caller** and `join` still has no cross-crate caller at all. 🔑 **The link's uniqueness key is `(observation_id, link_subject, valid_to)`, and the narrow `(observation_id, valid_to)` the story first prescribed was MEASURED refusing a legitimate write**: `join` is `for observation { for key in keys_of(observation) }`, so a multi-MAC observation sits on N interfaces, `multi-nic` is a committed trap family, and the second link came back `Err(Constraint("unique"))`. Guy widened the key at the validation pass; its abstention half (NULL `interface_id`, and MariaDB holds NULLs distinct) is closed by a **second sentinel** — the nil UUID in `link_subject` — so D21's NULL trap is closed twice by one idiom, `OPEN_END` on the other column. ⚠️ **The sentinel is a WRITTEN column plus a CHECK, not a generated column**: MariaDB 10.11 refuses to INDEX a generated column whose expression coalesces to a string literal (**error 1901**, measured both `STORED` and `VIRTUAL`), which neither the story nor its validation knew. Seven mutations, **every red assertion-carried, zero compiler-carried** — and M3 **first came back GREEN**: dropping the rule-XOR-cause CHECK left all 378 tests passing, because `insert_identity_link` derives the rule and the cause from one `match` and cannot emit an incoherent pair, so the guard is reachable only by going around the adapter. Two raw-SQL inserts now measure it. Its validation found **6 HIGH** (4 from the agent that COMPILED the story — the fifth consecutive story with that split): two of the six prescribed mutations were no-ops or not executable (M4, M5), and AC3's *"exactly one current"* assertion **existed in no prescribed test** (the `expect_err` shape panics before any count). **5.9 has now been code-reviewed** (three layers, 2026-08-03; two ran against their own live `mariadb:10.11.11` and the Auditor re-executed the whole mutation pass): **1 decision, 22 patches, 13 deferrals, 2 dismissed**, taking the workspace to **383 tests** with five NEW mutations (M8–M12), all assertion-carried. 🔴 **Guy's uniqueness key was arbitrated a SECOND time, on a measurement that falsified the first**: `(observation_id, link_subject, valid_to)` contains `valid_to`, which is `NOT NULL` on CLOSED rows too, so the key constrained HISTORY — two versions of one placement closed at the same DERIVED instant collided (`ERROR 1062`) and the second close was refused **while the link silently stayed current**. Since every instant here is data-derived and never the clock, a replay reproduces instants, so that is story 5.10's purge-and-replay, not an exotic path. The key is now `(observation_id, current_subject)`, where `current_subject` is the interface (or D21's `NIL_INTERFACE`) **while current and NULL once superseded** — NULL-distinctness used DELIBERATELY to drop closed rows out of the key, where D21 warns against suffering it accidentally. 🔑 **`architecture.md:1468` had already prescribed that sentinel** — *"Same reasoning for `NIL_INTERFACE`/`NIL_DEVICE`"* — which decision 9 re-derived over thirty lines instead of citing. Also caught: `ON DELETE CASCADE` was missing, so **story 5.10's purge failed `ERROR 1451`** the moment an engine link carried a candidate — the ambiguity case the table exists for; `close_identity_link` had no guard, so closing an unknown id returned `Ok(())`, a re-close rewrote history and closing AT the sentinel **resurrected a superseded link as current**; an `interface` minted with the nil UUID stole an abstention's slot; and **four more DDL guards plus three persisted tokens (`OPERATOR`, `no_match`, `absence_of_proof`) were measured droppable with the whole suite green** — the M3 family again, four more times. 🔑 **The guard the second arbitration exists to install had no recorded red at all**: the multi-NIC test was reddened by none of M1–M7, and M8 is that missing mutation. And **four of the review's patches were false numbers or sentences in the story's own documents**, including *"11 done"* in both twins while the same files said 5.9 was at `review` — a **direct violation of the story's own AC10**, committed in the very commit that claims to enforce it. **5.9b is `review`** (not `done` — that is the MERGE's business) — the resolver, and the first production caller of `identity::blocking::candidates` as well as the first cross-crate caller of `identity::l1::join`, in the new `crates/opencmdb-bin/src/resolver.rs`. **`join` NAMES the interface** (at L1 its map IS the set of interfaces), **the blocker and `decide_pair` JUSTIFY each placement**, and D13's order is the order of the pass. `resolve` delegates to `resolve_within(conn, observations, &universe)`, and that seam is not decoration: without it the universe check is unreachable and dropping it was measured leaving the **entire suite green**, because `candidates` is TOTAL. **383 → 408 tests.** 🔴 `epics.md`'s AC1 — *"each observation carrying a MAC lands on exactly ONE interface"* — is **falsified by the code it describes**: `join` loops `for key in keys_of(observation)`, so a two-MAC observation lands on two; Guy widened it at contexting to *one interface per L1 KEY*, `epics.md` was NOT edited, and the correction is registered with Epic 5's retrospective. 🔑 **`identity::l1::decide_singleton` is the one change to the engine**: an observation alone on its key has no pair, `insert_identity_link` requires a `Decision`, and both alternatives were worse — a struct literal with an empty `verdict_vector` is the *"merged, with no explanation"* shape D13 forbids, and composing an L1 verdict in `opencmdb-bin` is what `verdict_for_pair`'s `pub(crate)` exists to prevent. **No struct-literal `Decision` was needed anywhere**, so the register's *"first story that reconstructs a `Decision` outside `decide`"* clause is ANSWERED and still UNMET. `0003_resolver_guards.sql` installs the three guards the first writer owes; its foreign key reds **in two waves — 12 tests in `repo.rs`, then 2 more (one in `main.rs`) that are invisible until the twelve are fixed**, because a failing test rolls back and leaves no link for the cleanup to trip over. **All 14 mutations ran against a live `mariadb:10.11.11` and every red assertion- or panic-carried, zero compiler-carried**; M2 turns the connected-components refutation into a measurement (`left: 1, right: 2`), M7 reds on `last_seen_at` rather than `first_seen_at`, and M4 on the evidence rather than the rule **5.9b is `done`** (PR #67 squash-merged 2026-08-04 as `94314e9` after a green CI run, code-reviewed first) (three layers, 2026-08-04; two against their own live `mariadb:10.11.11`, the Auditor re-executing the whole mutation pass): **2 arbitrations by Guy, 23 patches, 5 deferrals**, taking the workspace to **408 tests** and the mutation set to **18**. 🔴 **It refuted a headline in five documents, the commit subject included** — *"every red is ASSERTION-carried"* is FALSE for M6b (1 assertion, 3 `.expect()` panics), and the story's own T7 bullet had recorded the truth one section above; the driver read the carrier off the whole output, so a MIXED set collapsed to one label. 🔴 **Four behavioural defects, three found separately by two layers**: `placement_decision` tested containment on ONE witness while its sibling filtered correctly twelve lines below — universe missing only `(1,2)` and observations 1 AND 2 abstained; a multi-key abstention collided on `ABSTAINED_SUBJECT` and rolled the whole pass back; `abstention_for` had **no observable effect** (its body replaced by `nothing_was_evaluated()` left every test green, a `Neutral` verdict's evidence being empty and the vector unpersisted); and a repeated MAC-less `obs_id` wrote two colliding links. 🔑 **Guy's arbitrations**: an observation abstains **at most once** whatever the key count — an abstention row names no key, so the two rows would be identical but for their id — and the smallest-other-`ObsId` witness convention is kept and now **measured on a group of three**, every earlier test having used a group of two where the two formulas coincide. Also caught: the reference-scale pass and its wall-clock existed in no test (now **44 850 pairs, 300 interfaces, ~73 ms**), AC7's *"shown to bite"* had no record (the gate now names `0003…sql:48`), §9 did not apply its own *"a divergence is a FINDING"* rule to its own three divergences, and a doc cited a test that had been invented. **5.10 is `done`** — PR #69 squash-merged 2026-08-05 as `e056b5d` after a green CI run, code-reviewed first — D14's own purge test, and the story whose deliverable is a TEST: purge the engine's links, re-run the pass, and every decision-bearing column comes back identical. `repo::purge_engine_links` (a **`DELETE`** — MariaDB's `TRUNCATE` takes no `WHERE`, measured at the parser, though D14 and `epics.md` both write it that way) and `repo::snapshot_links`, which has **no `id` field at all**: a v7 UUID embeds a wall-clock millisecond, so a replayed link cannot carry the same one, and Guy arbitrated that *"bit for bit"* means every column that CARRIES THE DECISION and not the row identifier. **`interface_id` IS compared**, which is what makes the exclusion safe — and mutation **M6 proves it load-bearing**: put the id back and the comparison reds at once. **408 → 416 tests.** 🔴 **`epics.md:1634` asks for something a purge-and-replay can never do, and that is now MEASURED rather than argued**: mutations M1 and M1-noop are the same mutation up to what they key on — the one keyed on whether the interface was FOUND reds; the one keyed on an EXISTING LINK reds **nothing**, because the purge restores the state run 1 started from. What the test really proves is that the engine's output depends only on the observations and on the interfaces, never on its own prior links — D14's *"cache of attention, not of truth"* made operational. 🔑 **An `ORDER BY` inside a function used on BOTH sides of a comparison is unreddenable**, so the snapshot's order has a test of its own; and the snapshot is restricted to CURRENT links, because `current_subject` is NULL on a superseded row and two versions of one placement would carry equal sort keys. 🔴 **The two natures are MUTUALLY EXCLUSIVE on one placement**: an operator can never confirm a placement the engine holds (`Err(Constraint("unique"))`) and an operator row in the slot makes the whole replay roll back — so AC5 names a subject the pass does not place, and *"may an operator override the engine?"* is registered with story 5.14. **5.11 is `done`** — PR #71 squash-merged 2026-08-05 as `ce0a770` after a green CI run, code-reviewed first — the pass is now IDEMPOTENT: `write_link` reads the current ENGINE version of the slot it is about to fill and either inserts, returns without writing, or closes and appends. `0004` relaxes `identity_link_interval` for CLOSED rows only, because every engine supersede is zero-length when the caller supplies stable instants — both versions of one placement carry the same observation's `observed_at`, so closing the old at the new one's `valid_from` closes it at its own (`ERROR 4025` under `0002`). 🔴 **The code review found that this story had turned a LOUD refusal into a SILENT orphan**: `write_link` only ever reads the slot it is about to FILL, so a key that vanished from the input left a current link standing — the `multi-nic` shape — and that orphan is a reachable counterexample to story 5.10's purge-and-replay invariant, through pure engine input. The uniqueness key had been doing the detection; the compare routes around it. Guy's arbitration: **`resolve_within` now closes every current engine slot the pass did not visit** (`links_vacated`). 🔴 Second arbitration: an `observed_at` that runs BACKWARDS was fatal to the WHOLE batch on one branch and silent on the other, so it is now refused by name — `RepositoryError::InstantRegressed`, a guard above the DDL, and **the first production caller in this codebase to compare an instant it HOLDS against one it STORED**, which DISCHARGES the `datetime_literal` debt the register carried for three stories rather than closing it as unreachable. 🔑 Two review mutations came back GREEN and each became a criterion: five of the six comparison columns are unreddenable through the database (`interface_id` structurally and forever, the other four by L1), and this story silently retired story 5.9b's abstention-dedup guard. **416 → 429 tests.** The pass is still deliberately **not wired into `main.rs`**. **5.11b is `review`** (not `done` — that is the MERGE's business) — the arrival-order measurement, and the story designed against ONE failure mode: a test that cannot fail. Three shapes: **A** pure and exhaustive (`join`/`candidates` over all **720** permutations, run twice — once over a synthetic slice carrying the four shapes the corpus cannot produce, once over `hostname-absence.jsonl`, the largest committed stream); **B** purge-and-replay permuted (12 sampled, `interface_id` literally comparable because the replay FINDS its interfaces); **C** the permuted slice into a POPULATED store as a NO-OP (12 sampled) plus a reference-scale seeded sweep (300 observations over 100 MACs, 8 fixed seeds, 692 ms). **429 → 446 tests.** One production change: `resolve_within` REFUSES a repeated `obs_id` carrying different decision-bearing content (`RepositoryError::ContradictoryObservation`, on 5.11's `InstantRegressed` precedent) — `raw` is excluded (D19: no decision reads it) and so is the ORDER of `facts`, each exclusion carrying a test that asserts the bare `a != b` WOULD have refused it. 🔴 **Two of the story's own predictions were REFUTED by its mutation pass, and both refutations are the deliverable.** M5: the golden-value test does **not** guard the seed sweep's provenance — it pins `shuffled` at a HARDCODED seed and never reads `SEED_SWEEP`, so a clock-derived sweep left **all 445 tests green**; closed by a guard reading the constant's VALUES, after which M5 reds exactly one test. *Reproducible WITHIN one process is trivially true for every seed; reproducible ACROSS runs is what a fixed seed buys, and only the second was ever at stake.* And M2 exposed that `shuffled` had **no consumer at all**, so AC4's reference-scale half was satisfied by nothing. 🔑 What HELD, also measured: shape C is **not** a duplicate of 5.11's idempotence test (under M2, 5.11's stayed green while shape C reddened); M1-noop leaves the whole suite green exactly as predicted; and under a first-key-wins `join` the **corpus** AC1 test stays GREEN while the synthetic one reds — so AC6 is satisfiable in letter and the synthetic slice is what carries the measurement. Ten mutation rows, **zero compiler-carried reds**; M6 reds here (7 tests) where the story measured it green, which is the count assertions working, and `M3b+COUNTS_DELETED` proves it by turning the four consumers green again. **5.11b is `done`** (PR #73 squash-merged 2026-08-07 as `f597882` after a green CI run, code-reviewed first) (three layers, 2026-08-06; the Edge Case Hunter ran its own mutations against a live `mariadb:10.11.11` and the Auditor re-executed the whole T7 pass): **446 → 450 tests**, six gates green, and it STAYS at `review` because `done` is the MERGE's business. 🔑 **Two layers independently found the same two defects** — the false `XorShift64` doc and the corpus test's weak oracle. 🔴 **The story's own headline defect recurred inside the story**: `SEED_SWEEP`'s doc asserted that the golden-value test guarded its provenance and that a clock-derived sweep left the suite green — both refuted by a test **eighty lines below it in the same commit**, the sixth consecutive story with a doc falsified by a measurement in its own commit and the first where the falsifying measurement was the story's own deliverable. 🔴 **Four behavioural defects, every one found BY MUTATION rather than by reading**: *"the refusal happens before anything is written"* was guarded by NOTHING — moving the `return Err` to the END of `resolve_within` left **446/446 green**, because every test runs inside `transact`, which rolls back on `Err`, so the claim held wherever the refusal sat; **no order test touched the `interface` table's derived instants**, so making `seen_window` follow arrival order left ALL FIVE order tests green; the `facts.len()` term in `contradicts` was **droppable with the suite green**, being the only term that catches `[x]` against `[x, x]`; and the corpus test's `join` oracle was `!is_empty()` where its synthetic twin pins an exact count, so a `join` collapsing every observation onto one interface left it green. Each now has a guard measured to red. 🔴 **Three of the story's own claims were refuted by re-measurement**: `M3b+` was wrong **in five documents** — there are **six** count assertions, not five, the sixth inside `sampled_permutations`, which guards shapes B and C a second time, so deleting only the four `consumed` lines leaves the two database consumers RED (*the code was better than the claim; the claim was the defect*); `M1-fkw` was 10 red, not 11, the driver's raw line count having been copied for one row of ten; and the timing figures rested on no check (692 ms here, 561 ms on another machine). 🔑 Also caught: `XorShift64`'s doc claimed *"state is never zero"*, false for the golden-ratio constant — exactly the value someone reaching for *"a good seed"* reaches for — and the degeneration is SILENT, a fixed rotation that both the multiset and reproducibility guards pass on. **AC5 is PARTLY MET and was corrected rather than ticked**: what shipped is a COMPILE-time guard, and a `..` in the pattern retires it with the suite green. Two findings were **dismissed with the check that dismissed them** — CI does export `DATABASE_URL` against a `mariadb:10.11.11` service (`ci.yml:24`), and `rand`'s transitive provenance verified — and the Edge Case Hunter refuted **eight** of its own suspicions by running them. **5.12 is `done`** — PR #75 squash-merged 2026-08-07 as `378f0f8` after a green CI run, code-reviewed and REPAIRED first — the never-overwrite anti-regression guard, and the project's **SEVENTH** `xtask` gate. 🔑 **Its design decision is the story's whole point**: the AC asserts the ABSENCE of a code path, and you cannot measure the absence of code by running code — a test exercises what exists, while the violation is what a future story ADDS. So the guard is a gate on `float-free`'s precedent, walking `.rs` **and** `.sql` under `crates/` **and** `docker/` (31 files). **450 → 463 tests.** 🔑 **The invariant was already held FIVE ways and not one was a test**: the write path binds SQL LITERALS rather than parameters (`'manual'`, `'operator'` — there is no actor parameter to pass a non-human author through), a DDL CHECK, the read's column list, and — the strongest, absent from the story's first draft — the PK `(entity_id, attr_key)` with **no `ON DUPLICATE KEY UPDATE` anywhere**, so the adapter cannot overwrite a declared value AT ALL. The fifth mechanism the draft listed was **FALSE** and both validation layers measured it: widening the SELECT compiles cleanly and leaves all 450 tests green, because sqlx decodes tuples POSITIONALLY and discards extras — so **AC3's carrier is the gate ALONE**, not the compiler. 🔴 **M1sql was GREEN on first measurement**, and the reason was not the scope: the gate DID read the planted `.sql` migration and found nothing, because `strip_line_comment` handles `//` only and the `--` header ran into the statement under whitespace normalisation. 🔴 **Two false positives, both the *"wrong in both directions"* family** — `SELECT COUNT(*)` read as a wildcard (the gate's very first red, on the committed tree at `repo.rs:106`) and a match spanning two string literals. 🔴 **A prediction of the story's, refuted**: the gate does NOT inherit `float-free`'s block-comment false positive, because it anchors on a statement's HEAD — and green is the right answer, a commented-out write being no code path. **THREE sanctioned sites where the story prescribed two**: `docker/seed-example.sql` is a second, SHIPPED writer forced by the story's own scope, and its consequence is registered. AC1 and AC2 had CONTRADICTED each other (AC2 needs a raw INSERT that AC1's gate reds on); resolved by a two-site function allowlist rather than a `cfg(test)` exemption, which validation measured hiding a planted write. ⚠️ **NFR5 has THREE assertions and this story covers ONE** (plus FR13's corollary); the other two need the `document` gesture and are registered with the triage epic, so *"NFR5 is covered by anti-regression tests"* is never read as met in full. **5.12 has now been CODE-REVIEWED (three layers) and REPAIRED** — Guy arbitrated **voie A: repair AND narrow the promise**, over (B) closing it with a database `GRANT` and (C) dropping the gate. 🔴 **The review did not find defects in the gate; it found that the gate did not hold the property it claimed.** Its Edge Case Hunter wrote **thirty** violations of NFR5 and **SIXTEEN passed** — three of them executing successfully against a live `mariadb:10.11.11`. They are now committed as a regression corpus (`xtask/probes/authorship/`, 30 + `e31` and `e32` added during the repair), with **all 32 verdicts pinned in BOTH directions**, because a gate that silently WIDENS is a gate whose stated limits have gone stale. 🔴 **The structural finding: the whole body of `gate_declared_authorship` was deletable with the xtask suite green (56/56)** — every test attacked `authorship_findings` directly, while `float-free` had carried the end-to-end test since 5.4b and its doc says in so many words that testing the helper alone *"would leave all three untested while the gate read as covered"*. ⚠️ It hid behind a mutation LABEL: **M5 said *"neuter the gate"* and was applied to the MATCHER** — re-measured, neutering the matcher reds 8 tests while replacing the gate BODY reddened **0**. The repair: `governing_keyword` (the nearest preceding verb or `select` governs, longest wins at equal ends) replacing the statement-HEAD anchor; block comments stripped ACROSS lines with `/*! … */` **kept as code** because MariaDB executes it; invisible characters DELETED rather than treated as whitespace; `enclosing_fn` bounded to real declarations with open braces; `statement_after` so a provenance column read in a predicate counts as read; two verbs added. **29 red, 3 green by stated decision.** 🔑 **The narrowing is half the deliverable**: this gate is a **TRIPWIRE against the good-faith violation, never a barrier against a determined one** — read it as *"a future story will not add such a write by accident"*, never as *"such a write cannot exist"*. The two residual classes are a query assembled at runtime (`e02`, one limit with two witnesses) and **guard NEUTRALISATION** (`e14`, `e31`) — *the gate guards the write, not the guard itself, and the guard of the guard is a privilege the database refuses*; **voie B's `GRANT` is registered as the real closure rather than implied by this one**. ⚠️ **And the criterion does NOT cleanly separate `e22`**: `CREATE OR REPLACE TABLE` destroys the guard just as thoroughly and REDS, and mutation M19b proves that red **incident** — it survives removing the verb from the list, the `REPLACE` inside the phrase governing the same reference. It is kept on purpose, not because the criterion demanded it; found by READING, by a second session that had been launched on the same story in parallel. The repair's own pass: **ten mutations, ten reds, every one carried by a NAMED assertion, zero compiler-carried and zero `.expect()`-carried** — classified by reading each red test's panic message one by one, because 🔴 **the earlier table's header said *"every red assertion-carried"* while its own M6 row four lines below recorded `.expect()` panic ×3**, story 5.9b's defect reproduced. 🔴 **Two mutations came back GREEN first and both refutations are the deliverable**: `is_invisible` was carried by nothing (`e06` puts its zero-width space BEFORE the verb, where a token boundary already exists — hence `e32`, inside the words), and `create or replace table` changes no verdict, only the finding's NAME. ⚠️ **Two more were MIS-DESIGNED rather than the guards being weak**: removing a test's own assertion cannot red that test; for an assertion the honest mutation breaks the INVARIANT (plant an orphan, make the count lie). seven gates green. 🔑 **Three documents described a state the code had already passed** — the mutation-table header, `docs/project-context.md`'s *"six gates"*, and `xtask/src/main.rs`'s own module doc, which enumerated **six** gates while the file implemented seven: the gate this story adds was missing from the list the file gives of itself. **Two of the three are in the gate's own file.** ⚠️ **And one hypothesis of the same family was MEASURED AND REFUTED, kept separate on purpose**: the twins were flagged as a place where a dated *"six gates"* and a living *"seven gates"* might share a line — measured, **there is nothing**, every *"six gates green"* there is a dated code-review record (5.6, 5.7, 5.8, 5.11b) and *"the sixth gate"* names `float-free` correctly. It must NOT be fused with the confirmed one (`main.rs`'s module doc, which really did enumerate six while implementing seven): fusing them would let an unverified suspicion inherit a measured one's credit — the very error this story spent its day correcting. 🔑 **The parallel session's tally, written as a RULE rather than a number**: **seven sends — seven confirmed findings** (the seventh outside the repository, in the resume-point memory a next session reads BEFORE the code), **one hypothesis measured and REFUTED**, and **one non-finding converted into a measurement**. Three of the seven were false sentences in the section that PROMISES. ⚠️ It first read *"six"* — and the session that produced the findings caught that too, the missing one being the resume point the same paragraph had just called the only place that really mattered for the morning: **a tally written in flight, inside the document explaining why not to write counts in flight.** The story file carries it as a table that can be recounted instead of a number that must be believed. 🔴 **After the repair passed its own mutation pass, a further defect was found by READING and re-measured before being believed**: `normalise_sql_text` built its offset→line map **one entry per CHARACTER** while `authorship_findings` indexes it by BYTE, so any multibyte string literal shifted every later finding — measured, **line 0 for a write on line 2**. NOT a detection hole (the write still reds); what was wrong is **where the gate says to look**. 🔑 **Neither the probes nor the ten mutations could catch it, and that is the transferable lesson**: `AUTHORSHIP_PROBES` pinned a BOOLEAN — *does it red*, never *where* — and `e23`, the corpus's own multibyte probe, reported the RIGHT line while the map was broken, its drift small enough for the line to absorb. **A pinned boolean proves THAT a gate fires and never WHERE.** The corpus changed with the fix: **29 boolean verdicts are now 29 LOCATED verdicts** (`Option<usize>`, the file AND the line the gate must name), and M23 — reverting the one-line repair — reds exactly one test, the dedicated multibyte one; the located corpus alone does NOT catch it, measured. **Eleven mutations, eleven reds.** 🔴 **A THIRD defect, and the only one INSIDE the narrowed promise**: `SANCTIONED_FNS` matched the enclosing function's NAME wherever it lived, so **a new file holding nothing but `fn insert_declared_attribute` writing `declared_attribute` left the gate GREEN** — measured on the committed tree, no trick, the name was enough. Not an adversary's probe but **the ordinary gesture** (copy the adapter into another module, or give a second one the name the job already has), which §12's promise puts on the HELD side — *a hole beside the promise is a stated limit; a hole inside it is a false sentence*. 🔑 The apparatus had already said so itself: the data-file half was keyed by PATH, the Rust half by name, and the docs called all three *"sites"*. Now one `SANCTIONED_SITES: [(&str, Option<&str>); 3]` — a path plus the enclosing `fn` — and `authorship_findings` takes the displayed path instead of a `bool` the caller had to know the meaning of. Probe **`e33`** carries it on an axis the corpus did not have (the other 32 ask *does it red*; `e33` asks whether the sanctioned NAME lets a write through from elsewhere). **M24b** (name-only key) reds 2 and **breaks no legitimate site**; **M25** (path compared unnormalised) reds 2 — the trap flagged without measurement, now measured. **Thirteen mutations, thirteen reds.** 🔴 **A FOURTH finding, and a SECOND hole inside the promise**: `statement_after` bounds a statement at `;` or `"`, but in a `.rs` file a `"` can be a quote INSIDE the SQL — so the read half was truncated and every predicate after it lost, which is exactly where provenance columns live. Measured GREEN on both spellings Rust permits (raw string and `\"`), with a **CONTROL** — the same read without the quoted literal — RED all along. 🔑 **The control is what makes the pair mean anything**: it is committed as `e36`, and under M26/M27 the two quoted forms go green while it stays red. `\"` is how anyone writes a quote in a Rust literal, so a developer appending `AND actor_id = ?` lost FR13 without knowing it existed. The `"` bound is KEPT (it is what stops a finding spanning two literals) and quotes *inside* a literal are rewritten to a sentinel during normalisation, both spellings handled, raw-string hash counts tracked. ⚠️ **And `is_invisible` was an ENUMERATION**: `INS<U+FE0F>ERT` and `INS<U+2063>ERT` passed. Ranges widened ONCE and deliberately **not to exhaustion** — *an enumeration cannot claim the completeness of a property*, and that sentence is the deliverable, not a longer list; the closure for that class is voie B's privilege. **Sixteen mutations, sixteen reds.** ⚠️ **A FIFTH finding, weaker, and given WITH its bound** — the parallel session presented it deflated rather than inflated, which is itself the lesson after four accepted ones. The exemption's comment said `CREATE TABLE` is *"never a write of a value"*: FALSE for `CREATE TABLE … AS SELECT`, which fills the table, its `select` sitting after the table name where `statement_before` cannot see it. 🔑 **But the bound is what sizes it**: against a schema `0001` has migrated, that form **fails** (the table exists) and `IF NOT EXISTS … AS SELECT` is **inert**, while the one form of the class that would really run — `CREATE OR REPLACE TABLE … AS SELECT` — **already reds**, measured. So it is a false SENTENCE with a narrow executable reach, not a sixth hole. **Chosen: fix the sentence, not the matcher** — detection for a form that cannot execute is code carried by nothing — with `e38` pinning the grammatical form GREEN by decision so the fact stays measured. 🔑 **And a NON-finding kept as a measurement**: the perimeter is `.rs` + `.sql` while the argument for walking `docker/` (*a SHIPPED writer the operator runs*) would equally cover a `.sh`, a `Dockerfile` or a compose carrying inline SQL — **there are none**, verified in both sessions: the compose declares NO database container (external MariaDB by decision), no `command:` SQL, no init volume. No finding was manufactured from the hypothesis; what is recorded is the sentence — *the perimeter is complete today because no other shipped file carries SQL, measured not supposed, and the day the compose gains a database container with an init script this gate's perimeter must be reopened* — placed in `AUTHORSHIP_ROOTS`' own doc where the next deployment story will meet it. **38 probes.** ⚠️ **One live count for the whole story, and it lives HERE**: **463 → 469 tests** (248 bin + 159 core + 62 xtask), **38 probes**, **sixteen mutations and sixteen reds**, seven gates green. The four earlier counts this paragraph carried were each true when written and stale two commits later — the verification pass found three of them contradicting each other inside one paragraph. A dated fact and a living one in the same sentence: it is always the living one that rots.

**5.13 is `done`** — PR #77 squash-merged 2026-08-10 as `4f7652d` after a green CI run, code-reviewed first — the monotone-honesty measurement, NFR8's first falsifiable assertion (D35(a): *"a fault may only REMOVE knowledge, never ADD an assertion"*), in the new `crates/opencmdb-bin/src/fault_injection.rs`. **SPLIT at contexting** — 5.13b INSERTED, **Epic 5 → 19 stories** — on four findings each established by code rather than by reading the epic: a trap cannot name the observation a fault REMOVED (`DanglingObservation`); the shape D35(a) needs is FORBIDDEN in a committed file and legal only in memory (`epics.md` carries the two contradictory 4.5a criteria two lines apart, and `deferred-work.md` named *"a future story"* that would revisit it — this is that story, and it revisits it by NOT giving in-memory streams a trap-like consumer); a `Trap` judges ONE stream where monotone honesty is a relation between TWO RUNS; and the register assigned it a fourth deliverable `epics.md` never mentions. **469 → 489 tests** (268 bin + 159 core + 62 xtask, after the code review), seven gates green, **fixtures still 25**, trap gate still RED, **`opencmdb-core` untouched**. 🔴 **The design turns on one sentence: `faulted ⊆ clean` is satisfied by EQUALITY** — green for a connector that ignores every fault, for a mutilation that removes nothing, and for a test whose faulted run IS the clean run. So every AC asserts the inclusion **and its strictness**, in two assertions with two messages, because an inclusion failure is a PRODUCT defect and a strictness failure is a TEST defect. 🔴 **The validation refuted the story's own headline mutation**: M2 — the only one named for the inclusion half, i.e. for the NFR8(a) violation itself — is invisible to a differential test, because it is UNCONDITIONAL and lands on the clean run and the faulted run alike, so the invented fact enters BOTH sides of `f ⊆ c`. **A differential test is blind to any mutation that is a pure function of the thing being differenced**; the mutation must be FAULT-CONDITIONAL. 🔑 **And the implementation found what neither the story nor its validation had.** M2 reported THREE invented facts where one was expected — the new one plus two DUPLICATES, the mutation having re-emitted an observation the cut had already emitted. That prompted **M2b**, which re-emits an observation *unchanged*: no new fact anywhere, and the oracle still reds with `INVENTED 2 fact(s)`. **And the CONTROL is what makes the pair mean anything** — the same mutation with the oracle switched to set semantics is measured **GREEN**. So *a run that invents nothing and merely REPEATS itself is a monotone-honesty violation, and a set-based oracle cannot see it*: Guy's arbitration (a MULTISET, inclusion by removal, on `PartialEq` alone, so `opencmdb-core` is not touched) was taken on story 5.11b's precedent and is now MEASURED. ⚠️ **A defect in the mutation DRIVER, caught by disbelieving its own result**: M6 first reported 0 red with a sound mutation — `cargo test --workspace A B` passes two filters where cargo accepts one, so nothing ran. It is the story's own subject applied to the story's own tooling, and it was caught **only because 0 red contradicted a prediction**; had the prediction been *"green"*, the driver defect would have been filed as a confirmation. ⚠️ **The headline *"eleven mutations, eleven reds, every one assertion-carried"* was REFUTED by all three code-review layers**: the table has eleven ROWS and at most nine reds (one row is the GREEN control, another reds no story-5.13 test), and one red under M2 is `.expect()`-carried — `ac4` dies on `.expect("the pass must resolve")` and **never reaches its own assertion**. Third recurrence of story 5.9b's family, in the story whose subject is measurements reporting what they did not measure. 🔑 The engine layer compares **PLACEMENTS** `(observation_id, interface_id)` and excludes `rule_id`/`evidence`/`outcome`, because a fault legitimately WEAKENS a justification — and at L1 today the first two are CONSTANT over every row carrying an interface, `decide_singleton` naming the same rule as `decide_pair` and carrying evidence, so `evidence` is the only excluded column that could have varied; M7's failure output printed exactly that, confirming by measurement a correction the fact-check had made by reading. 🔑 **An abstention is NOT an invented fact** — under the blinding the faulted pass writes rows the clean pass has not (measured: 2, `absence_of_proof`), and they are the honest answer (FR16). ⚠️ **NFR8 has FOUR assertions and this story covers ONE**; lattice monotonicity (D36) is **re-owned, not discharged** — the doubt order to Epic 6, the capability snapshot to 5.13b — and AC3's inverse direction moved to 5.13b on a measurement taken independently by both validation layers (`clean = 8 facts, faulted = 8 facts` on both committed control-record streams, because a `capability` record filters no fact at all).

**5.13b is `done`** (PR #79 squash-merged 2026-08-11 as `6c95115` after a green CI run, code-reviewed and repaired first) — the blinded-source family, and the first story whose centre of gravity is under `fixtures/` rather than under `crates/`. It commits a **TWIN PAIR** (`blinded-source.jsonl` and `blinded-source-blinded.jsonl`, the same four observations with a `capability` record after the second and the strip that record forces) plus a **two-pole trap family** judging the faulted twin — taking the corpus to **28 artefacts, 26 traps across TEN families** and the gate to **26 discovered, 15 scored, 0 failures, 0 wrong-rule, 11 unanswerable, `passed() == false`**, still RED by design until Epic 6. **489 → 493 tests** (272 bin + 159 core + 62 xtask), seven gates green, `opencmdb-core` untouched, no dependency, `epics.md` not edited. 🔴 **THE HEADLINE: its first draft prescribed an `obs_id` prefix that is RESERVED, with four UUIDs byte-identical to `scenario/wire/unifi-clients.expected.jsonl`.** The draft's measurement was RIGHT — it enumerated `scenario/replay/` correctly — and its CONCLUSION was drawn about the whole tree. ⚠️ **No gate would have caught it**: `no_obs_id_is_shared_across_replay_streams` walks `scenario/replay/` only, `wire/` sits outside every corpus walk ON PURPOSE, and **the story's own mutation M10 — written precisely to prove that anchor live — is blind to it**. The validation layer that BUILT the story reached 492 green tests without seeing it; the layer that merely READ it found it. 🔑 *A green build is not a uniqueness proof when the walk cannot see the file*, and the rule that follows is: verify a prefix by `rg` over `fixtures/` AND `crates/`, never against a walk and never by enumerating what the corpus uses — **an enumeration cannot establish absence**. Registered as a gate-shaped hole; the reservation lives in a README rather than in a check. 🔑 **The design turns on a measurement with a CONTROL.** The twins cannot share `obs_id`s (the anchor refuses it) and `blind_after` preserves them, so the STRICT claim stays on the IN-MEMORY derivation — ids equal by construction, oracle undiminished — while a **twin guard** holds the committed pair to it *modulo `obs_id` and nothing else*. And **"total and injective" was MEASURED INSUFFICIENT**: a keyed lookup, literally that letter with no position anywhere, leaves the guard **GREEN** on the very swap it exists to catch. The missing property is a claim about the SEQUENCE — whole-vector equality after a rekey, or the guard asserts no order at all. 🔑 **AC3's strictness half is structurally unreachable for a blinding, and `fault_injection.rs`'s module doc said so before the story was written** (*"under M-B strictness is guaranteed before the connector is invoked"*); Guy routed it to the **CUT**, which the clean twin joins FOR FREE by being control-free, and the routing is ASSERTED rather than inherited in silence. 🔴 **Two of the story's own claims fell to measurement, and both are the deliverable.** Story 5.2b's *"exchanging the two `observations` vectors leaves the whole suite green without the pin"* is **STALE since story 5.7 gave the corpus a real producer** — M6 reds four tests, three of them `trap_gate` tests that never consult the pin, so the pin is a SECOND oracle and not the sole carrier. And the draft's *"removing a control record can only ever WIDEN what is permitted"*, the sentence it labelled *"the transferable half"*, has a counterexample in the code it cites: an UPGRADE is legal, so removing one can **NARROW**. 🔑 **The implementation found what neither the story nor its validation had**: the twins' tight descriptor (`{Mac, IpV4, Rtt}` rather than `corpus_caps()`'s seven kinds) was justified by a sentence and carried by nothing — **M2b** plants a `Hostname` before the capability record and reds naming *"the INITIAL descriptor"*, while the CONTROL, the same fact under `corpus_caps()`, is measured **GREEN**. ⚠️ **Two mutations' carriers were not what the row predicted**: M7's completeness assertion is unreachable in a full run (the trap count fires first — story 5.13's assertion-order finding, a third time) and had to be ISOLATED; and M10 is `panic!`-carried, not assertion-carried. **Fourteen rows: twelve mutations and two CONTROLS green by design, carriers MIXED and named row by row** — the *"every red assertion-carried"* headline is not restated. ⚠️ **D36's precondition is supplied and is NARROWER than it reads**: the faulted twin is the first trap-named stream whose committed BYTES carry a `capability` record, but `read_jsonl` DROPS control records, so the trap path is **provably blind** to the very record the story commits. *"D36 is now testable"* must not be read as met. **5.14 was SPLIT at its VALIDATION on 2026-08-11 — 5.14b INSERTED, Epic 5 → 20 stories** — and the reason is a measurement the gap-hunt took by running the REAL BINARY against a real database. 🔴 **Wiring the identity pass replaces one structural zero with another**: `arp_ping.rs` emits `IpV4` + `Rtt` and declares those two kinds only — **no MAC, ever** — while `join` keys on `(l2_domain, mac)`, so every scanned observation abstains and the *evaluated* half of the counter is a permanent zero. 🔴 **And the counter AGES**: each scan mints fresh `obs_id`s, so each scan writes a new CURRENT abstention link and nothing supersedes anything — five runs over ONE host gave five links, i.e. ~105 000 after a year at a five-minute interval. The `current_subject IS NOT NULL` filter does not stop it. **So the story's own AC4 and the UX ban it exists to serve — *"six months of inaction and it still reads 113"* — were falsified by the wiring its own AC1 mandated: the counter measures uptime, not reach.** 🔑 Guy split it rather than choosing a denominator, because collapsing sightings of one unplaceable thing means deciding what makes two sightings the same thing WITHOUT an identity — **that is grouping, and grouping is Epic 6**. **5.14 now wires the pass and PINS both zeros with tests that red when they stop being true; 5.14b displays.** ⚠️ Two further arbitrations: the reach section is hoisted OUT of the declared side's visibility gate (measured — a scan-only deployment, the default at first boot, never saw it), and the guard seam **IS** the extracted helper, **written with the mutation that proves the last link is carried by nothing** — deleting the entire wiring from `spawn_startup_scan` leaves the whole suite green (the **282** first quoted was a validation worktree's count, not this tree's — corrected on review) — rather than claiming a guard on the startup path. **5.14 is `done`** (PR #81 squash-merged 2026-08-11 as `48b19b2` after a green CI run, code-reviewed and repaired first) (not `done` — that is the MERGE's business) — the WIRING and the MEASUREMENT, in the new `crates/opencmdb-bin/src/scan_pass.rs`. **The identity pass now runs in the shipped binary**, which it never had: `resolve` had no production caller at all, so any reach counter would have read ZERO permanently and greenly. **494 → 502 tests** (281 bin + 159 core + 62 xtask), seven gates green, built and mutated against a live `mariadb:10.11.11` on port 13314. 🔑 **The seam is GENERIC over `Connector`**, so a test drives poll→ingest→resolve with the committed `FixtureConnector` — and the story states the MEASURED size of what remains uncarried: **`spawn_startup_scan` itself** _(the story first said "three lines"; its code review found a duplicated network sweep living inside the region that sentence had shrunk to nothing — a region you have not counted is not a region you have measured)_ inside a `thread::spawn` whose handle is dropped. Deleting the call to the seam leaves the suite green (M1); deleting the `resolve` call inside it reds SIX tests (M1b, re-measured). *Recording an unavoidable GREEN is honest; recording it without measuring how much it covers is not.* 🔴 **Two structural zeros are PINNED rather than fixed**: the only connector `main.rs` reaches emits no MAC ever, so every scanned observation abstains; and nothing supersedes an abstention across scans, so the population accumulates — five runs over ONE host leave five current links. ⚠️ **The naive fix is measured WORSE than the defect**: widening the vacate pass reds four tests, three of them pre-existing `resolver` ones, because it **erases a host that missed a single scan**. The story's argument for deferring the denominator is therefore a measurement, not prose. 🔴 **A decision the story did not anticipate, taken and recorded**: it said *"does not touch the ARP/ping connector"* and it touches it — both literals were anonymous expressions inside `poll`, which opens an ICMP socket eagerly, so neither could be asserted without the network, and every test reaching a live poll is gated on a variable CI never sets. Two named functions, **no behaviour change** — and naming them is what let the two halves finally CROSS-CHECK each other, whose absence is exactly what let the first draft's pin stay green. ⚠️ **Two divergences from prediction, both recorded**: M1b reds 3 where 1 was predicted, and **M2 is NOT EXECUTABLE** — there is no single ingest unit to join, which the story's own §2 had established while its mutation table kept a row for it anyway (story 5.9's family). **Ten rows: nine executable mutations, one not executable, one GREEN by construction; every red assertion-carried.** **5.14 has now been CODE-REVIEWED (three layers) and REPAIRED.** 🔴 **ALL THREE LAYERS found the same production defect, and one measured it**: extracting the seam had removed the ingest loop from `main.rs` and **LEFT THE POLL**, so every startup swept the CIDR **TWICE** and threw the first sweep away — 4.009 s / 2.0075 s / 1.0025 s as the probe timeout moved, so it scaled with the timeout rather than being fixed overhead. A host answering the first sweep and missing the second was **silently lost**. `sink` stayed syntactically used, so the compiler said nothing; deleting the dead block left all 502 tests green. ⚠️ **And the story's own §2 measurement had been taken on `127.0.0.1/32`, where a double sweep of one host is invisible** — the case chosen to validate the wiring was the one case that could not reveal it. 🔴 **A second defect the pins were supposed to prevent**: a `Fact::Mac` added at the emit site inside `poll` — **the shape the upgrade MUST take**, since a MAC comes from a neighbour lookup keyed on the address, which `emitted_facts(ip, millis)` cannot reach — leaves **502 green**, both connector pins and the structural-zero pin included, while the real binary mints an interface and places a link. **Guy's arbitration: NARROW THE PROMISE on story 5.12's precedent** — a TRIPWIRE against a change made through the two named functions, never a barrier. 🔴 **And the story's own headline mutation was misrecorded**: M1b is **6 red, not 3**, because the mutation discarded the pass's RESULT instead of deleting its CALL — 3 was simply the number of tests reading `.resolution`. Re-measured live: all six, every one on a named assertion, four dying on the database count. **A mutation named for one thing and applied to another measures the other thing** — the same family as the connector pins, twice in one story. ⚠️ Also: *"two divergences from prediction"* was **four** (M5 predicted 7 observed 11, M6 predicted 7 observed 4 — and both predictions were a validation worktree's figures copied in as if they were this tree's); *"NOT EXECUTABLE"* over-claimed for M2, whose real cost is that the two-transaction comment is carried by nothing; and the accumulation is per-**OBSERVATION**, not per-abstention — two scans of one MAC leave 2 links and 1 interface, so **giving the connector a MAC does not fix the counter**. 🔑 **Registered, not fixed** (Guy): **two concurrent passes mint TWO interfaces for ONE MAC**, both reporting success — `interface_l1_key` is a plain index and the mint is read-then-insert — *not reachable today because the MAC-less connector shields it, so **the connector story that gives it a MAC removes the shield and must carry the race***; and `current_subject IS NOT NULL` is **not** equivalent to `valid_to = OPEN_END`, a CHECK evaluating to UNKNOWN accepting a row this story's read then misses.

**5.14b is `done`** (PR #84 squash-merged 2026-08-12 as `015f2a3` after a green CI run, code-reviewed and repaired first — _this status read `review` until story 6.1's push on 2026-08-14, two days stale in both twins while `sprint-status.yaml` was right_) — **the display**, and the first story since 3.7 to change what the operator sees. The gap page gains a SECOND reach section, fed by the persisted `identity_link` rows rather than recomputed: sightings placed, sightings not placed, and — for those — why, **one line per cause**. ⚠️ **ONE LIVE COUNT FOR THE WHOLE PROJECT — since story 6.1 (its AC8) it lives in the CURRENT story's file, today `6-1-write-route-writes-nothing.md`; at 5.14b's close it read 502 → 523 tests** (302 bin + 159 core + 62 xtask, after the code review); every other figure in this file is dated by the story that produced it, **including 5.14's `494 → 502` immediately above**. Seven gates green, 28 fixtures, trap gate still RED at 26/15/11, `opencmdb-core` untouched, `epics.md` and the UX spec **not edited**. 🔴 **THE TRAP THE STORY WAS WRITTEN AROUND: a section titled "Reach" ALREADY EXISTED on the page** (`_gap_card.html`) showing a DIFFERENT engine's abstentions — `gap::AbstentionCause` (three variants, declared FIELDS of one entity, recomputed per request) where this one shows `identity::cascade::IdentityAbstentionCause` (two variants, SIGHTINGS across the store, read from persisted rows). Same word, different type, different population. **Four arbitrations, each recorded WITH the alternative refused**: **(10)** TWO sections, never summed — two populations in one frame invite the reader to add them, and *the invitation is the defect*; this **diverges from the UX spec's one-panel mock deliberately** and the spec is not edited. **(11)** THE READER NEVER FAILS — `abstention_cause` is a plain `VARCHAR(32)` with no `CHECK` (measured: an invented token inserts cleanly) and `page.rs` turns any error into a 500 for the WHOLE page, so an unfamiliar token is COUNTED, labelled and rendered. The DDL `CHECK` was refused on a reason stronger than *"DDL in a display story"*: it **moves the failure from the DISPLAY to the WRITE**, so a future variant would break the identity pass rather than show an unfamiliar label — 🔑 *a display story may not be the place a write starts failing* — and the schema-side closure is REGISTERED rather than implied (story 5.12's `GRANT` precedent). **(12)** `count_engine_reach` SUBSUMES `counted_current_engine_links`, and `scan_pass.rs`'s *"story 5.14b is its production consumer"* — a PREDICTION about this story shipped as a statement — is **corrected**; the subsumption is now a TEST over the full set and the filtered one, not a sentence. **(13)** 🔴 Opened by the validation, which found that §8 recorded *"does not age"* as true by construction via the view builder's clock-freedom — **a strictly weaker property wearing the ban's name** — while the same story measures that the number GROWS, and that the UX spec's FIRST hard ban is literally *"No badge, no growing counter"*. **Guy: ship the number with an HONEST UNIT — *sightings*, not devices** — on both sides of the pair. 🔑 *A figure that rises because the product looked many times is the radar's range, not the operator's debt.* ⚠️ **The bans become STATED, never MET** — a true unit does not stop a number growing — and the unit is TEMPORARY: Epic 6 renames it, registered. 🔴 **THE MUTATION PASS'S OWN FINDING, and it is the transferable one: M6b came back GREEN.** The anti-sum guard tested that `build_view` and `build_identity_view` do not add each other's counts — and **neither of them can**, since neither sees the other's numbers. The only place a sum can be written is `reconcile_view`, the impure edge that assembles both, which no unit test reached. 🔑 ***A guard placed where the defect cannot occur reads as coverage and is none***, closed by a database-backed test through the composition; **reading the guard could not have found it, because the guard is correct about what it tests**. ⚠️ Also caught by the pass: **a false sentence in my own assertion message** (it claimed a render assertion would pass, refuted by M8's second red — a render assertion is vacuous only when it checks that *something* appeared or checks the key's own text, not when it names a distinctive phrase of the translation); **M11a left the total assertion unmeasured**, so M11a-bis was written to shrink the total itself; and a false premise of my own (three out-of-perimeter sightings are **4** reconciliation abstentions, not 2). **Twelve rows: eleven executable mutations and one GREEN by stated limit (M9c, the id-selector override the CSS check cannot see). Zero compiler-carried reds, carriers MIXED and named row by row** — M12 is **`.expect()`-carried** on AC8's premise and never reaches its own assertion, and M6b reds on a premise too; *"every red assertion-carried"* is **not** claimed. ⚠️ **The validation was worth its cost and the figure says so: FOUR guards this story prescribed were measured GREEN before shipping** — the anti-sum over a zero addend, AC5's own prescribed remedy for a missing key, AC8's assertion over a table a rollback left empty, and the colour check under an id selector. ✅ **CODE-REVIEWED (three layers) and REPAIRED, 2026-08-12**: 2 decisions, 14 patches, 4 deferrals, 0 dismissed; **three findings reached by two layers independently and one by all three**. 🔴 **THE HEADLINE FALSIFIED ARBITRATION 13 ON THE HALF ALREADY REPORTED AS SETTLED: `placed` and `not_placed` were in DIFFERENT UNITS**, side by side — `join` writes one row per L1 KEY while an observation abstains once per OBSERVATION, so a three-MAC sighting beside a MAC-less one displayed `placed = 3 · not placed = 1` for **two sightings in**; and two populations in one frame is exactly what arbitration 10 forbids between the sections, applied there and not inside one. **Guy chose to count SIGHTINGS on both sides** (`COUNT(DISTINCT observation_id)`) — ⚠️ **which kills arbitration 12's subsumption**, so the test that asserted the two reads AGREE now measures their DIVERGENCE (links 3 / sightings 1), the more useful thing to pin. 🔴 **`no_match` was folded into `placed` by a bare `else`, found by ALL THREE layers**: a row that *"names the interface it EXCLUDED"* was reported as a placement with *"Every sighting was placed."* rendered over it. 🔑 **GUY'S TAXONOMY SETTLES IT AND IS NOW THE TYPE'S DOC — no ambiguity → the software decides (`Match` AND `NoMatch`); ambiguity → the operator LIFTS THE DOUBT; unknown → the operator CREATES THE ENTITY.** Its real yield is outside this story: **case three's gesture is the documenting gesture the product does not have at all** (five routes, all read-only; the only `insert_declared_attribute` call outside `repo.rs` is inside a `#[cfg(test)]`), and case three is **the only case reachable today** — *the abstention section is the entry point of a gesture that does not exist*, which is why it reads as hollow. ⚠️ **Not announced in the surface, by decision: descriptive until the gesture is there, because announcing an absent gesture is a promise.** ⚠️ **And the apparatus did not find that**: nineteen findings and not one asked *"what can the operator DO with this number?"*, because three layers checked conformance to a specification and the specification was the author's — a blind spot of the METHOD, carried to the retrospective. 🔴 **AC10 was NOT MET: §11 required NINE register rows and SEVEN landed**, and the re-read that was supposed to catch it counted the seven written and compared them to the seven written. 🔑 ***A re-read that reads only what you wrote cannot find what you did not write.*** ⚠️ Its replacement check first reported 7/9 **for the wrong reason** (two needles split by a line break) — *a check that fails for the wrong reason is worth nothing* — normalised and re-run: 9/9. ⚠️ Four more guards of the author's were measured green (the `ORDER BY` doc claiming *"a test pins it"* while MariaDB's `GROUP BY` already sorted; the clock guard, blind to any clock coarser than two back-to-back renders, **whose real carrier is `chrono`'s `default-features = false` and not the test**; the `all_placed` branch, rendered by nothing — AC4's own comment records that defect found in validation FOR THE SIBLING KEY; and the pure anti-sum guard's two unfailable `assert_ne!`s), five mutations the ACs cite were missing from the table with **AC6's only recorded mutation being a GREEN control**, and *"643 lines"* was written in flight inside the bullet about numbers written in flight.

**EPIC 5's RETROSPECTIVE was HELD on 2026-08-12** (`epic-5-retro-2026-08-12.md`) — it is what CLOSES the epic, and it was held before PR #84's merge because nothing in it depended on the merge. **Epic 4's six action items: five done, one not due, ZERO lapsed**, and action 2 (*a cause needs a check, not a plausible story*) was EXERCISED the same day — a `set_locale` finding that looked like issue #38's cause was **refused as its cause** because the check refuted it. **142 consecutive green CI runs** across epics 4-5 (47 + 95), 42 PRs merged, 247 → 523 tests, 5 → 7 gates, corpus 25 → 28 artefacts. 🔴 **THE EPIC'S DOMINANT DEFECT CLASS, counted in at least NINE of its twenty stories and NAMED at its end: *a guard placed where the defect cannot occur reads as coverage and is none*.** Its clean specimen is story 5.14b's anti-sum guard, which asserted that two pure builders do not add each other's counts — and **neither of them can**, since neither sees the other's numbers; the only place a sum could be written was the impure edge no unit test reached. ⚠️ **Reading the guard could not have found it — the guard is CORRECT about what it tests.** Only running the mutation did. Second class, four recurrences in one epic: **the mutation driver lies** (two filters that run nothing, a filtered run reported as a full-suite figure, a measurement read through a truncation, a mutation named for one thing and applied to another). 🔴 **GUY'S DRIFT QUESTION WAS ANSWERED WITH NUMBERS**: epics 1-3 = **21 stories and everything usable today**; epics 4-5 = **43 stories and nothing operator-visible**. ⚠️ What is NOT drift and must not be "fixed": writing the corpus and the metrics harness before the engine is D19 and defensible — *a metric written after the engine is bent to fit the engine*. **What drifted is the RATIO of rigour to reach**, and the apparatus had begun measuring itself. 🔑 **THE SIGNIFICANT DISCOVERY: the missing gesture was already in the plan, positioned LAST.** Epic 6 closed with *"a one-click promote of an observed value into a declared record"* (FR13) — which the PRD itself calls **"the day-one case"**, which is case three of the operator's three cases, which is **the only case reachable today** (the shipped connector emits no MAC, so every abstention on a real network is `AbsenceOfProof`), and which ⚠️ **does not depend on grouping**, Epic 6's own subject. **Guy's decision (option a): the promote gesture MOVES TO THE FRONT of Epic 6** — `epics.md:419-421` edited (a retrospective may; a story may not) and recorded in **GitHub issue #85**, not silently. Refused and recorded as defensible: a short slice before Epic 6 (new scope where a reordering suffices), and leaving the plan (keeps the product unusable for a whole epic). ⚠️ **Epic 6's first obligation is its STORY BREAKDOWN**, which it does not have — exactly as Epic 5 had none at this point — and the breakdown puts FR13 first. 🔑 **And one finding the apparatus could not produce**: three review layers, nineteen findings, and **not one asked what the operator can DO with the number**, because they audit conformance to a specification the author wrote. A blind spot of the METHOD, now an explicit question in the Acceptance Auditor's mandate. **5.13b has now been CODE-REVIEWED (three layers) and REPAIRED.** 🔑 **Guy's arbitration is the deliverable, not the patch**: the story promised *"`opencmdb-core` is not touched"* in FOUR documents, and that promise is exactly what kept it out of `score.rs:443`, where a comment asserts as a MEASUREMENT the thing this story's own deliverable falsifies — *"eleven replay streams are named by a trap and not one of them carries a `capability` control record"*, today twelve and one does. **A promise of non-modification protects behaviour and shelters false sentences.** Narrowed to *"no BEHAVIOUR change in `opencmdb-core`"*, the sentence corrected in place. 🔴 **Three claims of the implementation record were refuted by re-measurement**: **M10 is 37 red, not 1** — the figure came from a FILTERED run, the driver defect the story's own Dev Notes warn about two paragraphs above, and the carriers are MIXED; **M7's *"unreachable in a full run"* is FALSE** (18 red, `trap_gate.rs:1183` among them, exactly as §7 predicted) and what let it stand was a `head -8` on my own measurement output — *a measurement read through a truncation is not a measurement*; and the headline *"fourteen rows, twelve mutations, two CONTROLS GREEN by design"* is **fifteen rows, thirteen mutations**, with each control's green scoped to the ONE test it names rather than to the suite — **fourth consecutive story with that family, in the story whose AC8 names it**. ⚠️ **A guard was measured carried by NOTHING**: swapping the twins' tight descriptor for `corpus_caps()` left all 493 tests green. M2b measured what the tightness CATCHES, never what catches its LOSS, and the house rule runs the other way — closed by a guard that reds on the ordinary DRY gesture. ⚠️ **The trap-text privacy floor had gone SLACK**: this story's own trap reasons introduce a FIFTH MAC, so a floor left at 4 would from then on have tolerated the loss of a previously-pinned value while still reading as a pass. Raised to 5 — *a floor is only a guard while it equals what is there*. ⚠️ And the count sweep had missed **thirteen more sites, including one my own §6c had LISTED** (`trap_gate.rs:873`, the failure message of a guard, now stating a false corpus). **493 → 494 tests**, seven gates green.

**EPIC 6 IS IN PROGRESS since 2026-08-12** — decomposed into 19 stories (+ **Epic 6b** INSERTED 2026-08-13: the mock's interface, the release moving inside the epic; order 6.1 · 6.2 · 6.3 → Epic 6b → 6.4 → v0.2.0 — whose release STORY is Epic 6b's 6b.12, sequenced after 6.4 → 6.5…6.19), the documenting gesture FIRST (issue #85). **Story 6.1 is `done`** (PR #89 squash-merged 2026-08-14 as `664693b` after a green CI run, code-reviewed first — same-day: dev, review, merge) — the write route's SHAPE, and the read surface's visibility decision priced in the open; developed 2026-08-14 on the rewrite (HTTP Basic) validated the same day (21 findings, all applied). What shipped: `POST /document-all` exists only under `OPENCMDB_DOCUMENT_ENABLED` and **writes NOTHING**; **HTTP Basic stands in `auth_deny`'s default arm** (arbitration 2′, decided ON the browser bench — an `hx-post` carries the cached credential) and **`is_public` shrank to `/healthz` + `/assets/*` — the product stopped being publicly readable**, the release-notes obligation registered with the first release containing the story (AC7(f)). Configuration enters as a PARAMETER — `AppConfig::from_env`, pure on `dburl`'s precedent, boot refusals BY NAME (non-ASCII half, half-configured pair, colon in the user half, unrecognised switch) — so **not one new test mutates an env var**. The no-write claim is carried in three NAMED layers: the document sub-router's state holds no pool, so M4 (add `State<MySqlPool>` to the handler) **fails to compile — measured `E0277` on the `Handler` bound**, the rewrite's re-prediction confirmed; the `authorship` gate covers a smuggled write to `declared_attribute`; and the own-connection shape is carried by NOTHING, stated as a TRIPWIRE on story 5.12's precedent. 🔑 **Fifteen runtime mutations executed, fifteen red results, plus M4's refusal to compile — sixteen mutation ids in all; among the fifteen runtime reds, zero compiler-carried and zero `.expect()`-carried, every red read from its own panic message** (M4's red IS the compiler, and only M4's); M9 (route registered below the layer) reds EXACTLY the one predicted test, the write-route challenge. ⚠️ Two divergences recorded as findings: **M10 reds only the right-user half of its predicted pair** — a user-only comparison still refuses a wrong user — so the symmetric password-only mutation was run and reds the mirror test, each half load-bearing by its own mutation; and **the M9 driver's first run failed on a rustfmt-moved anchor and printed the UNMUTATED tree's green** — caught by the driver's own assert, the retrospective's *"the mutation driver lies"* class, live again. The register gained rows (a)–(j) in `deferred-work.md` (CSRF and the Chromium bench → story 6.2; the `--accent` conflict and the native dialog → story 6.4; Basic's closure, constant-time compare and the lowercase-`bearer` defect → Epic 19; `document-field` → Epic 7; the D37 filename drift → Epic 6b 6b.1; the release note → the first release). **Its three-layer code review found 17 (1 decision, 14 patch, 1 defer, 1 dismissed; 8 edge-suspicions REFUTED by measurement), re-verified all 8 ACs MET independently, and re-executed M4/M5/M9/M10 — every one conformed to the recorded table, the documented M10 divergence included.** 🔴 Guy's decision (b): the 404 body becomes **"unknown subject: nothing can be documented"** — the shipped sentence was FALSE for a recorded observation under `AlwaysUnknown`, and the fix is the sentence true in BOTH eras, re-pinned in all three places. Also applied: a control-character boot refusal (the env-file newline passes `is_ascii()` and no dialog can type it), the NIL-UUID sentinel refused at the route (D21/D48, before 6.2 can forget it), the false universal *"never answers an empty body"* scoped to the POST pair (the route's own 405 is empty — found by two layers independently), RFC 9110's violated MUST named where arbitration 6 trades it, and the `&&`-short-circuit username oracle added to register row (d). **564 → 566 tests, both new guards proven red-when-removed.** ⚠️ And one incident recorded for the retrospective: the guard prove-to-red reverted with `git checkout -- <file>` while the review patches were UNCOMMITTED, silently discarding them from two files — caught only because the FULL suite re-ran against the live database before concluding; *"revert the MUTATION, never the FILE" has an unstated precondition — a file revert equals a mutation revert only on a COMMITTED baseline.* ⚠️ THE LIVE COUNT lives in the story file `6-1-write-route-writes-nothing.md` (its AC8). Seven gates green, 28 fixtures, trap gate still RED at 26/15/11, `epics.md` not edited, `page.rs` untouched byte-for-byte.

**Story 6.2 is `done`** (PR #91 squash-merged 2026-08-14 as `ead7738` after a green CI run, code-reviewed first — same-day: context, four-pass validation, dev, three-layer review, merge) — the route now WRITES an adopted declared value, and milestone J3's *corrected* half is measured end-to-end for the first time (document → reconcile shows the gap CLOSED). Contexted + validated by FOUR passes (two fact-check, two gap-hunt that each BUILT the design against a live MariaDB) + the two-browser CSRF bench, then developed 2026-08-14. 🔴 **The epic AC's letter was WRONG about the fn and the schema had the answer**: *"through `insert_declared_attribute`"* + *"gains exactly one entry"* contradict each other (that fn is already sanctioned), so the write is a NEW sibling `adopt_declared_attribute` — a documented value is an ADOPTED one (`origin='adopted'` + `origin_obs_id`, `0001`'s own vocabulary), divergence registered, `epics.md` not edited. 🔴 **Guy's arbitration (§6.5): the authorship gate gains a NAMED READ-sanction** (`SANCTIONED_READS`), FR13-framed — 5.12 guarded the WRITE of provenance, 6.2 is the first story with a legitimate provenance READER (a test verifier); production reads NO provenance (the 409 rides the unique index `declared_one_adoption_per_field`, keyed on its name). **CSRF (6.1's register row j) is CLOSED by an Origin check** decided first in the handler (measured on Blink: a cross-site POST carries the attacker's Origin ≠ Host → 403); residuals (pre-Origin browsers, the `proxy_set_header Host` requirement, scheme-blind compare) re-registered to Epic 19 — and the Gecko-cross-site cell, first reported unmeasurable, was captured on a second bench pass (the snap Firefox single-instance lock, not headless itself, was the blocker), so BOTH engines are measured and register row (a) is fully discharged. **`gap::project` went `pub`** so the documented fields ARE the compared fields (M11: a wrong key yields an ABSTENTION, not a gap — the J3 oracle is `gaps.is_empty()` AND `abstention_count == 0`). 🔴 **The mutation pass produced three findings, each fixed or recorded**: M6 first came back GREEN (no store-level empty-projection test — the *guard-where-the-defect-cannot-occur* class; a store-backed Rtt-only test was ADDED, after which M6 reds), M9b's explicit `null`-Origin check was measured DEAD (a `null` origin carries no `://` and is refused anyway — removed), and M7 is GREEN by structure (the write loop over an empty projection is a no-op — its real carrier is M6). **Sixteen mutation ids: 13 reds, M4 a compile refusal (`E0277` — no `FromRef`/downcast side door), M7 and M8 GREEN by structure.** ⚠️ THE LIVE COUNT lives in the story file `6-2-route-writes-a-declared-value.md` (its AC9) — cited by reference, never carried here (F2's rule). Seven gates green (authorship now over FOUR write sites + ONE read site), 28 fixtures, trap gate still RED 26/15/11, `page.rs` byte-identical, no new crate.

**Story 6b.1 is `done`** (PR #95 squash-merged 2026-08-18 as `0b42bd0` after a green CI run, code-reviewed first — three layers on a DIFFERENT model) — **EPIC 6b OPENS**, and the product is **LIGHT and set in Barlow**: the first operator-visible change since story 5.14b and the first time the reference mock's system is in the binary. `app.css` 150 → 297 lines on the mock's tokens (light base + three ramps in an UNCONDITIONAL `:root`, the dark set kept and selected by nothing), five Barlow faces embedded under SIL OFL 1.1 with no external request, `--accent` → `--accent-document` across **all six sites**, and D37 closed in BOTH directions (`assets/vendor/htmx-2.0.4.min.js` 200, the unversioned path 404). 🔴 **NO Tailwind chain, by Guy's arbitration (option (1)): the validation MEASURED that it generates nothing this UI uses** — the intersection between the 27 classes the templates carry and the 20 utilities Tailwind emits is **EMPTY**, all 20 being false positives harvested from Rust identifiers — so AC1/AC5/AC6 are re-owned to story 6b.2 with five measurements attached, among them four spellings D55 does not contain (`source(none)`, `@theme static`, the two narrow imports instead of the full one, and the htmx `@source inline` entries that can NEVER be generated). ⚠️ **D55's `@source` trap narrative is stale on v4.3.3**: correct path, wrong path and no `@source` at all produce ONE sha256 — the decision holds, its example does not. 🔴 **THE STORY'S OWN LESSON, and it is about guards rather than code: FIVE of its SEVEN guards read as coverage and were none**, each defeated by an ORDINARY gesture — an OS dark-mode `@media` above the real `:root` (which made a first-match scan return the CONDITIONAL block), spaces inside `var( … )`, a CSS fallback value, an inline `style=` in a template the guard never read, and a protocol-relative `url(//host/…)` that four fixed probes missed. **None was reachable by reading, because each guard is CORRECT about what it tests**; all five are now PROPERTIES rather than enumerations, and M13–M19 red. 🔴 **The record was wrong four ways**: the baseline was **591**, not 597 (an intermediate state read as a baseline, turning +7 into +1); **`cargo fmt --check` was NOT clean on the committed tree** — CI would have reddened, clippy having been fixed by hand AFTER fmt with only clippy re-run, *in the very note that narrates fixing a false "clean"*; `app.css`'s size was wrong in both units in three places; and **two divergences the story called *"registered"* were in NO register** — now three rows, the third recording that the story's own §Traps **prescribed** the check that would have caught it, citing story 5.14b's identical failure. *A prescribed check nobody runs is worth what no check is worth.* ⚠️ **Every existing deployment changes colour**; story 6b.12 owes that sentence to the release notes. ⚠️ THE LIVE COUNT lives in the story file `6b-1-design-system-tokens-and-accent.md` (its AC8) — cited by reference, never carried here. Eight gates green, 28 fixtures, trap gate still RED 26/15/11, `opencmdb-core` byte-identical, no migration, `_gap_card.html` untouched, `epics.md` not edited.

**Story 6b.2 is `done`** (PR #97 squash-merged 2026-08-19 as `d8cc438` after a green CI run, code-reviewed first — three layers on a DIFFERENT model, each in its own git worktree so their mutations could not collide) — **the product has more than one address for the first time**: a shell (header, grouped navigation, ten entries in three groups), ten server-rendered screens, `/` a 303 to `/triage`, and the reconciliation card moved inside the frame. `gap.html` is DELETED — the shell is the document now. **591 → 611 tests** (384 bin + 161 core + 66 xtask). 🔑 **The one structural decision is a SHAPE, not a sentence**: the nine demonstration screens live on a `Router<()>` merged AFTER `.with_state(pool)`, so a handler taking `State<MySqlPool>` **fails to compile** (`E0308`, re-measured independently by two review layers). On the main router — where the validation measured the first draft would have put them — the same handler compiles cleanly, and epic constraint 1 would have been carried by discipline alone. `/triage` stays on the main router BECAUSE it is the one screen fed by the real gap. 🔴 **THE STORY SHIPPED ITS OWN MUTATION, and that is the transferable finding**: its table predicted **M12** — *read the perimeter from `std::env::var` in the handler instead of `AppConfig`* — as a red, M12 was NEVER EXECUTED, and `page::triage` **was** M12, with all 608 tests green while T4b was ticked `[x]`. The cost was not stylistic: `AppConfig::from_env` discards a blank value and the second reader did not, so `OPENCMDB_SCAN_CIDR="  "` rendered *"not configured"* on nine screens and a dangling label on the tenth — one fact, one shell, two behaviours, on the screen `/` redirects to. ⚠️ **The perimeter guard could not see it and is CORRECT about what it tests**: it hands `render_shell` a `None` directly and never reaches the code that PRODUCES the value. Epic 5's dominant defect class, recurring in the story that inherited the lesson. 🔴 **The three-layer review then found the same family THREE more times, and two layers reached the first one separately**: (a) **no test named ONE of the ten routes** — `/devices` added to `is_public` left 609 green (the story's own **M7**, likewise predicted and never executed), and with the sub-routers merged below `auth_deny` a bare `GET /dashboard` answered **200 OK**, the suite reddening only through two GENERIC probes inherited from story 6.1; (b) **a guard whose own comment claimed a perimeter it did not walk** — `the_shell_shows_no_last_observation` said *"over the WHOLE shell"*, quoting story 5.14b's lesson BY NAME, and read `_nav.html` alone, so the mock's deferred text planted in `_shell.html`'s header left 382 green; (c) **`"\u{200B}".trim().is_empty()` is `false` in Rust**, so a zero-width space survived the emptiness filter and rendered a label with nothing after it — the exact outcome AC1 exists to forbid, reached through a channel neither the story nor its validation considered. All four are closed by guards measured red before they passed, and the new auth guard iterates `Screen::ALL` rather than a copied list, so an eleventh screen is covered the day it exists. ⚠️ **ELEVEN of the eighteen prescribed mutations were never re-executed** (M4, M5b, M6, M8–M11, M13–M15) — M12 was reported as an isolated incident and the review measured that it is not; *"the logic looks like it holds"* was substituted for execution twice, and both times it was hiding a real hole. 🔑 **The `carries_a_visible_glyph` fix states its own limit rather than implying it**: the invisible set is an ENUMERATION of Unicode's default-ignorable ranges, and *an enumeration cannot claim the completeness of a property* (story 5.12's sentence, third application) — a tripwire against the character a copy-paste carries, never a barrier. ⚠️ **A defect was introduced BY the repair and caught during it**: an insertion landed between an existing `#[test]` and its fn, silently disabling `a_half_configured_pair_…` — found by a duplicated-attribute warning and an anomalous *"2 passed"*, **not by reading**. ⚠️ **NINE of the ten screens render an empty `<main>` with nothing saying so**: nothing promises an absent gesture (the auditor checked the templates for exactly that), but between this merge and story 6b.3 the product reads as nine broken screens rather than nine deliberate placeholders — **the sharpest case is `/device`**, an entry called *"Fiche appareil"* that addresses no particular device and says so only where the operator cannot see it _(a DATED record: story 6b.6 gave the record a real address on `/devices/{id}`, and this sentence read as present until then)_. **Story 6b.3 is therefore priority in the strict sense.** 🔑 **And the CI run is the first measurement of the half no review layer could reach**: `DATABASE_URL` is unset locally, so every database-backed test returns early — the bin suite runs in **0.06 s** locally and **5.38 s** in CI against the `mariadb` service, which is the tell that they genuinely executed. All three layers flagged that blind spot independently. 🔴 **Every existing deployment changes address** (`/` → 303 `/triage`); story 6b.12 owes that sentence to the release notes, beside 6b.1's colour change. ⚠️ THE LIVE COUNT lives in the story file `6b-2-shell-navigation-and-ten-routes.md` — cited by reference, never carried here. Eight gates green, 28 fixtures, trap gate still RED 26/15/11, `opencmdb-core` byte-identical, no migration, `epics.md` and the UX spec not edited.

**Story 6b.3 is `done`** (PR #99 squash-merged 2026-08-19 as `d1bb128` after a green CI run, code-reviewed first — three layers on a DIFFERENT model, each in its own git worktree) — **the product shows example content for the first time and SAYS SO**, and the eight screens whose own story has not landed now say *that* instead of rendering nothing. `Nature` on `Screen` — `Fed` / `Example(ExampleContent)` / `Empty` — through a compiler-checked `match`; ONE partial and ONE key pair for the marker, on the neutral ramp and never `--accent-document`; the dataset in code (`example_data.rs`, RFC 5737 addresses and RFC 7042 MACs, so a published screenshot names nobody's network); `/devices` filled in TWO sections each carrying the marker; and the partition asserted over the ROUTE TABLE on the real HTTP body, in both directions for both markers. **611 → 614 tests** (387 bin + 161 core + 66 xtask). 🔴 **TWO DEFECTS WERE FOUND BY LOOKING AT THE SCREEN and neither was reachable by any test**: the example data rendered in ENGLISH under a French UI with the whole suite green — *a literal is not a key, and the locale guard can only see keys* — and the partition's `Fed` half was gated on `DATABASE_URL` while `lazy_pool()` is a hardcoded dead URL that ignores it, so that half **could never have passed and was never attempted**. *A status code is not a look, and neither is a green suite; and a gate keyed on a fact that does not govern the code under test is not a gate.* 🔴 **THE CODE REVIEW'S HEADLINE: three of the story's OWN recorded measurements were FALSE** — M3's carrier (it reds on the STATUS assertion, 404 vs 200, not the premise it named, **and the cause outweighs the correction**: the demo router's exclusion is DYNAMIC, keyed on `nature()`, while the main router's inclusion is HARDCODED to `/triage`, so a screen that becomes `Fed` is merged nowhere), M4's count (**25**, not 15) and M7's aftermath (**386**, not 385 — deleting an assertion does not remove a test). *The table had been believed rather than replayed*, in the story whose own Dev Notes warn that its driver lied twice. 🔴 **ONE FINDING WAS REACHABLE BY NO LAYER ALONE**: `.screen-section` was used twice and defined nowhere, `.rows` — a `<dl>` ruleset written for `_gap_card.html` — was applied to a `<table>`, and the sheet carried **no `table`/`th`/`td` rule at all**, so the one witness screen this story exists to produce rendered as browser defaults. The blind layer could only SUSPECT it without repository access; the measuring layer verified the marker's classes and not the content's. 🔑 **It was found by RECOUNTING the stylesheet, not by seeing the page** — the served TEXT was correct in every respect three layers could measure. ⚠️ *A recount catches a class that matches nothing; it cannot catch a page that is merely ugly* — the browser check is **deferred and not claimed**, for the third consecutive story, and is now owed by Epic 6b's DoD with 6b.12. 🔴 **And a guard was defeated by a key that RESOLVES**: `role_key: "example.badge"` — a real key from the wrong namespace — left all 613 tests, all eight gates and clippy GREEN while `/devices` rendered *"Exemple"* where *"Stockage"* belongs. Shape and resolvability were checked; WHICH key was not. Epic 5's dominant class, reproduced inside the guard added to close an operator-visible i18n defect. **GUY'S THREE ARBITRATIONS, each recorded with the option refused**: **(1)** AC4's `Screen::ALL` half is pinned by a source-scanning TEST, **not a ninth gate and not clippy-in-`xtask`** — 🔑 the story reasoned it *"would have to be a gate"* on story 5.12's *you cannot measure the absence of code by running code*, and **that rule was MISAPPLIED**: it governs an UNBOUNDED absence, while this property is bounded and present in one file, whose own module already held the idiom (`every_key_carries_both_locales` reads `include_str!`). Clippy was refused **on a MEASUREMENT** — one throw-away production line constructing the variant silences it — and `NavGroup::ALL` had the identical hole, so the guard is a PROPERTY over both enums rather than an enumeration for one; its limit is written, a tripwire blind if either enum moves. **(2)** The eight `Empty` screens **get the sentence NOW**: `epics.md:2092` is one of Guy's own four premises of 2026-08-13 — *"those whose code is not implemented show an example dataset with a text saying so"* — and **no layer of contexting, validation or arbitration had surfaced it**; ⚠️ spirit and not letter, the dataset stays owed by 6b.5–6b.9. **(3)** `Nature::Example` **CARRIES its content**, so *"declared `Example` with nothing of its own"* is unrepresentable (story 5.6's precedent, closed in the TYPE) — option (a), a match on the screen, was **WITHDRAWN at the arbitration** because every screen already has an arm, *so it does not close what it looks like it closes*. Nine mutations at implementation and **four more at the review** (M9/M10/M11 assertion-carried, M12 compiler-carried — `E0308`, the compiler naming the cause itself), each run on a scratchpad-restored base and **never `git checkout --`**, the gesture that destroyed an uncommitted fix in this story and in 6.1 before it. ⚠️ **AC2 ships as MECHANISM MET with its MIXED SPECIMEN STATED** (the case it names needs the dashboard, story 6b.5's) and **AC3 MET BY CITATION with no runtime test added** — the `Router<()>` refuses `State<MySqlPool>` at compile time and a run-time assertion would be strictly weaker. ⚠️ THE LIVE COUNT lives in the story file `6b-3-example-data-marker-and-its-gate.md` — cited by reference, never carried here. Eight gates green, 28 fixtures, trap gate still RED 26/15/11, `opencmdb-core` byte-identical, no migration, `epics.md` and the UX spec not edited.

**Story 6b.4 is `done`** (PR #101 squash-merged 2026-08-19 as `cea6853` after a green CI run, code-reviewed first — three layers on a DIFFERENT model, each isolated) — **the one screen the product actually feeds now looks like the product**: `/triage` is the mock's two panes on the real gap. A **queue** of gap rows, and **two photos side by side**, each side carrying **its own provenance and its own freshness** — AC1's *"neither side is the truth"* made structural rather than asserted — plus an **age sort available and OFF by default**. **614 → 625 tests** (398 bin + 161 core + 66 xtask). 🔑 **The queue's row vocabulary is MEASURED, not chosen**: `Écart`, `Absence` and `Conflit` are already typed by `gap::reconcile` (`AbstentionCause::NoObservedValue` / `ConflictingObservations`), `Nouveau` is an observed address no declared entity claims, and **`Ambigu` is OMITTED because nothing produces it** (FR16's ranked candidates, Epic 6's) — the story's contexting had claimed three of five kinds needed new engine work, and the validation refuted it by BUILDING one, citing `gap::project` where `reconcile` was the answer. A cause row is **ONE line carrying its count**, which is FR16b verbatim. 🔴 **THE STORY WAS SPLIT AT ITS VALIDATION — 6b.4b INSERTED, Epic 6b → 13 stories** (`epics.md` NOT edited; registered): 6b.4 keeps AC1+AC3, **6b.4b takes the action bar**, because *"labelled per 6b.3"* names a mechanism that **does not exist** — 6b.3 classifies CONTENT (a screen, a section), never a CONTROL, and `Screen::Triage` is `Fed`, which *"owes NO marker"*. ⚠️ And applied literally today the bar would ship **five buttons of which five are dead**; half of that was never an open question — **Guy's own premise (2) of 2026-08-13** (*"the four gestures Epic 7 owns are visible and labelled"*, under a heading reading *"premises and NOT open questions"*) had already decided it, and contexting put it to him as if it had not. 🔑 **GUY'S THIRD ARBITRATION SHIPS AS A SHAPE, not a sanction**: reading `origin` for DISPLAY reds the `authorship` gate (measured — `SANCTIONED_READS` admitted one test-only reader), and rather than sanction the existing reader — which feeds the divergence computation, putting provenance inside the scope the gate protects *"by accident"* — there are now **TWO readers**: the comparison's, which **cannot** see provenance because it is never given it, and a display-only one the gate names by path AND function. **M8 confirms it**: `origin` in the comparison's `SELECT` turns the gate red. 🔴 **FIVE DEFECTS WERE FOUND BY LOOKING AT THE SCREEN OR BY RENAMING A HELPER, and no test could reach any of them**: a pane that contradicted itself (*"2 champs déclarés"* over *"Rien de déclaré"*); a raw UUID where the operator needs a source — **and the route test REQUIRED it**, asserting `contains(<full uuid>)`, so it passed *on* the defect (*a test that pins the ugly thing is a test that requires it*); an arrow pointing from nothing; a bare count beside an address; and `page::tests::triage_html`, a helper that rendered `GapFragment` directly and never touched the route, so **the entire body of `/triage` was replaced with all 387 bin tests green**, ten tests built on it. 🔴 **THE MUTATION PASS'S OWN FINDING: M1 came back GREEN** — freezing `observed_at` in the repo left the whole suite green, because every unit test built `ObservedBatch` BY HAND and the widened column's round-trip was carried by **nothing**. *A guard that never touches the read cannot notice the read breaking.* 🔑 **And M6 settles by MEASUREMENT what the validation argued**: a clock inside the pure builder reds the new guard while story 5.14b's, run alone on the same mutation, stays **GREEN** — `chrono::Utc::now()` is `E0599` here and `SystemTime::now()` is not, so *a feature flag guards a spelling, never the act of reading a clock*. **6b.4 has now been CODE-REVIEWED (three layers) and REPAIRED**: 0 decisions, 11 patches, 2 deferrals, 5 dismissed with the check that dismissed them. 🔴 **ITS TWO STRUCTURAL FINDINGS ARE BOTH ABOUT GUARDS AND ONE IS NOT THIS STORY'S**: the shared `templates()` helper used a FLAT `read_dir`, so an undefined class in `templates/sub/_partial.html` left the new stylesheet guard green — **and a `--accent-document` leak in a subdirectory left story 6b.1's amber reservation guard green too**, which is exactly the smuggling vector its own comment claims to be hardened against. Askama accepts `path = "sub/…"` today, so this is **the ordinary gesture of organising templates**. *Two independently-written guards, one shared helper, one hole* — fixed at the helper, M9 reds both with a green control. ⚠️ And the new guard's own sentence *"covered the day the file exists"* was **FALSE when written**, which here is the defect rather than the hole. 🔴 **M2's recorded carrier was MISATTRIBUTED**: the mutation flipped the handler's flag, which also flips `aria-pressed`, so the route test reddened on the toggle's STATE and never on the ORDER — and its fixture seeded ONE entity, where **an ordering is structurally unobservable**. *A mutation that changes two things attributes its red to whichever you were looking at.* 🔑 **AND THE SHARPEST FINDING IS ABOUT THE METHOD**: the Acceptance Auditor certified *"611 → 625 tests"* as ✅ CONFIRMED by recomputing `398+161+66=625` — the RIGHT-hand side — while the Blind Hunter, with **only the diff**, refuted it: the baseline is **614**, and 611 is story 6b.3's, reused. **The layer with repository access validated the false figure and the blind one caught it**, which is the argument for keeping that layer blind — and the figure is 6b.3's own defect reproduced two days later, in the story whose review had caught it. *Verifying a delta means verifying both of its terms.* ✅ **Refuted by measurement and recorded so nobody re-chases them**: no XSS (Askama's escaping holds against a script tag and an `onerror` img seeded in raw SQL), hostile `?sel=` values all answer 200, `?sort=bogus` stays off, the missing URL escape is **latent not live** (`entity_id` is a v7 UUID at every call site), and **the pending count is NOT the banned badge** — the ban names nav items, the shape shipped is what the UX spec prescribes verbatim, and nothing changes colour, weight or position as the number grows. ⚠️ **Registered rather than hidden**: the product has **NO connector registry at all** — no table, no name, just a UUID; the mock's *"UniFi"* was its fixture's invention — so the observed side shows a short labelled id (→ 6b.8); two entities on one `ipv4` render **indistinguishable rows**, and what the row needs is a NAME, which is grouping (→ Epic 6); O(N·M) is now **measured**, 0.13 s at 300 entities and **4.4 s at 2000**; `/gap` is embedded by no page; and 🔴 **issue #38 RECURRED TWICE** during the mutation runs, recorded with its pattern and **no cause named**. ⚠️ **What the operator can DO with this, asked on purpose**: select a row and toggle a sort — **nothing else**. No `<form>`, no button, no POST anywhere on the screen, and `POST /document-all` is called by no template in the product. It is *a better-lit dead end, by explicit decision and not by omission*, and it is the state of things between 6b.4 and 6b.4b. ⚠️ THE LIVE COUNT lives in the story file `6b-4-triage-screen-on-the-real-gap.md` — cited by reference, never carried here. ⚠️ **T7 was a real look, in French, against seeded rows — and STILL not a browser, for the fourth consecutive story.** Eight gates green, 28 fixtures, trap gate still RED 26/15/11, `opencmdb-core` byte-identical, no migration, `epics.md` and the UX spec not edited.

**Story 6b.4b is `done`** (PR #103 squash-merged 2026-08-19 as `20e854c` after a green CI run, code-reviewed first — three layers on a DIFFERENT model, each isolated) — **INSERTED at story 6b.4's validation, taking Epic 6b from twelve stories to thirteen** (`epics.md` NOT edited; registered). `/triage`'s detail pane gains the mock's **action bar**: five controls, **not one of them live**, each labelled *À venir* with **one** sentence saying why — Guy's premise (2) of 2026-08-13 made visible rather than argued. **625 → 629 tests** (402 bin + 161 core + 66 xtask). 🔑 **`enum Gesture { Planned { owner } }` — ONE variant, by Guy's arbitration taken OVER the contexting's own recommendation of a struct**, and **M7 measures why**: adding `Live` gives `error[E0004]: non-exhaustive patterns` on every `match`, forcing the revisit at exactly the moment story 6.4 needs it, where the struct would have forced none ever. ⚠️ **The promise is NARROWED in writing** (5.12's precedent): a labelling and typing DISCIPLINE, never a compiler-enforced guarantee — the validation established **by building both** that a route pointing nowhere reds nothing under either shape, because clippy's dead-code lint asks only whether a variant was instantiated *with any value*. The real closure — a route typed as a member of a CLOSED SET — is registered to story 6.4, where the set stops being empty. 🔴 **THE CODE REVIEW'S FOUR HIGH FINDINGS ARE ONE MISTAKE MADE FOUR TIMES: every guard this story wrote checks the SOURCE, and every defect lived in the RENDER.** An attribute assembled in Rust and emitted with `|safe` — an idiom already in this codebase — put a real native `disabled` in the served page with both guards **green**; a bare uppercase `DISABLED` slipped a case-sensitive matcher, and the render test's own `" disabled="` missed it too because **a boolean attribute has no `=`**; a **YAML block scalar** made the digit guard walk past *"Ce geste arrive avec la story 6.4, prevu au sprint 12"*, which then **rendered on all five controls in French with every guard green** — exactly what the arbitration forbids, reached by one ordinary valid YAML idiom; and neither guard could see `tabindex`, the DOM property their spelling was only a proxy for. 🔑 **ONE CHANGE OF LEVEL CLOSES ALL FOUR — assert on the rendered HTML and on the RESOLVED string, never on the template or the YAML file.** *A guard that reads the source measures what was written, never what was served, and the operator gets what was served.* 🔴 **AND THE FIVE CONTROLS WERE NOT KEYBOARD-REACHABLE AT ALL**: a `<span role="button" aria-disabled="true">` with **no `tabindex`** has `tabIndex -1`, refuses `.focus()` outright, and **forty dispatched Tab presses reach none of them** — so §0b's whole justification for refusing the native `disabled` (*"it leaves the tab order, the blind operator is not even told the gesture exists"*) **described exactly what shipped**. The validation had measured a `<button>`; a `<span>` was built. *A measurement taken on one thing and applied to another.* **All three layers reached it**, two by pressing Tab in Chrome 151, with the control that makes it mean something (`btn: tabIndex 0`, `spanNoTab: -1`, `spanTab0: 0`). 🔴 **The repair found TWO MORE only a browser could see**: the sentence moved out of a `title=` was right, but a copy inside each of the five controls turned a compact row into a stack saying the same thing five times — now **one visible `<p>` for the group with `aria-describedby` from every control, visible once and announced five times**; and **the first screenshot of that fix was a LIE because the BINARY WAS STALE** — `cargo test` builds the test target, not `target/debug/opencmdb`, so the running server still carried mutation M8's planted attribute and the capture showed the defect just fixed. 🔑 *A screenshot of a stale binary is not a look at your code* — **the third time in one day a measurement landed on the wrong artefact**, and the cheap defence that caught it is to **grep the artefact you are about to believe, not the source you just edited**. ✅ **Refuted by measurement and recorded**: subdirectory coverage holds, forcing `not_built: None` by hand IS caught by the render count assertions, a sixth gesture is caught, and the badge breaks none of the UX spec's seven hard bans. 🔑 **AND THE VALIDATION FOUND WHAT FOUR STORIES HAD ASSUMED: a browser was there all along** — `google-chrome` **151** and `firefox` are installed, and stories 6b.1 through 6b.4 each deferred typography, spacing and layout on a sentence nobody had run `command -v` against. *A limit believed is a limit unmeasured.* ⚠️ **Registered rather than hidden**: the *"labelled per 6b.3"* divergence (the right reading of the criterion — *per 6b.3* names the DISCIPLINE, not the literal partial — but presented as settled design instead of raised) → Epic 6b's retrospective; the unterminated-`{#` hole still standing in every other source-scanning guard; and story 6b.4's stylesheet guard, which **scans Askama comments and silently skips any `class="…"` containing a brace**. ⚠️ **The operator still cannot DO anything after this story** — five controls, none live. It turns *a screen with nothing to click* into *a screen that says why*, which is honesty and not reach. **Story 6.4 ends it, and the type will red until it does.** ⚠️ THE LIVE COUNT lives in the story file `6b-4b-action-bar-and-the-gesture-nature.md` — cited by reference, never carried here. Eight gates green, 28 fixtures, trap gate still RED 26/15/11, `opencmdb-core` byte-identical, no migration, no route and no write, `epics.md` and the UX spec not edited.

**Story 6b.5 is `done`** (PR #105 squash-merged 2026-08-19 as `b8a5795` after a green CI run, code-reviewed first — three layers on a DIFFERENT model, each isolated) — **the product has its first MIXED screen**: the dashboard carries story 5.14b's real reach section beside two labelled example sections, each with its own heading and its own marker, plus the `MAX(observed_at)` the register handed this story by name — **in the body, never the frame**, because `the_shell_shows_no_last_observation` bans it there and the mock puts it in the nav footer (a divergence registered, not followed). **629 → 634 tests** (407 bin + 161 core + 66 xtask). 🔑 **`Nature::Mixed` was taken on a MEASUREMENT rather than a preference** (Guy, 2026-08-19): adding the variant produces exactly **three `E0004` sites** — one production, two test — so the compiler forces both the marker decision and the pending-badge decision and nothing defaults silently. ⚠️ **A fragment-loaded alternative was BUILT at validation and refused on a measured cost**: a pool-free `/dashboard` fetching its reach by `hx-get` from a pool-bearing route works and was screenshotted working, **but the route-table partition asserts on ONE synchronous body** and a second request is one a `oneshot` client cannot drive — so AC1 would then need two coordinated assertions or a headless-browser test **not in CI**. 🔴 *The story's first draft said the screen "MUST" leave the pool-free router; that word was false, the conclusion survived, and the reason given for it did not — a decision explained by a false premise is one nobody can re-derive.* ⚠️ The cost accepted and narrowed in writing: for this one screen the compile-time refusal of `State<MySqlPool>` no longer holds; it holds for the eight that remain. 🔴 **THE COMMIT SHIPPED A RED SUITE AND THE CODE REVIEW FOUND IT**: `probed >= 9` was a hardcoded floor, making the dashboard `Mixed` added a SECOND nature the no-database branch skips, the count fell to 8 — **and the breaking edit was three lines above the assertion, in this story's own diff** — while the Completion Notes claimed *"0.05 s without a database"*, a figure carried over from the previous session's habit and **never run on this tree**. 🔑 **CI could not catch it, because CI supplies a database**: *a floor CI cannot check is a floor nobody re-reads.* The floor is now DERIVED from `Screen::ALL`. 🔴 **AND THE EPIC'S DOMINANT DEFECT CLASS RECURRED A FOURTH TIME — inside the guard written to close the third.** The section-marker guard compared `markers == sections` as **totals**, so two markers in the first example section and **none** in the second left the **entire suite green, that guard included**: *it could not tell "each section has exactly one" from "they happen to add up".* Now per-section, and the mutation reds. 🔴 **A REACHABLE CONTRADICTION THIS STORY CREATED**: an observation ingested but not yet resolved rendered *"Rien d'observé pour l'instant — lancez un scan"* **directly above** *"Dernière observation il y a 8 h"*, in one div. This story is what co-located the two populations for the first time, and **every test fed them from ONE fixture**, so the whole suite was blind to it; the page now says what the state is and the guard seeds them independently. 🔴 **M1 took FOUR attempts to red, for THREE different reasons**, each a distinct way for a guard to be worthless and none visible by reading: the driver had **no assert** so nothing was mutated; the fixture used `"matched"` where the engine's token is `"match"`, so `placed` rendered **0** and the oracle's `>2<` was satisfied by an **example stat card**; and the test helper built the rendered struct from the **un-composed** identity while the handler builds it from the view — *the guard rendered a shape production does not use*. Fixed at the root: the duplicated field is gone. ✅ **And M1b keeps the control**: the same sum guarded over the pure builder alone stays **GREEN**, reproducing story 5.14b's specimen exactly. 🔴 **The pass also DESTROYED WORK, third occurrence in this project**: the script mixed a scratchpad restore with `git checkout --`, and `app.yml` — in the git set, not the snapshot — went back to its last COMMIT, deleting nine uncommitted keys. *A file revert equals a mutation revert only on a committed baseline*, and the new half is the cause: **one mechanism per script, because two sets always drift**. ⚠️ The symptom exposed a THIRD *source-against-artefact* instance: a key can be in `app.yml` and absent from the BINARY, and `every_key_carries_both_locales` cannot tell — it reads the file, `t!()` reads the embedded copy. ⚠️ Also repaired at review: **six sites saying *"nine demonstration screens"* for eight**; a doc saying *"three variants and not two"* six lines above the fourth; a comment claiming *"static class literals throughout"* **falsified by line 46 of its own file** (`spark-h{{ height }}`, with `.spark-h8` missing — **epic constraint 5 live**); the `"matched"` token recurring sixty lines below the comment explaining it; a dead `.clone()`; an unscoped anti-sum oracle; and **M3's recorded mechanism, which was wrong** — not *"the route disappears"* but **26 red, all `Overlapping method route`**, naming a real coupling between a **hand-written** route and a **derived** exclusion that nothing ties. 🔴 **THE SHARPEST FINDING IS DEFERRED, and it is the story's own stated risk**: **the example half is VISUALLY DOMINANT over the honest one** — fabricated cards at 22 px mono with sparklines against real counts in a body-size pill, so *"37 / 4 / 2"*, invented, are the loudest thing on the screen and *"2 sightings placed"*, real, is the quietest. **That is exactly what *"so that the honest part is not diluted by the demonstration around it"* exists to prevent, reached WITHOUT violating a single criterion** — the criteria cover presence and marking, the defect is **salience**, and no text guard can measure salience. → Epic 6b's retrospective for a criterion, 6b.12's sweep for the pass. ⚠️ **And AC2's *"the history a sparkline draws does not exist"* is OVERSTATED**: four temporal columns persist. What does not exist is a **deduplicated** history — nothing supersedes an abstention, so a curve over the raw rows would climb with every scan whatever the network did, which is the growing counter the UX spec bans. Right in substance, wrong in its stated reason; registered. ⚠️ THE LIVE COUNT lives in the story file `6b-5-dashboard-real-reach-beside-examples.md` — cited by reference, never carried here. Eight gates green, 28 fixtures, trap gate still RED 26/15/11, `opencmdb-core` byte-identical, no migration, `epics.md` and the UX spec not edited.

**Story 6b.6 is `done`** (PR #107 squash-merged 2026-08-19 as `7cc1602` after a green CI run, code-reviewed first — three layers on a DIFFERENT model, each isolated) — **the inventory reaches the mock's shape and the device record gets a real address**, on the stable slugs story 6b.3 minted for it. **634 → 653 tests** (426 bin + 161 core + 66 xtask). `page.rs` **1575 → 1524** of the 2000-line ceiling — the split into `example_screens.rs` was done **before** the gate asked, which `CLAUDE.md`'s *"split, not grown"* requires and which no prior story in this epic had honoured in advance. 🔴 **THE STORY'S CENTRE WAS AC2, AND ITS FIRST DRAFT WAS WRONG ABOUT IT THREE WAYS** — the fact-check layer refuted them and the corrected findings are sharper, so the refutation is KEPT in §0 rather than overwritten. The canonical glossary **exists**, in three places (`ux-design-specification.md:1332`, `prd.md:985`, the user manual's appendix); the draft established its absence by looking in **two directories**, the exact defect story 5.13b shipped when it reserved a UUID prefix by enumerating one directory and concluding about the tree. 🔑 **What replaced it: the glossary is BINDING and carries not one of AC2's five words, because its eleven rows are all GESTURES** — `observed`, `declared`, `gap`, `reconcile`, `document`, `accept-gap`, `snooze`, `attach`, `exclude`, `triage`, `source` — where AC2 names what an object **IS**. *The product had a binding vocabulary for its verbs and none for its nouns.* **And the table's own preamble is AC2's warning with one word changed** (*"if a **gesture** is named after an operation we forbid…"* → *"if a **state**…"*): that one-word edit IS the criterion. ⚠️ **Extending a binding artefact is a PLANNING act, so Guy took it** (2026-08-19), adding five rows to **both** documents — and two things fell out of writing them. 🔑 **The five ARE Guy's three-case taxonomy of 2026-08-12, exactly and without forcing**: *no ambiguity → the software decides* (`concordant`, `gap`, `conflict`) · *ambiguity → the operator lifts the doubt* (`ambiguous`) · *unknown → the operator creates the entity* (`undeclared`); a sixth state fitting none of the three would mean the taxonomy changed, not that the table is short. And **`gap`/« écart » is the SAME binding pair as the gesture axis**, which settles the story's open fork — *one term, one meaning, ONE key* — where minting a `state.ecart` beside `triage.kind.ecart` would have broken UX-DR64's *"glossary uniqueness"*. 🔴 **THE GLOSSARY CHECK REDDENED ON ITS FIRST RUN, ON THE PRODUCT'S CORE TERM**: the binding table says `gap`/« écart » and the English UI said **"Drift"** at four sites — on the one term the table calls *"the core object; the product"*, under a preamble forbidding synonyms in so many words. The French was already right everywhere and the code already said `gap`; **the sole test pinning it asserted `contains("Drift")` under a message reading *"a **Gap** must be a row"***. *A guard written to satisfy a criterion found a two-story-old defect in its first second of life.* 🔴 **FOUR MORE DEFECTS WERE FOUND BY LOOKING AT THE PAGE IN A BROWSER**, each green through the whole suite, eight gates and clippy first: two headings resolved `devices.unplaced_*`, keys that do not exist, so the page rendered **its own key names** (`rust-i18n` renders an unknown key verbatim, and `every_key_carries_both_locales` asks whether keys *in* `app.yml` have two languages — a key absent from the file is not in its population at all); the record's *Rôle* row printed `example.role.storage` **as a value**, invisible even to the guard just written; `/devices?kind=printer` served **all eight devices**, the route's closure taking no argument while the filter test called the pure builder — Epic 5's dominant class, story 6b.4's `triage_html` shape — ⚠️ **and the fix itself missed**, the parameter threaded through three signatures with the arm still passing `Default::default()`, because **Rust does not lint an unused function PARAMETER**; and the record shipped **four identical marker banners** down one page, story 6b.4b's finding reproduced, invisible because every marker guard asks whether the marker is PRESENT. 🔑 **The fourth resolved into the story's best structural change, by reading story 6b.3's own rule precisely**: the marker goes on the smallest unit that is **entirely** example — the SECTION on a `Mixed` screen, the SCREEN on an `Example` one — and it is now emitted by the **DISPATCH** rather than included by each template, so it comes from the same `match` arm as the body. ⚠️ **That immediately exposed the record route**, off `Screen` and bypassing the dispatch, which served four sections with no marker at all until the partition test named it: *the skip in `router` buys structural safety on one axis and costs it on another*. 🔴 **`/devices/{id}` IS THE FIRST ADDRESS `Screen::ALL` STRUCTURALLY CANNOT REPRESENT**, because `Screen::href` returns a `&'static str` used as a route PATTERN and FETCHED as a URL. Guy chose **off `Screen`** over a variant that compiles without a warning while shipping a literal `href="/devices/{id}"` in eleven navigations that every existing guard accepts; the gap-hunt had BUILT both and measured the alternative leaving **636 tests and clippy green** with a handler ignoring its slug. **THE THREE-LAYER REVIEW then produced its own three findings, and the pattern of which layer found what is the transferable part.** 🔴 **The BLIND layer — the diff alone, no repository, no build — found both HIGHs.** `page.rs` was **1524**, not the **1482** written in flight (true for twenty minutes, stale the moment `example_marker()` landed, never re-measured — *a number written in flight, in the story whose own §0 warns about numbers written in flight*), and it caught it from the diff's own hunk arithmetic **while two layers had the tree**. It also refuted *"a screen declared `Example` cannot render content without the marker"* with **a counterexample shipping in the same commit** — `Screen::Device` is `Example` and is served outside that `match`. And it named the most likely production break: **`Screen::Device.href()` is the literal `"/devices/nas-01"` with nothing tying it to the dataset**, so renaming that device would degrade the product's own primary link into *a correct 200 for a wrong reason* — 🔑 *reached precisely because it could not check the two against each other, which is the question having both in front of you does not prompt.* 🔴 **The ACCEPTANCE layer's NON-REPRODUCTION was itself the finding**: M2 was recorded as `RED ×3` under the description *"a literal key that does not exist"*, and it got 2 or 4 with other keys. Re-run with the exact key, it reproduces at 3 — **the defect is the row, which named a mutation CLASS and reported one key's count**. M2b is what that earned: a key no block assertion reads reds **one** test, so *the redundancy M2 appears to show is an artefact of the key chosen, and for most keys the literal-key guard is the SOLE carrier*. 🔴 **The EDGE-CASE layer measured Epic 5's dominant class INSIDE a guard this story wrote**: `router()` skipped **any** href under `/devices/`, so a future screen there compiles with **zero errors and zero warnings**, its render arm goes silently dead, and the address serves the *unknown device* page — ⚠️ **and the three tests that reddened were all bookkeeping COUNTS whose messages read *"update this number"*, which a developer follows, leaving 652 tests, clippy and eight gates green over a screen serving the wrong body.** Closed two ways, because the skip and the blindness are separate faults: the skip now names a **real device**, and the guard nobody had written — ***the body a screen SERVES matches the content it DECLARES***, an exhaustive `match` so a new `ExampleContent` variant cannot compile until someone says what its screen must show. ⚠️ Two more from the same layer: `keyish()` required lowercase segments, so a capitalised key rendered live with the suite green — **and the control it kept is what sizes it**, the same key lowercased WAS caught, so the blind spot needed a non-literal construction AND a capital; and the marker oracle counted the badge's **text** as a plain substring, which an unresolved key containing *"Example"* inflated to 9 — *an oracle that counts a word counts every word that contains it*, now counting the badge's CLASS. ⚠️ **`opencmdb-core` is NOT byte-identical, and the claim was narrowed rather than defended**: one doc line there defined a `Gap` as *"a drift"* — the retired synonym, as the DEFINITION, in the domain crate — and **the promise of non-modification is exactly what would have sheltered it**, story 5.13b's finding live again. Narrowed to *no BEHAVIOUR change*. **Seventeen mutation rows: thirteen at implementation, four added by the review, and the table says which** — fifteen reds, one **compiler-carried** (once the marker left the templates, `ExampleStrings` stopped carrying `example_badge`, so **a double marker became unrepresentable in the type**), one **GREEN by measurement** (`values_are_keys` set on a factual field changes nothing, because `t!` renders an unknown key verbatim — *the flag protects one direction only*, and its doc says so). ⚠️ **Two rows first came back GREEN and both were the DRIVER, not a weak guard** — a `sed` replacing a string with itself, and a `sed` with `\n` in the pattern, which GNU sed does not match across lines; caught because `RED=0` contradicted a prediction, and had the prediction been *"green"* both would have been filed as confirmations. *The mutation driver lies*, fourth epic running; the driver now exits non-zero when the mutation fails to apply. ✅ **The suite was run BOTH ways** — **0.04 s** without `DATABASE_URL` and **4.77 s** against a live `mariadb:10.11` on port **13316**, the clock being the tell that the database-backed tests genuinely executed; ⚠️ port **3306 holds another project's container** and was avoided rather than discovered. ✅ **T7 was a real browser look** (Chrome 151, 1280 px, French, on rebuilt binaries), the second story in a row to have one. ⚠️ **What the operator can DO with this, asked on purpose: look, filter, open a record, go back.** No form, no button, no write on either screen — which is what AC3 buys (*"the frame is bought here, the content is not"*) and which now makes **four** well-lit dead ends, a count Epic 6b's retrospective owes a look. ⚠️ Registered rather than fixed: the queue's `kindLabel` axis (`Absence`, `Nouveau`) is **still outside the binding table** and reconciling it rewrites story 6b.4's shipped copy; `Ambiguous` and `Undeclared` are **OBJECT states, not field states**, a distinction the glossary does not yet carry; `criticality`, `app` and `owner` are in the mock and deliberately **not rendered**, being three more nouns with no glossary row _(a DATED record: story 6b.7 RENDERS owner and criticality on 2026-08-20 under Guy's option (c), and registers all five nouns with their value sets; this sentence read as present until then)_; and *"a needle spelled `class="…"` breaks when a second class is added"*, which silently unmatched four of story 6b.5's dashboard guards. ⚠️ THE LIVE COUNT lives in the story file `6b-6-inventory-and-device-record.md` — cited by reference, never carried here. Eight gates green, 28 fixtures, trap gate still RED 26/15/11, no migration, `epics.md` and the UX spec **not** edited (the glossary edit is Guy's, in `prd.md` and `ux-design-specification.md`).

**Story 6b.7 is `done`** (PR #109 squash-merged 2026-08-20 as `f32c268` after a green CI run, code-reviewed first — three layers on a DIFFERENT model, each isolated) — **the two frames Epics 14 and 15 will fill**: `/apps` and `/ipam` leave `Nature::Empty` for an applications table and a 256-cell CSS-Grid occupancy map. **653 → 668 tests** (441 bin + 161 core + 66 xtask, after the code review). 🔑 **The two screens describe ONE network and it is ASSERTED rather than arranged**: every application's host names a device the inventory ships — seven of the mock's eight named machines that exist in no dataset — the office subnet is the `192.0.2.0/24` the devices already live in, and the address conflict sits at `.41`, `vm-billing`'s address, which the grid draws as occupied. 🔴 **A /24 carries 256 ADDRESSES and 254 HOSTS, and the mock conflates them**: it loops `0..256`, draws `.0` and `.255` as ordinary free cells, and its *next free address* panel then names the NETWORK address — reproduced on a real build at the validation. So `CellState` has a fourth variant and the occupancy line counts over the hosts; ⚠️ **the story's own first draft prescribed a test asserting the counts *"sum to 256"***, which would have pinned the defect as the expected behaviour. 🔴 **AC1's `aria-label` per cell is incompatible with the mock's `role="img"`** (*Children Presentational: True*), and `aria-label` on a bare `<div>` maps to `generic`, where **ARIA 1.2 prohibits it** — an axe-core violation, not a keyboard inconvenience. The grid ships as a `<ul role="list">` laid out by CSS Grid, `role="list"` explicit because `list-style: none` drops it in Safari/VoiceOver; *reserved* is a **hatched pattern** because three states told apart by colour alone is WCAG 1.4.1. ⚠️ **The `role="img"` half is REASONED and not measured** — Chrome's accessibility tree still shows the children, the pruning being a platform/AT mapping — and it is written as such. 🔑 **GUY'S THREE ARBITRATIONS OF 2026-08-20, each recorded with the option refused**: **option (c)** — render **owner and criticality**, NOT `exposure`, and register the five glossary-less nouns (`application`, `owner`, `criticality`, `exposure`, `host`) with their **three value sets**; extending the binding table was refused as **PREMATURE, not wrong**, on the measured cost that it is five rows *plus three value SCALES*, and a scale is a new KIND of row in a table whose every row reads *one concept, one translation* — **Epic 15 is where that closure belongs**. **Criticality is an i18n KEY, owner values are DATA** (measured: as literals, `/apps` renders five French words under an English UI with the whole suite green — story 6b.6's `role_key` defect verbatim). And the IPAM panel is titled **« Conflit d'adresse »**, qualified on the *"Écart · 2 champs"* precedent rather than minting a term, because the glossary's `conflict` already means *source against source* and `prd.md:988` forbids one word carrying two meanings; ✅ measured invisible to every existing check. ⚠️ **`host` is rendered anyway and registered with the other four**: an application that runs nowhere is not an application. 🔴 **TWO MUTATIONS CAME BACK GREEN AND BOTH REFUTATIONS ARE THE DELIVERABLE.** `state_of` tests `used` before `reserved`, so an octet in BOTH lists renders silently as *used* — a deliberate corruption of the dataset changed no cell, no count and no test; *a priority order is a rendering decision and must not double as a repair*. And **THE MUTATION DRIVER LIED BY FAILING TO *UN*APPLY**: `shutil.copy2` preserves the original mtime, so restoring a template left cargo seeing a file older than the artefact that embedded it — askama compiles templates INTO the binary — and **two full suite runs reported one test red over a clean `git status`**. ⚠️ Caught only because a failure contradicted a just-measured green; as a *pass* it would have been filed as a confirmation. *The mutation driver lies*, fifth epic running, in a form the earlier ones did not have. **Ten mutations, ten reds, carriers named per row.** ⚠️ **An assertion order was measured wrong**: under the `aria-current` mutation the test reddened on *"exactly one tab is in force"* while the assertion written for that defect was never reached — story 5.13's finding, met a fifth time; the negative now comes first. **THE THREE-LAYER REVIEW: 5 patches, 1 finding REFUTED with its check, 0 arbitrations.** 🔴 **The BLIND layer — diff only, no repository — found the HIGH again, in the code this story wrote to close a defect of the same family**: rewriting the key guard changed its premise counter from *text words* to `html.split_whitespace()` — **raw markup**, so every `href`, `class` and tag name counted as an inspected word — under a message still promising *"at least two hundred rendered words were inspected"*. The detection was intact; **the floor had stopped measuring what it names** and was satisfied by markup whatever the page said, ⚠️ while the neighbouring guard **in the same commit** warns that *"a floor is only a guard while it is near what is there"*. **Second story running where the layer with no repository access beats two that have it.** 🔴 **The AUDITOR refuted the story's own AC6 headline by reading the tree**: *"three inherited guards are now properties"* is FALSE — **the named marker guard was never edited**. A property was ADDED beside an enumeration left standing, and *superseded is not converted*. It is kept, and now says what it really owns: *the body carries no marker of its own*, which the route-table guard **cannot express**, since it sees only the served page where a template-side marker and the dispatch's are indistinguishable. ⚠️ **A ticked task's either/or was discharged in NEITHER branch** — the planned *Réserver* control, absent with no sentence saying why; the decision is now written at the site with its cost (`/triage` shows five dead controls and says why, this screen shows none, and the two stay inconsistent until Epic 14). ✅ **REFUTED with the check**: the auditor's *AC4 NOT MET — no register rows* is an **artefact of the review scope I gave it**, the diff handed to the layers being `crates/` only; the eleven rows are there, each with a named owner. 🔑 *A layer asked to audit registrations must be handed the file they live in* — the lesson is the reviewer's, not the layer's, and it is registered. ✅ **The mutating layer measured EVERY guard and found none green** — nine plants, nine reds — plus a hostile query battery on both routes (duplicate keys answer **400** from axum's own extractor; an unknown slug, a NUL, a traversal string, a 10 000-character value and a `<script>` all answer 200 and **nothing is reflected**), no `|safe` in either template, and the locale decision applied consistently in **both** languages. ⚠️ **One gap closed and one limit STATED**: no committed subnet is full, so `ipam.next_free_none` was **unreachable, not merely untested** — the function is now guarded against a synthetic subnet with a control that the committed three are not full, but **the RENDER of that sentence is carried by nothing**, and putting a full subnet in the dataset to exercise one line would be shaping the demonstration around the test. 🔴 **What the browser look found and this story does NOT fix**: **the applications table shows a gap and does not name it** — *Nextcloud 28.0.4 · 29.0.1* beside *Sage 50 2024.1 · 2024.1*, nothing distinguishing them, on the product whose founding sentence is *"the gap is the product"*. The mock does the same and AC2 asks only for owner and criticality, so fixing it here would be inventing scope; the state vocabulary exists and this is the first screen that displays a divergence without using it. **Registered, owner Epic 15.** ⚠️ **What the operator can DO: look, switch subnet, follow a link.** No form, no button, no write — **six well-lit dead ends now**, a count Epic 6b's retrospective owes a look. ⚠️ THE LIVE COUNT lives in the story file `6b-7-applications-and-ipam.md` — cited by reference, never carried here. Eight gates green, 28 fixtures, trap gate still RED 26/15/11, `opencmdb-core` byte-identical, no migration, no new address, no write, no new dependency, `epics.md` and the UX spec not edited.

**Story 6b.8 is `done`** (PR #111 squash-merged 2026-08-20 as `b18495a` after a green CI run, code-reviewed first — three layers on a DIFFERENT model, each isolated) — **THE EPIC'S FIRST WHOLLY REAL SCREEN.** `/sources` leaves `Nature::Empty` for a screen whose entire content is `FactKind::ALL` minus the ARP/ping connector's own declaration — **five kinds it cannot see, derived at runtime with no database, no fixture and no invention** — and which carries **no example marker, because there is nothing to mark**; `/alerts` becomes an example screen on **FR30's three named alert kinds** rather than the mock's invented severities. **668 → 676 tests** (449 bin + 161 core + 66 xtask, after the code review). 🔴 **THE STORY PLANNED `Mixed` AND THE CODE SHIPPED `Fed`, corrected while building rather than after**: AC3 puts the alert list on its OWN screen, which `Screen::ALL` has carried since 6b.2, so every section of `/sources` is real and it owes no marker at all — declaring it `Mixed` would have made the partition demand a marker for example content that does not exist. 🔑 *A nature is a statement about what the screen SHOWS, and the screen turned out simpler than the plan for it.* 🔴 **AC2 SHIPS MET ON ONE HALF AND NOT MET ON THE OTHER, and that is WRITTEN rather than ticked**: the product can establish *an observation arrived at T* and cannot establish *this source is blind*, so the liveness axis carries **no colour at all** — deriving `blind` from silence would read a clock and INVENT an incident, which FR8 exists to forbid. ⚠️ **The UX spec prescribes a colour for BOTH liveness values** (`:1113`), so shipping none is a **DIVERGENCE, registered as one**; the incident axis is named to **Epic 13**, the owner `score.rs`, `connector/mod.rs` and `epics.md` all already carry. 🔴 **FR7 IS COVERED ON ITS STATIC HALF ONLY, and the dated descriptor was being THROWN AWAY**: `PollSummary { capabilities, scopes_covered }` — FR7's dated descriptor and FR5's liveness unit — is computed on every scan and was discarded at `scan_pass.rs`'s seam by an `if let Err(...)` that legitimately drops the `Ok` payload, **with nothing the compiler could say**. It is now bound and traced. 🔑 **Persisting it was REFUSED with its PRECONDITION named** (Guy, 2026-08-20): a per-source descriptor cannot be stored without a stable source identity, and `connector_id` is **minted fresh at every boot**, so a table keyed on it would grow by a row per restart — **Epic 11** for the identity, then **D32 / Epic 13** for the row-per-scan question. *A register row that names its precondition is actionable; one that says "later" is not.* ⚠️ **Three arbitrations by Guy, each recorded with the option refused**, and the second was taken on a finding that changed the recommendation while it was being written: **`page::source_label` renders the top 32 bits of a v7 UUID's millisecond field**, so the operator-visible source id is a **clock reading that rolls every ≈65 s** and two different sources can share it — measured over three live boots. The story names its source from the connector's own code (a TYPE name, since the product has one connector of one kind and **no notion of a source INSTANCE at all**) and 6b.4's register row is **narrowed, not closed**, struck through rather than deleted. 🔴 **THE VALIDATION REFUTED TWELVE CLAIMS OF THE STORY'S FIRST DRAFT**, including its central premise — *"`blind` exists nowhere: no type, no function"* is FALSE (`pub enum SourceState {}` exists, uninhabited, naming Epic 13; `is_blinding()` exists) — and its distance to the epic that owns a source registry (**Epic 11, v0.9**, not Epic 17, v0.15: **Guy weighed an option against a distance six epics too far**). ⚠️ It also measured that **the prescribed liveness reader cannot tell FOUR states apart** — never scanned, scanned and nobody answered, an INVALID perimeter the product refused, a blank one — all NULL, on four live boots; the screen states the ambiguity rather than picking a reading. **THE THREE-LAYER REVIEW: 8 patches, 0 arbitrations.** 🔴 **The BLIND layer — the code diff and nothing else — found BOTH HIGHs for the THIRD STORY RUNNING, and both were the author's own sentences**: a module doc saying this story made `Sources` `Mixed` **twenty lines below the arm that sets it to `Fed`**, in the commit whose headline corrects the same slip elsewhere; and a doc claiming the `_` arm *"falls back to the kind's `Debug` name"* where the code returns a fixed generic pair. 🔑 **From that second false sentence it DEDUCED, with no repository access, a hole neither sighted layer named**: `FactKind::ALL`'s cross-crate guard pins the CONSTANT against the enum's declaration and says **nothing about the key map**, so an eighth kind correctly added to `ALL` satisfies the guard and **still renders *"Unrecognised kind"*** — *a guard placed where the defect cannot occur, one field over.* 🔴 **The MUTATING layer found the one defect only a BOOT could show**: with `OPENCMDB_SCAN_CIDR=nonsense` the product logs `ERROR invalid OPENCMDB_SCAN_CIDR — skipping scan` and `/sources` rendered *"Périmètre nonsense"* as an **IN-FORCE value** under the generic ambiguity sentence. ⚠️ *That is stronger than the ambiguity the story registered*: **we cannot tell which of four** is one thing, **we are showing a configuration we already refused as though it were live** is another. Closed with the connector's OWN parser, so screen and scan cannot disagree; the perimeter is **still shown**, because the operator must see what was rejected. 🔴 **The AUDITOR caught two numbers pretending to be one** — M6 is **18 without a database and 30 with one**, while the table said 30 and §0e said 18, neither naming its condition — plus **a ticked task that delivered nothing** (`not evaluated`, the spec's binding word for an out-of-capability field, **absent from the whole tree** in both languages, measured independently by two layers) and **a register row left silently FALSE at its origin**. ✅ **Every guard the story claims was measured and fired**, including both halves of the ordering hazard: **0 red locally and exactly 1 in CI** for the silent one (a nature changed with its route forgotten is a 404 the local suite cannot see), **18** for the loud one. **Ten mutations, ten reds, carriers named per row.** ⚠️ **What the operator can DO: nothing writable** — no form, no button, no POST — which makes **eight** well-lit dead ends. 🔑 **But it is the first screen that tells them something TRUE and USEFUL they did not know**: that their scanner cannot read hardware addresses, and therefore why the product abstains. *Not a gesture, and not decoration either — the first honest answer the product gives to "why is it not doing more?"* ⚠️ THE LIVE COUNT lives in the story file `6b-8-sources-and-alerts.md` — cited by reference, never carried here. Eight gates green, 28 fixtures, trap gate still RED 26/15/11, no migration, no new dependency, `epics.md` and the UX spec not edited. ⚠️ **`opencmdb-core` is NOT byte-identical** — `FactKind::ALL` lands there — and the claim is narrowed to **no BEHAVIOUR change**, on story 5.13b's finding that *a promise of non-modification protects behaviour and shelters false sentences*.

**Story 6b.9 is `done`** (PR #113 squash-merged 2026-08-21 as `81589ff` after a green CI run, code-reviewed first — three layers on a DIFFERENT model, each isolated) — **the product's report about itself, and the LAST screen that held nothing.** `/diagnostic` leaves `Nature::Empty` for **seventeen rows across four groups, every value measured at runtime**, and carries **no example marker because nothing on it is an example**; `/commissioning` becomes the last example surface. 🔑 **`Nature::Empty` IS DELETED** — the epic's ten screens hold either real content or labelled example content, and **none holds nothing**. ⚠️ The compiler named only THREE of the eight artefacts; the other five (a template, two orphan keys, two `Strings` fields, two CSS classes) came off a checklist, **itemised in the code rather than counted**. **THREE ARBITRATIONS (Guy, 2026-08-21), each with the option refused.** 🔴 **(c′), and the story's OWN first recommendation was the defect**: it proposed a *last scan* row fed by `MAX(observed_at)`, refuted **on a running binary** — a scan RAN and SUCCEEDED and that maximum did not move. The honest value already existed and was being discarded (`PollSummary::capabilities.as_of`, bound by 6b.8), never mentioned in the draft; it is now measured inside `poll_ingest_resolve` and scoped on screen to *"depuis ce démarrage"*. 🔴 **(b) the security group ships as a SHAPE**: three mock claims planted as a **French literal** left 679/679 and eight gates GREEN — *a literal is not a key* — so `security_rows` takes a `SecurityPosture` and nothing else, the public row is **DERIVED by probing `auth::is_public`**, and the credentials are `bool`s, which turns the prescribed leak guard into a compile property. ⚠️ **`OPENCMDB_METRICS_TOKEN` moved into `AppConfig` — that REMOVED a reader** and retired the crate's **last three `std::env::set_var` calls in tests**. 🔴 **(a) the rename** to `Screen::Commissioning` / `/commissioning`, on the UX spec's F11 correction — ⚠️ **the draft's stated reason was FALSE and was corrected**: the visible label already read « Mise en service », so only the URL and the identifier carried the retired framing. 🔴 **FOUR OF THE STORY'S OWN GUARDS CAME BACK GREEN**, each correct about what it tested: an oracle counting a word wherever it is, a guard on the BUILDER against a defect in the RENDER, an enumeration of whitespace defeated by a **newline**, and an oracle comparing against the constant it guards. ⚠️ M6 was green **twice**. 🔴 **AC7 WAS DEFEATED BY A TIMEOUT NOBODY CONSIDERED** — the page rendered without the store **after thirty seconds**, sqlx's default acquire timeout, surfaced only because the test itself took thirty seconds. 🔴 **A CLAIM INHERITED FROM THE VALIDATION WAS REFUTED BY BOOTING**: `EnvFilter` does not *discard* `notalevel`, it installs `notalevel=trace` (a TARGET); the conclusion survives in a stronger form, since `opencmdb=nope` and `""` collapse to **`error`**. ⚠️ **The mutation driver lied in a NEW way**: a batch edit writing at the END lost three repairs to one failed anchor, silently — caught only because the re-run **contradicted a prediction**. **Eighteen mutations: seventeen reds and one compile refusal, replayed in full.** **THE THREE-LAYER REVIEW: 11 patches, 10 register rows, 0 arbitrations.** 🔴 **THE BLIND LAYER FOUND BOTH HIGHs FOR THE FOURTH STORY RUNNING**, both the author's own sentences: **AC7's guard could not REACH the branch it exists for** (the pool's `acquire_timeout(150 ms)` sits under the 2 s budget, so the timeout arm was dead code under test — and the test's own comment said so *in the language of a justification*), 🔑 **the epic's dominant class a FOURTH time in one story and the FIRST instance no mutation caught**, because a mutation can only red a guard that executes the mutated code; and **the screen has SEVENTEEN rows where five documents said sixteen**, with nothing pinning the total. 🔴 **THE ACCEPTANCE LAYER FOUND THE REGISTER UNTOUCHED** — `deferred-work.md` unchanged since 6b.8 while the story says *"register"* twice and carries a section headed **REGISTERED RATHER THAN FIXED**. 🔑 *A section that says "registered" is not a registration, and nothing in the story could tell the difference.* Ten rows now written. 🔴 **THE EDGE LAYER FOUND A PROJECT-WIDE HAZARD: `app.yml` is INVISIBLE to Cargo's incremental build** — changing only the translation file rebuilds in **0.07 s** and the new string is **absent from the binary**. ⚠️ CI caches `target/`, and **any mutation editing `app.yml` alone measures nothing**; story 6b.10 is the first whose subject IS that file. ⚠️ Numbers corrected by the auditor: **284** keys not 285, **+63** not +60, seventeen rows not sixteen. ⚠️ **AC10's browser look was NOT done by the implementation** (curl plus text extraction, while T9 was ticked citing Chrome); the review's edge layer supplied the real one and it is recorded under that layer. ⚠️ **`git checkout --` destroyed an uncommitted repair during the review pass — FOURTH occurrence in this project, in the story whose review had just named the class.** ✅ **AC7 reproduced end to end on a real database paused mid-session: 200 in 2.002 s.** ⚠️ **What the operator can DO: nothing writable** — **ten** well-lit dead ends now. 🔑 But it is the second screen telling them something TRUE they could not learn otherwise: which build they run, whether their schema is current, that their instance is closed by default, where their logs went, and whether the scan they configured has ever run. ⚠️ THE LIVE COUNT lives in the story file `6b-9-self-diagnostic-and-commissioning.md` — cited by reference, never carried here. Eight gates green, 28 fixtures, trap gate still RED 26/15/11, `opencmdb-core` untouched, no migration, no new dependency, `epics.md` and the UX spec not edited.

**Story 6b.10 is `done`** (PR #115 squash-merged 2026-08-22 as `d40a613` after a green CI run of 1m09) — **CODE-REVIEWED (three layers) and REPAIRED first, 2026-08-22: 5 arbitrations by Guy, 27 patches, 1 deferral, 3 dismissed with the check that dismissed them** — **the copy, FR and EN, and the story whose scheduled deliverable had already been written.** 🔴 **THE CENTRAL MEASUREMENT: `epics.md:2296` budgeted *"32 keys today against roughly a hundred in the mock"* and it is STALE** — measured per merge commit, `app.yml` went **31 → 284** across Epic 6b's TEN story files (253 added against a comparable estimate of ~68, ≈3.7×), **and every key already carried both locales**. AC1's letter was met before the story opened. ⚠️ The per-commit table's first draft **skipped story 6b.7** (`f32c268`, 184 keys), making 6b.8 look like a 68-key story where it added 37 — *an enumeration written from memory rather than run*, in the story about figures that stopped matching what is there. **697 → 727 tests** (489 bin + 161 core + 77 xtask, after the review), **NINE gates green**, `app.yml` **287 keys**, suite re-measured at the review with the command named — `cargo test --workspace --locked`, wall clock, warm: **0.67 s** without a database and **7.54 s** against a live `mariadb:10.11`; the record's *"1.79 s / 5.75 s"* was reproducible on no tree and named no command, and *a timing with no command behind it is a recollection wearing a decimal point*. 🔴 **WHAT NINE STORIES COULD NOT PRODUCE IS A READING OF THE ENGLISH COLUMN, WHICH IS THE DEFAULT LOCALE** (`DEFAULT_LOCALE` near `main.rs:180`, applied by `set_locale` at `:471` — the twins cited `main.rs:373`, true of the baseline and FALSE in the commit that carried it): every browser look in this epic was `OPENCMDB_LOCALE=fr`. And the sweep found the proof on the product's primary control — **`gesture.merge` rendered `"Merge"` in English on `/triage`'s main button**, the one word the binding tables retire by name (*"it names the forbidden operation — the founding pillar is linked, never merged"*), while the French « Merger » was right all along. **Story 6b.6's *"Drift"* defect, one story later, on a different term.** 🔴 **BOTH OF THE STORY'S CENTRAL ARGUMENTS WERE REPLACED BY THE VALIDATION (twelve HIGH findings across two layers), AND THE SECOND WAS INVERTED.** The design was *"two carriers separated by YAML syntax"*: **both halves wrong** — the syntax question has a one-line answer (`yaml-rust2` was already in `Cargo.lock` via `config`; the naive parse misses **7 of 12** legal shapes) and the row picked to prove the carriers independent, a block scalar, **reds BOTH** under a real parse. **Measured with a control, what separates them is the FALLBACK**: a word retired in `fr` in a key with **no `fr` half` → carrier 1 (the gate, reading the file) **GREEN, announcing its own success over it**, carrier 2 (the resolver) **RED**; a retired word in a key NAME → carrier 1 RED, carrier 2 GREEN. And the framing was *"the English UI has never been looked at"*: **INVERTED** — 241 `t!(` call sites and **ZERO** `locale =` overrides, so every render test runs in English. **English carries ~470 assertions and no human look; FRENCH carries nine human looks and no assertion**, measured by setting `nav.dashboard`'s `fr` value to its own key name and watching 702 tests and nine gates stay green with the guard written for that defect blind to it. 🔴 **FOUR OF THE STORY'S OWN GUARDS OR MUTATIONS CAME BACK GREEN, none reachable by reading.** **M18 twice**: deleting `completeness()` from the gate reddened nothing (every test attacked the helper — story 5.12's finding reproduced inside the gate written with that lesson in hand), and the end-to-end test written to close it **failed to close it**, because its oracle was `contains(A) || contains(B)` and the two refusals mask each other — ***a guard covered only by a disjunction is a guard nothing covers***. **M9** was a no-op today (287 *is* today's count); **M9-bis** (constant + one new key) reds and **M9-ctl** (derived + the same key) stays green — *a mutation whose prediction has two halves measures nothing until both are run*. The two-carrier agreement guard reddened on a **legitimate** line (`"merger"` is a NEGATIVE CONTROL in the gate) — *an unbounded needle cannot tell a denylist entry from a mention of one*; and the 500-literal source scan reddened on **its own source**, bounded now to the code half. 🔑 **GUY'S FIVE ARBITRATIONS (2026-08-21), and in FOUR of them the recommendation had been changed by the validation**: **(1)(a)** `gesture.merge` → `gesture.document`, EN *"Document"*, FR « Merger » untouched — ⚠️ exactly ONE test broke, and it carried **both** retired terms in one line (`assert_eq!(…, "Merge", "a drift offers Merge")`), having required the defect for two stories; **(2)(a′)** the perimeter is the ten screens' rendered surface **plus the six `500`/`template error` bodies served at those same addresses** — an option that did not exist in the draft, whose *"and nothing else"* asserted a measurement nobody had taken; **(3)(d)** the floor **derived over a real YAML parse** — (c) was the draft's pick and was **measured insufficient** before it could be taken, a nested key with its `fr` half dropped leaving 702 tests and nine gates green while the French UI rendered English; **(4) YES** — `OPENCMDB_LOCALE` into `AppConfig`, refused **by name**, the refusal accepting **what `rust-i18n` accepts, MEASURED** (`fr-CH` works and a refusal built from the draft's example list would have rejected it) — 🔴 **BREAKING**, a deployment carrying `OPENCMDB_LOCALE=FR` stops booting, owed to 6b.12's release notes; **(5)(b)** the locale is **left to review** and AC5's browser pass IS that review, the UX spec having demanded the question be TAKEN rather than defaulted. 🔴 **THE NINTH GATE, `copy-vocabulary`** (`xtask/src/copy_vocabulary.rs` — **not** `main.rs`, at 1908/2000): a **per-locale** denylist over `app.yml`, the file `gate_vocabulary`'s `.rs`-only walk has never read, on a seam `app.yml:2` has advertised since story 3.8. ⚠️ **Scoped to that one file on a MEASURED refusal**: `merge` has ~310 legitimate whole-word uses under `crates/`, 87 of them `must-merge` inside **fourteen sha256-locked fixtures**. Its word matcher is **not** `gate_vocabulary`'s — `_` is a word character there, so `gesture.merge_all` would have passed. **18 located probes, 13 red and 5 green**, each green row saying what it protects. 🔴 **AND THE SECURITY ROW WAS CLOSED BY A CLOSED SET RATHER THAN A LONGER LIST**: the six sentences the diagnostic's security group can render are **PINNED in both languages** and the set is **proven closed** over every posture (32 renders) — M21 plants `deferred-work.md`'s own paraphrase **verbatim** and it now reds, where it left 696/696 green at 6b.9. ⚠️ **THREE COPY DEFECTS FOUND BY LOOKING AND NOT ONE BROKE A TEST**: *"level in force"* and *"Settle it in the triage"* (two Gallicisms) and the seven `example.kind.*` labels, **plural where the same key is also a single device's KIND cell** (*"nas-01 · Servers"*) — a set **already inconsistent with itself**, six plurals and one singular, which is what made it visible. ⚠️ **The instrument the story prescribed for its own receipt was BLIND**: `strings | grep` reports 0 for a value that IS in the binary, GNU `strings` breaking its run on any multibyte character — **163 of the 284 `fr:` values carried one AT THE BASELINE** — a dated numerator written against a live denominator in three places, which the review caught; on the shipped tree it is **168 of 290** (ASCII control: `Dashboard`, found). **`grep -a`, throughout.** ⚠️ **And a stale artefact again, on a PROCESS**: after `setsid` the shell's `$!` names the wrapper, so killing it left the old English server holding the port and every *"French"* page read was pre-fix copy — caught only because the served text contradicted a `grep -a` taken two minutes earlier. ⚠️ **And one commit went in with clippy RED**, because `cargo clippy … | grep` takes the pipeline's exit status; amended — story 6b.1's defect, whose cheap defence is to redirect and read `$?`. **25 mutation rows: 23 mutations and 2 controls — and the headline the story carried was WRONG THREE WAYS, corrected at the review.** It read *"22 ids, 19 reds"* over a table of 23 rows carrying 21 🔴, with **M4 counted among the greens while its own row reads *"reds BOTH carriers"***: one row classified two ways in one section, in the story whose §0i subject is enumerations written from memory. Two PRESCRIBED rows were absent and silently so — M6 (measured **3 red**) and M7 (**not executable as prescribed**: the gate builds no offset→line map, it takes `Marker::line()`; substance measured instead, `app.yml:341` located exactly under 166 accented lines). And three rows reported ONE KEY's count for a mutation named as a CLASS — M1 is 1 red on `ipam.state.free` and **3** on `nav.dashboard`, so its stated conclusion *"the SILENT direction, caught only by the file guard"* is FALSE for any key the story's own probes reference. No total is restated: *the table is the record, and a summary that can drift from it is the defect this story spent its day on.* ⚠️ THE LIVE COUNT lives in the story file `6b-10-copy-fr-and-en.md` — cited by reference, never carried here. **15 register rows** (12 written by the story — its File List said 11 — plus 3 the review added, and two INHERITED rows the story cited by name were left stale about the very gate count it changed, now corrected), derived from the story's own §0 and diffed before the commit (story 6b.9's defence, applied). 28 fixtures, trap gate still RED 26/15/11, `opencmdb-core` untouched, no migration, **one line each** in `Cargo.lock` for a crate already in the tree, `epics.md` and the UX spec not edited.

✅ **THE THREE-LAYER CODE REVIEW, 2026-08-22 — 45 raw findings, 35 after dedup: 5 arbitrations, 27 patches, 1 deferral, 3 dismissed with their check.** 🔴 **THE EDGE LAYER'S HEADLINE: a YAML ALIAS walked a retired term into the FRENCH NAVIGATION with all nine gates and 720/720 green** — `tmpl: &shared "Merge alerts"` aliased into `nav.alerts`, served and confirmed on a booted binary. `completeness()` was blind BY CONSTRUCTION: an alias increments none of `scalars`/`keys`/`values`, so the equality it checks held exactly as before. **Guy: refuse the alias** (option b) over resolving anchors — *an enumeration cannot claim the completeness of a property*, and the refusal cost was measured NIL (`app.yml` carries zero anchors). 🔴 **THE AUDITOR'S HEADLINE, and it is the epic's dominant class one file over: the prose-attribute guard reads the TEMPLATE while the defect lives in the PRODUCER.** Replacing `gap_card_label: t!(…)` with a bare literal put the removed English string back on the served page with 485/161/74 and nine gates green — and the story's OWN M8 row says in so many words that the guard must read the rendered page. Closed by `every_field_of_the_shared_strings_comes_from_a_key`, bounded to one constructor, stated as a TRIPWIRE. 🔴 **THE BLIND LAYER — the diff and nothing else — deduced from a false doc sentence that `completeness()` cannot detect the failure its own doc names, and it was right**, for the FIFTH consecutive story in which the layer with no repository access finds a HIGH the two sighted layers do not. ⚠️ **Three of its findings were REFUTED and are recorded with the check**: `store_unreachable_body`'s doc is TRUE (`render_shell`'s fallback IS a plain literal, `page.rs:106` — the layer had confused it with `render_error_body`); `set_locale` DOES apply the primary-subtag rule (booted with `fr-CH`, serves *Tableau de bord*); and the security oracle's empty-value disjunct reds anyway (one red is enough). 🔴 **FOUR MORE GUARDS WERE MEASURED BLIND, each correct about what it tests**: a BLANK `fr: ""` counted as a present locale (the French page served a button with no label at all — *rendering nothing is worse than rendering English*, the case that guard's own message names); the derived key count compared two DIFFERENT populations, so ordinary nested YAML of arity two made it FAIL over a correct file — *reds for the wrong reason, and its message invites the repair that opens the hole*; the 500-literal guard matched ONE status and TWO files while its name promised *"a status"*; and an INVISIBLE character inside a retired term walked past BOTH vocabulary carriers into the shipped binary. 🔑 **GUY'S FIVE ARBITRATIONS**: refuse the alias · REDO the browser pass and write it (fabricating the record after the fact was refused as an option) · the 401 body becomes a KEY on both sites, on arbitration 2(a′)'s own criterion *served at those same addresses* · a per-handler BUDGET on `/diagnostic`'s precedent, over `acquire_timeout` on the pool · `merger` AND `mergers` join the `en` denylist. 🔴 **THE 401 WAS THE FIRST BODY AN OPERATOR OF A CLOSED DEPLOYMENT READS AND IT WAS ENGLISH**, at every one of the ten addresses, before any screen — and nothing broke when it became a key, *because no test asserted it at all*. 🔴 **`/triage` AND `/dashboard` HELD THE BROWSER 30.00 s** before the calm French sentence arrived (measured against a paused MariaDB; `/diagnostic` answered in 2.00 s on its own budget). The new guard's test pool is deliberately SLOWER than the budget, because story 6b.9 shipped the mirror of it with the pool refusing FASTER and left the timeout branch dead under test. Prove-to-red reproduces **30.00253856 s**. 🔴 **AND THE REDONE BROWSER PASS FOUND WHAT NO TEST COULD SEE**: `/triage` rendered **`1 field(s)`** and **`1 champ(s) déclaré(s)`** on the product's primary screen while `/devices` showed the same fact as `1 field` / `2 fields` one click away — *a parenthetical plural is a perfectly resolvable key*. Fixed as a CLASS (`counted_fields` inflects; a property guards the whole file), with two forms justified by a MEASUREMENT — both call sites read a count out of `result.abstentions`, so `n >= 1` and the zero case where the two languages disagree cannot arise. ⚠️ **And this session committed once over a RED suite**: `cargo test | grep` takes the pipeline's status, so the `&&` after it proceeded over `484 passed; 1 failed`. Story 6b.1's defect, recorded there about clippy and warned about in this story's own Dev Notes, reproduced on `cargo test`. The failing test was a guard doing its job — *the tree was telling the truth and the reader was not listening* — and every status thereafter was read from a file with `$?`. 🔑 **The guard that reddened is itself the lesson**: `the_two_carriers_agree_on_what_is_retired` forbade `"merger"` anywhere in the constant, on the TRUE premise that « Merger » is the binding FRENCH word — so it forbade the noun in every column. *An unbounded needle cannot tell one locale's column from another's* any more than it can tell an entry from a mention of one, which is the defect that test's own doc narrates, met on a second axis.

⚠️ **AND CI CAUGHT WHAT EVERY LOCAL CHECK IN THIS PROJECT IS BLIND TO, on the review's own push.** The first CI run of PR #115 went **RED** on `unused variable: root` in a `#[cfg(test)]` module — a dead binding this review left when it rewrote the derived key count. 🔑 **The local commands the repository prescribes cannot see it**: `cargo clippy --workspace -- -D warnings` checks lib and bin targets and **not test targets**, while CI compiles the tests under `RUSTFLAGS=-D warnings` — so in a project where the tests are the bulk of the code, the lint the docs name is blind to most of it. ⚠️ **The MECHANISM was recorded wrong and is corrected here** (found by story 6b.11's validation): `RUSTFLAGS` appears **nowhere in `.github/`**; it is `actions-rust-lang/setup-rust-toolchain@v1` (`ci.yml:48`) that injects the flag. The effect and the commands were right and the cause was a plausible story — *which is the one thing this project's own rule forbids*, written the same day the rule was quoted. The command that reproduces CI locally is **`cargo clippy --workspace --all-targets -- -D warnings`**, and `RUSTFLAGS="-D warnings" cargo test --workspace --locked` for the other half. Both were run before the repair push and both are green. ⚠️ **And the CI monitor armed to report this said NOTHING** — it timed out in silence, and a watcher that emits nothing looks exactly like a watcher reporting success: *silence is not a green*, so the run was read directly rather than waited on.

**Story 6b.11 is `done`** (PR #117 squash-merged 2026-08-23 as `3ad8ef6` after a green CI run on the head commit, code-reviewed TWICE first) — **the keyboard layer, the focus contract, and the project's FIRST browser gates.** `/triage` gains ↑/↓ over the queue in shape (E) (Guy's arbitration 4, chosen on a 20× prototype measurement), the focus ring becomes the product's own, `aria-pressed` on a link becomes `aria-current` (axe rated the former CRITICAL), and three palette tokens are darkened, taking axe-core from **all ten routes red to zero violation nodes**. ⚠️ **THE LIVE COUNT lives in the story file `6b-11-keyboard-layer-and-focus-contract.md`** — cited by reference, never carried here. 🔴 **CODE-REVIEWED by three isolated layers, then TRIAGED and REPAIRED (2026-08-22/23): 60 raw findings → 33 distinct defects**, six reached by all three layers and ten more by two, *so nearly half of what was written is a second road to the same thing — a count over three isolated reports measures the isolation, not the product.* 🔑 **ONE DEFECT EXISTS IN NO SINGLE REPORT AND ONLY IN THE TRIAGE**: the AC2 hue guard was **too LOOSE and too TIGHT at once** — two layers measured any grey passing it (`hue()` returns a constant 240° for every achromatic colour; `--color-neutral-500` and `--color-bg` to `#ffffff` and `--color-accent-700` to a pale blue were all GREEN), the third measured it **REFUSING a legal lightness-only repair** (`--color-text` spans 3 channel units, so its smallest representable hue step is **20°** against a ±6° tolerance) — **and its failure message asserted as fact the very thing that mutation had just disproved.** Each token is now pinned by the property constrainable *for it*: near-neutral or a hue, plus a luminance BAND wider than the largest repair the sheet has taken; all five green mutations red, and the legal repair stays green as the control. 🔑 **AND THREE FINDINGS COMPOSE INTO ONE SENTENCE NONE OF THEM CARRIES**: the accessibility apparatus **reported success over a surface it did not reach** — CI seeded nothing, so the gate walked a `/triage` with **0 queue rows, 0 gesture controls, 0 panes**, and the story's own defect replanted there exits **0**; the route floor was a length check that could not tell ten screens from one screen ten times (planted: `✅ /triage ×10 … EXIT=0` while a violation was live on five screens it never visited); and the keyboard layer — the story's central deliverable — was carried by **nothing that runs**, `app.js` emptying with 490 tests, nine gates and the axe gate all green. **GUY'S FOUR ARBITRATIONS (2026-08-23), the recommendation taken in all four, each recorded with the option refused**: **(1)** repair the exit contract AND state the residual — a top-level `try` takes every *could not run* to **2**, ⚠️ but the SHELL keeps its own codes (`npm ci`, `curl` 22, a missing Node 127) and that is written rather than promised; **(2)** seed the store AND put the query-string states in front of axe, over seeding alone, which would have left the replacement for an axe-critical attribute verified by no browser; **(3)** `token_hex` reads the light `:root` BLOCK with comments stripped, over stripping comments alone — 🔴 *a CSS comment carrying the old value turned the contrast property off in SILENCE, the guard green over a sheet axe scored at 210 nodes*; **(4)** the operator's own gesture always outranks the pending timer, over stating the cost — at a 1200 ms document delay, inside 6b.10's own 2 s budget, the operator clicked row 6 and landed on row **2**, with the warm-store control showing the click winning. 🔑 **`a11y/kbd-probe.mjs` becomes the project's SECOND BROWSER GATE** — ⚠️ *not a tenth `cargo xtask ci` gate: that count stays at **nine**, and the two browser gates are CI steps because neither can be Rust* —: seventeen checks, the same 0/1/2 contract, and a floor **equal to what is there** rather than under it — a skipped check answers **2**, never a green. Emptying `app.js` now reds it; so does removing the two focus selectors, which leaves the Rust suite at **490 green** exactly as the review measured — *the DOM-level carrier AC5 demands.* 🔴 **AC5 itself was AMENDED by Guy on 2026-08-23, two words and no change of scope**: *a source-reading guard **does not SUFFICE** where the defect lives in the DOM*, where it read *is not counted*. The scope was earned by measurement and held three-for-three; what over-reached was *counted*, which reads as *worth nothing* — a source guard is cheaper and **names the cause** where a browser gate names only the symptom, so the two cumulate. ⚠️ The cost the amendment does not dissolve is **proportionality**: taken literally the criterion produced a browser gate, which is the minimum for a story whose subject IS the rendered page and would be zeal for a `font-family` declaration. *It is a rule about where the defect lives, never about how much apparatus to build.* ⚠️ **The repair pass produced six findings against ITSELF, all tabled**: my own driver carried the two-filter defect (`cargo test A B` runs nothing — caught because the control printed **nothing** where it owed a result); the first fix for arbitration 1 still exited **1**, a static `import` resolving before any `try` exists; **the review's own broken-payload mutation stopped measuring the exit contract** because the injection form changed under it; a probe check written from a finding's SUMMARY instead of its INPUT reddened a correct product; a red was nearly credited to a CSS mutation that did not cause it; and 🔴 **the test that reddened was matching its needle inside a COMMENT** — `no_screen_is_chosen_by_javascript` tripping on a sentence *describing* the defect being repaired, the same family as `token_hex`, one file over and the same day, ⚠️ where the tempting fix would have been the guard editing the record instead of the record editing the guard. 🔴 **The story's own recorded CAUSE for dropping a scroll check is FALSE and is corrected in place with its refutation attached**: *"arrows do not scroll under headless CDP at all"* — measured, shipped code `scrollY 40`, an unconditional `preventDefault` `scrollY 0`, and **§0e of the same document carried the refuting figure four sections above**. Against this project's own rule — *a cause needs a check, not a plausible story* — that sentence is exactly the kind it forbids. 🔴 Also repaired: *"six divergences registered"* was false in three documents (three rows written, and the **`j`/`k` + `⏎` divergence, which `epics.md:2306`/`:2308` prescribe and Epic 7 must know was skipped, appeared in no register at all**); AC5 had **no mutation table** in the story whose AC5 *is* the mutation record — **29 ids now, 26 reds, 2 controls green by design and 1 green for a reason the repair created, carriers MIXED and named per row**, with no *"every red assertion-carried"* headline claimed; the `aria-current` oracle was satisfied by a **queue row on the same page** and is anchored to the sort link; `.gitignore` closed the incident on one path and not the pattern (`node_modules/foo` was **not ignored**); the dead contract had **four** artefacts where T5 says three; and the amber was darkened **pre-emptively**, so *"two tokens"* was three and *"the tokens a screen paints text with"* names one that paints nothing. ⚠️ **Stated rather than closed**: the contrast table is still an enumeration — two painted grounds stay out, with the measurement that adding them reds pairings **no screen renders** — and `--color-accent-600` fails AA on all four grounds, latent because no rule paints it. **Nine `cargo xtask ci` gates green plus the two browser gates, 728 tests both ways** (**6.65 s** with `cargo test --workspace --locked` against a live `mariadb:10.11.11`, **0.65 s** without a store — the clock is the tell; ⚠️ *16.7 s was first written here and had a build inside it, corrected at the second review round*), clippy over `--all-targets`, `RUSTFLAGS="-D warnings"` green, 🔴 **A SECOND REVIEW ROUND ran on the REPAIR itself (three isolated layers, a different model, one worktree and one live store each) — 19 raw findings, 17 distinct, and NOT ONE reached by two layers**, where the first round found six by three roads each. 🔑 **Its most instructive pairing is a DISAGREEMENT of reading**: the edge layer measured *"kbd-probe on exactly 1 row → exit 2, `MIN_ROWS` enforced"* and filed it as a **refuted suspicion, the contract holds**; the acceptance layer measured the same fact and filed it **HIGH, the gate cannot run in CI**. Same number, opposite verdicts, and the difference is the question asked. 🔴 **THE HEADLINE: CI WAS GREEN ON RESIDUE.** From a virgin store the shipped demo seed renders **one** queue row (0 before, 1 after, re-measured by me) against a floor of two, so the keyboard gate exits **2**; the run that passed reported `queue: 3 row(s)` only because the `Tests` step uses the same database immediately before and what it left behind carried the queue over — ⚠️ and §T8's *"a seeded two-row queue"* was a state that existed in my session's database and **nowhere in the repository**. Closed by `a11y/seed.sql`, which TRUNCATES then seeds four entities; ⚠️ **`AUTHORSHIP_ROOTS` gains `a11y` and `SANCTIONED_SITES` names the file, in one act** — story 5.12's own instruction to reopen the perimeter rather than let a new SQL writer sit outside it. **M-P1 and M-P2 are one proof in two halves**: without the sanction the gate reds naming `a11y/seed.sql:37`, without the roots it goes **GREEN over the same file**, 42 files walked where 43 are. 🔴 **THREE MORE, each mine**: `assert_eq!(measured_tokens, 6)` was **as vacuous as the `checked == 16` it replaced** — a loop over a six-element array whose only escape is a `panic!` above it — with a comment claiming it *can fail*, found by the layer holding the diff and nothing else; the focus marker carried a bare `"1"` and **belonged to no navigation**, so a settle whose document never committed left it standing until the next plain load pulled focus into the queue, the autofocus the design refuses in its own comment; and **`sessionStorage` throwing silently restores the original defect** (URL catches up, `activeElement` is `<body>`), stated as a limit because without storage there is nowhere to leave a note. ⚠️ **And my M-D2 row said *four* checks red where there are EIGHT** — my driver piped the gate through `head -4`: *a measurement read through a truncation is not a measurement*, in the table whose job is to carry measurements. 🔑 **The repair of the repair produced four findings against ITSELF**: the stale-marker guard came back **GREEN under the reversion it exists to catch** (it planted the NEW representation, which the old code also refuses — now representation-free, planting both); the check floor said 18 for 19 and then 19 for 20, caught by running it twice; **the served-versus-source check is UNREDDENABLE in a debug build** (`rust-embed` reads from disk, so served and source are the same bytes by construction) and is recorded green-by-construction rather than counted; and 🔴 **widening the perimeter reddened three `xtask` tests while `cargo xtask ci` stayed GREEN** — the gate fails closed on a missing root and every synthetic tree owes one directory per root, so ***a gate green over the real tree says nothing about its own tests***, and only `cargo test --workspace` said so. ✅ Refuted with their checks: the blind layer's *"the CI step lacks its credentials and will 401"* (the `env:` block is step-level, and the CI run authenticated), a `//` inside a string literal (refuted by two layers independently), and fourteen more the edge layer ran itself. **Twenty checks in the keyboard gate now, nine `cargo xtask ci` gates, 728 tests both ways.** 28 fixtures, trap gate still RED 26/15/11, `opencmdb-core` untouched, no migration, `epics.md` and the UX spec not edited.

**Story 6b.12 is `done` — `v0.2.0` IS CUT AND PUBLISHED** (PR #119 squash-merged as `9079b45`; the tag sits on that commit, whose CI was green **on the commit itself** rather than inherited from the branch; release run `32719830377`). ✅ Verified after the fact rather than assumed: the image pulls (4.96 MB), boots against a MariaDB, answers `/healthz` 200, `/` **401 without credentials** and **303 to `/triage`** with them, and `/diagnostic` reports **v0.2.0**; the GitHub Release object carries the changelog's 76 lines; `gh-pages` was pushed **after** the tag and is live. 🔑 **The order was *review, then tag*** — a tag is not revertible in the ordinary sense, so nothing unreviewed was made permanent by one. ⚠️ **THE LIVE COUNT lives in the story file `6b-12-release-0-2-0-and-its-documents.md`** — cited by reference, never carried here. 🔴 **THE CONTEXTING'S OWN CENTRAL DEVICE WAS MEASURED INSUFFICIENT, AND ITS HEDGE IS WHAT SURVIVED.** §0b listed the falsified document claims six grep needles could find and wrote *"a claim phrased differently is not in this list and is not thereby absent"*; the validation's gap-hunt then found three whole classes no needle could reach. **The worst: three documents asserted that stored credentials are *encrypted at rest*** — a string `diagnostic.rs:1038` **forbids the product from ever rendering**, because there is no credential store and no crypto call site, and `/diagnostic` says *"none stored"* on screen. It was standing on the page Docker Hub renders as the storefront. Also: the status narrative was stale by ~15 epics (*"no triage inbox, no IPAM, no alerts, no admin UI"* — all four exist), and **Tailwind was named as the stack in three places** against a decision this project took **twice** the other way. 🔴 **And §0 itself was wrong four ways**, the sharpest being that `Screen::Device` **IS** in the navigation — ⚠️ *a correction `sprint-status.yaml:4578` had already made one story earlier and which did not carry*. AC3 named the Administrator Manual and no task did, while its Security chapter was **silent** on the closed-by-default posture; §0f claimed three of five rows owned by Epic 15, the retrospective and an unnamed story; and `cargo build --locked` **cannot** update `Cargo.lock`, measured with the error it prints. **GUY'S TWO ARBITRATIONS (2026-08-24), recommendation in both.** 🔑 **(1) The release notes get a VENUE**: `gh release list` was **empty** — neither `v0.1.0` nor `v0.1.1` had a Release object, there was no changelog, and `release.yml` created neither, story 3.10 having recorded its cut in its own story file. *So AC2 was satisfiable in letter while leaving zero operators informed* — which for this release means meeting a product that answers **401 on every screen** with no idea why. `CHANGELOG.md` ships, `release.yml` extracts the section by version and creates the Release, and ⚠️ **a missing or empty section FAILS the release** rather than publishing silence (proven both ways: `0.2.0` → 79 lines, `0.9.9` → refused). **(2) Issue #38's REPRODUCED candidate cause is fixed here** — ⚠️ and **the validation named two claimants of the scratch namespace where there are SIX**, four tests calling `scratch_dir` directly, so it is closed as a **CLASS** (an ownership registry refusing a tag a second caller claims) rather than as an instance; restoring the collision reds exactly one test, naming both. ⚠️ **The guard's first form turned one defect into EIGHTEEN failures**, the panic poisoning its own mutex — *a guard that multiplies one finding by ten is noise around it*. **Issue #38 stays OPEN**: one reproduced occurrence establishes *a* cause, never *the* cause. 🔑 **AC4's own instrument earned itself**: the prescribed check — every `Example` screen's manual chapter must carry `\begin{planned}` — found **six** unmarked sections where the validation had read four, adding *The UniFi source* (there is no UniFi connector at all) and *Documenting a discovery*. *A check sees what a reading misses, including a reading by a layer whose job was to look.* 🔴 **AND THE VISUAL SWEEP FOUND WHAT NO REPORT HAD: story 6b.5's repair is PARTIAL.** In the state `a11y/seed.sql` creates — observations present, none placed, **which is CI's own state on every gate run** — the page says *"Nothing observed yet — run a scan and this fills in."* immediately above *"A scan has landed; the identity pass has not placed any of it yet."* The second sentence **is** 6b.5's fix and **it explains the first without removing it**; on `/triage` the false line renders **below four queue rows dated one minute ago**, claim and refutation in one viewport. ⚠️ **The dashboard's SALIENCE finding is confirmed by eye** and is sharper than its description — the honest section is three lines of grey prose against three cards at 22 px with sparklines, so the invented `37 / 4 / 2` is the loudest thing on the page. Both registered with owners; neither is copy-and-CSS, and §0g forbids a release story from reshaping a screen. ⚠️ **And the lifted screenshots exposed my own check as insufficient**: I reused the gap-hunt's captures on a check that templates, assets and locales had not moved — *which did not cover the version the shell renders*. **729 tests both ways** (7.6 s live / 0.65 s without — the clock is the tell; 🔴 *the count read 728 until the code review found the scratch registry had shipped with NO test, on the sentence "this story added no `#[test]`" — which was true and was the defect*), nine `cargo xtask ci` gates, clippy `--all-targets`, `RUSTFLAGS="-D warnings"`, fmt, both LaTeX manuals build. ⚠️ **`gh-pages` is committed on its branch and NOT pushed** — publishing it would announce `v0.2.0` before the tag exists. Five register rows. `epics.md` and the UX spec not edited.

**Story 6.3 is `done`** (PR #93 squash-merged 2026-08-18 as `d284cfb` after a green CI run, code-reviewed first — three layers, on a DIFFERENT model from the implementer) — **NFR5 is now covered on all three assertions**, and the two parked since story 5.12 on 2026-08-07 are measured rather than asserted. Contexted, validated by TWO fresh-context layers and developed the same day (2026-08-15). 🔴 **The story's whole difficulty is that both assertions are TRUE BY CONSTRUCTION**: there is no `UPDATE observation_record` anywhere in `crates/`, and the documenting transaction issues one SELECT on `observation_record` plus N INSERTs on `declared_attribute` — so a before/after test **cannot fail**, and would pass with its assertions deleted. That is Epic 5's dominant defect class, which `epics.md:1792` names inside the AC itself. **The answer is a PAIR of carriers of different kinds**: bit-for-bit snapshots (observation on seven columns, link with its `id`) across the gesture and both refusal paths, PLUS an **eighth gate, `observed-immutable`**, because *you cannot measure the absence of code by running code* (5.12's sentence, second application). 🔑 **Neither subsumes the other and M1b MEASURES it**: a write planted in a new UNCALLED fn leaves the product suite green and reds the gate alone. 🔴 **Two arbitrations by Guy, each recorded with the alternatives refused**: (i) `epics.md:1790`'s *"a divergence opens … through the new write path"* is **UNSATISFIABLE as written** — the declared value can only come from the documented sighting, so every contradicting ingestion CONFLICTS `(0, 2)`; the test therefore DELETES that sighting, modelling *the old sighting aged out*; (ii) the gate plus its prescribed rustdoc broke the **`file-size`** ceiling (1829 → ≈2018 against 2000), so it lands in `xtask/src/observed_immutable.rs` — `CLAUDE.md`'s own *"split, not grown"* rule. ⚠️ **The gate's verb list is every OVERWRITING form, not `UPDATE` alone**: `REPLACE INTO` and `INSERT … ON DUPLICATE KEY UPDATE` were both measured GREEN under `UPDATE`-only, which would have re-opened for this table the hole 5.12 closed for its own — and the widening **preserves the EMPTY allowlist** (36 files, `docker/` included), the one form of allowlist nobody can quietly widen. 18 **located** probe verdicts (file AND line), driven END TO END from the first commit, so 5.12's *"the whole gate body deletable with the xtask suite green"* does not recur. ⚠️ **Eleven mutation ids — nine mutations and TWO CONTROLS green by design; the headline is NOT "nine reds"**, carriers MIXED and named per row (M1/M5 dual-carrier, three gate-message-carried, zero compiler- and zero `.expect()`-carried). 🔴 **Three findings the pass itself produced**: **M6 was not executable as designed** (a precondition guard fires before the comparison — story 5.13's assertion-order family, a FOURTH occurrence), replaced by M6-bis which measures what it meant to; the validation's *"the whole workspace suite stays green"* under M1b is **FALSE on this tree** (it was measured on a prototype gate with no real-tree test) and is narrowed to the PRODUCT suite; and the M4′/M4-naive pair turns *"a rolled-back write and an absent write are indistinguishable"* from a sentence into a measurement — the same write on the handler's transaction is invisible, on its own connection it reds. ⚠️ **What the story could NOT close, stated plainly**: the divergence half is unreachable through the gesture, and **on the shipped connector it can never fire at all** (`ipv4` is both the only declarable field and the perimeter key) — *a property of fixtures today, not of the product on a real network*. Also found and registered: a 6.2 review patch marked applied is **absent from the tree** (`same_origin` still reads `Host` with `.get()`); a stale `SANCTIONED_READS` entry is caught by **nothing**; and `cargo doc` reports **four** unresolved intra-doc links that no gate can see — one of them created by this story's own rename and caught only because the claim was verified. ⚠️ THE LIVE COUNT lives in the story file `6-3-nfr5-remaining-assertions.md` (its AC8) — cited by reference, never carried here. Eight gates green, 28 fixtures, trap gate still RED 26/15/11, **no migration**, `page.rs` and `templates/` byte-identical, `epics.md` not edited.

**Story 6.4 is `done`** (PR #127 squash-merged 2026-08-26 as `b6dfd69` after a CI run green on the head commit — code-reviewed by three isolated layers and REPAIRED first) — 🔑 **THE PRODUCT WRITES FROM A SCREEN FOR THE FIRST TIME.** `/triage`'s `Nouveau` row carries one amber control that posts to `POST /document-all`, a route story 6.2 mounted and no template had called since. **729 → 741 tests** (503 bin + 161 core + 77 xtask), nine gates, axe 10 routes + **4** states 0 violation nodes, kbd **30** checks. 🔴 **AC1 COULD NOT BE IMPLEMENTED AS WRITTEN and Guy re-aimed it (2026-08-24)**: the criterion puts FR13's gesture on the `AbsenceOfProof` line, an identity verdict about whether two sightings could be JOINED, while FR13's population is `undeclared` — *observed, and no declared record claims it* — which `page.rs` has labelled `Nouveau` since story 6b.4. ⚠️ A sighting can be `AbsenceOfProof` **and already fully declared**, so the letter offers to create a second entity for a machine already documented, the one thing FR13's invariant exists to prevent. `epics.md` NOT edited; the divergence is registered with Epic 6's retrospective, ***including that the story's own *so that* clause — "the reach section stops being a number and becomes a door" — is NOT met, and that the premise is what is wrong***: an identity abstention's answer is a better SOURCE (Epic 11), not a record the operator writes. Every cause line now SAYS that, in three keys forced by a TOTAL reader rather than the two the story budgeted. 🔴 **THREE DEFECTS WERE FOUND BY A BROWSER AND BY NOTHING ELSE.** The focus-after-swap contract story 6b.11 registered to this story was BROKEN: `hx-on::after-swap` sat on the button, `htmx:afterSwap` fires on the swap TARGET, and the handler never ran — `⏎ → 201`, the answer swapped in, `activeElement.id` **empty**, with every Rust assertion green. The amber **painted nothing**: `.btn-gesture.live` (0-2-0) beat `.btn-document` (0-1-0) on all four declarations while the guard counting `var(--accent-document)` reads found all four and passed — *a declaration that loses is still a declaration, and no source guard can see a cascade*. And the story made a shipped sentence FALSE — *"This gesture is not built yet."* under a bar whose loudest control now is. ✅ **Guy, 2026-08-25: FILL the control with the amber** (contrast **5.57**) over darkening the token (**4.56**) or dropping the tint (**4.59**) — margins of 0.06 and 0.09 that the next palette adjustment erases. 🔴 **THE CODE REVIEW THEN MEASURED, IN ALL THREE LAYERS, THAT THE ACT LEFT THE SCREEN UNCHANGED**: the narrow swap replaced one paragraph, so after a 201 the queue row, the amber button and the pane reading *« Rien de déclaré à cette adresse »* all stayed put over a success message, and a second press answered 409 and swapped **nothing** — the first press's sentence still on screen asserting the opposite. The body was a Rust `format!`: English under a French UI, `field(s)` (the parenthetical plural story 6b.10 fixed AS A CLASS on this screen) and a raw UUID (6b.4's review had removed one from this pane). **AC7's *"the queue's question disappears"* was FALSE.** ✅ **Guy's arbitration 2′ (2026-08-26)**: `HX-Redirect` carrying the confirmation in the URL, keyed bodies for all five statuses, `hx-on::before-swap` so a refusal is swapped at all — 🔑 **re-rendering from the route was refused BY THE TYPE**, the document sub-router's state holding no pool (story 6.1's M4: adding one fails to compile), and story 6.2's 201-with-a-body contract is untouched because only a browser reads the header. ✅ **Guy's arbitration (2)**: `Gesture::Disabled`, a THIRD state — with the route unmounted the product said *"not built yet"* about the gesture it had just built, on the configuration nearly every deployment runs; hiding the control was refused as contradicting *show and label rather than hide*. 🔴 The boolean it replaced conflated *the route is mounted* with *this row is eligible*, so an `Écart` pane would have named a switch that changes nothing there. 🔴 **THE BLIND REVIEW LAYER FOUND THE HEADLINE FROM THE DIFF ALONE, sixth story running**: the live count was a SESSION delta — `master` is 729, so the story is **+12** and not +3, and the baseline it named is story-commit 4 of 8. *An intermediate state read as a baseline* is story 6b.1's defect verbatim, committed inside a paragraph invoking *verify both of its terms*. ⚠️ **`page.rs` hit the `file-size` ceiling at 2033 and the answer was the SPLIT** (`identity_view.rs`), never shorter prose. ⚠️ **The seed and both browser gates had to be taught to reach the gesture**: `a11y/seed.sql` produced ZERO `Nouveau` rows and the axe gate derives its state from the FIRST queue row, which is never one — both gates now FIND the row by looking for the control, never by naming a selector or a translated word. **Ten repair mutations, ten conforming to predictions written first**, five of them the guards AC5 claimed and had never proved red. ⚠️ **What the operator can DO: select a `Nouveau` row and press one control that WRITES** — the record is documented, the screen re-renders, the question leaves the queue, and a refusal says so. ⚠️ **And on the DEFAULT configuration there is still no gesture**: `OPENCMDB_DOCUMENT_ENABLED` is off, so a stock deployment meets five labelled controls, one of which now says it is built and switched off. *The gesture exists; it is not yet what a fresh install meets.* ⚠️ THE LIVE COUNT lives in the story file `6-4-abstention-line-carries-the-gesture.md` — cited by reference, never carried here. 28 fixtures, trap gate still RED 26/15/11, no migration, no new dependency, `epics.md` and the UX spec not edited.

⚠️ **Issue #38 (the unexplained local test non-determinism) RECURRED** on `master` at `d47631b` and is recorded there with its measurement — 2 red runs out of ~11 on **two different tests**, then 8 consecutive green, clean tree, all 25 fixture sha256s verified green by the `fixtures` gate *during* the red runs, CI green on the same commit. One hypothesis (a scratch-path collision between tests) was raised and **refuted**: the six `write_traps` tags are distinct and `scratch_dir` embeds the pid. **The cause stays OPEN** — do not adopt one without naming the check that would have failed if it were wrong. _(This sentence named only 5.1 until 2026-07-29 — stale for a day, found by story 5.3's code review, not by the two stories that caused the drift.)_ Live status is `_bmad-output/implementation-artifacts/sprint-status.yaml`; grounding is `docs/project-context.md`.

### Build / lint / test commands (the stack is chosen and building)

- **Build:** `cargo build --workspace --locked` (Cargo.lock is committed; always `--locked`).
- **Test:** `cargo test --workspace`.
- **Lint:** `cargo clippy --workspace -- -D warnings` · **Format:** `cargo fmt --all`.
- **Browser gates (NOT in `cargo xtask ci`, and the exception is deliberate):** `node a11y/axe-gate.mjs`
  (axe-core over the ten routes the navigation offers **plus FOUR query-string states no href carries**;
  CI seeds the store and sets `AXE_REQUIRE_QUEUE=1`, because an empty queue is *the gate could not run*
  and not a pass; `AXE_REQUIRE_GESTURE=1` likewise, because the product's one live control on no page
  it walks is not a pass either) and `node a11y/kbd-probe.mjs` (**thirty** checks over the keyboard
  layer, the focus contract, and — since story 6.4 — the documenting gesture PRESSED for real). Both answer **0 clean / 1 the product / 2 the gate could not run**, ⚠️ *once `node` is
  running them* — the shell before them keeps its own codes. Neither can be Rust: they measure the
  RENDERED, COMPUTED page, and `cargo xtask ci` has no browser.
- **Project gates:** `cargo xtask ci` — **NINE gates** since story 6b.10, every one in Rust, never in YAML (D56/D65): the dependency-frontier check (D47), the DDL binary-collation grep (D64 cond. 1), the retired-vocabulary check (D65), and the fixture corpus lock (sha256 **and** orphan detection, both directions). The `architecture-views.md` staleness hash reports `ℹ STALE` and still exits 0 — that is by design, and **it must not be regenerated inside a story**; regenerate at a milestone. **Tracked as GitHub issue #50**, which carries the measured drift (two commits, 59 insertions, no decision body touched) — the debt no longer lives only in these notes. *(Some other xtask subcommands are still stubs.)*
- **Toolchain:** Rust 1.96+, edition 2024. **Stack:** axum 0.8 · askama 0.16 · sqlx `=0.9.0` (MariaDB-only, `mysql`+`tls-rustls-ring`) · tokio · `config` · `rust-i18n` (YAML) · `prometheus` (raw). ⚠️ **`app.css` is HAND-AUTHORED on the reference mock's tokens (story 6b.1); there is NO Tailwind chain and no `cargo xtask css`** — deferred twice by Guy on the measurement that it generates nothing this UI uses, with four spellings D55 lacks recorded in `deferred-work.md`. **Never invent a version — pin from the real `Cargo.lock`.**

### The dependency frontier is load-bearing (D47), and it is a gate

`crates/opencmdb-core` is the domain: it **must not** depend on `anyhow`, `axum`, `sqlx`, or `askama` — an error there is domain data, not a string. `crates/opencmdb-bin` is everything that touches the outside world (SQL, HTTP, HTML, files, the clock, secrets). `xtask/` is a workspace member and a dependency of nobody. Do not cross these lines; `cargo xtask ci` is meant to catch it.

### One database only

**MariaDB 10.11+ is the ONLY supported engine (D64).** SQLite and MySQL are OUT; PostgreSQL is out at MVP. Do not reintroduce a second backend or a dialect abstraction. Comparison/normalization never descends into SQL (D10) — it is a correctness rule, not portability.

## How work is done here: the BMad Method

This project uses BMad, a spec-driven, agent-orchestrated development framework. Instead of jumping straight to code, work flows through named agent personas and skills that produce planning artifacts first, then implementation. Agents are invoked as skills (e.g. `/bmad-agent-pm`) or by name.

Key agent personas (from `_bmad/config.toml`):
- **Mary** — Business Analyst (`bmad-agent-analyst`): research, requirements discovery
- **John** — Product Manager (`bmad-agent-pm`): PRD creation
- **Winston** — System Architect (`bmad-agent-architect`): solution/architecture design
- **Sally** — UX Designer (`bmad-agent-ux-designer`)
- **Amelia** — Senior Software Engineer (`bmad-agent-dev`): test-first (red/green/refactor) story implementation
- **Murat** — Test Architect (`bmad-tea`): risk-based test strategy, automation

Typical greenfield lifecycle (each step is a skill — see the skills list, prefix `bmad-`):
1. `bmad-product-brief` / `bmad-domain-research` → `bmad-create-prd` → `bmad-validate-prd`
2. `bmad-create-ux-design` → `bmad-create-architecture`
3. `bmad-create-epics-and-stories` → `bmad-sprint-planning`
4. Per story: `bmad-create-story` → **`bmad-create-story validate` (MANDATORY, see below)** → `bmad-dev-story` (or `bmad-quick-dev`) → `bmad-code-review` → `bmad-retrospective`

**`create-story validate` is NOT optional here** (Guy's decision, Epic 4 retrospective 2026-07-26 — it overrides the story template's "Validation is optional" banner). Every story gets a validation pass by **two fresh-context agents** (fact-check + gap-hunt) *before* `dev-story`. Self-review finds nothing; the measured payoff over the 9 Epic-4 stories that had it was **6 HIGH findings on 4 stories**, two of which would otherwise have shipped a trap that passed for the wrong reason.

🔴 **AN EPIC CLOSES ONLY AFTER *TWO* STEPS: the RETROSPECTIVE, then the PROJECT REVIEW** (Guy, 2026-08-12). The retrospective looks at *how the work went*; the **project review** — conducted on the project's folder in `~/travail/Projets actuels/opencmdb/`, under that tree's own `CLAUDE.md` — looks at *what the work did to the project's own record*, and **its job is to correct the problems the epic left there**. Neither is sufficient alone: at Epic 5's close the retrospective was held and everything looked finished, and the project review then found that **milestone J3 was unreachable by construction** (it demands a gap *detected AND corrected*, and **at that date** the product had no write surface at all — story 6.2 opened one, and story 6.4 put it on a screen), that `derniere_activite` was wrong under every valid reading, and that a registered risk was **false as written and was deleted**. ⚠️ **Order matters**: the review comes AFTER the retrospective, because it corrects what the retrospective surfaces. Do not flip an epic to `done` on the strength of the story count, or of the retrospective alone.

**The retrospective itself is NOT optional** (Guy's decision, 2026-08-10). An epic is **not closed until `bmad-retrospective` has run** — do not offer it as a choice, and do not skip it because an epic ended quietly. `sprint-status.yaml` reads `required` for epics 5–23; `optional` survives only on epics 1 and 2, whose non-back-filling was decided earlier and must not be retroactively rewritten. **The reason is measured, not asserted: both of this project's most load-bearing working rules were PRODUCED by a retrospective rather than decided in the abstract** — Epic 4's is where validation became mandatory, and where four documents were caught carrying a confident cause that measurement had refuted, which is the origin of both issue #38 and the rule *"a cause needs a check, not a plausible story"*. It is the only step that has ever changed how the work is done; skipping it keeps the lessons inside the epic that produced them. ⚠️ Items are REGISTERED for a retrospective by the stories that raise them (Epic 5 already carries several by name) — carry them in rather than rediscovering them.

**A cause needs a check, not a plausible story.** A symptom may be recorded from observation; a *cause* may not be written down without naming the check that would have failed if the cause were wrong. This is the code rule ("name the test behind every claim") extended to environment and infrastructure diagnoses — Epic 4 recorded a confident Synology-Drive explanation for a flaky local suite that measurement later refuted, and four documents carried it (issue #38).

Use `bmad-help` when unsure which skill applies next.

## Repository layout

- `_bmad/` — BMad framework: agents, workflows, and skills for modules `core`, `bmm` (core dev lifecycle), `bmb` (builder), `cis` (creative intelligence), `tea` (test architect). **Installer-managed — treat as read-only.** Edits here are overwritten on the next install.
- `_bmad/config.toml`, `_bmad/config.user.toml` — installer-generated config. **Do not edit directly.** To change values durably use `_bmad/custom/config.toml` (team, committed) or `_bmad/custom/config.user.toml` (personal, gitignored).
- `_bmad-output/` — where BMad writes generated artifacts: `planning-artifacts/`, `implementation-artifacts/`, `test-artifacts/`.
- `docs/` — project knowledge base (`modules.bmm.project_knowledge`). Includes `docs/manuals/` — the LaTeX **User Manual** and **Administrator Manual** (English, LuaLaTeX; shared style in `docs/manuals/common/`, one folder per manual; build with `make` — see `docs/manuals/README.md`).
- `.claude/skills/` — installed BMad skill definitions (SKILL.md, instructions.md, templates, checklists).

## Conventions

- **Communication language is French** (`config.user.toml`, user "Guy"). Converse with the user in French unless they switch.
- **Document/artifact output language is English** (`config.toml` → `document_output_language`). Generated PRDs, specs, and docs should be written in English.
- Application code, once it exists, should be placed at the project root (not under `_bmad*`). When a stack is chosen, add its build/lint/test commands to this file.
- **Issue tracking: all bugs, change requests, and other issues are recorded as GitHub Issues** on `guycorbaz/opencmdb` — never tracked only in local notes, commit messages, or planning docs. Reference the issue number in the related commit/PR (e.g. `Fixes #12`). This is the single source of truth for work items outside the BMad story flow.
- **Docs-current-before-push:** before any `git push`, make sure every affected document is updated and matches what is being pushed — the manuals (`docs/manuals/`), `README.md`, the GitHub Pages landing site (`gh-pages` branch), `docs/project-context.md`, and this file. A push whose docs contradict the code/state is not ready. If a change touches behaviour, stack, or layout that a document describes, update that document in the same push.

### Engineering conventions (2026-07-23)

Four rules Guy set as durable. The first two are `cargo xtask ci` / lint gates; the last two are review criteria the `bmad-code-review` layers enforce.

- **DRY — mutualise logic, but keep DELIBERATE redundancy.** No accidental duplication of logic; extract a shared helper. But this codebase has redundancy that is *on purpose* and must survive: a test that restates the corpus bytes as a second independent oracle (`fixtures.rs`'s `expected()`), two representations of one concept pinned by an equality test (`score.rs`'s `Column::as_str()` vs `Expectation::column()`), the per-test-module `scratch_dir`. DRY means "one source of truth for behaviour," never "one representation of everything" — do not collapse a redundancy that a test pins and a comment labels as deliberate.
- **No source file over 2000 lines of CODE.** Enforced by the `file-size` gate in `cargo xtask ci`. **Tests do not count** — the ceiling is the lines before the first `#[cfg(test)]` (the house convention is one trailing test module per file, D56b). A file approaching the ceiling is split into modules or a sub-crate, not grown. The gate names the offender and its count.
- **Document every public item, per rustdoc idiom.** Every `pub` struct, enum, **field**, **variant**, and function carries a `///` doc comment. Idiomatic rustdoc, NOT `@param`/`@return`: prose that says what the thing is; a `# Errors` section on a `Result`-returning fn; `# Panics` where relevant; `# Arguments`/`# Returns` only when the signature is not self-evident. **A doc comment must be TRUE** — three reviews caught doc comments asserting behaviour the code did not have; a false doc is a defect, so prefer the weaker true sentence. Enforcement is phased: `#![deny(missing_docs)]` is ON for `opencmdb-bin` and `xtask` (both already clean); `opencmdb-core` has ~70 outstanding field/variant docs (mostly `observation`) and gains the lint once that sweep lands — the CI clippy gate runs `-D warnings`, which would promote a `#![warn]` to a hard error before then, so the lint waits rather than blocks.
- **Test every function, where possible.** Tests live inline in a trailing `#[cfg(test)] mod tests` (D56b). Prove-to-red is the house rule (story 1.3): a guard is observed failing before it passes, and the mutation is recorded. "Where possible" excuses the genuinely untestable (a `Display` impl exercised elsewhere, `main`) — it does not excuse a new guard shipping without a test that reds when it is removed.
