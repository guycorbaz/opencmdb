// A note ABOUT the rule is not a violation of it, and this gate must not punish
// the documentation that explains itself:
// UPDATE declared_attribute SET entity_id = ? -- never do this
pub fn nothing() {}
