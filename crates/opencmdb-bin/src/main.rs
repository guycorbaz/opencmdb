//! opencmdb — the single binary.
//!
//! The composition root (D55): SQL, HTTP, HTML, files, the clock, secrets. This is the
//! walking-skeleton entry point; the `Repository`/`transact` skeleton (D49), the two
//! adapters (D51), the axum surface and the askama templates arrive with story 1.

fn main() {
    // Placeholder. Story 1 (D1-rev) replaces this with the walking skeleton that shows
    // a REAL gap on a cardinality-1 perimeter and abstains, visibly, everywhere else.
    println!("opencmdb — skeleton");
}
