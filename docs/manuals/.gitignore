# LaTeX build artifacts (regenerated by `make`)
*.aux
*.fdb_latexmk
*.fls
*.log
*.out
*.toc
*.lof
*.lot
*.synctex.gz
# Built PDFs — produced by `make`; not tracked (commit them explicitly if you prefer).
*.pdf
