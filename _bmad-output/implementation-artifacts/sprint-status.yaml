# generated: 2026-07-21
# last_updated: 2026-08-11   (this commented twin had read 2026-08-01 while the live
#                             `last_updated:` field below said 2026-08-07 — two representations of
#                             one date, one of them stale. Synced 2026-08-10; keep them in step.)
# project: opencmdb
# project_key: NOKEY
# tracking_system: file-system
# story_location: _bmad-output/implementation-artifacts

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog → in-progress: Automatically when first story is created (via create-story)
#   - in-progress → done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#
# Retrospective Status:
#   - required: MUST be held before the epic is closed (Guy, 2026-08-10 — see PROJECT NOTES)
#   - optional: Can be completed but not required — SURVIVES ONLY on epics 1 and 2, whose
#     non-back-filling was decided earlier; do not retroactively rewrite a recorded decision
#   - done: Retrospective has been completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows
# - Developer typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)
#
# PROJECT NOTES (opencmdb):
# =========================
# - This file was created on 2026-07-21, AFTER epics 1-3 were already built and released. Their
#   statuses are recorded from the artefacts on disk and from git history, not from live tracking.
# - Story keys match the story FILE names on disk, which are shortened slugs of the epic titles
#   (e.g. `1-5-mariadb-service-and-cargo-deny` for "MariaDB service container and cargo-deny in
#   CI"). The files are the established identifiers; the keys follow them so status detection
#   resolves.
# - ⚠️ THIS NOTE WAS STALE AND IS CORRECTED (2026-08-12). It said *"Epics 5-23 carry no story
#   entries because epics.md defines them at epic level only"* -- false since 2026-07-26, when Epic 5
#   was decomposed into 14 stories (delivered as 20), all of which have entries below.
#   **Epics 1-6 are decomposed in epics.md and carry story entries here. Epics 7-23 are still at
#   epic level only.** Epic 6 was decomposed 2026-08-12, into TWENTY stories -- sliced deliberately
#   finer than Epic 5's fourteen, because Epic 5's six insertions were all found while contexting a
#   story that carried two ideas.
#   (An earlier version also said the Epic 4 breakdown was outstanding — it is not;
#   verified 2026-07-22 against epics.md.)
# - Retrospectives: Epic 3's and Epic 4's were held. Epics 1 and 2 remain 'optional' and will not
#   be back-filled.
#   🔴 **THE RETROSPECTIVE AT THE END OF AN EPIC IS MANDATORY** — Guy's decision, 2026-08-10, while
#   Epic 5 stood three stories from closing. Epics 5 through 23 therefore read 'required', not
#   'optional'. It is not a step to offer and not one to skip when an epic ends quietly: an epic is
#   NOT CLOSED until its retrospective has run.
#   **Why, and it is measured rather than asserted: this project's two most load-bearing working
#   rules were both PRODUCED by a retrospective, not decided in the abstract.** Epic 4's is where
#   `create-story validate` became mandatory (measured payoff: 6 HIGH findings over 9 stories, two
#   of which would have shipped a trap passing for the WRONG REASON), and where four documents were
#   found carrying a confident cause that measurement had refuted — the origin of both issue #38 and
#   the rule *"a cause needs a check, not a plausible story"*. A retrospective here is not a
#   ceremony reporting on work already understood; **it is the only step that has ever changed how
#   the work is done.** Skipping it keeps the lessons inside the epic that produced them.
#   ⚠️ Epic 5's retrospective already has items REGISTERED BY NAME in the stories that raised them —
#   among them `epics.md`'s AC1 falsified by the code it describes (5.9b), the `TRUNCATE … WHERE`
#   correction (5.10), and story 5.12's second-session-reading lesson with its clause (*"a reading
#   without measurement yields a list of SUSPICIONS"*). Carry them in; do not rediscover them.

generated: 2026-07-21
last_updated: 2026-08-19  # 6b.4 ✅ ARBITRATED by Guy (three rulings, before any code), governing
                          # `dev-story`. **(1) THE SPLIT IS TAKEN -- 6b.4b INSERTED, Epic 6b -> 13
                          # stories**: 6b.4 keeps AC1+AC3, 6b.4b takes the action bar. `epics.md` NOT
                          # edited; registered instead. **(2) The dead-gesture mechanism is a GESTURE
                          # NATURE CLOSED IN THE TYPE** -- `enum Gesture { Live { route }, Planned
                          # { owner } }`, one partial, one key pair (fr *"À venir" / "Ce geste n'est
                          # pas encore construit."*, en *"Not yet" / "This gesture is not built
                          # yet."*). 🔴 `<button disabled>` REFUSED ON NFR25: a disabled button leaves
                          # the tab order and vanishes from a screen reader, so the blind operator is
                          # not even told the gesture exists -- `aria-disabled` keeps the
                          # announcement. ⚠️ And the owning story's NUMBER stays out of the surface:
                          # *"arrives in 6.4"* turns the label into a calendar, therefore a promise,
                          # which is what 5.14b refused. **(3) `origin` may be read for DISPLAY, via
                          # a SECOND reader** -- `load_declared_attributes` stays provenance-free
                          # because it feeds the divergence computation (`page.rs:568`), a new
                          # `load_declared_provenance_for_display` goes only to the view, and
                          # `SANCTIONED_READS` gains ONE entry naming path AND function. 🔑 A bare
                          # sanction on the EXISTING reader was refused: it would put provenance
                          # inside the scope of the path the gate exists to protect *"by accident"*.
                          # Two readers make the divergence computation STRUCTURALLY unable to see it.
                          # 🔴 AND ONE FINDING ABOUT THE CONTEXTING ITSELF: half of §0c was never an
                          # open question -- Guy's premise (2) of 2026-08-13 (*"the four gestures
                          # Epic 7 owns are visible and labelled"*, under the heading *"premises and
                          # NOT open questions"*) had already decided it, and the count lands exactly
                          # on the mock's four secondary buttons. *Reading a document as if it had not
                          # already decided is the same error as citing a line without opening it* --
                          # made twice in one day, once on a story number and once here.
                          # NEXT = `dev-story` 6b.4, scoped to AC1 + AC3.
                          # (Earlier the same day) 6b.4 ✅ VALIDATED, two fresh-context layers, and it paid for itself
                          # BEFORE a line of code. **Fact-check: 61 assertions, 59 confirmed, 2
                          # REFUTED, both mine.** 🔴 The load-bearing one is a STORY NUMBER, not a
                          # line number: register row (e)'s *"Owner: story 6.4"* does NOT mean this
                          # story -- 6.4 is *"The abstention line carries the gesture"*
                          # (`epics.md:1794`), sequenced AFTER all of Epic 6b, and three further rows
                          # use the name the same way; the register predates Epic 6b's insertion. So
                          # the `--accent` narrowing is NOT owed here; T6 and M5 rewritten.
                          # *The trap was a number that reads like this one's.* (Also: 63 locale
                          # keys, not 62.)
                          # **Gap-hunt: it BUILT the plumbing against a live mariadb.** 🔴 ITS
                          # HEADLINE, MISSED BY CONTEXTING ENTIRELY: reading `origin` for DISPLAY
                          # REDS THE `authorship` GATE -- `SANCTIONED_READS` allows ONE test-only
                          # reader of the three provenance columns, so this story would be the FIRST
                          # legitimate PRODUCTION one and needs its own arbitration (story 6.2's
                          # precedent). **That REFUTES the split's first rationale**: AC1 is not
                          # arbitration-free either, so T0 is THREE arbitrations, not two.
                          # 🔴 Second refutation: FOUR of the mock's five row kinds are producible
                          # today, not two -- the story cited `gap::project` where `gap::reconcile`
                          # already TYPES `Absence` and `Conflit` (`gap/mod.rs:49-57`); proven with
                          # four seeded scenarios. Only `Ambigu` has no producer. ⚠️ And the cost the
                          # story lacked: `reconcile` is written for ONE perimeter, so looping it
                          # per entity is O(N·M) with every other entity's observations as
                          # `OutOfPerimeter` noise.
                          # 🔴 Third: M6's prediction is WRONG -- `chrono::Utc::now()` will not
                          # compile (no `clock` feature) but `SystemTime::now()` does, and a bucketed
                          # clock left the guard GREEN; worse, that guard calls `build_view` with
                          # EMPTY data, so it proves clock-freedom for `build_identity_view` and, for
                          # `build_view` -- the function this story touches -- NOTHING.
                          # ✅ M2's warning reproduced LIVE: the order-pinning guard reds, the
                          # *"does the toggle do something"* guard stays GREEN under the exact
                          # mutation it exists to catch. The dominant class, measured not cited.
                          # 🔑 Four undocumented seams named: `sqlx` has NO `chrono` feature so every
                          # DATETIME goes through `DATE_FORMAT` + `parse_from_rfc3339` (the pattern
                          # already exists at `repo.rs:371`); `read_declared_provenance_for_test` is
                          # the right MODEL with the wrong SANCTION; `uuid` has no `v4`, house
                          # convention `now_v7()`; no D47 issue, core untouched.
                          # 🔑 And the operator question answered more sharply than asked: the ONE
                          # live gesture (`POST /document-all`) has NO BUTTON IN THE MOCK AT ALL --
                          # the bar is not merely all-dead, it does not even offer the live one.
                          # NEXT = Guy's THREE rulings, then `dev-story` 6b.4 on the chosen scope.
                          # (Earlier the same day) 6b.4 CONTEXTED -> `ready-for-dev`. 🔴 SEVEN findings, THREE needing Guy
                          # BEFORE any code. (a) THE SCOPE: the story carries FOUR deliverables and
                          # three start from NOTHING -- the queue (`build_view` selects ONE entity and
                          # returns), the provenance/freshness plumbing, the action bar, the age sort.
                          # **SPLIT RECOMMENDED, 6b.4b INSERTED** (AC1+AC3 stay, AC2 moves) -- and the
                          # reason is DEPENDENCY, not size: AC2 cannot be written until (b) and (c) are
                          # settled, and neither is about the triage screen. (b) 🔴 *"labelled per
                          # 6b.3"* NAMES A MECHANISM THAT DOES NOT EXIST: 6b.3 classifies CONTENT
                          # (screen/section), never a CONTROL, and `Screen::Triage` is `Fed`, which its
                          # own doc says "owes NO marker". A dead Merger button is not example data --
                          # a different axis. Story 6b.3's own §0b, one story later. (c) 🔴 APPLIED
                          # LITERALLY TODAY THE ACTION BAR IS FIVE BUTTONS OF WHICH FIVE ARE DEAD
                          # (`/document-all` is whole-entity, flag-gated, off by default and called by
                          # NO template; Résoudre needs FR16; accept/snooze/attach/exclude have no
                          # column anywhere; and story 6.4 is sequenced AFTER Epic 6b) -- which runs
                          # into Guy's own recorded decision of 2026-08-12, *"announcing an absent
                          # gesture is a promise"* (`deferred-work.md:3283`). 🔑 The retrospective's
                          # unasked question, asked on purpose: what does the operator DO with an
                          # action bar in which nothing acts?
                          # ✅ ONE SUSPICION OF MINE MEASURED AND REFUTED: the freshness does NOT
                          # collide with `the_shell_shows_no_last_observation` -- that guard scans
                          # `_shell.html` + `_nav.html` only and excludes `_gap_card.html` BY NAME.
                          # ✅ And provenance + freshness are SUPPLIED BY THE SCHEMA ON BOTH SIDES
                          # (`0001_initial.sql:14-17,27-28`); they are dropped at the SELECT. The mock
                          # is WEAKER than the AC here (5 of its 8 declared rows carry no date at all)
                          # -- fixture laziness, not design: follow the AC and the spec, not the mock.
                          # ⚠️ Inherited: the `--accent` guard must be RE-EXAMINED not merely satisfied
                          # (`:3319`, condition lifted now 6b.1 landed), and NFR25 names this screen a
                          # key view while NOBODY owns axe-core (`:3748`).
                          # ⚠️ THE CONTEXTING'S OWN LESSON: three of the four register line numbers the
                          # analysis supplied were WRONG, and the claim that the epic's stale
                          # *"seven gates"* DoD was registered is FALSE -- `grep -n "seven gates"`
                          # returns nothing. Every citation in the story was re-measured before being
                          # written. *A citation believed is a citation wrong.*
                          # NEXT = `create-story validate` 6b.4 (MANDATORY), then Guy's three rulings.
                          # (Earlier the same day) 6b.3 -> `done`: PR #99 squash-merged as `d1bb128` after a green CI run
                          # (1m04s; bin suite 5.48 s in CI against 0.06 s locally -- the tell that
                          # the database-backed half really executed). Branch deleted, twins updated
                          # in this same push. **611 -> 614 tests**, eight gates green.
                          # Next: story 6b.4 (the triage screen on the real gap).
                          # (Earlier the same day) 6b.3 CODE-REVIEWED (three layers, Sonnet 5 -- a different model from the
                          # implementer; each in its own git worktree) and REPAIRED. **3 decisions by
                          # Guy, 10 patches, 1 deferral, 4 dismissed with the check that dismissed
                          # them.** 611 -> **614 tests** (387 bin + 161 core + 66 xtask), eight gates
                          # green, fmt and clippy clean, measured against a live `mariadb:10.11.11`
                          # (bin suite 5.93 s against 0.06 s without -- the tell that the DB half ran).
                          # 🔴 THREE of the story's OWN recorded measurements were refuted by
                          # re-execution: M3's carrier (the STATUS assertion, 404 vs 200 -- and the
                          # cause is the real finding: the demo router's exclusion is dynamic while
                          # the main router's inclusion is hardcoded to /triage), M4's count (25, not
                          # 15), M7's aftermath (386, not 385). *The table had been believed rather
                          # than replayed*, in the story whose Dev Notes warn its driver lied twice.
                          # 🔴 ONE FINDING WAS REACHABLE BY NO LAYER ALONE: `.screen-section` used
                          # twice and defined nowhere, `.rows` (a `<dl>` ruleset) on a `<table>`, and
                          # NO table/th/td rule in the sheet -- so the witness screen the story exists
                          # to produce rendered as browser defaults. The Blind Hunter could only
                          # suspect it, the Edge Case Hunter checked the marker's classes and not the
                          # content's. 🔑 *Found by RECOUNTING the sheet, not by seeing the page: the
                          # served TEXT was correct in every respect three layers could measure.*
                          # 🔴 And the i18n guard could not catch a WRONG key that RESOLVES --
                          # `role_key: "example.badge"` left all 613 tests, all eight gates and clippy
                          # green while /devices rendered "Exemple" where "Stockage" belongs. Epic 5's
                          # dominant class, inside the guard added to close an i18n defect.
                          # GUY'S THREE ARBITRATIONS (2026-08-19), each recorded with the option
                          # refused: (1) AC4's `Screen::ALL` half is pinned by a source-scanning TEST,
                          # NOT a ninth gate and NOT clippy-in-xtask -- the story's "it would have to
                          # be a gate" misapplied story 5.12's rule, which governs an UNBOUNDED
                          # absence, where this property is bounded and present in one file; clippy
                          # was refused on a MEASUREMENT (one throw-away production line silences it),
                          # and `NavGroup::ALL` had the identical hole, so it is a property over both
                          # enums. (2) The eight `Empty` screens GET THE SENTENCE NOW: `epics.md:2092`
                          # is one of Guy's own premises of 2026-08-13 -- *"those whose code is not
                          # implemented show an example dataset with a text saying so"* -- and no
                          # layer of contexting, validation or arbitration had surfaced it. ⚠️ Spirit,
                          # not letter: the dataset stays owed by 6b.5-6b.9. (3) `Nature::Example`
                          # CARRIES its content, so "declared Example with nothing of its own" is
                          # unrepresentable (story 5.6's precedent, closed in the TYPE) -- option (a),
                          # a match on the screen, was WITHDRAWN at the arbitration because every
                          # screen already has an arm, so it does not close what it looks like it
                          # closes. Four review mutations, four measured (M9/M10/M11 assertion-carried,
                          # M12 compiler-carried), each on a scratchpad-restored base, never
                          # `git checkout --` -- the gesture that destroyed an uncommitted fix here and
                          # in 6.1. ⚠️ STAYS at `review`: `done` is the MERGE's business.
                          # ⚠️ Deferred: the look is STILL unverified by eye for the third consecutive
                          # story (no browser in any environment) -> Epic 6b's DoD with 6b.12.
                          # (Earlier the same day) 6b.3 -> `review`: IMPLEMENTED. The product shows example content for the
                          # first time, and says so. `Nature` (Fed/Example/Empty) on `Screen` by a
                          # compiler-checked match; ONE partial + ONE key pair for the marker;
                          # `example_data.rs` (no connection, constraint 1); `/devices` filled in TWO
                          # sections, each carrying the marker; the partition asserted over the ROUTE
                          # TABLE on the real HTTP body. **611 -> 613 tests** (591 was 6b.2's
                          # baseline, reused; corrected at the code review, which took it to 614).
                          # 🔴 TWO DEFECTS FOUND BY LOOKING, neither reachable by any test:
                          # (1) the example data rendered in ENGLISH under a French UI, whole suite
                          # green, because a literal is not a key and the locale guard only sees
                          # keys -- an NFR26 violation with no possible carrier. Fixed into keys,
                          # closed by a guard asserting the value IS a key AND RESOLVES.
                          # (2) the partition's Fed half was gated on `DATABASE_URL`, which does not
                          # govern the code: `lazy_pool()` is a hardcoded dead URL, so that half
                          # could never pass and was never attempted. Found by running WITH a
                          # database (500 vs 200). *A gate keyed on a fact that does not govern the
                          # code under test is not a gate.*
                          # ⚠️ AC4 is PARTIALLY met and says so: `Screen::ALL`'s blind half is closed
                          # by `clippy -D warnings` -- measured M2: build passes, 613 tests pass,
                          # ALL EIGHT GATES GREEN, only clippy reds. A dev running `cargo xtask ci`
                          # alone never sees it, and NOTHING PINS IT. Ninth-gate question -> GUY.
                          # ⚠️ AC2 = mechanism met, MIXED specimen stated (-> 6b.5). AC3 met BY
                          # CITATION, no runtime test added, on purpose.
                          # ⚠️ The driver lied twice: a mutation run on a non-compiling tree read as
                          # GREEN, and `git checkout -- <file>` silently discarded the i18n fix
                          # (story 6.1's incident, reproduced). Both re-run on a committed base.
                          # Suite run BOTH ways: 0.07s without a DB, 5.71s against a live
                          # mariadb:10.11.11 -- the tell that the DB half really ran.
                          # NEXT = `code-review` 6b.3 (three layers, different model).
                          # (Earlier the same day) 6b.3 ✅ VALIDATED, two fresh-context layers, and it paid for itself.
                          # Layer 1 (fact-check): 30 assertions verified, 28 confirmed, 2 REFUTED --
                          # both mine, and the graver one was a PREMISE OF THE ARBITRATION ("each of
                          # 6b.5-6b.9 titled `(example)`" -- TWO of five). The two load-bearing
                          # claims were reproduced BY COMPILATION: triple E0004, and E0308 on
                          # `State<MySqlPool>` in the pool-free sub-router.
                          # Layer 2 (gap-hunt) BUILT the whole mechanism against a live
                          # mariadb:10.11.11 -- Nature, the marker, the dataset, /devices filled, the
                          # route-table partition on the real HTTP body. Compiles, 611 -> 612, gates
                          # and clippy green, file-size unmoved.
                          # ✅ T0 ANSWERED BEFORE ANY PRODUCTION CODE: `dead_code` DOES catch a
                          # `Screen` absent from `ALL` under `clippy -D warnings`, and the obvious
                          # bypass (a #[test] constructing it) FAILS because clippy without
                          # --all-targets never compiles cfg(test). ⚠️ But it holds by a property of
                          # the GATE, not of the language; it lives OUTSIDE `cargo xtask ci`, so a
                          # dev running only the gates never sees the red; and NOTHING PINS IT.
                          # AC4's closure is PARTIAL -- say so, never "AC4 met" alone.
                          # 🔴 THE CENTRAL FINDING: MY OWN mutation M7 does not guard what I said it
                          # guards. Content dispatched from `nature()` makes the lying-marker case
                          # STRUCTURALLY IMPOSSIBLE, so M7's sole carrier is a bookkeeping assertion
                          # a future tidy-up would delete as redundant. Only BUILDING could reach it.
                          # ✅ Two of my suspicions REFUTED: the `--accent-document` guard survives a
                          # brand-new template (it reads the directory), and AC3's compile carrier
                          # holds -- so T5 "cite, add no test" is right. ⚠️ T2 is DATABASE_URL-gated
                          # and invisible locally (~5.9s with a DB vs 0.06s without).
                          # NEXT = `dev-story` 6b.3.
                          # (Earlier the same day) 6b.3 ✅ ARBITRATED by Guy on finding (a), before any code: the story
                          # ships the example DATASET, the marker, the gate, AND ONE WITNESS SCREEN
                          # filled from it -- not the mechanism alone, and not all nine screens.
                          # 🔑 THEREFORE THREE NATURES, not two: `Fed` / `Example` / `Empty`. With
                          # two, the eight unfilled screens would have to be declared *example* and
                          # the marker would assert that an empty `<main>` is a demonstration --
                          # the exact false sentence the finding was about. `Empty` is TEMPORARY by
                          # decision and must be gone when 6b.9 closes.
                          # ✅ WITNESS = `/devices`, decided at contexting for its section-bearing
                          # content. ⚠️ Its COST is stated, not discovered: `/devices` belongs to
                          # story 6b.6, which keeps the device RECORD, the `/devices/{id}` debt and
                          # the fidelity pass -- registered so 6b.6 meets a narrowed scope.
                          # 🔴 AC2 ships as MECHANISM MET + MIXED SPECIMEN STATED: section-level
                          # placement proves the marker attaches below screen level, but the case
                          # AC2 names -- real content BESIDE example content in one frame -- needs
                          # the dashboard's reach section and is 6b.5's. Recording AC2 as simply met
                          # would be the sentence this epic has been caught writing five times.
                          # `epics.md` is NOT edited; the divergence is registered.
                          # (Earlier the same day) 6b.3 CONTEXTED -> `ready-for-dev`. 🔴 SIX findings, and FOUR need Guy
                          # BEFORE any code. (a) SCOPE CONTRADICTION: `epics.md` puts the example
                          # CONTENT in 6b.5-6b.9 and titles 6b.3 "the marker and its gate", while
                          # `screens.rs:170` -- merged in `d8cc438` -- says 6b.3 "owns the example
                          # dataset AND the marker". On the first reading this story marks nine
                          # EMPTY screens, and a marker over an empty screen says "this emptiness is
                          # an example", which is false. A third reading (mechanism + ONE specimen,
                          # and a THIRD nature `empty`) is what the task list assumes.
                          # (b) 🔴 THE MARKER IS SPECIFIED BY NO DOCUMENT THIS PROJECT HAS, measured:
                          # the mock (390 lines) carries NO marker -- the only `demo` hits are base64
                          # font payloads -- and the UX spec is SILENT. "The mock prevails" gives
                          # nothing here; this story INVENTS an operator-visible surface, a first for
                          # Epic 6b, and needs an arbitration on form and words.
                          # (c) AC4's carrier is HALF-BLIND and the register gave the blind half to
                          # the WRONG story: a `nature()` match IS compiler-checked, but `Screen::ALL`
                          # is a literal array that is NOT, so a fully-wired variant absent from `ALL`
                          # vanishes from nav, routing AND any test iterating it -- *the eleventh
                          # screen* AC4 names. Register says owner 6b.6; AC4 makes it 6b.3's.
                          # (d) AC3 IS ALREADY DISCHARGED by 6b.2's `Router<()>` (E0308); a runtime
                          # test would be strictly WEAKER and is the very class AC4 quotes -- cite,
                          # do not add. (e) AC2's mixed specimen DOES NOT EXIST (the dashboard is
                          # empty until 6b.5), so AC2 is STATED not met unless (a) supplies one.
                          # (f) ⚠️ "eight of which are demonstrations" is FALSE in THREE files
                          # (`page.rs:53`, `screens.rs:510`, `_nav.html:16`) against "nine" in four --
                          # shipped in `d8cc438` and missed by ALL THREE review layers.
                          # 🔑 The mutation table is deliberately SEVEN rows, not eighteen: 6b.2
                          # prescribed 18 and played 7, and BOTH holes that reached production (M12,
                          # M7) were rows written at contexting and never played.
                          # NEXT = `create-story validate` 6b.3 (MANDATORY), then Guy's four rulings.
                          # (Earlier the same day) 6b.2 -> `done`: PR #97 squash-merged as `d8cc438` after a green CI run,
                          # code-reviewed first (three layers on a DIFFERENT model, each in its own
                          # git worktree). The product has TEN ADDRESSES: shell, grouped nav, `/`
                          # a 303 to `/triage`, `gap.html` deleted. **591 -> 611 tests.**
                          # 🔴 THE STORY SHIPPED ITS OWN MUTATION: M12 (read the perimeter from
                          # `std::env::var` in the handler) was predicted red, NEVER EXECUTED, and
                          # was the shipped code -- 608 green with T4b ticked `[x]`. The guard that
                          # should have caught it hands `render_shell` a `None` directly and never
                          # reaches the producer: Epic 5's dominant class, in the story that
                          # inherited the lesson. 🔴 The review found the family THREE more times --
                          # no test named ONE of the ten routes (`/devices` public = 609 green, the
                          # story's own M7, also never executed; and merged below `auth_deny`,
                          # `GET /dashboard` = 200 OK), a guard whose comment claimed a perimeter it
                          # did not walk, and `"\u{200B}".trim().is_empty() == false` letting a
                          # zero-width space render as a blank. All four closed, each measured red
                          # first. ⚠️ ELEVEN of eighteen prescribed mutations were never re-executed.
                          # 🔑 CI is the FIRST measurement of the DB half: bin suite 0.06s local vs
                          # 5.38s in CI -- the tell that the database-backed tests really ran.
                          # ⚠️ Nine of ten screens render an EMPTY `<main>` with nothing saying so,
                          # so **6b.3 is priority in the strict sense**; `/device` is the sharpest
                          # case. 🔴 Every existing deployment changes address -- 6b.12 owes that to
                          # the release notes, beside 6b.1's colour change.
                          # NEXT = `create-story` 6b.3 (the example dataset and its marker).
                          # (Earlier) 6b.2 ✅ VALIDATED, two fresh-context layers, and it cost the story
                          # dearly: **8 of 37 claims refuted** by the fact-check, **7 HIGH + 5
                          # MEDIUM** by the gap-hunt, which BUILT the whole shell. 🔴 The two
                          # carriers the story called its strongest were both measured FALSE:
                          # AC4's compiler claim (the ten routes land on the main router, whose
                          # state IS the pool -- it compiles; the carrier must be BUILT, as a
                          # pool-free sub-router, and the error is E0308 not E0277) and AC7's pin
                          # (an enumeration of six gated paths, none of them the ten new ones --
                          # `/devices` public leaves 371 tests green). 🔴 A premise of GUY'S OWN
                          # ARBITRATION was false and is corrected: the perimeter and the last
                          # observation ARE in the mock, in the nav footer. Re-arbitrated -- the
                          # perimeter ships (config, no DB read), the last observation goes to
                          # 6b.5 -- and the re-arbitration RESTORES AC4, since a shell needing
                          # config needs a pool-free state. 🔴 And two findings only building
                          # could reach: `page::index` becomes dead code so `clippy -D warnings`
                          # FAILS on day one (behind which the only fed screen would have gone
                          # unreachable until 6b.4), and **story 6b.1's two repaired guards go
                          # BLIND on new templates** -- both violations planted, 607 tests green.
                          # ⚠️ THIRD consecutive story to miss a register row naming it by number.
                          # NEXT = `dev-story` 6b.2, after Guy rules on the responsive question.
                          # (Earlier the same day) 6b.2 ✅ ARBITRATED by Guy (five decisions, before any code), governing
                          # sentence: **THE MOCK PREVAILS**. Ten nav entries in three groups and
                          # NO Topology (UX-DR33 retired, registered); the header is the mock's
                          # three static slots with the mock's version replaced by
                          # `CARGO_PKG_VERSION`; `/device` as the mock has it, for now; `/`
                          # REDIRECTS to `/triage`; and NO Tailwind chain, again.
                          # 🔑 Arbitration 2 DISSOLVED a contradiction instead of resolving it:
                          # with no "last observation" in the header there is no MAX(observed_at),
                          # so the shell reads NOTHING and the epic's constraint 1 ("no demo
                          # screen opens a connection") holds STRUCTURALLY -- the compiler carries
                          # it, since no handler takes a pool. The cheapest way to satisfy two
                          # contradictory requirements was to notice one of them was not in the
                          # reference. ⚠️ `epics.md`'s four-part header now ships three; registered.
                          # 🔴 And a trap the contexting found: the mock writes `href="#"` on all
                          # ten entries because its nav is JavaScript -- so transcribing the
                          # reference FAITHFULLY produces the defect AC3 forbids (M5b).
                          # NEXT = `create-story validate`, TWO fresh-context layers, MANDATORY.
                          # (Earlier the same day) 6b.2 CONTEXTED -> `ready-for-dev`. 🔴 FOUR arbitrations are put to
                          # Guy BEFORE dev, and two are contradictions rather than choices:
                          # (§1) the mock's nav CONTRADICTS UX-DR33 (`epics.md:278`) on two axes --
                          # ten entries against six, and "Topologie" appears ZERO times in the
                          # mock's 496 KB, so the topology loses its nav entry while a RECORDED
                          # decision still names it; (§2) the AC's header asks for "perimeter" and
                          # "last observation", which the mock does not show, and *last observation
                          # is a DATABASE READ on every screen* -- colliding head-on with the
                          # epic's own constraint 1 ("no demo screen opens a connection"). Three
                          # resolutions costed, (a) recommended; (§3) the nav carries `device`,
                          # a DETAIL screen with no addressable URL, against an AC promising
                          # bookmarkability; (§4) what becomes of `/`, the product's only screen.
                          # Plus (§5): this story is the REGISTERED owner of 6b.1's withdrawn
                          # Tailwind ACs, and whether the chain lands here is a choice, not an
                          # inheritance -- the shell is ten rules.
                          # NEXT = `create-story validate`, TWO fresh-context layers, MANDATORY.
                          # (Earlier the same day) 6b.1 -> `done`: PR #95 squash-merged as `0b42bd0` after a green CI
                          # run (1m07). **The product is LIGHT and set in Barlow** -- the first
                          # operator-visible change since 5.14b. Next: 6b.2 (the shell: header,
                          # navigation, ten routes), which INHERITS the Tailwind chain with the
                          # four spellings D55 lacks -- read `deferred-work.md` first.
                          # (Earlier the same day) 6b.1 CODE-REVIEWED (three layers, Sonnet 5 -- a different model from
                          # the implementer) and REPAIRED: 14 findings, ALL applied. 🔴 The
                          # headline is not a product defect: FIVE of the story's SEVEN guards
                          # read as coverage and were none, each defeated by an ORDINARY
                          # gesture -- an OS dark-mode media query above the real `:root`,
                          # spaces inside `var( … )`, a CSS fallback value, an inline `style=`
                          # attribute in a template the guard never read, and a
                          # protocol-relative `url(//host/…)` that four fixed probes missed.
                          # All five rebuilt as PROPERTIES rather than enumerations; M13-M19
                          # red, five of them replaying a mutation that was GREEN before its
                          # patch. 🔴 And the record itself was wrong four ways: the baseline
                          # was 591 not 597 (the delta is +7, not +1), `cargo fmt --check` was
                          # NOT clean on the committed tree (CI would have reddened -- clippy
                          # was fixed by hand AFTER fmt and only clippy re-run), app.css's size
                          # was wrong in both units, and TWO divergences the story called
                          # "registered" were in no register. The last one is now three rows,
                          # the third recording that the story's own §Traps PRESCRIBED the
                          # check that would have caught it.
                          # NEXT = `gh pr create` -- and note that a pushed branch triggers NO
                          # CI (ci.yml fires on push:[master] and pull_request only).
                          # (Earlier the same day) 6b.1 -> `review`: IMPLEMENTED. The product is LIGHT and in Barlow --
                          # the first change to what the operator sees since story 5.14b, and the
                          # first time the mock's system is in the binary. `app.css` 150 -> 281
                          # lines, five faces + OFL.txt embedded (measured served: 200 font/woff2),
                          # `--accent` -> `--accent-document` across all six sites, D37 closed in
                          # BOTH directions (versioned path 200, old path 404). NO Tailwind chain,
                          # by Guy's arbitration. 🔴 The test-first order caught an omission of the
                          # implementer's: the whole sheet was rewritten while `data-theme="dark"`
                          # stayed in gap.html -- the one line T1 names -- and every other test was
                          # green. 🔴 A second guard was measured hollow before shipping: the first
                          # AC2 test read only the SHEET, so it asserted a @font-face was DECLARED
                          # and never that a face was EMBEDDED. Eleven mutation ids: nine reds, one
                          # control green, and TWO rows struck out as MIS-DESIGNED (a preserved
                          # prefix; a pinned value) -- caught only because both contradicted a
                          # written prediction. Carriers mixed: one panic!-carried, zero
                          # compiler-carried; "every red assertion-carried" is NOT claimed.
                          # The live count lives in the STORY FILE (its AC8), never here.
                          # NEXT = `code-review`, on a DIFFERENT model from the implementer.
                          # (Earlier the same day) 6b.1 ✅ VALIDATED (two fresh-context layers) and ARBITRATED by Guy --
                          # **option (1): the mock's tokens now, the Tailwind chain LATER**, with
                          # AC1/AC5/AC6 re-owned to story 6b.2 and the five measurements that
                          # produced the decision attached to them in `deferred-work.md`.
                          # 🔴 The gap-hunt layer BUILT the chain (v4.3.3, headless browser, live
                          # MariaDB) and refuted FOUR of the story's own predictions plus one of
                          # D55's: the `@source` trap no longer exists (correct path, wrong path
                          # and NO @source give one sha256); auto-detection makes app.css a
                          # function of the repo's PROSE; `@theme` strips the tokens; M7 reds where
                          # the draft called it uncarried while the unwritten M7b is GREEN; and
                          # preflight ALONE breaks the first-boot screen. 🔑 The decisive
                          # measurement: the intersection between the 27 classes the templates use
                          # and the 20 utilities Tailwind generates is EMPTY -- so AC5 was a gate
                          # over an empty set, this story's own quoted defect class applied to
                          # itself. The fact-check layer refuted 10 of 62 claims, 5 HIGH.
                          # NEXT = `dev-story` 6b.1, scoped to option (1).
                          # (Earlier the same day) 6b.1 CONTEXTED -> `ready-for-dev`, and **EPIC 6b OPENS**. 🔴 The
                          # contexting measured that the story's first AC presupposes a toolchain
                          # this repository does NOT have: `cargo xtask css` does not exist (xtask
                          # has ONE subcommand, `ci`), there is no `assets/tailwind.css`, no
                          # `tools/` binary, and `app.css` is HAND-AUTHORED -- its own header says
                          # so, while `.gitignore:40` and CLAUDE.md's stack line both call it
                          # generated. Three documents disagree; two become true when this story
                          # lands. 🔴 Second measurement: the reference mock carries NO font bytes
                          # (one `base64` in 495 KB, inside its own viewer JS; every @font-face
                          # src is an artifact-host UUID), so Barlow comes from `jpt/barlow` under
                          # OFL 1.1 -- five faces, ~291 KB into the binary. ⚠️ A SPLIT QUESTION is
                          # put to Guy in §7 (the CSS chain vs the design system: two deliverables,
                          # one AC set -- Epic 5's shape, six insertions) and is NOT decided here.
                          # NEXT = `create-story validate`, TWO fresh-context layers, MANDATORY.
                          # (Earlier the same day) 6.3 -> `done`: PR #93 squash-merged as `d284cfb` after a green CI run
                          # (1m08) -- dev-story, the mutation pass, a three-layer code review on a
                          # DIFFERENT model (12 patches, 1 deferral, 2 dismissed with the
                          # measurement that dismissed them) and the merge. 🔑 The branch had been
                          # pushed on 2026-08-15 and CI had NEVER run on it: `ci.yml` triggers on
                          # `push: [master]` and on `pull_request` only, so a pushed feature
                          # branch triggers nothing -- every green figure was LOCAL until the PR
                          # existed. 🔑 Same day, `master`'s protection was extended to
                          # administrators (`enforce_admins`, Guy's arbitration): PR-required, the
                          # `ci` check, strict-up-to-date, linear history and the force-push ban
                          # were all already in place and all bypassable by the repo owner -- that
                          # was the only hole. Scope is `master` ALONE; `gh-pages` stays
                          # direct-push (no check runs there) and `v*.*.*` tags are untouched.
                          # Next: Epic 6b (the mock's interface), then 6.4.
                          # (Earlier: 2026-08-15) 6.3 -> `ready-for-dev`: contexted, and its own numbering comment
                          # corrected (it said "assertions 2 and 3"; the parked pair is 1 and 2).
                          # ✅ VALIDATED same day, TWO fresh-context layers, BOTH of which BUILT a
                          # prototype of the prescribed eighth gate rather than reasoning about
                          # one. Corrections all applied; 2 arbitrations raised and TAKEN by Guy
                          # (AC2's divergence half is unsatisfiable as epics.md words it; the
                          # gate breaks the file-size ceiling). epics.md NOT edited; the live
                          # count lives in the story file. Nothing is blocked -- next: dev-story.
                          # (Earlier: 2026-08-14) 6.2 -> `done`: PR #91 squash-merged as `ead7738` after a green CI run
                          # -- dev-story, three-layer code review (13 findings, 6 patched, 1
                          # deferred by Guy, core measured sound) and the merge. Next: 6.3 (NFR5's
                          # two parked assertions). (Earlier note: 6.2 -> `review`; the live count
                          # lives in the
                          # STORY FILE, its AC9 -- this comment must not carry it, F2's rule).
                          # The route now WRITES an adopted declared value, J3's corrected half
                          # measured end-to-end. Mutation pass: 13 reds + M4 compile + M7/M8
                          # green-by-structure; three findings (M6's missing empty-projection test
                          # ADDED, M9b's dead null check removed, M7 recorded). `done` is the
                          # MERGE's business. (Earlier: VALIDATED -> stays `ready-for-dev` (now MEANT: contexted AND
                          # its mandatory validation done -- three sources, incl. a gap-hunt that
                          # BUILT the design against a live MariaDB, + the two-browser CSRF bench).
                          # 1 HIGH arbitration taken by Guy (option A: the authorship gate gains a
                          # named READ-sanction, FR13-framed -- 5.12's write-guard did not cover a
                          # legitimate provenance READER); 4 mutation-row defects the build
                          # measured (M8 GREEN by the index, M11's oracle is abstention not gap,
                          # M12 needs a source scan not a behavioural test, M2's gate message);
                          # ~10 text corrections; ALL APPLIED. CSRF (row j) CLOSED by the Origin
                          # check, Chromium bench (row a) discharged, Gecko-cross-site residual
                          # re-registered. 🔴 The contexting found the epic AC's letter
                          # WRONG about the fn: "through `insert_declared_attribute`" would add
                          # ZERO sanctioned sites while the same AC says "gains exactly one" --
                          # the schema resolves it (a documented value is an ADOPTED one,
                          # `origin='adopted'` + `origin_obs_id`, 0001's own vocabulary), so the
                          # write is a NEW sibling `adopt_declared_attribute`; divergence
                          # registered, epics.md not edited. The story also OWNS: CSRF (6.1's
                          # row (j), Origin check), the Chromium bench (row (a), at validation),
                          # the canonical-UUID closure, the anti-double-count unique index
                          # (0005), and gap::project going pub so documented == compared.
                          # (Earlier the same day: 6.1 -> done, PR #89 `664693b`, review's 17
                          # findings applied, docs flipped by PR #90.)
                          # (The note below was written at 5.14b's contexting and is DATED.)
                          # 5.14b CONTEXTED -> `ready-for-dev`. **Epic 5 stands at 19/20**; 5.14b is
                          # the last story, then the RETROSPECTIVE, which is REQUIRED and which is
                          # what CLOSES the epic -- not the last story.
                          # ✅ ITS THREE OPEN ARBITRATIONS WERE TAKEN by Guy the same day (10: two
                          # sections never summed; 11: the reader never fails; 12: the grouped read
                          # subsumes 5.14's counter and its false doc sentence is corrected) -- see
                          # the 5.14b block below, where each carries the alternative REFUSED and
                          # the reason. Two of them leave something REGISTERED rather than done: the
                          # schema-side domain of `abstention_cause`, and the deliberate divergence
                          # from the UX spec's one-panel mock.
                          # ⚠️ NEXT = `create-story validate` 5.14b, then `dev-story`.
                          # (superseded entry:) 5.14 **DONE** -- PR #81 squash-merged as `48b19b2` after a green CI run
                          # (1 min 17 s). **Epic 5 is now 19/20.** ⚠️ CI DOES exercise the six new
                          # DB tests: `ci.yml:24` exports `DATABASE_URL` against a `mariadb:10.11.11`
                          # service, so the green means something -- a suspicion the review's blind
                          # layer raised, checked rather than assumed. Its sibling concern STANDS:
                          # `OPENCMDB_NET_TESTS` is set nowhere, so a test gated on it never runs.
                          # ⚠️ ONLY **5.14b** REMAINS, then the RETROSPECTIVE, which is REQUIRED and
                          # which is what CLOSES the epic -- not the last story.
                          # NEXT = `create-story` 5.14b.
                          # (superseded entry:) 5.13b **DONE** -- PR #79 squash-merged as `6c95115` after a green CI run
                          # (1 min 4 s). **Epic 5 is now 18/19.** Branch deleted locally and on
                          # `origin`. `master` carries **494 tests** (273 bin + 159 core + 62 xtask),
                          # seven gates green, **28 fixtures**, **26 traps across TEN families**,
                          # trap gate still RED. NEXT = `create-story` 5.14, then Epic 5's
                          # RETROSPECTIVE, which is REQUIRED and closes the epic.
                          # (superseded entry:) 5.13b **CODE-REVIEWED (three layers) and REPAIRED** -> stays `review`
                          # (`done` is the MERGE's business). **493 -> 494 tests**, seven gates
                          # green, 28 fixtures. NEXT = PR.
                          # 🔑 THE LESSON, and it is Guy's arbitration rather than a patch: the
                          # story promised *"`opencmdb-core` is not touched"* in FOUR documents, and
                          # that promise is precisely what kept it out of `score.rs:443`, where a
                          # comment asserts as a MEASUREMENT what this story's own deliverable
                          # falsifies. **A promise of non-modification protects behaviour and
                          # shelters false sentences.** Narrowed to *"no BEHAVIOUR change"*, the
                          # sentence corrected, and the shape carried to the retrospective.
                          # 🔴 THREE CLAIMS OF THE DEV RECORD WERE REFUTED BY RE-MEASUREMENT: M10 is
                          # **37 red, not 1** (the figure came from a FILTERED run -- the driver
                          # defect the story's own Dev Notes warn about two paragraphs above);
                          # M7's *"unreachable in a full run"* is FALSE (18 red, `trap_gate.rs:1183`
                          # among them) and what let it stand was a `head -8` on my own measurement
                          # output -- **a measurement read through a truncation is not a
                          # measurement**; and the headline *"fourteen rows, twelve mutations, two
                          # CONTROLS green by design"* is **fifteen rows, thirteen mutations**, with
                          # each control's green scoped to the ONE test it names. Fourth consecutive
                          # story with that last family, in the story whose AC8 names it.
                          # ⚠️ A guard was measured carried by NOTHING and is now closed: swapping
                          # the twins' tight descriptor for `corpus_caps()` left all 493 green.
                          # M2b measured what the tightness CATCHES, never what catches its LOSS --
                          # the house rule runs the other way. `the_blinded_source_twins_keep_their_
                          # tight_descriptor` reds on the ordinary DRY gesture.
                          # ⚠️ The trap-text privacy floor had gone SLACK: this story's own trap
                          # reasons introduce a FIFTH MAC, so a floor left at 4 would have tolerated
                          # losing a previously-pinned value. Raised to 5. **A floor is only a guard
                          # while it equals what is there.**
                          # ⚠️ And the count sweep missed thirteen more sites, including one my own
                          # §6c had LISTED. All corrected.
                          # (superseded entry:) 5.13b CONTEXTED -> `ready-for-dev`. **Epic 5 stands at 17/19.** See the
                          # `5-13b-*` block in development_status below for Guy's FOUR arbitrations,
                          # the three inherited clauses refuted by code, and the eight literal counts
                          # the new artefacts move.
                          # 🔑 The one to carry forward: **arbitration 4 REVERSES the literal form of
                          # arbitration 2**, on a fact of the code (`fixtures.rs:1857`, the
                          # corpus-wide `obs_id` anchor) found after arbitration 2 was taken. Both
                          # are recorded; neither overwrites the other. The consequence is that the
                          # STRICT monotone-honesty claim stays on the IN-MEMORY derivation, where
                          # `obs_id`s are equal by construction, and the committed twin is held to
                          # it by a guard rather than trusted to stay in step.
                          # ⚠️ NEXT = `create-story validate` (two fresh-context agents, MANDATORY).
                          # (superseded entry:) 5.13 **DONE** -- PR #77 squash-merged as `4f7652d` after a green CI
                          # run (58 s). **Epic 5 is now 17/19.** Branch deleted locally and on
                          # `origin`. `master` carries **489 tests** (268 bin + 159 core + 62
                          # xtask), seven gates green, 25 fixtures, trap gate still RED.
                          # NEXT = `create-story` 5.13b.
                          # 🔑 THE STORY'S LESSON FOR THE EPIC 5 RETROSPECTIVE, and it is not the
                          # one the story set out to teach: **one family of defect struck THREE
                          # times inside this single story** -- a measurement reporting success
                          # because it measured nothing. (1) M2, the mutation named for the very
                          # assertion the story exists to defend, was invisible to a differential
                          # test. (2) M6 reported 0 red because the driver passed two filters to a
                          # command accepting one. (3) The driver's `git checkout` ATE the
                          # uncommitted review repairs, so four mutations measured the wrong tree.
                          # ⚠️ **All three were caught for the same reason and only that reason: a
                          # GREEN contradicted a PREDICTION.** Not one was found by reading. A
                          # mutation pass without written predictions would have filed all three as
                          # confirmations.
                          # (superseded entry:) 5.13 CONTEXTED and **SPLIT** -- see the `5-13-*` block in
                          # development_status below for the four findings and Guy's three
                          # arbitrations. **5.13 = the monotone-honesty MEASUREMENT**
                          # (`ready-for-dev`); **5.13b INSERTED = the committed trap family**
                          # (`backlog`). **Epic 5 is now 16/19.**
                          # Baseline RE-MEASURED on `6078246`, not quoted: **469 tests**
                          # (248 bin + 159 core + 62 xtask, 1 ignored), **seven gates green**,
                          # **25 fixtures**, `views-hash` ℹ STALE by design (issue #50).
                          # 🔑 The story is designed against ONE failure mode, and it is NOT
                          # 5.11b's: **`faulted ⊆ clean` is satisfied by EQUALITY.** A connector
                          # that ignores every fault, a mutilation that removes nothing, and a test
                          # whose faulted run IS the clean run all pass the inclusion. So every AC
                          # asserts the inclusion **and its strictness**, as two assertions with two
                          # messages -- (i) failing is a product defect, (ii) failing is a TEST
                          # defect, and one combined assertion cannot tell them apart.
                          # 🔑 And the sharpest design point, which the naive reading gets wrong:
                          # **an abstention is NOT an invented fact.** Under the blinding mutilation
                          # the faulted run writes a row the clean run does not have -- legal, and
                          # the honest answer (FR16). So the engine-layer subset is over PLACEMENTS
                          # `(observation_id, interface_id)`, with `rule_id`/`evidence`/`outcome`
                          # deliberately excluded: a fault legitimately WEAKENS a justification, and
                          # a row-level subset would red on a run that did exactly the right thing.
                          # ⚠️ That same exclusion is what leaves D36's lattice monotonicity
                          # UNCLAIMED -- re-owned (Epic 6 for the doubt order, 5.13b for the
                          # capability snapshot), never implied. And **NFR8 has four assertions;
                          # this story covers ONE.**
                          # (superseded: validate, dev-story and code-review have all run. The
                          # live state is the `5-13-*` block in development_status below.)
                          # (5.12 entry:) 5.12 **DONE** -- PR #75 squash-merged as `378f0f8` after a green CI
                          # run. **Epic 5 is now 16/18.** Branch deleted locally and on `origin`.
                          # `master` carries 469 tests (248 bin + 159 core + 62 xtask), **SEVEN**
                          # gates green, 38 authorship probes.
                          # NEXT = `create-story` 5.13.
                          # 🔑 THE STORY'S REAL LESSON, for the Epic 5 retrospective: a SECOND
                          # session read the deliverable without the context of having written it
                          # and produced **seven confirmed findings**, plus one hypothesis measured
                          # and REFUTED and one non-finding converted into a measurement. **NONE
                          # was reachable by the mutation pass** -- one does not mutate what one
                          # did not think of -- and **three were false sentences in the section
                          # that PROMISES**. The corpus could not carry them either: it took a
                          # LOCATED verdict, a new AXIS, and a CONTROL.
                          # 🔑 **AND THE CLAUSE WITHOUT WHICH THE LESSON PRODUCES NOTHING: the
                          # reading produced nothing until it MEASURED.** Each of the seven sends
                          # arrived with its reproduction, and each was RE-MEASURED here before
                          # being believed -- including the one that refuted the reader. A reading
                          # without measurement yields a list of SUSPICIONS: work for the writer,
                          # nothing for the story. **"Have it read by a fresh context" without "and
                          # require measurement on BOTH sides" will produce suspicion lists.**
                          # (The clause is the reader's own, proposed rather than claimed.)
                          # (review+repair entry:) 5.12 CODE-REVIEWED (three layers) **AND REPAIRED** under Guy's
                          # arbitration -- **voie A: repair AND narrow the promise**, over (B) a
                          # database GRANT and (C) dropping the gate. Still `review`: `done` is the
                          # MERGE's business. **463 -> 469 tests** (248 + 159 + **62** xtask),
                          # seven gates green. NEXT = PR, CI, squash merge.
                          # 🔴 THE REVIEW DID NOT FIND DEFECTS IN THE GATE -- it found the gate did
                          # not hold the property it claimed. Thirty hand-written violations of
                          # NFR5, **SIXTEEN passed**, three of them executing against a live
                          # mariadb:10.11.11. Committed as a regression corpus at
                          # `xtask/probes/authorship/` (+ e31, e32 added during the repair), all
                          # **32 verdicts pinned in BOTH directions** -- a gate that silently
                          # WIDENS is a gate whose stated limits have gone stale.
                          # 🔴 STRUCTURAL: the whole BODY of `gate_declared_authorship` was
                          # deletable with the xtask suite green (56/56). Every test attacked
                          # `authorship_findings`; `float-free` had carried the end-to-end test
                          # since 5.4b and says so in its own doc. ⚠️ It hid behind a mutation
                          # LABEL -- M5 said "neuter the gate" and hit the MATCHER. Re-measured:
                          # matcher neutered = 8 reds; gate body replaced by a pass = **0** before
                          # this pass, 2 now (M20).
                          # 🔑 THE NARROWING IS HALF THE DELIVERABLE: a TRIPWIRE against the
                          # good-faith violation, never a barrier against a determined one. Two
                          # residual classes -- a query assembled at runtime (e02), and guard
                          # NEUTRALISATION (e14, e31): the gate guards the WRITE, not the guard
                          # itself, and voie B's GRANT is REGISTERED as the real closure rather
                          # than implied by this one.
                          # ⚠️ The criterion does NOT cleanly separate e22: CREATE OR REPLACE TABLE
                          # destroys the guard just as thoroughly and reds, and M19b proves that
                          # red INCIDENT (it survives removing the verb -- the REPLACE inside the
                          # phrase governs). Kept on purpose, not by the criterion. Found by
                          # READING, by a second session launched on this same story in parallel.
                          # 🔑 Ten mutations, ten reds, **every one carried by a NAMED assertion**,
                          # zero compiler- and zero `.expect()`-carried -- classified by reading
                          # each panic message, because the earlier table's header claimed
                          # "every red assertion-carried" while its own M6 row said `.expect()` x3
                          # (5.9b's defect, reproduced).
                          # 🔴 TWO MUTATIONS CAME BACK GREEN FIRST and both refutations are the
                          # deliverable: `is_invisible` was carried by nothing (e06 puts its
                          # zero-width space BEFORE the verb, already a token boundary -> e32 puts
                          # one INSIDE the words), and the long verb changes no verdict, only the
                          # finding's NAME. ⚠️ Two more were MIS-DESIGNED, not weak: removing a
                          # test's own assertion cannot red that test; for an assertion the honest
                          # mutation breaks the INVARIANT (M21b plants an orphan, M22b makes the
                          # count lie).
                          # 🔴 A THIRD defect, the ONLY one INSIDE the narrowed promise:
                          # `SANCTIONED_FNS` matched the function NAME wherever it lived, so a new
                          # file holding nothing but `fn insert_declared_attribute` writing the
                          # table left the gate GREEN (measured; the name was enough). Not an
                          # adversary's probe but THE ORDINARY GESTURE -- copy the adapter into
                          # another module. **A hole beside the promise is a stated limit; a hole
                          # INSIDE it is a false sentence.** Now one `SANCTIONED_SITES` keyed on
                          # (path, enclosing fn). Probe e33 carries a NEW AXIS: the other 32 ask
                          # *does it red*, e33 asks whether the sanctioned NAME lets a write
                          # through from elsewhere. M24b reds 2 and breaks no legitimate site;
                          # M25 (path compared unnormalised) reds 2 -- the Windows trap, measured.
                          # **THIRTEEN mutations, thirteen reds.** Counts: **463 -> 469**
                          # (248+159+62), **38 probes**.
                          # 🔴 A FOURTH finding, a SECOND hole INSIDE the promise: the `"` bound
                          # of `statement_after` truncated the READ half, because in a .rs file a
                          # `"` can be a quote inside the SQL -- every predicate after it lost, and
                          # predicates are where provenance columns live. GREEN on both spellings
                          # Rust permits (raw string, `\"`), with a CONTROL -- the same read with
                          # no quoted literal -- RED all along. 🔑 **The control is what makes the
                          # pair mean anything**: committed as e36, and under M26/M27 the quoted
                          # forms go green while it stays red. The `"` bound is KEPT; quotes INSIDE
                          # a literal become a sentinel during normalisation.
                          # ⚠️ `is_invisible` was an ENUMERATION and missed the variation selectors
                          # (INS<U+FE0F>ERT passed). Widened ONCE, deliberately NOT to exhaustion:
                          # **an enumeration cannot claim the completeness of a property** -- that
                          # sentence is the deliverable, not a longer list. Closure = voie B.
                          # **SIXTEEN mutations, sixteen reds, 37 probes.**
                          # ⚠️ A FIFTH finding, WEAKER, and given WITH its bound (the session
                          # deflated it rather than inflating it after four accepted -- itself the
                          # lesson): the exemption's comment said CREATE TABLE is "never a write of
                          # a value", FALSE for `CREATE TABLE … AS SELECT`. 🔑 But against a schema
                          # 0001 has migrated that form FAILS and `IF NOT EXISTS … AS SELECT` is
                          # INERT, while the form that would really run -- CREATE OR REPLACE TABLE
                          # … AS SELECT -- ALREADY REDS (measured). A false SENTENCE with narrow
                          # executable reach, not a sixth hole. **Chosen: fix the sentence, not the
                          # matcher**; e38 pins the form GREEN by decision.
                          # 🔑 A NON-FINDING kept as a MEASUREMENT: the `.rs`+`.sql` perimeter would
                          # miss a .sh/Dockerfile/compose carrying inline SQL -- THERE ARE NONE
                          # (compose declares no DB container, external MariaDB by decision, no
                          # init volume). No finding manufactured from the hypothesis; the SENTENCE
                          # is recorded in AUTHORSHIP_ROOTS' doc: complete TODAY, measured not
                          # supposed, and **reopen the perimeter the day the compose gains a DB
                          # container with an init script** -- the deployment story owns that debt.
                          # **38 probes.**
                          # 🔑 THE PARALLEL SESSION'S TALLY, AS A RULE NOT A NUMBER: **seven
                          # sends -- seven CONFIRMED findings** (the 7th outside the repo, in the
                          # resume-point memory a next session reads BEFORE the code), **one
                          # hypothesis measured and REFUTED**, one NON-finding converted into a
                          # measurement. **Three of the seven were false sentences in the section
                          # that PROMISES.** ⚠️ It first read "six" -- caught by the session that
                          # produced the findings, the missing one being the resume point that same
                          # paragraph had just called the only place that mattered for the morning:
                          # a tally written in flight, inside the document explaining why not to.
                          # 🔑 THREE documents described a state the code had passed -- the
                          # mutation-table header, project-context's "six gates", and
                          # `xtask/src/main.rs`'s OWN module doc, which enumerated six gates while
                          # the file implemented seven. **Two of the three in the gate's own file.**
                          # (implementation entry:) 5.12 IMPLEMENTED -> `review` (NOT `done`: that is the MERGE's business).
                          # **450 -> 463 tests** (248 bin + 159 core + 56 xtask), **SEVEN gates
                          # green**, both clippy forms clean, no new dependency. Built against a
                          # live mariadb on **13307** (5.11b still holds 13306).
                          # NEXT = `code-review` 5.12, then PR.
                          # 🔴 M1sql was GREEN ON FIRST MEASUREMENT and the reason was NOT the
                          # scope: the gate DID read the planted `.sql` migration (31 -> 32 files)
                          # and found nothing, because `strip_line_comment` handles `//` only, so
                          # the `--` header ran into the statement under whitespace normalisation
                          # and the fragment no longer BEGAN with `update`. Closed by
                          # `strip_sql_comment` and pinned.
                          # 🔴 TWO FALSE POSITIVES, both the "wrong in both directions" family:
                          # `SELECT COUNT(*)` read as a wildcard -- the gate's VERY FIRST red, on
                          # the committed tree at repo.rs:106 -- and a match spanning two string
                          # literals. Both closed, both pinned.
                          # 🔴 A PREDICTION OF THE STORY'S, REFUTED: the gate does NOT inherit
                          # float-free's block-comment false positive. It anchors on a statement's
                          # HEAD, so a verb inside /* ... */ matches nothing -- and green is the
                          # right answer, a commented-out write being no code path.
                          # 🔴 THREE sanctioned sites where the story prescribed two:
                          # `docker/seed-example.sql` is forced by the story's own scope, and its
                          # consequence is registered (an edit changing its actor would pass).
                          # 🔑 AC1 and AC2 no longer contradict: AC2's raw write lives in the
                          # allowlisted `raw_declared_write_for_ddl_test`, NO cfg(test) exemption.
                          # 🔑 M4's carrier is the GATE ALONE (0 compile errors, all runtime tests
                          # green); M3's is an ASSERTION as AC2 demanded, not `.expect_err`; M6 is
                          # ENTIRELY SILENT without DATABASE_URL.
                          # 🔑 A FIFTH mechanism, absent from the first draft and the strongest: the
                          # PK with no ON DUPLICATE KEY UPDATE means the adapter cannot overwrite a
                          # declared value AT ALL.
                          # ⚠️ A DRIVER DEFECT cost 64 spurious reds: `git checkout` does not restore
                          # a DATABASE, and a planted migration leaves `_sqlx_migrations` naming a
                          # file that no longer exists. Registered as a method note.
                          # ⚠️ NFR5 has THREE assertions and this story covers ONE (plus FR13's
                          # corollary). The other two need the `document` gesture -- registered with
                          # the triage epic, so "NFR5 is covered" is never read as met in full.
                          # (validation entry:) 5.12 CONTEXTED **AND VALIDATED** -> `ready-for-dev`. NEXT = `dev-story`.
                          # Two fresh-context agents; the gap-hunt BUILT the whole story on a
                          # scratch worktree against its own mariadb on 13312 -- the seventh gate
                          # (~210 code lines), its tests, AC2's raw SQL and all six mutations --
                          # reaching **453 tests, SEVEN gates green**, both clippy forms clean.
                          # 3 HIGH from the fact-check, 6 from the gap-hunt.
                          # 🔑 TWO WERE FOUND INDEPENDENTLY BY BOTH LAYERS:
                          #   . §1's mechanism 4 is FALSE. Adding `origin` to the SELECT COMPILES
                          #     CLEANLY and leaves ALL 450 TESTS GREEN -- sqlx decodes tuples
                          #     POSITIONALLY and discards extra columns. The compile error fires
                          #     only if the TUPLE is widened too. So AC3's carrier is the GATE
                          #     ALONE, where the first draft predicted a compiler carrier.
                          #   . AC1 CONTRADICTED AC2: AC2 needs a raw INSERT in a test, AC1's gate
                          #     reds on any INSERT outside the adapter -- the gate reddened on the
                          #     story's OWN test. Resolved by a TWO-SITE ALLOWLIST by function name,
                          #     NOT by the cfg(test) exemption the gap-hunt tried, which was
                          #     measured to hide a planted write inside a test module.
                          # 🔴 THREE SCOPE HOLES, all measured GREEN when they should red: a planted
                          # `.sql` MIGRATION, backticked and schema-qualified table names, and
                          # `INSERT` without `INTO` (MariaDB accepts it). And the census said "whole
                          # workspace" while meaning "under crates/" -- `docker/seed-example.sql:28`
                          # is a second, SHIPPED writer the gate could not see.
                          # 🔴 The DDL has THREE provenance columns and AC3 named two
                          # (`origin_obs_id` is the adopted observation); `SELECT *` defeats them
                          # all. The READ half had NO evasion table and its naive matcher produced
                          # FALSE POSITIVES on the clean tree -- float-free's "wrong in both
                          # directions" lesson reproduced exactly.
                          # 🔑 A FIFTH MECHANISM WAS MISSING AND IS THE STRONGEST: the PK
                          # (entity_id, attr_key) with NO `ON DUPLICATE KEY UPDATE` anywhere means
                          # the adapter cannot overwrite a declared value AT ALL (ERROR 1062). For a
                          # story called *never overwrite*, it belonged first.
                          # ⚠️ The two layers DISAGREED on `line_has_float`'s line and I measured it
                          # myself: **915** (the fact-check was right). A subagent finding is a lead.
                          # ⚠️ Also: AC2's prescribed red was PANIC-carried; M6 is entirely silent
                          # without DATABASE_URL; `0001`'s unmodifiability is CONDITIONAL (fresh test
                          # DBs migrate fine, the NAS is what breaks); the `0002:83` trap belongs to
                          # 5.11 not 5.9; `:1737` is a TEST banner; and §7's port collided with
                          # 5.11b's still-running container (now 13307).
                          # 🔑 AC5 ANSWERED IN ADVANCE: xtask 1136 -> **1346** code lines, 654 to
                          # spare under the 2000 ceiling. Not at risk.
                          # (contexting entry:) 5.12 CONTEXTED -> `ready-for-dev`. NEXT = `create-story validate`
                          # (MANDATORY here, two fresh-context agents), THEN `dev-story`.
                          # 🔴 THE LOAD-BEARING FINDING: the never-overwrite invariant is ALREADY
                          # held, by FOUR mechanisms and not one of them is a test --
                          #   (1) the write path binds SQL LITERALS, not parameters (`origin` is
                          #       'manual', `actor_id` is 'operator'), so a caller CANNOT supply a
                          #       non-human author: there is no parameter to pass one through;
                          #   (2) a DDL CHECK, `declared_actor_not_scanner`;
                          #   (3) the read's COLUMN LIST omits `origin` and `actor_id`;
                          #   (4) the tuple type `Vec<(String, String, String)>` could not carry it.
                          # So this is 5.11b's shape, and 5.11b's pass (FOUR behavioural defects
                          # plus THREE claims of the implementer's refuted) is the evidence that the
                          # shape is dangerous. _(This read "8 claims refuted, 5 of them the
                          # implementer's" until 5.12's validation: the 8 was 5.11b's count of
                          # suspicions its reviewer REFUTED -- the opposite sense.)_
                          # 🔴 ITS HARDER HALF, AND THE REAL DESIGN DECISION: the AC asserts the
                          # ABSENCE OF A CODE PATH, and **you cannot measure the absence of code by
                          # running code**. A test exercises what exists; the violation is what a
                          # future story ADDS. So the guard is a SEVENTH `xtask` gate on D56/D65's
                          # idiom and `float-free`'s precedent -- whose OWN first matcher was
                          # measured wrong in BOTH directions (passed `1e-3` and `1.`, reddened
                          # "192.168.0.1"). The evasion table is prescribed and must be MEASURED
                          # before the matcher is called sound.
                          # ⚠️ ALSO FOUND AT CONTEXTING: `CHECK (actor_id <> 'scanner')` bans ONE
                          # STRING LITERAL, not a property -- 'engine', 'system' and '' all pass.
                          # Its own name is honest; `0001_initial.sql:16`'s comment ("a human; never
                          # 'scanner'") is NOT, and sqlx checksums migration files INCLUDING
                          # comments, so it is UNMODIFIABLE. Prescribed reading: the CHECK is a
                          # TRIPWIRE, the gate is the guard. AC2 pins the limit with a test that
                          # asserts 'engine' is ACCEPTED, labelled as the honest limit.
                          # 🔑 `xtask/src/main.rs` is 1136 CODE lines against the 2000 ceiling and is
                          # the workspace's largest file -- a seventh gate fits, and AC5 makes the
                          # cost explicit rather than a later surprise.
                          # ⚠️ `declared_attribute` DOES exist (since `0001`); the concern that 5.9's
                          # DDL had not created it was checked and is unfounded.
                          # (previous entry:) 5.11b **DONE** -- PR #73 squash-merged as `f597882` after a green CI run
                          # (1m10s). **Epic 5 is now 15/18.** `master` carries **450 tests**
                          # (245 bin + 159 core + 46 xtask); the branch is deleted locally and on
                          # `origin`. NEXT = `create-story` 5.12.
                          # ⚠️ CI DID NOT TRIGGER ON THE FIRST PUSH -- no run was created for the
                          # branch OR the PR, and none anywhere in the repo for ~15h, while the
                          # workflow was `active` and GitHub reported Actions `operational`. Closing
                          # and REOPENING the PR re-triggered it and it passed first time. **The
                          # cause is UNKNOWN and is deliberately not guessed** (this project's rule:
                          # a cause needs a named check). If it recurs, that is an issue to open.
                          # (review entry:) 5.11b IMPLEMENTED **AND CODE-REVIEWED** -> was `review` (NOT `done`:
                          # that is the MERGE's business). **429 -> 446 -> 450 tests** after the
                          # review (245 bin + 159 core + 46 xtask). NEXT = PR, then squash-merge.
                          # THREE LAYERS, 2026-08-06; the Edge Case Hunter ran its own mutations
                          # against the live DB and the Auditor re-executed the whole T7 pass.
                          # 🔑 TWO LAYERS INDEPENDENTLY FOUND THE SAME TWO DEFECTS (the false
                          # `XorShift64` doc, the corpus test's weak oracle) -- the strongest signal
                          # this process gives.
                          # 🔴 THE STORY'S OWN HEADLINE DEFECT RECURRED INSIDE THE STORY:
                          # `SEED_SWEEP`'s doc asserted the golden-value test guarded its provenance
                          # and that a clock-derived sweep left the suite green -- BOTH refuted by a
                          # test EIGHTY LINES BELOW IT IN THE SAME COMMIT. Sixth consecutive story
                          # with a doc falsified by a measurement in its own commit, and the first
                          # where the falsifying measurement was the story's own deliverable.
                          # 🔴 FOUR BEHAVIOURAL DEFECTS, every one found BY MUTATION, not by reading:
                          #   . "the refusal happens before anything is written" was guarded by
                          #     NOTHING -- moving the `return Err` to the END left 446/446 GREEN,
                          #     because every test runs inside `transact`, which rolls back on Err.
                          #     Now probed on a POOLED connection under autocommit; R1 reds it alone.
                          #   . NO order test touched the `interface` table's derived instants --
                          #     making `seen_window` follow arrival order left ALL FIVE order tests
                          #     green. Shape C now compares the windows; R4 reds it.
                          #   . the `facts.len()` term in `contradicts` was DROPPABLE with the suite
                          #     green -- it is the only term catching [x] vs [x,x]. R2 reds it now.
                          #   . the corpus test's join oracle was `!is_empty()` where its twin pins
                          #     an exact count. Pinned at 5 interfaces; R3 reds it.
                          # 🔴 THREE OF MY OWN CLAIMS WERE REFUTED BY RE-MEASUREMENT:
                          #   . `M3b+` was WRONG IN FIVE DOCUMENTS -- there are SIX count assertions,
                          #     not five; the sixth is inside `sampled_permutations` and guards
                          #     shapes B and C a second time. Deleting only the four `consumed` lines
                          #     leaves the 2 DB consumers RED. The code was better than the claim.
                          #   . `M1-fkw` was 10 red, not 11 -- I copied the driver's raw line count
                          #     (which includes `result: FAILED`) for that one row of ten.
                          #   . the timing figures rested on no check (692 ms here, 561 ms there).
                          # 🔑 DISMISSED WITH THE CHECK THAT DISMISSED THEM: "DB tests may not run in
                          # CI" is REFUTED (`ci.yml:24` exports DATABASE_URL against a mariadb
                          # service); the `rand` transitive claim VERIFIED. The Edge Case Hunter
                          # refuted EIGHT of its own suspicions by running them.
                          # 🔴 AC5 is PARTLY MET and was CORRECTED rather than ticked: the "test that
                          # reds when a new field is added" is a COMPILE error, and a `..` retires it
                          # with the suite green. Registered.
                          # (implementation entry:) 5.11b IMPLEMENTED -> `review`.
                          # Built against a live mariadb:10.11.11 on 13306. **429 -> 446 tests**
                          # (241 bin + 159 core + 46 xtask), six gates green, both clippy forms
                          # clean, `Cargo.lock` diff EMPTY. NEXT = `code-review` 5.11b, then PR.
                          # Three measurement shapes: A pure+exhaustive (720 permutations, twice --
                          # once synthetic, once over `hostname-absence.jsonl`), B purge-and-replay
                          # (12 sampled), C the no-op (12 sampled) + a reference-scale seeded sweep
                          # (300 obs over 100 MACs, 8 seeds, 692 ms).
                          # 🔴 THE STORY'S OWN CENTRAL QUESTION GOT TWO NEW ANSWERS, BOTH BAD, WHICH
                          # IS WHY IT WAS WORTH ASKING A SECOND TIME:
                          #   . M5's prediction is REFUTED. The golden-value test does NOT guard the
                          #     seed sweep's provenance -- it pins `shuffled` at a HARDCODED seed and
                          #     never reads SEED_SWEEP. A clock-derived sweep left ALL 445 TESTS
                          #     GREEN. Closed by a guard that reads the constant's VALUES; M5 now
                          #     reds exactly one test. Two properties had been conflated:
                          #     reproducible WITHIN one process is true for every seed; reproducible
                          #     ACROSS runs is what a fixed seed buys.
                          #   . M2 exposed that `shuffled` and SEED_SWEEP had NO CONSUMER at all, so
                          #     AC4's reference-scale half was satisfied by nothing. The new test
                          #     uses groups of THREE, because the existing reference-scale test gives
                          #     every observation its own MAC and a singleton never exercises the
                          #     witness convention.
                          # 🔑 WHAT HELD, MEASURED: shape C is NOT a duplicate of 5.11's idempotence
                          # test (under M2, 5.11's stayed green while shape C reddened); M1-noop
                          # leaves the whole suite green exactly as predicted; and M1-fkw leaves the
                          # CORPUS AC1 test green while the synthetic one reds -- so AC6 is
                          # satisfiable in letter and the synthetic slice carries the measurement.
                          # 🔑 M6 reds HERE (7 tests) where the story measured it green -- that is
                          # §5.3's count assertions working, and `M3b+COUNTS_DELETED` proves it:
                          # delete the five count assertions and the four consumers go green again.
                          # ⚠️ Ten mutation rows, ZERO compiler-carried reds. The one compile failure
                          # was an artefact of the mutation DRIVER (M5's rename hit a `use` line),
                          # repaired and re-run -- recorded so it is not read as a measurement.
                          # 🔑 One production change only: `RepositoryError::ContradictoryObservation`.
                          # `raw` AND the ORDER of `facts` are excluded from the comparison, each
                          # with a test asserting the bare `a != b` WOULD have refused it.
                          # (previous entry:) 5.11b CONTEXTED **AND VALIDATED** -> `ready-for-dev`.
                          # Two fresh-context agents; **14 findings applied, 5 HIGH**. The gap-hunt
                          # BUILT THE WHOLE STORY against a live mariadb:10.11.11 on 13311:
                          # **429 -> 441 tests**, six gates green, both clippy forms clean, and
                          # NO new dependency (Cargo.lock diff empty).
                          # 🔴 THE ANSWER TO THE STORY'S OWN CENTRAL QUESTION IS BAD, WHICH IS WHY
                          # IT WAS WORTH ASKING: **FOUR prescribed mutations were measured leaving
                          # the ENTIRE SUITE GREEN.**
                          #   . M1 as prescribed is a NO-OP -- the resolver consuming groups in
                          #     slice order cannot reach a shape-A test that never calls the
                          #     resolver. AC1 was reddenable by NO permitted mutation.
                          #   . M5 is GREEN: reproducibility WITHIN one process is trivially true
                          #     for any seed, so the seed's PROVENANCE was guarded by nothing.
                          #   . M6 is GREEN and turns AC1 into a tautology: permutations()[0] IS
                          #     the identity.
                          #   . A degenerate ENUMERATOR leaves AC1-AC3 green unless every consuming
                          #     test asserts its own permutation count -- proven by deleting the
                          #     four count assertions and re-running.
                          # 🔑 GUY'S TWO ARBITRATIONS:
                          #   (1) a MUTATION may edit `identity/` -- the ban is on the SHIPPED DIFF,
                          #       not on the prove-to-red pass. That is what makes AC1 measurable.
                          #   (2) AC5's comparison EXCLUDES `raw`: D19 says no decision reads it, so
                          #       refusing on it would red a case where nothing was at stake. The
                          #       comparison is explicit field-by-field, with a test that reds when
                          #       a new `Observation` field is forgotten.
                          # 🔑 WHAT HELD, ALSO MEASURED: shape C is a no-op today AND is not a
                          # duplicate of 5.11's idempotence test (under M2, 5.11's stayed green
                          # while shape C reddened); shape B's `interface_id` claim holds over 7
                          # samples; and the load-bearing sizing decision is exact -- **720
                          # permutations of the largest stream in 11.5 ms**.
                          # ⚠️ Also: partial-then-failed.jsonl ends in a Failure record so the
                          # obvious `.expect("poll")` PANICS; two streams carry their own
                          # connector/scope; the corpus context helpers are private; `main.rs`
                          # needs a `#[cfg(test)] mod` line so "untouched" was unsatisfiable; and
                          # resolver.rs:635 says "Nothing here reads fixtures/", which T5 falsifies.
                          # (contexting entry:)
                          # 🔑 THE LOAD-BEARING FINDING AT CONTEXTING: the committed replay streams
                          # carry **3 to 6 observations**, so `n!` is at most 720 and permutations
                          # can be ENUMERATED EXHAUSTIVELY rather than sampled. No `rand` (it is in
                          # no Cargo.toml), no seed, no flakiness -- and strictly STRONGER than the
                          # fuzz epics.md asks for. A seeded sweep is kept only for the 300-strong
                          # reference slice, from a FIXED seed range, never the clock.
                          # 🔴 THE STORY IS DESIGNED AROUND ONE FAILURE MODE: a test that cannot
                          # fail. The property it measures is already true by construction (four
                          # mechanisms, all BTree-based), so a test measuring nothing looks exactly
                          # like a success. Every AC names the mutation that must red it, and a
                          # mutation leaving the suite GREEN is a HIGH finding, not a reassurance.
                          # 🔑 THREE measurement shapes, not one: (A) pure+exhaustive over `join`
                          # and `candidates` -- the only one covering the derived interface SET;
                          # (B) purge-and-replay with a PERMUTED slice, where `interface_id` is
                          # literally comparable because the replay FINDS its interfaces; (C) the
                          # permuted slice into a POPULATED store must be a NO-OP -- the strongest,
                          # and possible only because 5.11 shipped idempotence.
                          # ⚠️ The generator gets its OWN test: a shuffle returning its input makes
                          # every other test in the story vacuously green.
                          # 🔴 ONE production change: `resolve_within` refuses a repeated `obs_id`
                          # with DIFFERING content (`by_id` is last-duplicate-wins, and the losing
                          # copy's keys still reach `join`). A repeated IDENTICAL observation stays
                          # legal -- 5.9b's and 5.11's tests for it must both keep passing.
                          # (previous:) 5.11 **DONE** -- PR #71 squash-merged as `ce0a770` after a green CI
                          # run. **Epic 5 is now 14/18.** `master` carries **429 tests**; the
                          # branch is deleted locally and on `origin`.
                          # NEXT = `create-story` 5.11b, which inherits by name from 5.11's
                          # contexting and review: `resolve_within` reads the slice's ARRIVAL ORDER
                          # twice (`by_id` is a last-duplicate-wins `.collect()`, and the tail
                          # abstention loop walks the raw slice), and two passes from an EMPTY store
                          # mint different `interface.id` values, so `snapshot_links` cannot compare
                          # `interface_id` literally between two orders -- the shape 5.10 hit on
                          # `id`.
                          # (previous:) 5.11 IMPLEMENTED **AND CODE-REVIEWED** -> `review`. `master`-branch at **429 tests**, six gates
                          # green, both clippy forms clean, corpus still 11 unanswerable.
                          # The pass is IDEMPOTENT: read the current ENGINE version of the slot,
                          # then insert / do nothing / close-and-append. `0004` relaxes
                          # identity_link_interval for CLOSED rows only.
                          # 🔴 THE REVIEW FOUND THAT THIS STORY TURNED A LOUD REFUSAL INTO A SILENT
                          # ORPHAN. `write_link` only reads the slot it is about to FILL, so a key
                          # that vanished from the input left a current link standing (the
                          # `multi-nic` shape) -- and that orphan is a REACHABLE counterexample to
                          # story 5.10's purge-and-replay invariant, through pure engine input.
                          # The uniqueness key had been doing the detection; the compare routed
                          # around it. Attributed by MEASUREMENT: the Edge Case Hunter neutralised
                          # the new read with `AND 1 = 0` and got the pre-5.11 loud failure back.
                          # 🔑 GUY'S TWO ARBITRATIONS AT THE REVIEW:
                          #  (1) `resolve_within` now CLOSES every current engine slot the pass did
                          #      not visit -> new counter `links_vacated`. M10 reds it.
                          #  (2) an `observed_at` running BACKWARDS was fatal to the WHOLE batch on
                          #      one branch and SILENT on the other -> refused by name,
                          #      `RepositoryError::InstantRegressed`, a guard ABOVE the DDL. M11
                          #      reds it, and its pre-guard output was `Constraint("check")`.
                          # 🔴 CONSEQUENCE THAT REVERSES A DISPOSAL MADE THE SAME DAY: that guard is
                          # the FIRST production caller comparing an instant it HOLDS against one it
                          # STORED, so the `datetime_literal` debt is **DISCHARGED**, not "closed as
                          # unreachable" as the register said an hour earlier. The register records
                          # the reversal rather than rewriting it.
                          # 🔴 4 HIGH from the auditor were DOCUMENT defects, and the first lands
                          # INSIDE the AC written to stop it: AC9 was ticked while NEITHER doc twin
                          # had been updated -- both still said "the pass is NOT idempotent, which
                          # is story 5.11's". Seventh consecutive story with a twin out of step.
                          # ⚠️ Also: the `decided_by='ENGINE'` doc claimed the engine would
                          # SUPERSEDE a human while every test measured it ADOPTING one (the
                          # operator rows were byte-identical to the engine's) -- a new test
                          # measures the claimed path; `same_decision`'s "at L1" reason was wrong
                          # for `interface_id`, which is structural and unreddenable FOREVER; two
                          # of four new register entries carried a CONDITION, not an owner; and
                          # `0002`'s "never zero-length" comment is now false and UNEDITABLE (sqlx
                          # checksums the file), so it is registered rather than left silent.
                          # NEXT = push -> PR -> green CI -> squash merge.
                          # (contexting entry:) 5.11 CONTEXTED **AND VALIDATED**.
                          # Two fresh-context agents; **14 findings applied, 5 HIGH**. The gap-hunt
                          # BUILT it against its own live mariadb:10.11.11 on 13311: T1-T4 plus
                          # T5's four tests, **416 -> 424 tests**, six gates green, both clippy
                          # forms clean, all 7 prescribed mutations run plus 3 of its own.
                          # 🔑 THE STORY BUILDS: AC1, AC3, AC4, AC5 all reachable and all HOLD.
                          # 🔴 TWO mutations came back GREEN, and each became a new criterion:
                          #   . M8 -- this story SILENTLY RETIRES Guy's own 5.9b arbitration.
                          #     Deleting `!abstained.insert(..)` leaves all 424 green, because the
                          #     new write path reports the second write "unchanged". -> AC2c.
                          #   . M9 -- FIVE of the six comparison columns are unreddenable through
                          #     the database (at L1 the interface, outcome, rule, cause and ruleset
                          #     version cannot differ within a group). -> AC2b makes the comparison
                          #     a PURE FUNCTION with database-free tests.
                          # 🔴 §3's "EVERY engine supersede is zero-length" was FALSE as a
                          # universal: `valid_from` comes from the IN-MEMORY observation and nothing
                          # reads `observation_record.observed_at` back, so the same obs_id handed a
                          # later instant gives a NON-zero-length supersede. `0004` is still right;
                          # its justification was too strong and is retracted to the conditional.
                          # 🔴 The evidence is SORTED ascending (l1.rs:277-278), so the prescribed
                          # `[o2, o1]` -- in THREE places incl. the headline test -- reds as written.
                          # 🔴 M5 is not executable as prescribed: sqlx checksums migrations, so
                          # editing 0004 in place gives VersionMismatch(4) on every DB test. It
                          # needs a DROP DATABASE first, or the pass records a false result.
                          # ⚠️ Also: AC7's closure sentence was false (8 TEST sites compare a held
                          # instant against a stored one; no PRODUCTION caller does), AC6 measures
                          # 0 or n-1 depending on the newcomer's ObsId and now names both,
                          # `current_subject_of` is PRIVATE and costs a panic path at the call site,
                          # §4 overstated a guard 5.10 already ships, and §9's file-size sentence
                          # was falsified by its own next clause.
                          # 🔑 11 of the 14 came from the agent that COMPILED it -- the SIXTH
                          # consecutive story with that split. NEXT = `dev-story` 5.11.
                          # (contexting entry:) 🔴 **SPLIT at contexting (Guy):
                          # 5.11b INSERTED, Epic 5 -> 18 stories.** 5.11 is the idempotence
                          # (compare -> supersede or do nothing), 5.11b the seeded arrival-order
                          # fuzz. NEXT = `create-story validate` on 5.11 with TWO fresh-context
                          # agents, and the gap-hunt MUST compile it against a live
                          # mariadb:10.11.11 on **13306** -- this story is nothing but write path.
                          # 🔑 FOUR arbitrations by Guy, the third with a live measurement in hand:
                          #  (1) the split above;
                          #  (2) the EVIDENCE is part of the decision -- a singleton and a pair both
                          #      produce `l1-exact-mac` and differ only in evidence, so not counting
                          #      it leaves a link asserting a FALSE justification that FR16 renders;
                          #  (3) 🔴 EVERY engine supersede is ZERO-LENGTH, measured: both versions
                          #      of one placement carry the same `valid_from` (the observation's own
                          #      immutable `observed_at`, pinned by an existing test), so closing
                          #      the old at the new one's `valid_from` closes it at its own --
                          #      `ERROR 4025`, `identity_link_interval`. The engine may not read the
                          #      clock (5.10's replay test HOLDS that), so no later data-derived
                          #      instant exists. `0004` relaxes the CHECK for CLOSED rows only;
                          #      measured: it admits the zero-length close, still refuses an
                          #      INVERTED closed interval and a zero-length CURRENT row.
                          #      ⚠️ Also measured: the schema does NOT constrain the CHAIN -- two
                          #      versions may OVERLAP and nothing refuses it.
                          #  (4) the replay owes HISTORY nothing: the purge stays global, its doc
                          #      must say it deletes superseded rows, and AC5 measures it.
                          # 🔑 The compare is filtered to `decided_by='ENGINE'`: unfiltered, the
                          # engine would SUPERSEDE AN OPERATOR'S row. AC4 pins today's behaviour
                          # (Constraint("unique") + rollback); the model question stays 5.14's.
                          # ⚗️ The `datetime_literal` open half may be UNREACHABLE here after all --
                          # both versions share one `valid_from`, so AC7 is a DISPOSAL: discharge it
                          # or CLOSE it with the measurement. A third re-own is a FINDING.
                          # (previous:) 5.10 **DONE** -- PR #69 squash-merged 2026-08-05 as
                          # `e056b5d`, bookkeeping PR #70 as `63e452d`. **Epic 5 13/18.**
                          # `master` carries **416 tests** and six green gates (`views-hash` STALE
                          # and exiting 0, as designed).
                          # (previous:) 5.9b was merged as `94314e9`, PR #67, taking master to 408.
                          # 408 -> 411 tests at implementation, six gates green, corpus still
                          # 11 unanswerable.
                          # D14's own test PASSES: purge the engine's links, re-run, and every
                          # decision-bearing column comes back identical.
                          # 🔴 epics.md:1634 asks for something a purge-and-replay can NEVER do,
                          # and it is now MEASURED: M1 (keyed on the interface being FOUND) reds;
                          # M1-noop (keyed on an EXISTING LINK) reds NOTHING, because the purge
                          # restores the state run 1 started from. Registered with Epic 5's retro.
                          # 🔑 An ORDER BY inside a function used on BOTH sides of a comparison is
                          # UNREDDENABLE -> the snapshot's order has a test of its own.
                          # 🔴 The two natures are MUTUALLY EXCLUSIVE on one placement -> owner 5.14.
                          # 5.10 CODE-REVIEWED: 1 decision open, 18 patches, 3 deferrals.
                          # 411 -> 416 tests, mutations 7 -> 14. Six of seven reproduced exactly.
                          # 🔑 The datetime_literal debt is SPLIT (Guy): the 'nothing asserts it'
                          # half is CLOSED here by a two-line test (M10/M11 red it); the 'a
                          # comparison can be wrong' half is RE-OWNED to 5.11, which supersedes
                          # and is the first to hold TWO instants for one placement.
                          # 🔴 MY HEADLINE WAS REFUTED by a fixture I had declared impossible:
                          # `purge_engine_links` has NO current_subject filter, so it deletes
                          # SUPERSEDED engine rows the snapshot excludes -- the store after a
                          # purge-and-replay is NOT the store before it. Now a test + M3c.
                          # 🔴 I diagnosed that an ORDER BY used on BOTH sides of a comparison is
                          # unreddenable and did NOT generalise it to the PROJECTION: eight of the
                          # ten compared columns could be blanked with the whole suite green, and
                          # rule_id was asserted NOWHERE. Fixed with one-sided oracles (M7/M8/M9).
                          # (that NEXT is spent: 5.10 was merged as `e056b5d`, PR #69.)
                          # (contexting entry:) TWO ARBITRATIONS by Guy:
                          #  (1) 🔴 "bit for bit" = every decision-bearing column EXCEPT `id`.
                          #      uuid v7 stamps a wall-clock ms into the primary key, so it is
                          #      NOT reproducible by construction; D14 says "the same DECISIONS"
                          #      and an id is not a decision. `interface_id` IS compared, which
                          #      is what makes the exclusion safe.
                          #  (2) the decided_by='OPERATOR' rows are written BY THE TEST through
                          #      the adapter -- nothing writes them before 5.14, but D14's
                          #      two-natures frontier is real in the schema today.
                          # Also: the purge is a DELETE, not epics.md's pseudo-SQL
                          # `TRUNCATE ... WHERE` (registered with Epic 5's retrospective).
                          # (its validation is done.)
                          # 🔴 5.10 INHERITS TWO THINGS FROM THIS REVIEW: uuid v7 stamps a
                          # wall-clock millisecond into every interface.id and identity_link.id,
                          # so its *bit for bit* can only mean MODULO THE IDS -- write that into
                          # 5.10 before writing anything against it; and `datetime_literal`
                          # truncates below the microsecond, which its replay comparison will meet.
                          # (before the merge:) 5.9b was CODE-REVIEWED and stayed `review`.
                          # **402 -> 408 tests** (203 bin + 159 core + 46 xtask), mutations 14 -> 18,
                          # six gates green, clippy clean twice, fixtures/ untouched, corpus still
                          # 11 unanswerable / passed() == false.
                          # Three layers; two ran their own live mariadb:10.11.11 and the Auditor
                          # RE-EXECUTED the whole mutation pass, reproducing 13 of 14 exactly
                          # including all three named predictions. **2 arbitrations, 23 patches,
                          # 5 deferrals, 0 dismissed.**
                          # 🔴 IT REFUTED A HEADLINE OF MINE IN FIVE PLACES, the commit subject of
                          # 18844ea included: "every red is ASSERTION-carried" is FALSE for M6b
                          # (1 assertion + 3 .expect() panics), and the story's own T7 bullet said
                          # so one section above. Cause: the driver read the carrier off the WHOLE
                          # output, so a MIXED set collapsed to one label. It now reports per test.
                          # 🔴 FOUR behavioural defects, three found separately by two layers:
                          #   . placement_decision tested containment on ONE witness while its
                          #     sibling filtered correctly 12 lines below -- universe missing only
                          #     (1,2) and observations 1 AND 2 abstained;
                          #   . a multi-key abstention collided on ABSTAINED_SUBJECT and rolled the
                          #     WHOLE pass back (0 links written);
                          #   . abstention_for had NO observable effect -- its body replaced by
                          #     nothing_was_evaluated() left every test green;
                          #   . a repeated MAC-less obs_id wrote two colliding links.
                          # 🔑 GUY'S TWO ARBITRATIONS:
                          #   (1) an observation abstains AT MOST ONCE whatever the key count. The
                          #       deciding measurement: an abstention row NAMES NO KEY, so the two
                          #       rows would be identical but for their id -- the duplicate was
                          #       never information.
                          #   (2) the smallest-other-ObsId witness convention is KEPT and now
                          #       MEASURED on a group of three. Every earlier test used a group of
                          #       TWO, where "smallest other" and "largest other" name the same
                          #       observation, so swapping them left all 402 tests green.
                          # Also: the reference-scale PASS and its wall-clock existed in no test
                          # (now 44 850 pairs, 300 interfaces, ~73 ms); AC7's "shown to bite" had no
                          # record (the gate now names 0003...sql:48); §9 did not apply its own
                          # "a divergence is a FINDING" rule to its own three divergences; a doc
                          # cited a test that had been INVENTED; RESOLVER_RULESET_VERSION was dead
                          # code with a false doc; and this very block contradicted itself.
                          # (that NEXT is spent: 5.9b was merged as `94314e9`, PR #67.)
                          # ⚠️ For any DB story the gap-hunt MUST run a real MariaDB (13306; 3306 is
                          # held by an unrelated container). And a mutation that changes the ARITY
                          # of a statement's bind parameters HANGS the suite instead of reddening
                          # it -- run every mutation under a timeout.
project: opencmdb
project_key: NOKEY
tracking_system: file-system
story_location: _bmad-output/implementation-artifacts

development_status:
  # ── Epic 1: Les gates tiennent (foundation) ──────────────────────────────
  epic-1: done
  1-1-dependency-frontier-gate: done
  1-2-fixture-manifest-sha256-gate: done
  1-3-ddl-collation-prove-to-red: done
  1-4-ci-runner-thin-yaml: done
  1-5-mariadb-service-and-cargo-deny: done
  1-6-pinned-toolchain-and-renovate: done
  epic-1-retrospective: optional

  # ── Epic 2: Le contrat de connecteur (foundation) ────────────────────────
  epic-2: done
  2-1-domain-observation-types: done
  2-2-connector-error-taxonomy: done
  2-3-connector-trait: done
  2-4-minimal-in-memory-connector: done
  2-5-connector-contract-test: done
  epic-2-retrospective: optional

  # ── Epic 3: Mon premier écart réel (v0.1) ────────────────────────────────
  epic-3: done
  3-1-binary-bootstrap-healthz: done
  3-2-mariadb-pool-first-migration: done
  3-3-repository-skeleton-two-traits: done
  3-4-clock-port: done
  3-5-arp-ping-connector: done
  3-6-first-real-gap: done
  3-7-one-page-shows-the-gap: done
  3-8-transversal-anchors: done
  3-9-packaging: done
  3-10-release-0-1-0: done
  epic-3-retrospective: done

  # ── Epic 4: Infra fixtures & corpus de pièges (v0.2) ─────────────────────
  # Build order is dictated by ARCH-24/D19: format → FixtureConnector →
  # metrics harness BEFORE the engine → the trap families. 4.18 and 4.19 are
  # authored here but only become executable with the UniFi parser (Epic 11).
  epic-4: done
  4-1-jsonl-observation-stream: done
  4-2-truth-labelling-format: done
  4-3-manifest-lockfile-and-orphans: done
  4-4-fixture-connector-replays-jsonl: done
  # 4.5 was split into 4.5a/4.5b at story preparation (2026-07-22) — see epics.md:936.
  # The letter suffix follows the house idiom already used for inserted items (D56b, AC5b/7b/7c).
  4-5a-fixture-connector-replays-failures: done
  # 4.5b built on 4.5a's record dispatcher; both shipped together in commit 5887fb9
  # ("Story 4.5a+4.5b") and are merged on master.
  4-5b-fixture-connector-replays-capability-changes: done
  # 4.6 was split into 4.6a/4.6b/4.6c at story preparation (2026-07-22) after a validation pass —
  # see epics.md. Build them in order: 4.6b depends on 4.6a, 4.6c on both.
  4-6a-scoring-algebra: done
  4-6b-metrics-harness: done
  4-6c-run-comparability: done
  # 4.7 was split into 4.7a/4.7b at story preparation (2026-07-23), same idiom as 4.5/4.6 —
  # see epics.md:1017. 4.7a is the (verdict, rule) assertion the trap runner owns (the criterion
  # score() deliberately leaves it, per score.rs's module doc + D19/D46b), carrying AC1 and folding
  # AC2's "a rule leaves its rule_id + evidence" as the Epic-5 contract it is pre-engine. 4.7b is
  # the positive/negative completeness check (AC3), independent of the rule comparison.
  4-7a-trap-runner-asserts-the-rule: done
  4-7b-trap-corpus-positive-and-negative: done
  4-8-reality-debt-register: done
  4-9-trap-randomized-mac: done
  4-10-trap-multi-nic: done
  4-11-trap-shared-hardware-vm: done
  4-12-trap-cloned-mac: done
  4-13-trap-dhcp-churn: done
  4-14-trap-vrrp-hsrp-virtual-mac: done
  4-15-trap-hostname-collision: done
  4-16-trap-ephemeral-docker-veth: done
  4-17-trap-hostname-absent-or-empty: done
  # 4.18 shipped IN FULL as a spec (synthetic measured body + expected Observations + shape
  # test + charter naming every hole) at fixtures/scenario/wire/ — a declared deviation from
  # the architecture's capture/ placement, forced by the privacy rule. Nothing runs it under a
  # parser yet; its first real red arrives with Epic 11 (issue #34).
  4-18-wire-format-traps: done
  # 4.19 was SPLIT at the epic's close (2026-07-25, party-mode decision — full record in
  # epic-4-correct-course-2026-07-25.md). 4.19a — the drift-surface record (127 payload keys
  # vs 7 Fact variants) and the binding layer charter for Epic 11 — SHIPPED inside
  # fixtures/scenario/wire/README.md with story 4.18. 4.19b — the mutation generator, its ~30
  # generated fixtures and their expected parse outcomes — MOVED to Epic 11: expected outcomes
  # for an error taxonomy that does not exist would be written from belief (D45), and a
  # generator has no test that reds without the parser it attacks. Carried by GitHub issue #34,
  # by the wire charter, and by CONSUMER PENDING markers on both MANIFEST entries.
  4-19-mutation-fixtures: done-partial  # 4.19a shipped; 4.19b → Epic 11 (issue #34)
  # Held 2026-07-26 with Guy — record: epic-4-retro-2026-07-26.md. Three decisions carried
  # forward: `create-story validate` is now MANDATORY; Epic 3's `cargo xtask verify` action item
  # is CLOSED as obsolete (47/47 green CI runs, the failure class did not recur); the two corpus
  # debt themes (byte-fidelity, privacy) move INTO Epic 5 at its head. The Synology-Drive
  # explanation for the local suite's non-determinism was REFUTED by measurement during the
  # retro — the cause is open and carries its own issue.
  epic-4-retrospective: done

  # ── Epic 5: Identité d'interface fiable (v0.3) ───────────────────────────
  # Decomposed 2026-07-26 into 14 stories (epics.md); story 5.2b was INSERTED the same day,
  # bringing it to 15, and story 5.4b on 2026-07-29, bringing it to 16. Build order is the
  # numbering: the THREE inherited debt stories first (this epic bumps the corpus), then the
  # engine's vocabulary, the verdict algebra, the pure join, the blocker, the corpus wiring,
  # persistence, the invariants, and last the operator-visible abstention surface.
  # ⚠️ NFR4 CANNOT go green here — D18 gates at the DEVICE level (Epic 6). Of the 24 committed
  # traps, 13 name an l1-* rule, 8 an l2-* rule, 3 a cause. Story 5.8 keeps the 8 unanswerable
  # ones IN the denominator so a green gate can never mean "we did not ask".
  # ✅ **CLOSED 2026-08-12, after BOTH steps** -- 20/20 stories, retrospective HELD, and the
  # PROJECT REVIEW conducted on `~/travail/Projets actuels/opencmdb/`. 🔴 **Neither step alone was
  # sufficient, and the review proved it**: after the retrospective everything looked finished, and
  # the review then found **milestone J3 unreachable BY CONSTRUCTION** -- it demands a gap *detected
  # AND CORRECTED*, and the product has no write surface at all, so the correction half had nowhere
  # to happen. It also found `derniere_activite` wrong under every valid reading, and a registered
  # risk (R2, *"no version is published"*) **FALSE as written** -- two tags exist and v0.1.1 has
  # been on Docker Hub since 2026-07-22 -- **deleted on Guy's decision**.
  # ⚠️ THE RULE, now in `CLAUDE.md`: **an epic closes only after the retrospective THEN the project
  # review, in that order**, because the review corrects what the retrospective surfaces. Never flip
  # an epic on the strength of the story count, or of the retrospective alone.
  epic-5: done
  # 5.1 DELIVERED (2026-07-27): the `obs_id` ↔ line binding is pinned on `dhcp-churn` and
  # `vrrp-virtual-mac` through one shared `assert_obs_ids` helper (four existing copies folded in,
  # six call sites total); all 13 committed streams now load through `FixtureConnector::load` with a
  # hand-authored context table checked in BOTH directions; all 13 round-trip to their committed
  # bytes line by line, control records included (`ControlRecord` gained `Serialize` — no
  # mirror-struct fallback needed). Corpus bytes UNCHANGED, `MANIFEST.toml` untouched. 247 → 249
  # tests. SIX mutations proven to red and recorded in the story file.
  # (This line said "Five" until 2026-07-28 — six were enumerated all along, and this file is the
  # live source of truth, so it was the worst place to carry the wrong number.)
  #
  # CODE REVIEW held 2026-07-27, three parallel layers; the fixes landed 2026-07-28.
  # 12 of 12 `[Review][Patch]` items resolved, 5 `[Review][Defer]` items registered in
  # deferred-work.md. What it actually caught, beyond wording:
  #   · the tree handed back by the review pass DID NOT COMPILE — the two byte-level guards had
  #     replaced the `let records = read_records(path)` binding, so nothing had been measured
  #     against that state; the whole gate and the whole prove-to-red log were re-run, not cited.
  #   · `assert_obs_ids` asserted no COUNT, so an empty or truncated slice passed it — the vacuity
  #     the story exists to close, re-introduced in the helper that closes it. It now takes
  #     `expected_len` (Guy's call; this knowingly revises what story 5.2b was promised).
  #   · the round-trip compared `str::lines()`, which strips a trailing `\r`, so a stream
  #     re-authored as CRLF or with its final newline dropped round-tripped GREEN. Two guards added.
  #   · the register, this story and epics.md all cited a `grep` that story 5.1's OWN commit
  #     falsified. Worst placement was epics.md, where it was story 5.2b's `Given` — its premise.
  # SIX more mutations taken (12 total), and every citation in the log re-observed on the final
  # tree: two had pointed at code that was not in it. One assertion is named as unfalsifiable
  # defence-in-depth rather than credited with a red it cannot have.
  # Gate re-run in full 2026-07-28: clippy clean · 249 tests · all xtask ci gates green · corpus
  # bytes unchanged (`git status` under fixtures/ empty).
  # WHAT MOVED, and did not close: the FOURTH corpus byte-fidelity register entry (story-4.13's
  # review — the dhcp-churn byte-pin pins MAC/hostname RELATIONALLY, never by value) is annotated
  # STILL OPEN, deliberately: 5.1's round-trip starts from the FILE, so it pins byte-SHAPE and never
  # authored values. And a new `## Deferred from: story-5.1` entry records that four committed
  # families (randomized-mac, multi-nic, shared-hardware-vm, cloned-mac) are named by NO test —
  # owned by story 5.2b, not a deferral.
  # `done` 2026-07-28: PR #41 squash-merged as c84e14a after a green CI run (58s). `done` is the
  # MERGE's business in this project's flow, which is why this sat at `review` from the end of
  # dev-story until the merge, through the code review and its 12 patches.
  5-1-corpus-pins-obs-id-binding: done
  # 5.2 contexted 2026-07-28. Four measurements were taken against master (e846836) BEFORE the
  # story was written, so the dev does not re-derive them and a surprise reads as a finding:
  #   · trap text already carries 4 distinct MACs and 3 distinct IPs across 4 of the 10 .toml files
  #     (six carry none) — the new scan is non-vacuous, and all 7 values pass today;
  #   · `Observation.raw` has exactly ONE committed value corpus-wide (minimal.jsonl:3) and it
  #     holds no address — so that new call site is VACUOUS and the story says so, with its
  #     prove-to-red on a scratch fixture rather than on the corpus;
  #   · 39 distinct committed MACs, ZERO with the I/G bit set, so refusing multicast (which is what
  #     makes the 33:33:… / 01:00:5e:… inconsistency go away) reds nothing;
  #   · a probe file under fixtures/scenario/traps/.claude/ reds `every_trap_file_in_the_corpus_is_valid`
  #     AND six trap_gate tests — story 5.1 closed that dot-entry class on the REPLAY tree only,
  #     and on the trap tree it reaches the PRODUCTION walk (`discover_trap_files`), not just a test.
  #     ⚠️ NOT a cause for issue #38: the directories postdate the flaky runs and are empty.
  # ⚠️ NEXT: `create-story validate` (two fresh-context agents) is MANDATORY before dev-story.
  #
  # 5.2 DELIVERED (2026-07-28, branch `story-5.2-privacy-floor-reaches-the-bytes`). What holds, in
  # the weaker true sentence: trap-file TEXT (header comments included, scanned before TOML parsing
  # discards them) and `Observation.raw` go through the same address-shaped rule as everything else,
  # and the scanner's three named evasions are closed. A hostname in prose, `OuiVendor.vendor`,
  # `Uplink.peer_port` and every `README.md` are STILL invisible, and NO committed `raw` exercises
  # the new call site. 249 → 258 tests (130 bin + 86 core + 42 xtask). Corpus bytes UNCHANGED —
  # `MANIFEST.toml` untouched, only the two orphan-exempt `README.md` files moved under `fixtures/`.
  # `fixtures.rs` still 728 code lines (the hoist sits after the first `#[cfg(test)]`, so the gate's
  # count moved by zero).
  # TWELVE mutations proven to red, plus two run measurements: the trap-text scan really does find
  # 4 distinct MACs / 3 distinct IPs (the story's pre-written number, reproduced by the new
  # tokenizer), and the FIFO case really does HANG — `timeout 60 cargo test` returned 143 (SIGTERM)
  # with no output without the `is_file()` guard, and a named failure with it.
  # ONE FINDING, recorded rather than smoothed over: AC3's boundary ANCHOR is NOT what keeps the
  # corpus green. Removing it alone leaves all tests green; only removing the RESUME too reds the
  # corpus (`92.0.2.90` out of `192.0.2.90` in `unifi-clients.json` — the story's measured trap,
  # reproduced). The specified shape was kept, but the doc now says which conjunct earns the safety,
  # and the anchor's one observable consequence (`ab198.18.0.1` stays invisible) was turned from a
  # doc sentence into a guard.
  # ONE CORRECTION to the story: AC6 cites a "review-enforced" sentence at
  # `scenario/traps/README.md:96-101` that is not in that file — it is in `deferred-work.md`'s 4.14
  # bullet. What IS false there is the MAC rule, narrowed by the multicast tightening; fixed.
  # WHAT MOVED, and did not close: all three walk items the hoist inherited were closed IN
  # `walk_trap_files` (root symlink check, `is_file()`, `sort()`) — including (i), which AC5 allowed
  # registering instead. The consequence is named: the two sibling walks now DIFFER on three points
  # because the REPLAY-side twins stay open, registered under `## Deferred from: story-5.2` with an
  # owner. Also registered there: the three surfaces the privacy floor still does not see.
  # `review`, not `done` — `done` is the MERGE's business in this project's flow (5.1 established
  # this and its own review confirmed it). Next: `code-review` → push → PR → green CI → squash merge.
  #
  # CODE REVIEW HELD 2026-07-28 (three parallel layers; every claim re-verified against the tree).
  # AC1 PARTIAL · AC2 MET · AC3 PARTIAL · AC4 MET · AC5 PARTIAL · AC6 MET · AC7 PARTIAL.
  # 9 patches APPLIED, 4 items deferred, 6 dismissed as refuted. The code was sound and the corpus
  # never moved; what the review caught is that four documents this story wrote asserted more than
  # the tree does — in a story whose stated bar is "a doc comment must be TRUE".
  # The two load-bearing findings, both MEASURED rather than argued:
  #   · the sentence written to REPLACE the removed "only call site" claim is falsified by this very
  #     commit — `vrrp-virtual-mac.toml:37` carries 00:00:5e:00:01:0a in a header COMMENT that this
  #     story's own trap-text scan reads, so dropping the VRRP leg reds it. Two rotted inventories in
  #     one doc block, one story apart;
  #   · the FIFO hang is closed in `walk_trap_files` ONLY. `discover_trap_files` never gained the
  #     guard, six trap_gate tests drive it, and `timeout 90 cargo test -p opencmdb-bin` still
  #     returns 143 with NO output — the Debug Log's two-sided claim compared a FILTERED run to a
  #     full one. Resolved as (b) on Guy's call: production code untouched, four sentences narrowed,
  #     the production walk's hang registered with an owner.
  # Also patched: a doc contradicting itself about the anchor two paragraphs apart, plus two wrong
  # mutation citations and a stale "127 tests"; the coverage doc crediting `reason` strings for a MAC
  # that lives only in a comment; `fixtures/README.md` promising an owner the register denies; and a
  # residual-floor enumeration presented as complete that missed FIVE measured shapes — IPv6
  # unscanned entirely, zero-padded IPv4, the Cisco dotted and bare MAC forms, the glue limit being
  # any hexdigit not just a hex letter, and the resume swallowing an address adjacent to an accepted
  # one. A 13th mutation was recorded: the new blindness guard asserted NOTHING and passed on a
  # gutted scanner; it now carries a control address and reds.
  # Gate re-run WHOLE after the patches: fmt, clippy -D warnings, 258 tests, xtask ci all green,
  # fixtures.rs still 728 code lines, MANIFEST.toml untouched, only the two READMEs moved.
  # `done` 2026-07-28: PR #44 squash-merged as 4d2044b after a green CI run (1m03s). It sat at
  # `review` from the end of dev-story until the merge, through the code review and its 9 patches —
  # `done` is the MERGE's business here, and the code-review workflow's own default (which would
  # have set `done` at the end of the review) is WRONG for this project. Same shape as 5.1.
  5-2-privacy-floor-reaches-the-bytes: done
  # 5.2b was INSERTED on 2026-07-26, one day after the decomposition and before any Epic 5 code
  # existed — surfaced while preparing story 5.1, which could not absorb it (its AC1 strengthens
  # byte-pins that EXIST; randomized-mac / multi-nic / shared-hardware-vm / cloned-mac have none,
  # verified by grep). The letter suffix is the house idiom for an inserted item (D56b, AC5b/7b/7c)
  # and keeps 5.3-5.14 at their numbers. It sits in the debt block ahead of 5.5 deliberately: the
  # corpus is the oracle the L1 join is judged against. It closes the LAST two corpus byte-fidelity
  # entries in deferred-work.md (story-4.13's and the one story 5.1 raises).
  # 5.2b CONTEXTED 2026-07-28. All five streams read verbatim off the committed corpus at 95d4d69
  # and tabulated in the story, together with the obs_ids each of the NINE traps judges — so the dev
  # re-derives nothing and a surprise reads as a FINDING. THREE traps measured for the dev, each of
  # which would otherwise cost an hour and a false diagnosis:
  #   · `shared-hardware-vm`'s fact count is NOT uniform — 4, 4, 4, 3. W4 is the must-abstain pole
  #     and carries no Hostname, so the dhcp-churn idiom's per-line `assert_eq!(facts.len(), N)`
  #     REDS on it and reads as a corpus defect. It is the premise, not a defect;
  #   · instants are NOT strictly increasing in three of the four families (multi-nic M1/M2,
  #     shared-hardware-vm W1/W2, cloned-mac K1/K2 each share one) — two NICs seen in one sweep
  #     SHOULD share an instant. dhcp-churn asserts strict increase only because its churn lives in
  #     time alone; copying that line reds;
  #   · ONE CORRECTION to the epic: epics.md:1391 says "both `reason` strings cite" the MAC,
  #     doc-host-golf and doc-host-hotel. Counted: the MAC is in ONE reason, hotel in ONE, golf in
  #     BOTH. The union cites all three; neither reason does. Conclusion unchanged.
  # Also corrected during contexting, before any dev sees it: the four-families register bullet is
  # under `## Deferred from: story-5.1` (:516, raised BY the story), NOT under `## Deferred from:
  # code review of story-5.1` (:556) — two similarly-named sections, and striking the wrong one is
  # exactly the class of error this project's reviews keep catching.
  # VALIDATED 2026-07-28 by two fresh-context agents (fact-check + gap-hunt) — 3 HIGH, 6 MED, 3 LOW,
  # ALL APPLIED. The measured corpus data in the story survived fact-checking intact (every byte
  # array, hex rendering, IP, hostname, peer_port, obs_id prefix, instant, fact count and all nine
  # trap→obs_id rows VERIFIED). The three HIGH findings were all about what the story CLAIMED:
  #   · **A NEW AC (AC4b) was added.** Every pin the story asked for lives on the .jsonl — but the
  #     inversion is available entirely from the .toml. Exchanging the two `observations` vectors in
  #     `cloned-mac.toml` (three characters, no stream byte touched) makes the corpus DEMAND the
  #     FALSE MERGE, and `cargo test --workspace` in that state reports 130+86+42, ZERO failures.
  #     Reproduced independently before the AC was written; file restored, md5 verified. AC4 had
  #     claimed `assert_obs_ids` closed that hole — it closes only the stream-side half. This is
  #     D10's catastrophic-and-asymmetric direction, in the family the corpus calls its ONLY
  #     pre-release guard against the false merge;
  #   · the story's closing promise (inherited from epics.md:1397, "the corpus byte-fidelity theme
  #     carries no open item") is STALE — 5.1's own review registered `unifi-clients.expected.jsonl`
  #     as having no round-trip pin ONE DAY after the epic was written (owner Epic 11, issue #34) —
  #     and validation surfaced a SIXTH unpinned stream, `example-traps.jsonl`, whose reason cites
  #     `02:00:5e:00:53:10`, verbatim the 4.13 shape. The story now writes the narrow true claim and
  #     forces an explicit decision on the sixth stream;
  #   · AC3 had offered a choice between the exact fact-count vector and `>= 3`; the second branch is
  #     MEASURED vacuous (a second contradicting Uplink on W4 reds nothing under it) and was
  #     withdrawn.
  # MED/LOW applied: the story-4.13 register section has TWO bullets and closing one leaves a
  # "STILL OPEN" bullet standing; the false reason-distribution sentence is in the register too, not
  # only the epic; the W4 mutation must REPLACE not ADD (adding reds the count loop FIRST, so the
  # recorded red would name the wrong guard) and needs the `doc-` prefix and exact HostnameSource
  # spelling or it reds two tests; `cargo xtask ci` DOES consult MANIFEST.toml and must never run
  # mid-mutation; every MAC now given in DECIMAL with the `:40`=64 transcription trap named; STOP now
  # has a procedure; three line ranges corrected.
  # IMPLEMENTED 2026-07-28 → review. 258 → 263 tests (135 bin + 86 core + 42 xtask), all four local
  # gates green, NO byte moved under fixtures/ (git status verified empty before the gate ran).
  # FIVE new byte-pin tests + the dhcp-churn one EXTENDED, all in the trailing test module — the
  # file-size gate number for fixtures.rs is UNCHANGED at 728 code lines, as the story predicted.
  #   · the four unpinned families each got a test in the dhcp-churn idiom (length → exact per-line
  #     fact count → assert_obs_ids → authored values → instants vector). Three of them pin an
  #     instants vector that is NOT strictly increasing, as measured;
  #   · AC4b's `assert_trap_binds` pins, per trap id, the exact `observations` vector IN ORDER, the
  #     whole `Expectation`, and the declared `family` — ALL 24 committed traps across all TEN trap
  #     files. The measured inversion (exchange the two vectors in cloned-mac.toml, no stream byte
  #     touched) now REDS; before this it left the whole suite green while the corpus DEMANDED the
  #     false merge;
  #   · AC7's open question was DECIDED: `example-traps.jsonl` was PINNED as a sixth test rather
  #     than registered — closing the theme while leaving a known instance open would have made the
  #     closure claim narrower than it read.
  # NINE mutations recorded across eight artefacts, each the SOLE red and each naming its line.
  # The dhcp-churn one is two-sided and is the justification for closing 4.13: with doc-host-hotel
  # renamed, the PRE-STORY tree reported 130+86+42 zero failures (the relational assert_ne! never
  # noticed) while the extended test reds. One test-internal ORDERING change was needed: W4's
  # hostname-absence assertion moved AHEAD of the shared-uplink loop, because the loop's one-of-each
  # find() PANICS on a line whose Uplink was replaced — with it first, AC6's mutation would have red
  # the wrong guard. Reasoned in the test's own comment.
  # REGISTER: both story-4.13 bullets and the story-5.1 four-families bullet closed by append-and-
  # strike; the false "both `reason` strings cite" distribution corrected IN the closure note (it
  # was in the register, not only in epics.md:1391). New `## Deferred from: story-5.2b` opens ONE
  # item — a trap's `reason` prose is still not mechanically tied to the values it cites — and the
  # narrow true claim was verified by RE-READING the register after the last edit: it names both
  # things that stay open (unifi-clients.expected.jsonl, owner Epic 11 / issue #34, and the new
  # item this story itself opened), so the claim is not falsified by its own commit.
  # CODE REVIEW HELD 2026-07-28 — three parallel layers, 1 decision + 10 patches + 5 defers + 1
  # dismissed. All three layers CONVERGED on the same two HIGH findings and two of them MEASURED
  # the first one independently:
  #   · the trap-binding pin covered 14 of the corpus's 24 traps, while FOUR documents claimed
  #     "all ELEVEN committed traps" — a figure contradicting its own enumeration (9+2+3=14) and
  #     the corpus total. Exchanging the two `observations` vectors in hostname-collision.toml left
  #     the suite at 135+86+42 ZERO failures while the corpus DEMANDED must-merge/l1-exact-mac on
  #     two DIFFERENT MACs. **Guy's call: close the CLASS, not the instance** — docker-veth,
  #     hostname-absence, hostname-collision and vrrp-virtual-mac are now bound too, folded into
  #     their existing byte-pins. ALL 24 traps bound; the hostname-collision exchange REDS, proven;
  #   · `assert_trap_binds` did not pin `family`. Measured: deleting BOTH family keys from
  #     cloned-mac.toml left the suite green while silently exempting the family from
  #     incomplete_families (deleting ONE already reddened trap_gate; both did not). Now pinned
  #     and proven red.
  # Also fixed: the helper's doc gave a FALSE reason for pinning order (a set comparison DOES catch
  # the cloned-mac exchange — its poles differ as sets); dhcp-churn's D2/D3 MACs were not
  # value-pinned while the closure claimed they were; "all six committed streams that had none" was
  # false (13 streams, 8 had none, 2 still do — now registered); the narrow claim's enumeration
  # omitted two entries whose owner phrase wraps across a line break; the mutation tally disagreed
  # across three files (counted: NINE across eight artefacts); `re_randomized` named the MAC that
  # has NOT been re-randomized; shared-hardware-vm drove a MAC octet and an IP octet from one loop
  # variable. FIVE items deferred under `## Deferred from: code review of story-5.2b`.
  # Test COUNT is unchanged at 263 (135+86+42) — the four new bindings were FOLDED INTO the four
  # existing family byte-pins rather than added as tests, so the count says nothing about them;
  # what says something is the two recorded reds. ⚠️ NEXT: push → PR → green CI → squash merge.
  # `done` is the MERGE's business, not the review's.
  # ✅ MERGED 2026-07-28 — PR #46, CI green in 52s, squashed as `fe6a19e`. Epic 5's THREE inherited
  # debt stories (5.1, 5.2, 5.2b) are now all done; the corpus byte-fidelity and privacy themes
  # both carry only entries with named owners. NEXT = `create-story` 5.3.
  5-2b-four-families-byte-pins: done
  # 5.3 CONTEXTED 2026-07-28 — the FIRST story of Epic 5's engine proper; the three inherited-debt
  # stories are all merged. It writes a TYPE and its tests, and NO engine: no cascade, no rule, no
  # `Decision`, no join, no producer (5.4/5.5/5.6 own those). The arbitration it carries was taken
  # at the epic's decomposition (epics.md:1311): the identity engine gets its OWN abstention cause,
  # `Expectation` and the sha256-locked corpus untouched.
  # FIVE measurements taken against master (12be2fc) BEFORE the story was written, so the dev
  # re-derives nothing and a surprise reads as a FINDING:
  #   · `cargo test --workspace` → 135 bin + 86 core + 42 xtask = 263, zero failures — the baseline;
  #   · `Outcome::Abstained` has exactly SIX construction sites and EVERY one is in a test module
  #     (score.rs:628, :717; trap_gate.rs:450, :508, :652, :658). Production code constructs an
  #     abstention NOWHERE — which is why retyping it is cheap today and will not be after 5.5;
  #   · `Expectation::MustAbstain` has THREE committed occurrences (example.toml:44,
  #     hostname-absence.toml:47, shared-hardware-vm.toml:46) and keeps `AbstentionCause`. A diff
  #     touching one is a mistake, not a consequence;
  #   · `page.rs:112-119`'s `cause_label` match is exhaustive with NO `_` arm and `locales/app.yml`
  #     carries exactly three `cause.*` keys — which is what makes AC4's mutation (add a variant to
  #     the reconciliation enum → error[E0004]) a real red rather than a story;
  #   · `crates/opencmdb-core/src/identity/` DOES NOT EXIST. This story creates the subtree the
  #     architecture's source tree has described since planning (architecture.md:3365-3373).
  # DECISIONS taken at contexting, so they are not re-litigated at review: name
  # `IdentityAbstentionCause` (NOT `AbstentionCause` — lib.rs:38 re-exports the reconciliation type
  # FLAT at the crate root, so a homonym is error[E0252]); location `identity/cascade.rs`
  # (architecture.md:3369); exactly TWO variants, `Ambiguous` and `AbsenceOfProof`, each traced to a
  # ROW of D13's table (architecture.md:969-974), a third is a FINDING; NO serde, NO `Ord`, NO
  # `Display`, NO `#[non_exhaustive]` (the last would force a `_` arm downstream and destroy the
  # E0004 mechanism AC4 rests on); NO `From` bridge between the two cause types.
  # The register: the story-4.6a bullet (deferred-work.md:160-167) is CLOSED by this story; the two
  # `NoMatch` bullets (:217-224, :287-291) are annotated PARTLY closed only — the mapping has no
  # producer until 5.4/5.5, and striking them would be the over-claim the reviews keep catching.
  # VALIDATED 2026-07-28 by two fresh-context agents (fact-check + gap-hunt), 28 findings applied.
  # The two that would have broken the build: BOTH import instructions were inverted —
  # trap_gate.rs:389 must be REPLACED (zero uses remain after the retype → unused_imports under
  # -D warnings), and score.rs:34 must be replaced AND re-added inside `mod tests`, because a
  # SEVENTH `MustAbstain` site (score.rs:608, `fn must_abstain()`) still needs it while CI's clippy
  # compiles the lib without cfg(test). Also: AC5's "SOLE red" was unsatisfiable (M2/M3 red five or
  # more tests each — the story now requires the complete OBSERVED set), `all()` as specified was
  # not implementable, and AC1's naming evidence cited a co-presence that does not exist in
  # trap_gate.rs. Scope confirmed correct: nothing squatted from 5.4/5.5/5.9/5.14.
  #
  # 5.3 IMPLEMENTED 2026-07-28 → review (branch `story-5.3-engine-abstention-cause`).
  # In the weaker true sentence: `Outcome::Abstained` carries `IdentityAbstentionCause`
  # (`identity/cascade.rs`, variants `Ambiguous` and `AbsenceOfProof`, each traced to a D13 row);
  # four tests pin its claims; nothing compares the two abstention vocabularies and a test says so;
  # NO engine produces one yet — no cascade, no rule, no `Decision`, no join. `Expectation::
  # MustAbstain` and `gap::AbstentionCause` are unchanged in substance (two doc-only edits) and NO
  # byte moved under fixtures/ (`git status fixtures/` and `MANIFEST.toml` both verified empty).
  # 263 → 267 tests (135 bin + 90 core + 42 xtask); all four gates green INCLUDING the CI form of
  # clippy (no `--all-targets`), which is the only one that catches an import kept alive by a test
  # module. `cargo doc` was also run — its three warnings are all pre-existing, so none of the new
  # full-path doc links is broken.
  # SEVEN mutations run, not the five specified. The two additions are the point:
  #   · `all()`'s doc asserts TWO mechanisms (E0004 on a new variant, E0308 on a widened literal)
  #     and the five specified mutations could only measure the first. M1b and M1c measure both,
  #     rather than shipping half a doc sentence as a prediction;
  #   · M1 does NOT behave as the story predicted, and that is recorded, not smoothed: deleting a
  #     variant gives `E0599`, never the predicted `E0308`, and `trap_gate.rs`'s four sites never
  #     red at all because opencmdb-core fails to compile first, so opencmdb-bin is never compiled;
  #   · M2 reds EIGHT tests, not the seven predicted — the story missed
  #     `scoring_ignores_the_rule_because_the_trap_runner_owns_it`, whose last assertion IS that
  #     cell. M3's five-red set matched the prediction exactly.
  # M4 is AC4's evidence and was measured on both sides: adding `Ambiguous` to `gap::AbstentionCause`
  # gives exactly ONE `error[E0004]` at `page.rs:114:11` — and under that mutation core still reports
  # 90 passed and xtask 42, so the cost of widening is precisely a label plus two locale strings for
  # a variant `reconcile` can never produce.
  # ONE count in the story's own Dev Notes is falsified by this story's commit and is corrected in
  # the Completion Notes rather than left standing: `Outcome::Abstained` now has NINE construction
  # sites, not six (three were added by the new test loops). ALL NINE are still in test modules —
  # M5 measured that independently: reverting the field type broke only the `lib test` target.
  # REGISTER: the story-4.6a bullet is CLOSED by append-and-strike, naming the branch taken and the
  # two tests that hold it — and correcting, in the closure note, its own sentence claiming a
  # different outcome-side type "would make comparison asymmetric" (there is no comparison). The two
  # `NoMatch` bullets are annotated ↺ PARTLY closed, NOT struck: the vocabulary exists, the mapping
  # has no producer until 5.4/5.5. A new `## Deferred from: story-5.3` section appended at the END
  # (the file is chronological) opens FIVE items. THREE name a story (5.9, 5.14 twice); the other
  # two name the condition that would produce an owner rather than a person — `Owner: nobody` for
  # the `#[non_exhaustive]` refusal, which is a decision recorded so it is not re-litigated, and
  # `Owner: whoever needs the comparison` for the absent `From` bridge. The register's own idiom
  # allows both. (This line read "each with a named owner" until the code review counted them.)
  # PUSHED 2026-07-28: PR #48, CI GREEN in 1m00s (run 30389174367). NOT merged — `done` is the
  # MERGE's business and `code-review` comes first.
  #
  # CODE REVIEW HELD 2026-07-29 — three parallel layers, 1 decision + 18 patches + 6 defers + 1
  # orchestration defect of the reviewer's own. NO behavioural defect in any layer; the code was
  # sound. What fell is documentation truth, in a story built to guard it. The Acceptance Auditor
  # reproduced SIX of the seven mutations and NOT ONE observed red set differed from the record —
  # including both places where the spec's own predictions were wrong.
  #   · THE DECISION: `all()`'s witness stops the build on a new variant (`error[E0004]`) but does
  #     NOT force the variant into the list. MEASURED by running the lazy repair — a bare match arm
  #     leaves `all()` returning 2 of 3 with 90 tests GREEN. Guy's call: keep the mechanism, state
  #     its limit truthfully, register the residue (owner 5.14). Two closures were BUILT and
  #     MEASURED before deciding and both rejected: an `ordinal()` witness is GREEN with the variant
  #     missing (91 passed — it closes nothing, and it was the reviewer's own first recommendation,
  #     withdrawn on measurement not on argument); a single-source `macro_rules!` works but violates
  #     AC1's literal `[Self; 2]` return type AND makes adding a variant frictionless, which is what
  #     AC1b calls a FINDING. `strum` refused as disproportionate for a two-variant enum (D47).
  #   · ALL THREE LAYERS CONVERGED on a false row count in the enum's own guard paragraph — "the
  #     four D13 rows that produce none" when it is TWO — inherited from an AC1b that contradicts
  #     itself four lines apart ("the OTHER two rows" vs "the four-row remainder"). The dev copied
  #     the wrong half instead of raising it as a finding, which the story preamble invites.
  #   · A re-count the story PRE-NAMED and then skipped: `Expectation::MustAbstain` went from SEVEN
  #     construction sites to EIGHT, the eighth being this story's own new test.
  #   · AC1's `Given` ("which `reconcile` matches exhaustively") is refuted by the story's own M4 —
  #     `reconcile` only CONSTRUCTS a cause; the sole value-level match is `page.rs`'s `cause_label`.
  #     Traced back to the story-4.6a register bullet, now struck there together with a SECOND false
  #     clause the first closure had announced but never struck.
  #   · The cross-vocabulary test covered 2 of 6 pairs while its name states the general claim —
  #     widened to all six and RE-PROVEN red under M2.
  # ⚠️ REVIEWER'S OWN DEFECT, recorded: two layers with write access ran against ONE working tree,
  # and the Edge Case Hunter observed another agent's M3 mid-pass. Nothing was corrupted (tree
  # verified clean, md5 stable, 267 green) but the next multi-layer review gives each writing layer
  # its own git worktree.
  # Test count UNCHANGED at 267 (135 + 90 + 42) — the widened test is still one test, so the number
  # says nothing about the patches; the re-run red set does. `CLAUDE.md`'s Epic 5 line, stale since
  # 5.2/5.2b merged, was fixed here rather than deferred.
  # ✅ MERGED 2026-07-29 — PR #48, CI green (56s on the review patches, three green runs on the
  # branch), squashed as `62f9c83`. Epic 5's engine proper has its first story in: the identity
  # engine speaks its own abstention vocabulary, and NOTHING produces one yet. `done` is the MERGE's
  # business — the `code-review` workflow's default of setting it at the end of the review was again
  # not applied, as for 5.1/5.2/5.2b.
  # (This entry carried a "NEXT = `create-story` 5.4" line until story 5.4's code review struck it:
  # 5.4 has since been contexted, validated, implemented and put in `review`, so a second NEXT forty
  # lines above the file's own `last_updated` pointed at work already done.)
  5-3-engine-abstention-cause: done
  #
  # 5.4 CONTEXTED 2026-07-29 — the engine's return type, and NO algebra: no `decide`, no rule, no
  # join, no producer. It writes FIVE types in the existing `identity/cascade.rs` — `Verdict` (D13's
  # five, spelled as architecture.md:964 spells them), `RuleVerdict { rule, verdict, evidence }`,
  # `RulesetVersion`, `Conclusion` (Match | NoMatch | Abstained, mirroring `score::Outcome`'s shape)
  # and `Decision { conclusion, verdict_vector, ruleset_version }` — plus four tests and SIX touched
  # doc locations, five of them rewritten because this story falsifies them.
  # (This line read "five tests and five rewritten doc blocks" until the code review counted them —
  # the same 4/5-tests and 2/4/5-doc-blocks counting defect the validation pass had already caught
  # once in this file, corrected in the implementation record below and not in this summary.)
  # SIX measurements taken against master (505379e) BEFORE the story was written, so the dev
  # re-derives nothing and a surprise reads as a FINDING:
  #   · `cargo test --workspace --locked` → 135 bin + 90 core + 42 xtask = 267, zero failures;
  #   · ZERO `f32` and ZERO `f64` in the whole Rust workspace — so D13's "if the output is a float,
  #     B has won in disguise" is true by ACCIDENT today, held by no gate. That measurement is what
  #     justifies story 5.4b's gate;
  #   · the committed corpus writes SEVEN distinct rule names and FIVE of them are `l2-*` (Epic 6's).
  #     That is why `RuleId` does NOT close into an enum here, although `trap.rs:33-35` says Epic 5
  #     closes it: doing so would enumerate five undesigned rules or unparse five locked trap files.
  #     That doc sentence is therefore corrected by this story (doc-only; `:38` byte-unchanged);
  #   · `Conclusion`, `RulesetVersion`, `RuleVerdict` occur ZERO times in crates/ and xtask/;
  #     `Decision` occurs TWICE, both in PROSE (`score.rs:46`, `fixtures.rs:1662`), never as an item;
  #     `Verdict` on a word boundary returns two prose lines only. No name is contested, none is
  #     E0252 against lib.rs's flat re-export block;
  #   · `ObsId` already derives Ord/Eq/Copy (uuid_newtype!), so `Vec<ObsId>` is PartialEq for free;
  #   · `cascade.rs` is 162 lines with the first `#[cfg(test)]` at :122 → 121 CODE lines, ceiling
  #     2000. Ample.
  # DECISIONS taken at contexting, so they are not re-litigated at review: `Decision` is a STRUCT and
  # `Conclusion` an ENUM (a version repeated on three variants would rest on three declarations
  # agreeing, which is exactly what D14 says must not be forgettable); `evidence: Vec<ObsId>` is the
  # smallest non-invented shape (the architecture mentions evidence on SIXTEEN lines, five of them
  # about the identity link's evidence, and SHAPES it on none); NO serde, NO `Ord` anywhere (D20: strength returns as an ORDINAL under a four-condition
  # ADR, so an orderable `Verdict` would let magnitude compile), NO `Default` on `Decision` (that IS
  # the "mandatory" mechanism), NO `const CURRENT_RULESET_VERSION` (there is no ruleset), NO
  # `From<Decision> for Outcome` (story 5.7's decision about the gate), NO `Decision::cause()` (no
  # consumer until 5.14), and `score::VerdictVectorEntry` is NOT replaced — doing so would falsify
  # four places at once with no producer to justify any of them (owner 5.7).
  # ⚠️ A FINDING was measured at contexting and is recorded rather than absorbed: enumerating D13's
  # table over the PRESENCE of each verdict yields SEVEN input classes and its six rows cover SIX.
  # `>=1 Opposes` with no `Decisive`, no `Supports` and no `Disqualifying` is covered by NO row.
  # Guy's arbitration 2026-07-29: it concludes `Abstained { AbsenceOfProof }`. The arbitration
  # belongs to 5.4b (the function that must be total); 5.4 may NAME the gap and must not answer it.
  # Story 5.4 also CLOSES the register entry that named it by name: "Owner: whoever writes 5.4's AC7
  # — require the PR and stop" (the AC7-drafting entry under `## Deferred from: code review of
  # story-5.3`). Its AC8 stops at the PR and says nothing about the merge — the SILENCE is what
  # resolves the contradiction, and the wording that answers the entry is 5.4's AC8, not its AC7.
  # VALIDATED 2026-07-29 by two fresh-context agents (fact-check + gap-hunt). Both verified against
  # the tree, and every finding they raised was checked before being applied. 6 HIGH + 8 MEDIUM from
  # the gap-hunt, 5 HIGH + 8 MEDIUM/LOW from the fact-check; all applied. What they caught:
  #   · a MUTATION THE STORY PREDICTED THAT CANNOT HAPPEN — the gap-hunt COMPILED the case: deleting
  #     `Verdict::Disqualifying` gives `error[E0599]` twice and NEVER the `E0308` M3 announced, which
  #     the story's own AC1 already said two pages earlier ("alternatives along one repair path,
  #     never simultaneous"). Same class as 5.3's self-contradicting AC1b, caught BEFORE dev this time;
  #   · A GAP NO AC, DOC OR REGISTER ENTRY COVERED: a `Decision` whose `Conclusion` names a rule
  #     ABSENT from its own `verdict_vector`, or a `Match` with an EMPTY vector, is representable and
  #     nothing refuses it — precisely what the two quotes the story leans on (D13's "the list IS the
  #     explanation", D19's "a rule that fires without leaving its rule_id…") exist to prevent. Now an
  #     AC3 bullet and a register item, owner 5.4b (the first place conclusion and vector are built
  #     together, hence the first place a test can red);
  #   · THE COUNTING DEFECT, four times in one file — 2/4/5 doc blocks and 4/5 tests. The story's own
  #     inherited lesson #3 ("a count in a doc is a claim"), violated by the story that states it.
  #     Now SIX doc locations, five of them falsified and rewritten, counted once and listed once;
  #   · AC6's blanket "every claim is pinned by a test" was UNSATISFIABLE: six binding claims are
  #     carried by an ABSENT derive and are untestable without a compile-fail harness this project
  #     does not have. Scoped to behaviour/shape, with the six named;
  #   · a STALE MEASUREMENT RE-INTRODUCED from 5.3's pre-merge notes — `Outcome::Abstained` has NINE
  #     construction sites, not six, and 5.3's OWN completion record already said so (:452-453 of
  #     this file). Re-introducing a corrected number is the same defect as inventing one;
  #   · a D19 "quote" that was `deferred-work.md`'s PARAPHRASE, not architecture.md's sentence; two
  #     greps whose reported output was a hand-picked subset presented as the whole; ~a dozen line
  #     cites off by one to four; and `Uuid::new_v4()` — which does NOT compile here (`features =
  #     ["v7","serde"]`), so Task 6 now names the crate's own `Uuid::from_u128` idiom.
  # BOTH AGENTS INDEPENDENTLY RE-DERIVED THE D13 TABLE GAP and confirmed it: exactly ONE uncovered
  # input class, the one the story names. The fact-check also confirmed the type design compiles as
  # specified (derives, E0252, visibility, orphan rules all checked) and that all six baseline
  # measurements reproduce exactly.
  # ⚠️ The agents DISAGREED on one point and the tree settled it: register annotations sit ABOVE
  # their target at :241/:319 and BELOW it at :490 — both idioms exist. What 5.3's review actually
  # settled is that an annotation must NAME its target, not which side it sits on. AC7 now says so.
  # Two documents falsified by 5.4b's insertion were fixed at contexting rather than left for the
  # dev: `docs/project-context.md` and `CLAUDE.md` both said "15 stories"; `epics.md:22` said 15 too,
  # while `:1313` already said sixteen — a half-updated document the fact-check caught.
  #
  # 5.4 IMPLEMENTED 2026-07-29 → review (branch `story-5.4-decision-and-ruleset-version`).
  # In the weaker true sentence: `identity/cascade.rs` holds FIVE new types — `Verdict` (D13's five,
  # spelled as architecture.md:964 spells them), `RuleVerdict { rule, verdict, evidence: Vec<ObsId> }`,
  # `RulesetVersion(u32)`, `Conclusion { Match | NoMatch | Abstained }` and `Decision { conclusion,
  # verdict_vector, ruleset_version }` — plus `Verdict::all()` and `Decision::rule()`. Four tests pin
  # their claims. NOTHING combines a verdict set, no rule produces a `Verdict`, nothing produces a
  # `Decision`. No byte moved under fixtures/ (verified empty, MANIFEST untouched).
  # 267 → 271 tests (135 bin + 94 core + 42 xtask); only core moved, as predicted. Both forms of
  # clippy green INCLUDING the CI form. `cargo doc` → the same THREE pre-existing warnings, so none
  # of the new full-path links is broken. `cascade.rs` = 456 CODE lines after the code review's doc
  # patches (444 at the end of dev-story), ceiling 2000; re-measured, not carried.
  # FOUR mutations run:
  # (Line numbers below are MUTATED-tree coordinates — the positions after each mutation's own
  # deletion, which is what the compiler printed. On the committed tree the M1 construction sites are
  # :530/:543/:555/:600/:637 and the read is :655. Labelled after the code review found `:528`
  # presented as a file location when it is a closing brace.)
  #   · M1 (drop `ruleset_version`) → SIX errors: 5× E0560 at construction sites + 1× E0609 at
  #     :653:22, the read. That is "mandatory by construction", measured;
  #   · M2 (`rule()` returns None for NoMatch) → TWO failing tests, both assertion PANICS, both new.
  #     The only one of the four that reds an assertion rather than a name;
  #   · M3 (delete `Verdict::Disqualifying`) → `error[E0599]` at THREE sites and NEVER `E0308` — the
  #     same error KIND the validation agent measured by compiling the case, at three sites rather
  #     than its two, because this story adds the third reference (the test that spells the variant).
  #     NOT as the story's first draft predicted: that prediction was corrected BEFORE dev, and the
  #     measurement agrees with the correction;
  #   · M4 (drop `evidence`) → FOUR errors (3× E0560, 1× E0609) PLUS an unpredicted side effect,
  #     recorded rather than smoothed: `warning: unused import: crate::observation::ObsId` at :30:5,
  #     which the CI form of clippy turns into a hard error. The field is what keeps that import live.
  # ⚠️ THREE of the four reds are COMPILER-carried: M1 and M4 are construction-site errors and M3 is
  # a spelling error — all three would fire identically on a test body of `assert_eq!(1, 1)`. ONLY M2
  # reds an assertion. (This line said "TWO … M1 and M2 are not" until story 5.4's code review, where
  # two layers measured M1 producing six compile errors and ZERO executed assertions — the same class
  # this very line already assigned to M4, and contradicting the M2 bullet six lines above. Correcting
  # it makes the record weaker and true.) The story does NOT claim to close the "two of 5.3's four
  # tests are carried by the type system" entry (deferred-work.md:1080-1088) — owner stays 5.5.
  # `the_verdict_vector_carries_the_whole_triple_in_order` has NO possible behavioural mutation: a
  # `pub` field has no code to break, and the record says so.
  # ONE count in the story's own Dev Notes is falsified by its own commit and corrected in the
  # Completion Notes rather than left standing: `Decision` occurred TWICE in prose at 505379e and now
  # occurs 26 times, because the type exists. That is the exact defect 5.3's review caught.
  # SIX doc locations changed, FIVE because this story falsified them (cascade.rs module doc,
  # identity/mod.rs, score.rs's "Not named `Decision`", score.rs's VerdictVectorEntry element
  # sentence — its "uninhabited"/"provably empty" claims SURVIVE — and trap.rs's `RuleId` doc, whose
  # "Epic 5 names them" prediction the seven-corpus-rule-names measurement refutes).
  # REGISTER: the AC7-drafting entry CLOSED by append-and-strike (this story was its named owner —
  # its AC8 stops at the PR and says NOTHING about the merge, which is what resolves the
  # contradiction). THREE entries annotated ↺ and NONE struck: the two `NoMatch` bullets (owner →
  # 5.4b/5.5) and the firing-rule/evidence contract (owner → 5.5). The new annotation sits ABOVE its
  # target, the idiom of its two nearest neighbours, and 5.3's own annotation was RE-READ after
  # insertion: its "entry BELOW"/"entry above it" both still resolve, so no amendment was needed.
  # A new `## Deferred from: story-5.4` section appended at the END (the file is chronological).
  # ⚠️ CITATIONS: this entry cited deferred-work.md by LINE, using pre-commit numbers that the same
  # commit shifted by +21 — found by three review layers and fixed by citing entries by TITLE, which
  # does not rot. Post-review positions, measured after the last edit: AC7-drafting closure
  # :1117-1126 · "two of 5.3's four tests" :1080-1088 · `IdentityAbstentionCause::all()` residue
  # :1054-1079 · test-placement convention (now CLOSED) :1089-1103 · flat re-export :1111-1116.
  # The section now opens EIGHTEEN items — FIFTEEN name a story, ONE names an EPIC (`Epic 6`), TWO
  # name the CONDITION that would produce an owner (`whoever writes D20's ADR`, `whoever revisits
  # the crate's re-export policy`). It read "FIFTEEN items, THIRTEEN name a story" until the code
  # review counted them: the thirteenth was `Epic 6`, an epic being called a story in the sentence
  # that forbids exactly that. The review then added three items (a duplicate-`RuleId` vector, the
  # first `f64` token, and 5.4b's owner/spec gap), which is where eighteen comes from.
  # ✅ MERGED 2026-07-29 — PR #52 squash-merged as `da87b62`, CI green on both commits (the story and
  # its 22 review patches). `review` → `done` at the MERGE, which is what makes a story done here;
  # the code-review workflow's default of setting it at the end of the review was again NOT applied.
  # Master re-verified after the merge: 135 + 94 + 42 = 271, zero failures.
  5-4-decision-and-ruleset-version: done
  #
  # 5.4b INSERTED 2026-07-29 with Guy, at 5.4's contexting and before any Epic 5 engine code existed
  # — the same shape as 5.2b's insertion, and for the same reason: the thing being split out is the
  # one whose correctness has to be the SUBJECT of a story, not a subsection of one. It owns
  # `decide` (D13's six rows as a TOTAL pure function, including the uncovered class above), the
  # order-independent choice of which rule a decision names when several qualify (which pre-empts
  # 5.11), and the `cargo xtask ci` gate that reds on any `f32`/`f64` under `identity/` — in the
  # idiom of the existing DDL-collation and retired-vocabulary greps (D56/D65: every gate lives in
  # Rust, never in YAML). The letter suffix keeps 5.5–5.14 at their numbers (D56b idiom).
  #
  # 5.4b CONTEXTED + VALIDATED 2026-07-29 → ready-for-dev.
  # ⚠️ PREREQUISITE: PR #52 must be MERGED before dev-story. This is the first Epic 5 story contexted
  # while its predecessor's PR was still open (the four before it were each merged first); every
  # `cascade.rs` line number in the story was measured on `3d63544`, the post-code-review tree, which
  # is what master holds once #52 merges. The deviation is recorded at the top of the story, not
  # smoothed.
  # It answers the THREE findings story 5.4's code review handed forward on purpose: what `decide`
  # RETURNS, who owns the conclusion↔verdict_vector invariant, and the pre-existing `f64` token
  # sitting in the very subtree this story's gate greps.
  # THE DESIGN DECISION: `decide(Vec<RuleVerdict>, RulesetVersion) -> Decision` returns a Decision,
  # NOT a bare Conclusion — so the returned verdict_vector IS the input, and "a Match with an empty
  # vector" / "a conclusion naming a rule absent from its own vector" become unrepresentable THROUGH
  # THE FUNCTION rather than merely unenforced. That is what makes the invariant testable at all.
  # VALIDATED by two fresh-context agents (fact-check + gap-hunt): 9 HIGH + 12 MEDIUM/LOW applied.
  # FOUR of them changed the DESIGN and every one was MEASURED, not argued:
  #   · the arms are now bound to a `match` on the presence tuple — the gap-hunt agent COMPILED all
  #     three shapes the story permitted and found that with an `if`-chain, deleting the arbitration
  #     arm "compiles and changes 0 of 16 input classes". M2 would have been a silent no-op and the
  #     story's central claim would have shipped unproven;
  #   · the permutation test was VACUOUS as written: three mixed verdicts conclude `Abstained`, which
  #     names no rule, so no tiebreak can change it — and the 32-subset walk gives AC4 ZERO coverage
  #     (one RuleVerdict per verdict means `min()` over a singleton). It now requires three verdicts
  #     of the same rule-naming kind with lex order ≠ vector order;
  #   · an implementation taking `min()` over the WHOLE vector passed every specified test while
  #     returning `NoMatch { rule }` naming the rule that argued FOR the merge. Closed by a named
  #     test and by requiring the oracle to return a full Conclusion (assert_eq, never matches!);
  #   · the float gate was BUILT AND RUN: the specified word-boundary match misses `0.85f64` and
  #     `let confidence = 0.85;` — the likeliest shapes a weight takes — and reds on a block comment,
  #     a FALSE POSITIVE, the one direction the draft said could not happen.
  # BOOKKEEPING corrected before dev rather than after: the register set is EIGHT entries across FOUR
  # sections (not six in one — two `NoMatch` entries in the 4.6a/4.7a sections still say "nothing
  # decides which side an input falls on", which `decide` falsifies); ELEVEN doc blocks are falsified,
  # not five; and the story cited `cascade.rs:42` for the f64 token when it is `:52` — a number
  # inherited from the register without re-measuring, which is the story's own inherited lesson #1
  # committed by the story that quotes it. Caught before dev this time.
  #
  # 5.4b IMPLEMENTED 2026-07-29 → review (branch `story-5.4b-verdict-algebra-and-float-gate`).
  # In the weaker true sentence: `identity/cascade.rs` gains `decide(Vec<RuleVerdict>,
  # RulesetVersion) -> Decision`, a TOTAL pure function over D13's table, plus `smallest_rule_with`;
  # `xtask` gains a SIXTH gate, `float-free`. SIX new core tests + THREE new xtask tests.
  # NOTHING produces a `Verdict`, so nothing calls `decide` outside its own tests: no rule, no join,
  # no blocker, no persistence, no corpus wiring.
  # 271 → 280 tests (135 bin + 100 core + 45 xtask), zero failures. Both clippy forms green.
  # `cargo xtask ci`: SIX gates green + `ℹ views-hash STALE` exit 0 (issue #50, NOT regenerated).
  # THE DESIGN: `decide` returns a `Decision`, not a bare `Conclusion`, so the returned
  # verdict_vector IS the input and the rule is selected FROM it — which makes "a conclusion naming
  # a rule absent from its own vector" and "a Match with an empty vector" unreachable THROUGH THE
  # FUNCTION rather than merely unenforced. A struct literal elsewhere is still free (owner 5.9).
  # THE ARMS are a `match` on the presence tuple, not an `if`-chain, and that is load-bearing:
  # M2 (delete the arbitration arm) gives `error[E0004]: non-exhaustive patterns:
  # (false, false, false, true) not covered` — the compiler NAMES the missing class. Validation had
  # measured that an `if`-chain swallows the same deletion with all 16 classes keeping their answer.
  # FIVE mutations run, and TWO predictions were corrected BY MEASUREMENT:
  #   · M1 (swap the conflict arm's cause) → exactly 2 of 32 subsets, as predicted — but only after
  #     the 32-walk was made CUMULATIVE. With a bare assert_eq! inside the loop it aborts on the
  #     first mismatch and the count is unobservable; the fix is what makes "2 of 32" a measurement;
  #   · M2 → `error[E0004]`, naming the class. As predicted, because of the match construct;
  #   · M3 (min() → next(), i.e. "first in the vector") → ONLY the permutation test reds; the 32-walk
  #     stays green, confirming validation's finding that the walk gives the tiebreak zero coverage;
  #   · M4 (remove the comment-stripping) → **47 offenders on the real tree, not 1**. The prediction
  #     said "reds on the committed citation". The extra 46 are STORY REFERENCES in prose — `5.4b`,
  #     `4.6a`, `4.7a` are literally digit-dot-digit, and the gate also matches BARE float literals
  #     because `let confidence = 0.85;` carries no f32/f64 token at all. Recorded, not smoothed;
  #   · M5 (a real float in code) → 2 reds naming file and line (the type AND the literal), not 1.
  # ⚠️ ONE of the five reds is compiler-carried and four are not: M2 is `error[E0004]`, which would
  # fire on a test body of `assert_eq!(1, 1)`. M1 and M3 red through the assertions of the 32-walk and
  # the permutation test; M4 reds through the gate's three xtask tests. M5 was observed through
  # `cargo xtask ci`'s own output rather than through a test run, and saying so is the honest form —
  # it is the gate reporting, not a test asserting. (Story 5.4's record claimed two of four were
  # assertion-carried when only one was, and its review measured it; this is the corrected shape.)
  # ⚠️ A PROCESS DEFECT, recorded because it cost the implementation once: the first mutation pass
  # ran against an UNCOMMITTED baseline and used `git checkout` to restore, which reverted to HEAD
  # and destroyed `decide` and its tests. Re-applied, then COMMITTED (1ced9e2) before re-running the
  # pass. A mutation pass needs a committed baseline to restore TO — the story's own warning
  # ("restore after every mutation and verify with git status") named the symptom, not the mechanism.
  # TWELVE doc locations corrected, found by `grep -rn '5\.4b' crates/ xtask/` on the final tree
  # rather than from the story's list of five — nine of the twelve name 5.4b, three do not:
  # `AbsenceOfProof`'s doc (it said "nothing argues either way", falsified by the arbitrated class,
  # which carries an Opposes — validation caught this one), `IdentityAbstentionCause`'s "six rows"
  # (seven classes now), and the test module's inventory.
  # REGISTER: EIGHT entries annotated across FOUR sections (validation corrected "six in one"): the
  # coherence invariant ✅ CLOSED by construction with the residue moved to 5.9; the D13 table gap
  # ✅ CLOSED in code with the architecture.md correction moved to **GitHub issue #54**; the
  # duplicate-rule entry ↺ PARTLY closed (totality yes, refusal → 5.5); the f64-token entry
  # ✅ CLOSED; the owner/spec-gap entry ✅ CLOSED; and the four `NoMatch` annotation lines in the
  # 4.6a/4.7a sections ↺ CLOSED IN PART, whose "nothing decides which side" sentences `decide`
  # falsifies (the `Outcome` mapping half moves to 5.7). The empty-vector-literals entry is CLOSED by
  # its own prediction being REFUTED: the four literals compiled untouched, and its count was wrong
  # (four sites, not six).
  # A new `## Deferred from: story-5.4b` section opens NINE items — six name a story, two name a
  # condition, one names a MILESTONE (issue #54); one of the six also names Epic 6 as a second owner.
  # Counted after the last edit: a first draft of that preamble added to nine only by counting the
  # epic as its own item and forgetting the milestone.
  # `epics.md` VERIFIED and NOT edited, as AC9 requires.
  #
  # CODE REVIEW held 2026-07-30, three parallel layers (Blind Hunter · Edge Case Hunter ·
  # Acceptance Auditor). 1 decision resolved by Guy, 22 patches applied, 5 deferrals registered,
  # 3 layer claims REFUTED. PR #55, CI green. 280 → 281 tests (135 bin + 100 core + 46 xtask).
  # ⚠️ SEVEN of the eight HIGH findings were about CLAIMS, not behaviour, and the numbers this block
  # carried were among them. Corrections, each re-measured before being written:
  #   · "47 offenders when the stripping is removed" → **45 under the matcher that shipped** (44 in
  #     cascade.rs + 1 in mod.rs, measured two independent ways). 47 was true at the WIP commit
  #     `1ced9e2` and was never re-measured after the doc pass. ⚠️ And the figure moved AGAIN when the
  #     review replaced the matcher with a tokeniser: story references like `5.4b` stopped counting,
  #     so the same command now gives 42. THREE values for one sentence in one story — which is why
  #     no figure is quoted in code any more and a test asserts the gap instead.
  #   · "EIGHT register entries annotated" → **TEN** (`grep -c 'by story 5.4b, 2026-07-29'` = 10).
  #     The sentence that said eight then enumerated ten; the 4.6a/4.7a sections carry two each.
  #   · "TWELVE doc locations corrected" → not reproducible by any consistent method; counting doc
  #     BLOCKS gives ten. The figure is replaced by the method and its result, not re-guessed.
  #   · "three sentences naming 5.4b left standing" → exactly **ONE** pre-existing sentence was; the
  #     other mentions are sentences the story wrote.
  #   · "M5 → 2 reds, the type AND the literal" → arithmetically impossible: `line_has_float` returns
  #     on the first match, so the gate pushes at most one offender per line. The mutation text was
  #     never recorded, so the count cannot be reproduced and is withdrawn rather than restated.
  #   · `docs/project-context.md` had Task 9 CHECKED while its test count and one falsified sentence
  #     were untouched — the checkbox was the claim, and it was wrong.
  #   · a test doc called the D13 citation "the workspace's only `f64` token" (it is not) and its
  #     premise guard had gone INERT: deleting the citation left `source.contains("f64")` green,
  #     measured by renaming it. Now pinned to the citation's TEXT.
  # WHAT CHANGED IN THE CODE, and why each was more than cosmetic:
  #   · `decide` matches on `(Option<RuleId>, Option<RuleId>, bool, bool)` as AC1 had prescribed and
  #     the implementation had not followed. The two `None` arms are GONE rather than justified: they
  #     had mapped an impossible state onto `Abstained { AbsenceOfProof }` — a legitimate answer for
  #     two other classes — so a broken invariant would have turned a refusal into an honest-looking
  #     abstention. Exhaustiveness survives: deleting the arbitration arm is still
  #     `error[E0004]: non-exhaustive patterns: (None, None, false, true) not covered`.
  #   · the float gate's matcher is a numeric-literal TOKENISER. Measured on the old form:
  #     `1e-3` and `1.` were GREEN (both `f64`, and the exponent form is the shape a weight takes),
  #     while `"192.168.0.1"`, `t.0.1` and `fn a_f64_never_decides()` were RED. Story 5.5 writes the
  #     first MAC/IP rule under the guarded subtree, so the false positives were not theoretical.
  #     Guy chose the tokeniser over a `#[cfg(test)]` skip or an escape comment.
  #   · the comment stripper tracks string literals: `let sep = "//"; let c: f64 = 0.85;` was GREEN.
  #   · the gate fails closed on a guarded directory holding no `.rs` file, and sorts its offenders.
  # SIX mutations, and ONE PREDICTION REFUTED BY MEASUREMENT — recorded because it changed the work:
  # halving the totality walk's derived bound left ALL 100 core tests GREEN, because the oracle agrees
  # on every class the walk still visits. "Every input class" was asserted by NOTHING. An assertion
  # that each `Verdict` variant appeared in some walked subset now reds on it, naming `Disqualifying`.
  # ⚠️ A PROCESS DEFECT REPEATED: that assertion was written, proven, and then DESTROYED by a
  # `git checkout` restoring to HEAD during the next mutation — the exact defect this story's own
  # implementation notes record. Re-applied and committed before continuing. The lesson does not
  # transfer by being written down.
  #
  # `done` 2026-07-30: PR #55 squash-merged as 4f4e774 after a green CI run (1m05). As with every
  # story in this project, `done` is the MERGE's business — which is why this sat at `review` from the
  # end of dev-story through the whole code review and its 22 patches. ⚠️ The review workflow's own
  # step 6 would have set `done` as soon as the patches were applied; that default was deliberately
  # NOT followed, because it would have marked a story done while its PR was still open.
  5-4b-verdict-algebra-and-float-gate: done
  #
  # 5.5 CONTEXTED 2026-07-30 -> ready-for-dev. Three parallel analyses (architecture via the Decision
  # Index, existing code, accumulated debt). ⚠️ Still owes `create-story validate` — two fresh-context
  # agents — before dev-story; that is MANDATORY here and is not optional for this story either.
  # THREE findings changed the story rather than decorating it:
  #   · **AC3 as the epic words it is a TRAP.** D13 calls the U/L bit and the IANA prefix
  #     `Disqualifying` *"as GROUPING anchors"* (architecture.md:1002) — and grouping is L2 (:891).
  #     Two committed traps demand `l1-exact-mac` FIRE on a locally-administered MAC
  #     (randomized-mac.toml:27) and on a VRRP MAC (vrrp-virtual-mac.toml:71), so implementing AC3
  #     literally reds them. AC3 is restated as a NEGATIVE requirement and no IANA predicate is added.
  #     The VRRP refusal the corpus does want is named `l2-virtual-mac-prefix` — an l2-* id, Epic 6's.
  #   · **`l1-distinct-mac` must emit `Disqualifying`, and that is FORCED, not chosen.** The corpus
  #     demands the must-not-merge pole be answered BY NAME; only `NoMatch { rule }` names a rule for a
  #     non-merge; and `decide` reaches `NoMatch` from a `Disqualifying` alone. An `Opposes` would land
  #     on the arbitrated class, abstain, and name nothing — making story 5.7's rule comparison
  #     unsatisfiable. AC5 carries the five-step chain and points the validation agents at it.
  #   · **The bare-MAC key passes the ENTIRE corpus.** Every committed replay stream carries exactly
  #     one `l2_domain` (D61 measured `client.network_id` at ONE distinct value and `client.vlan`
  #     missing 48/48), so AC2 can only be defended by a synthetic two-scope test. Write it first.
  # Measured at contexting, so the story creates neither: `Scope { l2_domain, vantage }` already EXISTS
  # (observation/mod.rs:211-214) and so does `MacAddr::is_locally_administered()` (:78-80). `Scope` is
  # NOT `Ord`, so a BTreeMap keyed on it does not compile — the key is `(L2DomainId, MacAddr)`.
  # No `InterfaceId`/`EntityId` exists anywhere in crates/ and none is minted here (5.9's surface).
  # ⚠️ Enumeration warning carried into the story: `grep -n '5\.5' deferred-work.md` gives **19** lines
  # while `grep -n 'story 5\.5'` gives 11 and MISSES two owner strings that wrap across a newline —
  # the same undercount that made 5.4b claim eight register entries where ten existed. The story
  # therefore groups the obligations by REQUIREMENT (eight) rather than asserting an entry count.
  # ⚠️ Flagged FORWARD, not solved: story 5.6's `blocking_recall >= 0.999` (epics.md:1507) is a bare
  # float literal that WILL red the `float-free` gate if the blocker lives under `identity/`.
  #
  # 2026-07-31 — VALIDATION PASS held (two fresh-context agents, MANDATORY per Guy's Epic 4 retro).
  # Coverage: ~108 factual claims measured (97 true) + a REAL `identity/l1.rs` written from AC1-AC7,
  # gated locally (296 tests, six gates green) and mutated 8 ways. **27 findings: 6 HIGH, 13 MEDIUM,
  # 8 LOW — all applied to the story.**
  # 🔑 THE MEASUREMENT OF THE DAY: **all 6 HIGH came from the agent that COMPILED the story; ZERO from
  # the agent that checked its claims.** The fact-checker found 0 HIGH over ~108 claims — citations,
  # greps, counts and all 26 `float-free` cases were right. What broke was not what the story ASSERTS
  # but what it PRESCRIBES, and only building it surfaced that. A fact-check alone would have passed
  # this story clean. This is the strongest evidence yet for the two-agent rule, and it argues the
  # gap-hunt agent should be told to BUILD, not just to read.
  # The six HIGH:
  #   · AC1 was SELF-CONTRADICTORY — order-independence demanded while prescribing `Vec<ObsId>`, whose
  #     values are order-dependent. The literal implementation failed AC1's own test. → Guy chose
  #     `BTreeMap<(L2DomainId, MacAddr), BTreeSet<ObsId>>`, which also settles the duplicate-Fact::Mac
  #     case for free (the property holds by TYPE, not by a sort() a refactor can drop).
  #   · The `float-free` gate REDS on `"story 5.7"` inside an ASSERTION MESSAGE — a one-dot decimal
  #     with an empty suffix is a bare float (xtask/src/main.rs:1011-1018). `"story 5.4b"` survives
  #     ONLY by its trailing `b` — and it sat in the story's own GREEN list, inviting exactly the wrong
  #     generalisation. `//` comments are stripped; an assertion message is a CODE line.
  #   · The canonical-`RuleId` requirement was guarded by NOTHING: renaming the constants to
  #     `"L1-Exact-MAC "` and `"l1_distinct_mac"` left the whole suite GREEN (296/296), because every
  #     test built its expectation from the constant it was checking. Self-referential assertion — the
  #     same anti-pattern the conclusion oracle exists to prevent, left open on the rule id. Story 5.7
  #     would have failed on all thirteen `l1-*` expectations. → AC8 now demands the ids as STRING
  #     LITERALS, and mutation M6 (non-blank but non-canonical) was added.
  #   · AC4 told the dev to DOCUMENT A FALSEHOOD — that a `Neutral`'s rule id is "unobservable today".
  #     `Decision::verdict_vector` is a pub field and `Decision` derives Debug/PartialEq, so any
  #     whole-`Decision` assert_eq! pins it. Replaced with the weaker true sentence.
  #   · AC4's table did not DECIDE the multi-MAC pair: for A={X,Y}, B={Y,Z} both rows matched and the
  #     two readings gave OPPOSITE verdicts. → Guy chose EXISTENTIAL (share >=1 key -> l1-exact-mac),
  #     grounded in D12 (:884) and in multi-nic.toml:18, which expects that very pair to MERGE.
  #   · `score.rs` was marked LEAVE ALONE while carrying two claims this story falsifies (:285-286
  #     "Nothing produces one", :293-295 "not a mechanism"). → Guy granted a DOCS-ONLY exception; code,
  #     `VerdictVectorEntry` and both size_of tests stay untouched.
  # Also corrected: the §1 rule table claimed `l1-exact-mac` answers EVERY must-merge pole — it is
  # SEVEN OF TEN (multi-nic, shared-hardware-vm, docker-veth name l2-* rules); the E0560/E0609 figure
  # was a story-5.4-era measurement restated as current (6+2 on this tree, not 5+1 — the story's own
  # inherited lesson #2 applied to itself); "three of ten falsified doc blocks did not contain 5.4b"
  # is RECORDED NOWHERE (the register says twelve sites -> ten BLOCKS, and "three sentences" -> ONE);
  # `RuleId` is not const-constructible (RuleId(pub String) -> error[E0015]); `Observation` has no
  # `origin` field; Task 6's M1 and M4 predictions were wrong (both compiler-carried as worded);
  # 15 falsified doc claims in cascade.rs, 7 invisible to the grep; and 5 citations were off.
  # Scope was CHALLENGED and the challenge FAILED by measurement: 5.7's AC1 (epics.md:1525) needs a
  # real engine to answer every l1-* trap and NO story is dedicated to writing the L1 rules, so 5.5
  # absorbing them is forced, not creep; and at 8 tasks it is BELOW the epic's norm (5.3=10, 5.4=11,
  # 5.4b=10). No 5.5b proposed. Epic 5 stays at 16 stories.
  #
  # 2026-07-31 — IMPLEMENTED -> `review`. `identity/l1.rs` is NEW: the join, the two corpus rules,
  # `decide_pair` (**the first caller of `decide` outside its own tests**) and
  # `CURRENT_RULESET_VERSION = RulesetVersion(1)` with NO `Default`. 25 tests added; workspace
  # **281 -> 309 = 135 + 128 + 46** after the code review, counted mechanically. Six gates green,
  # `float-free` now walks 3 files; `fixtures/` untouched.
  # Prove-to-red, six mutations, EVERY red assertion-carried and NONE compiler-carried:
  #   M1 bare-MAC key (type-preserving) = 1 · M2 `Opposes` = 4 · M3 evidence dropped = 3 ·
  #   M4 `vantage` in the key = 5 · M5 blank id = 7 · M6 non-canonical id = 10.
  # 🔑 **M6 is the mutation the VALIDATION added, and it earned its place.** The gap-hunt agent had
  # measured 0 reds for it (whole suite green) on self-referential tests; on the shipped tests it
  # reds TEN, because the assertions restate the two rule ids as independent string literals. The
  # validation finding was not theoretical.
  # AC5's derivation was confirmed by RUNNING its counter-hypothesis, not by arguing it: with
  # `Opposes`, the conclusion is `Abstained { AbsenceOfProof }` and `Decision::rule()` is `None`.
  # Register: EIGHT requirements dispositioned, five reported closed — R1 evidence, R4 the version
  # constant, R6 canonical id, R7 5.3's compiler-carried tests, R8 the NoMatch producer half.
  # ⚠️ R7 was DOWNGRADED to partial by the code review (see below), leaving FOUR closed.
  # Three left OPEN with the measurement that says why: R3 "one verdict per rule" is
  # TRIVIALLY true at L1 (one verdict per pair -> no mutation reds it for the right reason, owner
  # Epic 6); R2 ships as an ASSERTION not a refusal (a runtime refusal has no reachable branch under
  # AC6); R5 keeps its placeholder because **L1 supplies no TIE** — its two rules never meet in one
  # vector. Reporting three as open rather than eight as closed is the point.
  # 🔴 ONE FINDING, reported not absorbed: `xtask/src/main.rs:1821` predicted *"story 5.5 writes IP
  # literals under the guarded subtree"*. **Measured: zero `Ipv4Addr` and zero dotted quads under
  # `identity/`** — the L1 key is a MAC and an opaque domain id. The validation had classified that
  # site "verify-only" ON THE ASSUMPTION the prediction would come true; verifying is what showed it
  # false, and the message was corrected. A green-case rationale naming a future story is a claim
  # with an expiry date.
  # Also caught and NOT propagated: the story's `E0560`/`E0609` figure is a 5.4-era measurement
  # (6+2 on this tree under `--tests`, not 5+1); `cascade.rs` now states the property with no number.
  # ⚠️ HONEST NOTE: tests and implementation were written together, so there was **no failing-test
  # phase** on the initial write. The prove-to-red evidence is the mutation pass alone, and the
  # initial green counts as evidence of nothing.
  # ⚠️ Still flagged FORWARD for 5.6: `blocking_recall >= 0.999` (epics.md:1507) reds `float-free`.
  #
  # 2026-07-31 — CODE REVIEW held, three layers (Blind · Edge · Auditor). ~30 findings, **6 HIGH**
  # after dedup, all applied. 306 -> **309 tests** (28 in l1.rs). PR #57 CI green.
  # 🔑 TWO findings arrived INDEPENDENTLY from two layers (the evidence asymmetry, the self-pair) —
  # that agreement is why they were fixed rather than argued.
  # The three CODE highs:
  #   · **`keys_of`'s `_ => None` catch-all.** Measured: adding `Fact::Uplink { peer_mac }` as an
  #     identity key left the ENTIRE workspace green — a switch merging with everything plugged into
  #     it. Now exhaustive over `Fact`, in `fixtures.rs`'s idiom ("Exhaustive on purpose — no `_`
  #     arm"), so the compiler forces the decision on the next address-carrying variant.
  #   · **Evidence was not symmetric**: decide_pair(a,b) != decide_pair(b,a), same conclusion but
  #     unequal Decisions, so a downstream dedup would see one logical pair as two. Now sorted.
  #   · **A GROUP address (broadcast/multicast) is treated as an interface identity.** The I/G bit
  #     names no interface — `opencmdb-bin`'s privacy scan says exactly that, written in this same
  #     epic by 5.2 — yet three devices reporting ff:ff:ff:ff:ff:ff merge into ONE group. FALSE
  #     MERGE, the class NFR4 exists to prevent, and the corpus can NEVER catch it (0 I/G-set MACs
  #     across 14 streams; the privacy scan now refuses to let one be committed). Refusing them
  #     would red NO committed trap — measured. **Guy's decision: DOCUMENT + REGISTER, do not
  #     filter** — consuming a structural reading at L1 is what 5.5's own doc says L1 does not do.
  #     Current behaviour PINNED by a test so it cannot change in silence. Owner: a decision at
  #     Epic 6's contexting; a trap would have to be authored, since the corpus cannot express it.
  # Surface narrowed on Guy's call: `verdict_for_pair` -> `pub(crate)` (exposing it beside `decide`
  # made cascade.rs's "its two rules never appear in one vector" FALSE), `join` off the flat list.
  # 🔴 THREE of the six HIGH were about CLAIMS, and all three were MINE — the FOURTH consecutive
  # story whose completion record over-claimed:
  #   · cascade.rs's corrected `evidence` doc dropped the qualifier -> false for a `Neutral`;
  #   · the File List's "17 hunks - 12 claims = cargo fmt reflow" was REFUTED by measurement
  #     (`rustfmt --check` on master's cascade.rs exits 0 — all 17 hunks are content);
  #   · the Debug Log tabulated 6 mutation counts and commented only on the ONE that matched,
  #     leaving FOUR divergences unstated (M2 2->4, M3 2->3, M4 7->5, M5 1->7).
  # R7 DOWNGRADED from CLOSED to partial: its owner clause was discharged, its TITLE (5.3's two
  # compiler-carried tests) was not, and AC8's two options were neither taken. The weaker true
  # sentence is now written — the test CANNOT be made behavioural, because `Outcome::Abstained`
  # carries no rule field by design. So of the eight requirements: FOUR closed, FOUR open.
  #
  # 2026-07-31 — MERGED. PR #57 squash-merged as `0ebd50f` after a green CI run. `master`: 309 tests
  # (135 + 128 + 46), six gates green, `views-hash` STALE by design (issue #50).
  # NEXT = `create-story` 5.6, the blocker — and its contexting must OPEN on the fact that
  # `blocking_recall >= 0.999` (epics.md:1507) reds the `float-free` gate if the blocker lives under
  # `identity/`. That was measured during 5.5's validation, not read. It is a decision 5.6 takes
  # deliberately (move the assertion out of the guarded subtree, or express the floor without a
  # float literal), never a surprise it discovers on its first red CI.
  5-5-l1-join-pure: done
  #
  # 2026-08-01 — story 5.6 CONTEXTED -> `ready-for-dev`. Four findings, each MEASURED at contexting
  # rather than reasoned:
  #   · THE FLOAT IS SETTLED BY MEASUREMENT. A probe file under `identity/` run through
  #     `cargo xtask ci` reported exactly three offenders — `0.999`, the epic's own assertion text
  #     quoted inside an ASSERTION MESSAGE, and `"story 5.6"` in a string on a code line — while
  #     `999`, `1000`, `fn blocking_recall_above_999()` and a `///`-quoted `0.999` were GREEN.
  #     The floor becomes an INTEGER IN PER-MILLE (`>= 999`), on D13's own milli-units corollary
  #     [architecture.md:988-993] and on the architecture's ratified test name
  #     `blocking_recall_above_999` [:2954], which already carries no float. The gate is NOT
  #     weakened, skipped or given an escape hatch.
  #   · D18 REFUSES A PAIRWISE-RECALL GATE BY NAME (*"a gate that cannot fall is decoration"*,
  #     [:1246-1252]), so the story owes the distinction: different subject (generator input
  #     coverage, not engine output), different venue (a unit test, not a release gate) — and the
  #     honest arithmetic that at a 10-pair denominator `>= 999‰` IS zero tolerance, and only
  #     becomes a real tolerance above 1000 required pairs, where NFR4's "any fraction is theatre"
  #     would bite.
  #   · THE TRUTH SET IS THE COMMITTED CORPUS, not synthetic pairs: 24 traps, 23 name a pair, 10
  #     `must-merge` — of which **7 share a MAC and 3 do not** (the three whose rule is `l2-*`).
  #     So an exact-MAC blocker scores **700‰** and the assertion has a real red. Because core
  #     cannot read files (D47), the corpus assertion lives in `fixtures.rs`'s TEST MODULE and the
  #     blocker in `identity/blocking.rs` — the file the architecture already names [:3368].
  #   · THE WRONG BLOCKER THAT PASSES THE WHOLE CORPUS is the same-`l2_domain` one: all 23 trap
  #     pairs are same-scope, measured. Only a SYNTHETIC cross-domain test stands between it and
  #     green, and the story requires that test to be written FIRST.
  # Also measured: `grep '5\.6'` over `crates/`+`xtask/` returns ONE unrelated hit, so the doc
  # worklist is by MEANING (five sites, listed in the story) — the grep idiom story 5.5 used yields
  # an EMPTY worklist here. There is no `dormant` anywhere in the tree, so D17's *"the blocker
  # excludes dormant from automatic candidate generation"* [:1205-1206] cannot be implemented and is
  # registered instead of written from belief (D45). Baseline: 309 tests = 135 bin + 128 core + 46
  # xtask, re-measured with `cargo test --workspace --locked`.
  # ⚠️ Branch from `master` only AFTER PR #58 (5.5 bookkeeping) merges — it is still OPEN.
  #
  # 2026-08-01 — story 5.6 VALIDATED (two fresh-context agents, MANDATORY per Guy's Epic 4
  # retrospective decision). 128 factual claims measured (120 true, 6 false, 2 unverifiable) and the
  # story IMPLEMENTED END TO END in an isolated worktree: it compiled first try, 331 tests
  # (309 + 22), six green gates with `float-free` over 4 files, `file-size` unmoved, all six
  # mutations run. 16 findings applied (3 HIGH, 3 MEDIUM, 4 LOW + 6 citation fixes).
  # 🔑 AGAIN: EVERY HIGH CAME FROM THE AGENT THAT COMPILED THE STORY, none from the one that checked
  # its claims — the citations, greps, corpus counts, register enumeration, the 309 baseline and all
  # eight rows of the float probe reproduced EXACTLY. What breaks is what a story PRESCRIBES.
  #   · (H1) M4's second half is FALSE by measurement: 0 of 23 trap pairs list their two ids in an
  #     order the stream does not (0 out of ascending-UUID order either), so `opencmdb-bin` stays
  #     138/138 GREEN under M4. The corpus cannot see that mutation at all. New row in the story's
  #     corpus table; the prediction now says so, so no dev hunts a red that cannot exist.
  #   · (H2) AC4 was SELF-CONTRADICTORY — "per stream, never across streams" cannot coexist with
  #     "recall over the whole truth set" except through a UNION of the per-stream universes, which
  #     the story never named. The union is honest only because the per-trap containment assertion
  #     proves each required pair is in its OWN stream; backstopped by a new measurement — 39
  #     distinct obs_ids across the 11 trap-named streams, ZERO collision.
  #   · (H3) AC1 forbade calling `join` while AC7's test 9 requires it. The prohibition binds
  #     `candidates`, NOT the module: the test module imports `join` deliberately, and that import is
  #     the only way a superset property is checkable.
  #   · MEDIUM: M3 predicted a red on a test that CANNOT red (`candidates` offers only i<j, so the
  #     `Equal` branch is unreachable on distinct input); M1's 700‰ is UNOBSERVABLE unless AC4's two
  #     assertions are two tests (measured: one test panics on `docker-veth-must-merge` before any
  #     recall is computed) — AC7's own split rule now binds AC4; and `fn obs(n)` is NOT importable
  #     (`l1.rs` spells its five helpers obs_id/l2/ts/mac/observation, all private), so the copy is
  #     sanctioned explicitly instead of colliding with the DRY rule.
  #   · Claims refuted by the fact-check: test 8's justification (all six `hostname-absence`
  #     observations carry a MAC — the family encodes an absent HOSTNAME); the 296/296 figure was
  #     story 5.5's VALIDATION PROTOTYPE, whose shipped tests red 10 on the same mutation; "one per
  #     family" omitted `example-must-merge`, which has no family; `xtask:1821`'s prediction was
  #     already corrected during 5.5; `score_corpus` has NO production caller (all 10 sites below
  #     `:385`); NFR4 runs to prd.md:1207 and D18's quote to :1253.
  #   · Two rows added to the float table, both measured: the citations AC6 demands are GREEN even on
  #     a code line, and `"story 5.7"` REDS too — the rule is any story number without a letter
  #     suffix, not just this story's.
  #   · Left explicitly unmeasured rather than assumed: `"v0.1"` (one dot), and whether R1/R2 carry
  #     citable TITLES (now a task, since AC8 forbids citing by line number).
  # The prototype implementation was DISCARDED (Guy's call): dev-story rewrites from the corrected
  # story, so the red/green phase is not short-circuited by unreviewed code.
  # NEXT = `dev-story` 5.6.
  # 2026-08-01 — PR #58 merged as `440b30e`; branched `story-5.6-blocker-and-recall-assertion`
  # from `master` at that commit, which is what the story's contexting note required.
  #
  # 2026-08-01 — story 5.6 IMPLEMENTED -> `review`. 309 -> 332 tests (139 bin + 147 core + 46
  # xtask), re-counted mechanically. Six green gates; `float-free` walks 4 files under `identity/`
  # where it walked 3, and it was NOT weakened, skipped or given an escape hatch — the floor is an
  # INTEGER, `BLOCKING_RECALL_FLOOR_PER_MILLE: u32 = 999`, on D13's own milli-units corollary and on
  # the architecture's ratified test name `blocking_recall_above_999`, which already carried no
  # float. That closes register entry R1.
  #   · A REAL RED PHASE ran before the implementation: with degenerate bodies, 11 of the 19 core
  #     tests failed and EVERY red was assertion-carried — zero compile errors. The 8 that passed
  #     against the stub are NAMED in the story rather than glossed, and each is redded by a
  #     mutation instead.
  #   · ALL SIX MUTATIONS run against the committed baseline `eea3ef1`, each restore verified by
  #     `md5sum` AND `git status`. M1 (blocking on an L1 key) scores **700‰ EXACTLY** as measured at
  #     contexting — 7 reds. M2 (blocking on `l2_domain`) leaves the WHOLE CORPUS GREEN and reds only
  #     the synthetic cross-domain test — 1 red, and that is the mutation proving the test is
  #     load-bearing. M3 reds 2 and correctly does NOT red the `n*(n-1)/2` count test. M4 reds 3 in
  #     core and leaves the corpus entirely green — corpus-invisible, as H1 measured in validation.
  #     M5 reds 1, M6 reds 1. ZERO compiler-carried reds.
  #   · THREE DIVERGENCES from the predictions, stated rather than left implicit (the defect four
  #     consecutive stories carried): M4's bin count is 139, not the predicted 138 — same verdict,
  #     one more test, because AC4's split rule added a fourth bin test; M1's bin red set is 3, not
  #     2, the extra being the containment test that did not exist at contexting; M1's core red set
  #     is 4 where the story said "several".
  #   · The corpus counts are ASSERTED, not quoted: 24 traps, 23 naming a pair, 1 residue
  #     (`example-must-abstain`), 0 naming more than two, 10 required pairs, recall 1000‰.
  #   · A SIXTH falsified doc site was found by RE-READING, not by grep — `identity/mod.rs`'s D54
  #     paragraph — and it turned out to be TRUE (`verdict_for_pair` is `pub(crate)`, the crate and
  #     not the subtree), so it was verified and annotated rather than rewritten.
  #   · Register: R1 (the float) and R2 (the self-pair, now closed IN THE TYPE — `CandidatePair::new`
  #     returns `None`) disposed of BY TITLE; FOUR new entries opened (D17's `dormant` exclusion,
  #     unimplementable because no lifecycle state exists — measured; the quadratic universe; the
  #     floor's arithmetic past 1000 required pairs); four entries belonging to others read and left
  #     OPEN, including the `&str` allocation one whose condition is measured NOT met, since
  #     `candidates` calls no rule.
  #   · `fixtures/` and the planning artifacts are byte-unchanged; no STOP condition arose.
  # NEXT = `code-review` 5.6, then merge. ⚠️ `done` is the merge's business, not the review's.
  5-6-blocker-and-recall-assertion: done
  # 5.7 CONTEXTED 2026-08-01 — the seam. `score_corpus` has scored ZERO since story 4.6b; this
  # story fills its `answers` map from the real engine and `scored` becomes 13.
  # EVERY committed trap was run through `decide_pair` at contexting, by a temporary probe test in
  # `fixtures.rs` (deleted afterwards, md5 back to 3616d8e4…, `git status` clean), so the dev
  # re-derives nothing and a surprise reads as a FINDING. What it measured:
  #   · answering ALL 24 traps makes the gate RED — 6 truth-table failures and 4 wrong-rule
  #     failures. Answering only the 13 whose expected rule is `l1-*` makes it GREEN: 13 scored,
  #     0 failures, 0 mismatches, every rule right;
  #   · 🔴 the residue is ELEVEN, not eight. `epics.md:1545` gives story 5.8 the premise *"the 8
  #     traps whose expected rule is `l2-*`"*. Measured: 8 `l2-*` + 2 `must-abstain` naming a pair
  #     + 1 `must-abstain` naming a SINGLE observation. The three abstain traps are invisible to a
  #     `l2-*` selector because `Expectation::rule()` returns `None` for them. Registered with 5.8
  #     as owner; `epics.md` is verify-only and NOT edited;
  #   · 🔴 `epics.md:1527` calls hostname-absence a *"pure-L1 family"* whose traps *"pass in BOTH
  #     poles"*. It holds THREE traps and the third is a `must-abstain` the L1 engine gets WRONG.
  #     True of the two DECISION poles, false of "all its traps" — the story writes the narrow
  #     claim and asserts the third trap is unanswered;
  #   · 🔴 the single-observation trap `example-must-abstain` WOULD PASS via `decide(vec![], _)` →
  #     `Abstained { AbsenceOfProof }`. Refused: the pass would come from the engine evaluating
  #     NOTHING. Measured precisely so the tempting one-liner is a named refusal, not an omission;
  #   · 11 replay streams are referenced by a trap and NOT ONE carries a `capability` record, so a
  #     `ScoredRecord` here would need an invented snapshot (D36/D45). That is why the
  #     `VerdictVectorEntry` unification — which FIVE doc sites pin on story 5.7, only TWO of them
  #     stating the condition (corrected at validation) — is DECIDED and re-owned rather than done,
  #     with the obstacle recorded;
  #   · all seven rule ids the corpus writes are canonical (`trim`/`lowercase` fixed points), which
  #     is what makes `run_trap`'s unnormalized comparison trustworthy — story 4.7a's registered
  #     gap, closed by measurement plus a test.
  # DECISIONS taken at contexting: `outcome_of(&Decision) -> Outcome` as a NAMED free function in
  # `score.rs` (no `From` impl — the existing refusal is honoured, not overturned); the producer in
  # a NEW module `l1_runner.rs`, NOT in `trap_gate.rs`, so the harness's *"never calls a producer"*
  # stays a FILE-level property on the day it first has something to promise; `score_corpus`
  # unchanged; `read_jsonl` not `FixtureConnector`; the blocker is NOT called (a trap hands the
  # runner its pair) and story 5.6's entry gets the narrow true sentence rather than a closure.
  # ⚠️ Compile-level traps recorded for the dev: `discover_trap_files` is PRIVATE (make it
  # `pub(crate)`, and write no third walk), `walk_trap_files` is `#[cfg(test)]`,
  # `committed_traps_root()` is inside `mod tests`, `verdict_for_pair` is `pub(crate)` in core.
  # ⚠️ NEXT: `create-story validate` (two fresh-context agents) is MANDATORY before dev-story.
  # Baseline: master `6cc137b`, clean tree, 333 tests (139 bin + 148 core + 46 xtask).
  # 5.7 IMPLEMENTED 2026-08-01 -> `review` (branch `story-5.7-trap-runner-stops-scoring-nothing`).
  # THE SEAM IS CROSSED: `score_corpus` reports `discovered=24, scored=13, failures=0,
  # rule_mismatches=[], incomplete_families=[], passed=true` — `scored` had read **0** since story
  # 4.6b, nine stories. 333 -> 350 tests (151 bin + 153 core + 46 xtask). Six gates green
  # (`float-free` over 4 files), BOTH clippy forms clean, `git status fixtures/` AND
  # `git diff HEAD -- fixtures/` both empty, `MANIFEST.toml` md5 `1db9798116a0942eb65f4153ec7b22b6`.
  # `epics.md`, `architecture.md`, `architecture-views.md` all untouched.
  #  · `score::outcome_of(&Decision) -> Outcome` — exhaustive, no `_` arm, **no `From` impl**: the
  #    refusal recorded in `cascade.rs` was KEPT and updated, because a `From` makes the conversion
  #    free at every call site and that invisibility is what the refusal was about;
  #  · a NEW module `crates/opencmdb-bin/src/l1_runner.rs` (233 code lines) — NOT in `trap_gate.rs`,
  #    whose "the harness never calls a producer" is a FILE-level property that would have degraded
  #    to a per-function promise on the day it first had something to promise about. `score_corpus`'s
  #    signature and body are UNCHANGED; the seam is the `BTreeMap<TrapId, Outcome>`, which is data;
  #  · `scored_in` = 7 / 6 / 0. The `must-abstain` column is measured by NOTHING after this story
  #    and the test says so in its own message — the vacuity `scored_in` was built to expose.
  # SIX MUTATIONS on a committed baseline (`50470d1`), every red assertion- or panic-carried, ZERO
  # compiler-carried, and every number the story predicted reproduced:
  #  · M2 (drop the `l1-` prefix) -> `21 scored, 4 truth-table failure(s), 4 wrong-rule failure(s)`
  #    with the four expected/actual pairs verbatim;
  #  · M3 (answer `must-abstain` too) -> `16 scored, 2 truth-table failure(s)` — so
  #    `example-must-abstain` PASSED: §4's manufactured pass, live;
  #  · M6 (a selector that answers nothing) -> `24 discovered, 0 scored`, 8 tests red including the
  #    reproducibility one, which is what its `scored() == 13` precondition exists for.
  # 🔑 M4 WAS RUN ON BOTH SIDES and the difference is the whole point: dropped from `answer_trap` it
  # panics (`index out of bounds: the len is 1 but the index is 1`); dropped from `l1_answers` the
  # ENTIRE SUITE STAYS GREEN, because the level selector removes the corpus's only pairless trap
  # first. That invisibility is the measurement — validation predicted it, and it is why AC2's
  # assertion lives on `answer_trap`.
  # 🔑 M5 NEEDED THREE VARIANTS, not the one specified: a single rename is masked by the "either
  # constant" assertion firing first, and only a corpus with NO `l1-` id at all reds the both-occur
  # guard — the guard that would otherwise let BOTH constants be renamed to anything and stay green.
  # TWO DIVERGENCES from the story's own predictions, recorded rather than absorbed: that, and
  # M1 NOT reddening `the_mapping_preserves_the_rule_mirror_on_every_row` (a wrong ROW keeps the
  # rule, so `Outcome::rule()` still agrees — the mirror and the three row tests each close what the
  # other cannot).
  # WHAT MOVED, AND DID NOT CLOSE:
  #  · the `VerdictVectorEntry` unification is **RE-OWNED, not done**, and the obstacle was
  #    RE-MEASURED on the tree before being written down: 11 replay streams are named by a trap and
  #    NOT ONE carries a `capability` control record, while the only two streams that do
  #    (`capability-downgrade`, `partial-then-failed`) are named by no trap — so a `ScoredRecord`
  #    here would mean inventing a capability snapshot for all 24 traps (D36 in reverse, D45's
  #    "a gate on a false truth"). Owner: the story that gives a trap run a real snapshot;
  #  · the BLOCKER still has no production caller, and that was a DECISION: a trap NAMES its pair,
  #    so the runner has nothing to generate. Story 5.6's entry is answered with the narrow sentence
  #    and stays open, re-owned to the first caller holding observations and no trap;
  #  · 🔴 the residue is **ELEVEN, not 8** — `epics.md:1545` gives story 5.8 the wrong premise. Three
  #    classes: 8 `l2-*`, 2 `must-abstain` naming a pair, 1 `must-abstain` naming ONE observation.
  #    The three `must-abstain` traps are invisible to an `l2-*` selector because
  #    `Expectation::MustAbstain` carries a CAUSE and no rule. Registered with 5.8 as OWNER;
  #    `epics.md` is deliberately NOT edited (an edit there would have been a finding).
  # ALL 19 doc sites honoured or narrowed, verified by re-grepping the FINAL tree: zero remaining
  # sites claim story 5.7 owns anything — the promise-re-made defect five reviews have caught.
  # `the_committed_corpus_is_discovered_and_scored_by_nothing` renamed
  # `an_empty_answers_map_scores_nothing_over_the_committed_corpus`: its name now states what its
  # CALL does, not a claim about the corpus this story falsifies.
  # ⚠️ NEXT: `code-review` -> push -> PR -> green CI -> squash merge. `done` is the MERGE's business.
  5-7-trap-runner-stops-scoring-nothing: done
  # 5.8 CONTEXTED 2026-08-02 against master `60107fa` (clean tree, 352 tests = 153 bin + 153 core +
  # 46 xtask). **This story turns the committed gate RED on purpose**: 24 discovered, 13 scored,
  # 0 failures, **11 unanswerable**, `passed() == false` — and it stays false until Epic 6, exactly
  # as `epics.md:416` says (*"NFR4 stays RED and is closed by Epic 6"*).
  # What was MEASURED at contexting, so the dev re-derives none of it and a surprise reads as a
  # finding:
  #  · the corpus re-read trap by trap from the ten committed `.toml` files — 24 traps, 9 families
  #    + 3 family-less example traps, and **exactly one trap names other than two observations**
  #    (`example-must-abstain`, one);
  #  · the residue is **ELEVEN in three classes (8 / 2 / 1)**, not the 8 `epics.md:1545` still
  #    gives this story. AC8 makes 5.8 the story that CORRECTS `epics.md` — the one lifting of the
  #    verify-only rule, because 5.7 registered the correction with 5.8 as owner;
  #  · the classification ORDER is a real arbitration: `example-must-abstain` is in two classes at
  #    once, so level-first gives 8/3/0 and pair-first gives 8/2/1. **PAIR-FIRST taken** — 8/2/1 is
  #    what 5.7 published to the register, and "cannot be asked" outranks "cannot be routed".
  #    Mutation M4 measures the difference, both halves;
  #  · the per-column arithmetic, derived and re-checked: corpus 10/11/3, scored 7/6/0,
  #    unanswerable **3/5/3**, and `scored_in(c) + unanswered_in(c) == corpus_total_in(c)` for all
  #    three. That equality is the story's STRONGEST guard — the only assertion that fails when a
  #    trap vanishes from both sets, which `discovered` alone cannot see;
  #  · **eleven** live `passed()` assertions exist (4 true, 7 false) and **exactly ONE flips**
  #    (`trap_gate.rs:534`). `trap_gate.rs:830` is the canary: it asserts a GREEN gate over the
  #    committed corpus with an empty map, i.e. 4.6b's AC1, and it must stay green — the bucket is
  #    filled by an explicit `Answer::Unanswerable`, NEVER by absence. If a second one flips, the
  #    4.6b guarantee has been broken silently and that is a finding;
  #  · the seam widens in VALUE, not arity: `&BTreeMap<TrapId, Answer>`. Measured: 26 call sites,
  #    13 pass `&BTreeMap::new()` (no edit — the element type is inferred), 13 pass a named
  #    binding. A second parameter would have touched all 26.
  # 🔴 The one semantic guard the whole story rests on: **`Unanswerable` is NOT an abstention.**
  # Map it onto `Outcome::Abstained` and `score`'s bottom-right cell returns `Pass`
  # [`score.rs:198`] — `example-must-abstain` passes because nothing was asked. That is D18's
  # cowardice moved from the engine up to the harness. Mutation **M3** exists to prove the refusal
  # is load-bearing; if M3 reds nothing, AC1 is decoration and the story says so rather than
  # absorbing it.
  # Also owned here, inherited from 5.7's code review: `l1_answers` has no cross-file `TrapId`
  # uniqueness check (AC10) — harmless today (24 distinct ids across 10 files, measured) and not
  # harmless once the map is TOTAL over the corpus, because a duplicate then shortens the residue
  # denominator with no diagnostic.
  #
  # ✅ VALIDATED 2026-08-02 (two fresh-context agents, MANDATORY). The gap-hunt agent IMPLEMENTED
  # AC1-AC10 end to end in an isolated worktree: **352 -> 364 tests** (163 bin + 155 core + 46
  # xtask), six gates green, both clippy forms clean, `fixtures/` untouched. **20 findings applied,
  # 0 dismissed.** 🔑 THE PATTERN HELD A FOURTH TIME: every finding needing a compiler came from the
  # agent that compiled.
  # **Every numeric prediction reproduced EXACTLY** — 24/13/11, causes 8/2/1, per-column 3/5/3,
  # corpus 10/11/3, `scored_in` 7/6/0, `passed() == false`, the render, 26/13/13 call sites. And
  # §7's table was verified byte-exact: after the seam change and BEFORE any new test existed,
  # exactly ONE `passed()` site flipped (`trap_gate.rs:534`), canary `:830` included.
  # The four HIGH, three of them "aimed wrong" rather than "false":
  #  · **M5 never reached the guard it was written for** — it panics on the first literal
  #    (`unanswered_in(MustMerge)`, 0 vs 3) long before the arithmetic loop §3 calls the story's
  #    strongest guard. **M5b added** (one trap leaves BOTH sets -> `9 != 10`), and AC5 now fixes
  #    the assertion ORDER so both are reachable;
  #  · **decision 7's TOTALITY inverts three `l1_runner.rs` assertions, two of them SILENTLY** —
  #    `len()==13`, the residue derived as a set `difference` against the map's KEYS (now empty),
  #    and a `!contains_key` guard. §7 scoped only `passed()`, so a dev would have diagnosed them
  #    as the FINDING condition. Now a second table. **Both agents found this class independently**;
  #  · **three `l1_runner.rs` doc sites are falsified by §4's OWN pair-first decision**, not by the
  #    bucket (`:19-21`, `:25-29`, and `:106-110`'s "the three must-abstain traps are excluded
  #    before the pair condition is ever consulted" — under pair-first only TWO are);
  #  · the story said "**eight** surviving `passed()` assertions" in AC2 and its Change Log while
  #    §7 said **eleven** — the SEVENTH consecutive story carrying a number falsified inside its
  #    own file. Also: AC8 sent the dev to a `score.rs` site that does not exist (`Report` is in
  #    `trap_gate.rs`; `grep -c Report score.rs` = 0) and named 3 of the 7 measured `5.8` sites.
  # Measured answers to two open questions: **M3 reds 7 tests** (`scored` 13->24, all three
  # must-abstain traps PASS, `failures` 0->3) so AC1's refusal is NOT decoration; and
  # **`unaccounted()` has no production or render consumer** — decision 4 said "rendered when
  # non-zero" and AC6 contradicted it, resolved as accessor-only with M8 giving it a consumer.
  # ⚠️ M7 measured: with the runner's guard removed `score_corpus` STILL refuses the corpus, so an
  # AC10 test written through the harness would be GREEN and the new guard untested — AC10 now
  # REQUIRES the direct `l1_answers` call.
  # ⚠️ ISSUE #38 RECURRED A THIRD TIME during validation, in a clean isolated worktree: 1 red in
  # ~11 runs on `fixtures::tests::a_decision_carrying_an_abstention_cause_is_refused` — a THIRD
  # distinct test, not reproduced in 10 further runs. The failure keeps MOVING between tests, which
  # argues against any single test being the defective one. **Cause still OPEN.**
  # ⚠️ NEXT = `dev-story` 5.8. Three arbitrages remain UNANSWERED by Guy and AC8 depends on the
  # third: pair-first ordering (taken), absent-entry-does-not-block (taken), and **this story
  # EDITING `epics.md:1545`** — the one lifting of the verify-only rule. If that is refused, AC8
  # changes and that part must be re-validated. A reference implementation from the validation sits
  # unpushed on branch `worktree-agent-a2472234e896d6899` at `8c97a55`.
  #
  # (superseded) NEXT was the MANDATORY two-agent validation (fact-check + gap-hunt). The
  # gap-hunt agent must COMPILE the story: on 5.5, 5.6 AND 5.7 every HIGH came from that agent and
  # none from the fact-checker, and on 5.7 all three HIGH were NO-OPS (a mutation that reddened
  # nothing, an assertion placed where it was unreachable, a walk blind to the mutation aimed at
  # it). Point it at AC2, AC5 and AC9's M3.
  #
  # 5.8 IMPLEMENTED -> `review` (branch `story-5.8-unanswerable-bucket`, baseline `2871ebe`).
  # 🔴 **The committed gate is RED, on purpose**: `24 discovered, 13 scored, 0 failures,
  # 0 wrong-rule, 0 incomplete families, **11 unanswerable**, passed = FALSE` — and it stays false
  # until Epic 6 implements `l2-*`, which is what `epics.md:416` has always said.
  # **352 -> 364 tests** (162 bin + 156 core + 46 xtask). Six gates green, both clippy forms clean,
  # `fixtures/` untouched. AC1-AC10 all MET; every numeric prediction of the story reproduced
  # exactly (24/13/11, causes 8/2/1, per-column 3/5/3, corpus 10/11/3, scored_in 7/6/0).
  # 🔑 §7's prediction verified **BEFORE any new test existed**: after the seam change alone the
  # suite gave exactly ONE failure, the `passed()` site it named. Ten kept their value.
  # TEN mutations, **every red assertion-carried, ZERO compiler-carried**. M3 probed directly:
  # `scored=24 failures=3 must_abstain_scored=3 must_abstain_failures=0` — all three `must-abstain`
  # traps PASS under it, `example-must-abstain` included, so AC1's refusal is measured not argued.
  # 🔴 **Two of the story's OWN mutation prescriptions were corrected by running them** — the
  # fourth consecutive time a prescription broke only under a compiler:
  #  · **M5 reds on the arithmetic LOOP**, not on a literal. The story said it "panics on the first
  #    literal and never reaches the loop" — true of the tree the VALIDATION agent had, and
  #    falsified by AC5's own reordering, which the sentence was never re-derived against;
  #  · that left the three literals 3/5/3 proven by NOTHING, so **M5c was added**: a redistribution
  #    inside one column keeps the per-column total at 10 (loop green) while `unanswered_in`
  #    goes 3 -> 4 and the literal reds at `4 != 3`. M5 and M5c prove different assertions and
  #    neither masks the other.
  # A **FOURTH** silently-inverted assertion was found during implementation and is in no story
  # table: the scratch prefix-selector test asserted `answers.len() == 1`, which a TOTAL map
  # satisfies whether the trap is answered or bucketed — the very behaviour that test refuses. It
  # now asserts the VARIANT.
  # Divergences stated rather than smoothed: M3 reds 6 where the story predicted 7; 364 splits
  # 162/156 where §11 predicted 163/155; the three touched files run 10-15 lines longer than the
  # validation's tree. `score.rs` (766) is now the workspace's second largest file by code lines;
  # `file-size`'s largest is unchanged at 1136 (`xtask/src/main.rs`), far under the 2000 ceiling.
  # `epics.md:1545` CORRECTED (8 -> eleven, three classes, dated parenthetical) — the one lifting
  # of the verify-only rule, nothing else in that file touched. Both inherited register entries
  # CLOSED; the cross-file id test calls `l1_answers` DIRECTLY, and M7 confirms why: with the
  # runner's guard removed `score_corpus` still refuses the corpus, so a harness-level test would
  # have stayed green. Both twins (`CLAUDE.md`, `docs/project-context.md`) updated.
  # ⚠️ Issue #38 did NOT recur across ~14 full-suite runs here. Recorded as an observation only —
  # it is not evidence about the cause, which stays OPEN.
  #
  # 5.8 CODE-REVIEWED (three layers, 2026-08-03) and PATCHED -- commit `e4591e6`. It STAYS at
  # `review`: `done` is the MERGE's business. **2 decisions applied, 19 patches, 1 deferral, 2
  # dismissed with the measurement that killed each. 364 -> 367 tests**, six gates green, both
  # clippy forms clean, `fixtures/` untouched, and **rustdoc back to 9 warnings -- master's own
  # baseline**, the three regressions closed.
  # 🔑 SEVENTH consecutive story where most defects are SENTENCES -- and sharper than its
  # predecessors: **four of them violate the story's OWN AC8** (*"update BOTH twins of every
  # duplicated sentence"*), including the twin AC3 named explicitly. The implementer wrote the
  # instruction and broke it four times: `trap_gate.rs`'s `passed()` doc twin, `trap_gate.rs`'s
  # module doc (twin of a sentence corrected in `l1_runner.rs` IN THE SAME COMMIT), and both
  # markdown files still asserting the gate PASSES in the present tense.
  # Guy's two decisions, both option 2:
  #  · **the NFR4 line is VENTILATED by cause.** Measured: `NoLevelToRouteOn` and
  #    `NoPairUnderJudgement` do not depend on a level at all (`Expectation::rule()` is `None` for
  #    any `must-abstain`, at every level present and future), so Epic 6 takes the bucket 11 -> 3
  #    and the old sentence would have gone on naming as its closer the epic that had just
  #    shipped. Each class now names the story that closes it; a class with no members renders
  #    nothing. **The "deletes itself by construction" claim was FALSE and is now narrowed.**
  #  · **`TrapError::RuleMalformed`**, mirroring `FamilyMalformed`: a padded rule id was mis-routed
  #    into the bucket and explained there with a false sentence -- `" l1-distinct-mac "` rendered
  #    as *"at a cascade level this engine does not implement"*, about a level that IS. CASE is
  #    deliberately NOT refused (a different id, not a dirty one) and that boundary is pinned.
  # Six review mutations, every red assertion-carried:
  #  · **R4 reddened NOTHING before its patch** -- the Edge Case Hunter measured `unaccounted()`'s
  #    whole arithmetic deletable (`self.discovered`) with the entire suite green. M8 had proved
  #    the CALL, not the calculation. Same shape the story itself recorded for M5.
  #  · **R6 run on BOTH sides**: the literal-oracle partition assertion stays GREEN under M5b --
  #    the very mutation its message claims to catch -- and the map-reading one reds at 23 != 24.
  # Also closed: the cross-file id guard now folds `trim().to_lowercase()` like
  # `TrapFile::validate` (raw-keyed, `"Shared-Id"` + `"shared-id"` passed, inflating `discovered`);
  # the mixed-family test derives `scored` from the RUNNER's map, not corpus-minus-bucket, which
  # had folded `unaccounted` into "answered"; AC2's unknown-key case is pinned; the arithmetic test
  # is SPLIT in two per story 5.6's idiom.
  # ONE deferral: **totality is ROOT-relative** -- `score_corpus(root_a, &l1_answers(root_b))`
  # leaves the difference unaccounted and PASSES. What keeps it closed today is a convention in a
  # test helper, not a property. Owner: the same story that answers *"should a partial map
  # block?"* -- they are one decision.
  # ⚠️ NEXT = push -> PR -> green CI -> squash merge.
  5-8-unimplemented-level-counts-as-not-passing: done
  # ── Story 5.9 was SPLIT at its contexting, 2026-08-03, with Guy ──────────
  # As written in `epics.md` it carried two ideas: (a) the schema + the persistence contract,
  # and (b) the path that runs the engine over a set of observations and WRITES the links.
  # (b) is the heavier half, it is the first production caller of `identity::l1::join` and
  # `identity::blocking::candidates` (a residue the register has owned as "story 5.9 or
  # Epic 6" since story 5.6), and it is what story 5.10's "the engine re-runs" requires.
  # 5.9 = the schema and its adapter. 5.9b = the resolver that fills it. Epic 5 -> 17 stories;
  # the letter suffix keeps 5.10-5.14 on their numbers (house idiom, D56b, 5.2b and 5.4b).
  # Second arbitration taken at the same contexting: an `identity_link` binds
  # `observation -> interface` (`join` is what forms an interface at L1; `decide_pair` judges a
  # pair and returns none). NO `device` table and NO `entity` supertype -- both Epic 6.
  5-9-persist-interface-and-identity-link: done
  5-9b-engine-resolves-and-writes-links: done
  5-10-purge-test: done
  # 🔴 SPLIT at 5.11's contexting, 2026-08-05 (Guy). `epics.md:1636` gives story 5.11 three ACs
  # that are TWO deliverables: AC2 (idempotence) is new PRODUCTION code, AC1/AC3 (the seeded
  # arrival-order fuzz) is a MEASUREMENT of what already holds -- `join` returns a BTreeMap,
  # `candidates` a BTreeSet, the witness is a `min` over a BTreeSet, the window is a min/max fold,
  # and not one of them reads the slice's order. Epic 5: 17 -> 18 stories. `epics.md` NOT edited;
  # the correction is registered with Epic 5's retrospective.
  5-11-idempotent-supersede: done
  5-11b-order-independence: done
  5-12-never-overwrite-anti-regression: done
  # 🔴 5.13 SPLIT at contexting 2026-08-10 with Guy, before any code existed. `epics.md`'s story
  # 5.13 carried two-to-four ideas and THREE of its clauses are not implementable as written against
  # the committed corpus format -- four findings, each established by code in the tree:
  #   (a) `read_traps` refuses a trap naming an observation absent from its stream
  #       (`DanglingObservation`, fixtures.rs:694) -- and the removal IS the assertion;
  #   (b) the shape D35(a) needs (observations AFTER the failure record, epics.md:1012) is FORBIDDEN
  #       in a committed FILE and legal only in memory -- `epics.md` carries the two contradictory
  #       ACs one line apart, and `deferred-work.md:76` / `fixture_connector.rs:147` record the
  #       asymmetry, naming D35(a) and saying "if a future story gives in-memory streams a trap-like
  #       consumer, this needs revisiting". THIS is that story, and it revisits it by NOT doing so;
  #   (c) a `Trap` judges ONE stream and ONE pair; monotone honesty is a relation between TWO RUNS,
  #       and `incomplete_families` (trap.rs:412) demands a must-merge AND a must-not-merge pole per
  #       family -- a subset assertion projects onto neither;
  #   (d) `deferred-work.md:324` assigns this story a FOURTH deliverable epics.md never mentions
  #       (lattice monotonicity), which needs capability snapshots 11 of 11 trap-named streams lack.
  # So: **5.13 is the MEASUREMENT, 5.13b (INSERTED) is the committed FAMILY.** Epic 5: 18 -> 19
  # stories. The letter suffix is the house idiom for an inserted item (D56b; 5.2b, 5.4b, 5.9b,
  # 5.11b), so **5.14 keeps its number**. `epics.md` NOT edited; the correction is registered with
  # **5.13b as owner**.
  # ⚠️ The KEY was renamed with the file (`5-13-monotone-honesty-trap-family` ->
  # `5-13-monotone-honesty-measurement`), because the family it was named after is no longer in it.
  # Guy's two other contexting arbitrations, both recorded in the story's §2: the faulted run is
  # derived by two named mutilations IN MEMORY from a committed clean stream (no new fixture, no
  # MANIFEST bump -- the `fixtures` gate must still report **25**); and `device_facts` is read at
  # BOTH layers -- the facts emitted into the sink, and the identity links `resolve_within` writes.
  # ✅ VALIDATED 2026-08-10, two fresh-context agents. **The gap-hunt BUILT the story** against its
  # own live `mariadb:10.11.11` on 13308 and left `master` untouched. 6 HIGH, 5 MEDIUM, 11 LOW
  # applied; **2 further arbitrations by Guy**. Two findings were reached INDEPENDENTLY by both
  # layers, one by reading and one by running — the strongest signal of the pass.
  # 🔴 M2 -- THE ONLY MUTATION NAMED FOR THE INCLUSION HALF, i.e. for the NFR8(a) violation itself
  # -- CAME BACK GREEN. `poll` invents a fact on every emit -> 0 new reds, because the mutation is
  # UNCONDITIONAL: it lands on the clean run and the faulted run alike and the invented fact enters
  # BOTH sides of `f ⊆ c`. **A differential test is blind to any mutation that is a pure function of
  # the thing being differenced.** The mutation must be FAULT-CONDITIONAL; the measured form reds 2.
  # 🔴 AC3's inverse direction was a measured NO-OP on both committed streams (`clean = 8 facts,
  # faulted = 8 facts` on each): the failure is LAST and must be, and a `capability` record FILTERS
  # NOTHING -- `poll` only reassigns `in_force`. **Guy: it moves to 5.13b**, which commits streams
  # capable of carrying it.
  # 🔴 The oracle was the wrong STRUCTURE. `Fact` derives neither `Ord` nor `Hash`, so the prescribed
  # `BTreeSet<(ObsId, Fact)>` does not compile; the obvious fix is two derives in `opencmdb-core`.
  # But a SET swallows `[x]` against `[x, x]` -- verbatim the hole story 5.11b measured on
  # `contradicts`' `facts.len()`. **Guy: a MULTISET, inclusion by removal, `PartialEq` alone.** No
  # core change, and right on the merits rather than a workaround.
  # 🔑 WHAT HELD, also measured, and it is most of the story: AC5 is NOT vacuous (2 abstention rows,
  # `absence_of_proof`); AC4(a) IS reddenable (M8); `interface_id` IS literally comparable
  # (`minted=0 found=1`); a new `fault_injection.rs` passes **all seven gates**, clippy clean; and
  # **M1 and M3 behave exactly as predicted**.
  # ⚠️ `DB_TEST_LOCK` (`main.rs:40`) is MANDATORY on both layer-2 tests -- without it they collide on
  # `ERROR 1062` INTERMITTENTLY, which is indistinguishable from issue #38. This is the first story
  # to run the engine over the CORPUS's obs_ids, which two of its own tests share.
  # ⚠️ Also corrected: `fixtures.rs` is **728** code lines, not 1787 (that is `xtask/src/main.rs`);
  # `blind_after` needs a fourth argument, an `as_of` (two of four obvious choices refuse to load);
  # the sweep is 50 positions of which **11 are degenerate**, plus a second degeneracy axis on
  # `kinds` the count does not guard; `decide_singleton` names the SAME rule as `decide_pair` and
  # DOES carry evidence, so at L1 `rule_id` and `outcome` are CONSTANT over placements and
  # `evidence` is the only justification column that could vary; and §6(b) is largely IMPLIED by
  # (a), its one independent clause being `outcome == "abstained"`.
  # 5.13 IMPLEMENTED -> `review` (NOT `done`: that is the MERGE's business). **469 -> 485 tests**
  # (264 bin + 159 core + 62 xtask), seven gates green, **fixtures still 25**, trap gate still RED,
  # `opencmdb-core` untouched, no new dependency. Built and mutated against a live
  # `mariadb:10.11.11` on **13308**. NEXT = `code-review` 5.13, then PR.
  # **ELEVEN mutations, eleven reds, every one carried by a NAMED assertion** -- classified by
  # reading each panic message one at a time; zero compiler-carried, zero `.expect()`-carried (M5's
  # carrier is an `expect_err` on a Result and is recorded as such, not folded in).
  # 🔑 THE FINDING THE STORY DID NOT PRESCRIBE, and it upgrades Guy's arbitration from a precedent
  # to a measurement: M2 invented a Hostname and the oracle reported **three** facts -- the new one
  # plus TWO DUPLICATES, the mutation having re-emitted an observation the cut had already emitted.
  # That prompted **M2b**, which re-emits the first observation UNCHANGED: no new fact anywhere, and
  # the oracle still reds with `INVENTED 2 fact(s)`. **And the CONTROL is what makes the pair mean
  # something** -- same mutation, oracle switched to set semantics: **GREEN**. So *a run that
  # invents nothing and merely REPEATS itself is a monotone-honesty violation, and a set-based
  # oracle cannot see it.* The multiset was chosen on story 5.11b's precedent; it is now measured.
  # ⚠️ A DEFECT IN THE MUTATION DRIVER, caught by disbelieving its own result: M6 first reported
  # **0 red**, and the mutation was sound -- `cargo test --workspace A B` passes two filters where
  # cargo accepts one, so nothing ran. Re-measured over the full suite, M6 reds 1 test on the
  # expected assertion. **It is the story's own subject applied to the story's own tooling**, and it
  # was caught only because 0 red CONTRADICTED a prediction: had the prediction been "green", the
  # driver defect would have been filed as a confirmation.
  # 🔑 A claim of the story confirmed by a mutation's OUTPUT rather than by reading: M7's failure
  # message prints `rule_id: Some("l1-exact-mac")` and `evidence: [obs1]` on a placement the faulted
  # pass settled through `decide_singleton` -- the fact-check's correction to the story's §6,
  # arriving a second time from a different direction.
  # ⚠️ M2-naive was REFUTED IN ITS PHRASING and CONFIRMED IN ITS SUBSTANCE: it reds **3** tests, not
  # zero -- but **not one of them is a story-5.13 test**; all three are byte-equality tests in
  # `fixture_connector`. The differential tests are blind to it exactly as measured.
  5-13-monotone-honesty-measurement: done
  # 🔴 5.13b CONTEXTED 2026-08-11 with Guy -- **FOUR arbitrations, and the fourth REVERSES the
  # LITERAL FORM of the second** on a fact of the code found after the second was taken. Both are
  # recorded in the story's §2; neither overwrites the other, because an arbitration that quietly
  # replaces an earlier one leaves no trace of what was learned in between.
  #   1. PERIMETER: the pair of streams AND the trap family -- clean twin + blinded twin + two traps
  #      (both poles) + the MANIFEST bump + the count updates. `epics.md`'s clause in full.
  #   2. The INVERSE DIRECTION (moved here by 5.13's arbitration 4) is REQUALIFIED: no longer
  #      "derive the clean run by removing the control record" but "the committed blinded twin IS
  #      the committed clean twin, blinded", carried by a guard rather than by two files trusted to
  #      stay in step.
  #   3. D36's capability-snapshot half stays **REGISTERED, NOT DISCHARGED**. This story supplies
  #      the PRECONDITION -- the first trap-named stream to carry a `capability` control record,
  #      where the count was 11 of 11 without one -- and stops there. The doubt order is Epic 6's,
  #      and the other 24 traps still have no snapshot. *"D36 is now testable"* must never be read
  #      as met.
  #   4. The twins carry **DISTINCT `obs_id`s** and the STRICT claim stays on the IN-MEMORY
  #      derivation. See below -- this is arbitration 2's literal form falling to a measurement.
  # 🔴 THREE INHERITED CLAUSES ARE IMPOSSIBLE AS PHRASED, each refuted by code in the tree:
  #   (a) **the inverse direction is a no-op for EVERY committed file, not only for the two that
  #       exist** -- which STRENGTHENS 5.13's finding rather than repeating it. Two independent
  #       reasons: a `failure` record must be LAST in a file (`reject_if_after_terminal`,
  #       fixtures.rs:606, message at :419), so there is no suppressed tail to restore; and a
  #       `capability` record's strip is BAKED INTO the committed observations, because
  #       `UndeclaredFactKind` is POSITIONAL and refuses at load (fixture_connector.rs:250) while
  #       the capability arm (:207-210) only reassigns `in_force` and filters nothing.
  #       🔑 The general form: *removing a control record from a committed file can only WIDEN what
  #       is permitted; it can never restore what the file does not contain.* A category error, not
  #       a hard problem -- so arbitration 2 REPLACES the clause rather than retrying it;
  #   (b) **a monotone-honesty TRAP still cannot exist** (5.13's finding 1c, re-verified):
  #       `Expectation` (trap.rs:69) has three columns, all about ONE merge decision on ONE stream,
  #       and `incomplete_families` (trap.rs:412) needs both poles while a `must-abstain` counts for
  #       NEITHER. So `epics.md`'s *"positive AND negative form"* cannot be honoured for the SUBSET
  #       claim. What the family really judges is the **identity decision under a reduced
  #       capability**. ⚠️ STATE IT WEAKLY: an earlier phrasing here said *"the traps assert that
  #       losing `Rtt` changes no L1 verdict"* and the story file refutes that in the SAME commit --
  #       both traps judge the faulted twin ONLY, nothing judges the same pair on the clean twin, and
  #       `keys_of` reads `Fact::Mac` alone, so `Rtt` is irrelevant to L1 BY CONSTRUCTION. What they
  #       really assert is that the two L1 rules fire correctly on a stream carrying a capability
  #       record. Caught by the code review's Blind Hunter, reading the diff and nothing else;
  #   (c) **`capability-downgrade.jsonl` cannot carry a complete family**, read from its committed
  #       bytes: obs1 and obs4 carry DISTINCT MACs and obs2/obs3 carry NO MAC at all, so no
  #       `must-merge` pole is constructible there. A new stream is FORCED, not preferred.
  # 🔴 THE FOURTH ARBITRATION, and it is the one a validation pass should attack first.
  # `no_obs_id_is_shared_across_replay_streams` (fixtures.rs:1857) refuses any `obs_id` present in
  # two committed streams, corpus-wide -- and `blind_after` PRESERVES `obs_id`s (it clones the
  # observation and only retains facts, fault_injection.rs:163). So two LITERAL twins red that
  # anchor. Giving them distinct ids breaks the comparison from the other side: 5.13's oracle
  # compares `Claim::Fact(ObsId, Fact)`, so every claim would differ. And normalising by POSITION
  # was REFUSED -- it would blind the oracle to a run substituting a different observation into
  # slot n, verbatim the M2b class 5.13 measured, where *a run that invents nothing and merely
  # repeats itself is still a violation*.
  # **So: the STRICT claim runs between committed stream A and its IN-MEMORY blinding** (`obs_id`s
  # equal by construction, no normalisation, oracle undiminished), **and a separate TWIN GUARD
  # asserts the committed blinded twin IS that derivation, modulo `obs_id` and nothing else**, under
  # a mapping stated as a table and asserted total and injective -- never a positional zip. ⚠️ Why
  # commit the blinded twin at all, then: it is the TRAP-NAMED stream (arbitration 3's precondition)
  # and the corpus is a SPEC (D56). The redundancy is DELIBERATE, which the house DRY rule permits
  # only when a test pins it and a comment labels it -- the twin guard is that test.
  # ⚠️ THE COUNTS THE CORPUS FEEDS MOVE IN EIGHT PLACES ACROSS THREE FILES, and each new value must
  # be RE-DERIVED with the measurement that produced it, never patched to whatever makes the suite
  # green (the M5 family, caught four times here): fixtures 25 -> **28**; `corpus.traps` 24 -> 26
  # (fixtures.rs:4658); `corpus.pairs` 23 -> 25 (:4636); `corpus.required` 10 -> 11 (:4573);
  # `all.len()` 24 -> 26 (l1_runner.rs:496); `expected_answered()` 13 -> 15 ids (:390); `discovered`
  # 24 -> 26 at SIX sites in trap_gate.rs (:652, :671, :689, :991, :1060, :1198); `"13 scored"` ->
  # `"15 scored"` (:992, :1199); `scored_in(MustMerge)` 7 -> 8 (:1217), `MustNotMerge` 6 -> 7
  # (:1218). And the ones that must NOT move, asserted as such: `unanswered.len() == 11`,
  # `scored_in(MustAbstain) == 0`, recall **1000‰**, `failures() == 0`, and
  # **`passed() == false`** -- a GREEN trap gate is a FINDING (D18).
  # ⚠️ Prescribed values, each chosen against a measured constraint: `obs_id` prefixes `bdbdbdbd` /
  # `bebebebe` (the thirteen committed streams use aaaaaaaa, abababab, acacacac, adadadad, aeaeaeae,
  # afafafaf, babababa, bbbbbbbb, bcbcbcbc, cccccccc, dddddddd, eeeeeeee, ffffffff); MAC range
  # `02:00:5e:00:56:xx` (the corpus uses :53:, :54:, :55: and one universal); subnet
  # **203.0.113.0/24**, entirely unused and one of the three ranges the privacy walk admits
  # (fixtures.rs:1452). Sharing a `connector_id` between the twins is PRECEDENTED -- only `obs_id`
  # is checked across streams (`first_cross_stream_obs_id`, fixtures.rs:1807).
  # 🔑 Registered rather than fixed (story §9): `epics.md` is NOT edited, and 5.13's contexting named
  # **5.13b as owner** of the correction against it -- discharged by carrying it into **Epic 5's
  # retrospective**. The clean twin is judged by no trap, making THREE such streams: APPEND to the
  # `deferred-work.md` entry, never rewrite a bullet. NFR8 has four assertions; 5.13 covered one and
  # this story covers none of the other three.
  # ✅ VALIDATED 2026-08-11, two fresh-context agents. **The gap-hunt BUILT the story** in its own
  # worktree -- three artefacts, both guards, the count sweep, the MANIFEST bump, thirteen mutations
  # -- reaching **492 tests / 28 fixtures / seven gates green**, then left `master` untouched.
  # **6 HIGH per layer; 2 further arbitrations by Guy (5 and 6 above).**
  # 🔴 THE HEADLINE CAME FROM ONE LAYER ONLY, AND THE OTHER HAD BUILT THE STORY WITHOUT SEEING IT:
  # the draft's `obs_id` prefix **`bdbdbdbd` is RESERVED** by `fixtures/scenario/wire/`, and the four
  # UUIDs the draft prescribed are **byte-identical** to `unifi-clients.expected.jsonl` lines 1-4
  # (pinned at fixtures.rs:4386). The draft's measurement was RIGHT and its CONCLUSION was FALSE --
  # it enumerated `scenario/replay/` and concluded "free of the tree". ⚠️ **No gate would have caught
  # it**: `no_obs_id_is_shared_across_replay_streams` walks `scenario/replay/` only, `wire/` sits
  # outside every corpus walk ON PURPOSE (fixtures.rs:1063-1067), and **M10 -- the mutation the story
  # wrote to prove that anchor live -- is blind to it too**. The gap-hunt reached 492 GREEN tests
  # without noticing, precisely because nothing reds. 🔑 *A green build is not a uniqueness proof
  # when the walk cannot see the file*; the rule is now `rg` over `fixtures/` AND `crates/`, never
  # against a walk and never by enumerating what the corpus uses. Prefix is now `dbdbdbdb`;
  # `bebebebe` was verified free by both layers. **This is registered as a gate-shaped hole**
  # (the reservation lives in a README rather than in a check) -- story §10.
  # 🔴 FIVE FINDINGS REACHED INDEPENDENTLY BY BOTH LAYERS, one by reading and one by running: §6's
  # count table wrong AND incomplete; `fault_injection.rs` missing from it entirely; the
  # `committed_stream_contexts()` table covered by no task; M2's carrier mispredicted (panic-carried,
  # not `Err`-carried); AC5's "green without the pin" refuted.
  # 🔴 FOUR OF THE DRAFT'S OWN CLAIMS REFUTED BY MEASUREMENT:
  #   * **"total and injective" is NOT enough.** The gap-hunt built BOTH guard forms and swapped
  #     stream B's obs 3 and 4 (both AFTER the record, so the stream still loads): whole-vector
  #     equality after rekey reds 1 test; **the KEYED lookup -- literally the draft's letter, no
  #     position anywhere -- leaves `271 passed; 0 failed`**. The missing property is a claim about
  #     the SEQUENCE. AC2 now prescribes the rewrite-then-`assert_eq!` form and REFUSES the keyed one
  #     by name. And M5 as drafted was CONFOUNDED: reversing ALL observations makes the stream
  #     inadmissible, so it reds via the LOAD and proves nothing about the mapping;
  #   * **AC3's strictness half is structurally unreachable for a blinding, and `fault_injection.rs`'s
  #     module doc (lines 21-28) SAID SO BEFORE THE STORY WAS WRITTEN**: *"under M-B strictness is
  #     guaranteed before the connector is invoked … what M-B really measures is the INCLUSION
  #     half."* M3 reds the non-degeneracy first (5.13's assertion-order finding again) and the
  #     mutation targeting strictness directly dies at the LOAD. **Guy's arbitration 5: route it to
  #     the CUT**, which the clean twin joins FOR FREE -- being control-free it enters 5.13's own AC3
  #     sweep, where M1 was measured redding strictness with inclusion green. AC3 keeps 5.13's pair,
  #     each half on the mutilation that can carry it, and the routing is ASSERTED not inherited;
  #   * **AC5's justification was STALE.** The draft quoted story 5.2b's *"swapping the observations
  #     vectors leaves the suite green without the pin"* -- true when measured, false since story
  #     5.7 gave the corpus a real producer: the swap now demands a merge the engine refuses, so the
  #     gate reds INDEPENDENTLY of the pin (`left: 2, right: 0`). The pin is a SECOND oracle, not the
  #     sole carrier. A claim outrunning its measurement, in the story that warns against it;
  #   * **§3a's generalisation has a counterexample in the code it cites.** The draft wrote *"removing
  #     a control record can only ever WIDEN what is permitted"* and labelled it *"the transferable
  #     half"*; `fixture_connector.rs:208-209` says *"An UPGRADE is legal … Nothing here compares the
  #     new descriptor to the old one"*, so removing a widening record NARROWS. The two specific
  #     claims stand; only the sentence the draft was proudest of was wrong.
  # ⚠️ THE COUNT SWEEP IS ~4x WIDER THAN THE DRAFT CLAIMED -- ~31 literals across FIVE files plus a
  # hand-maintained data table, where it said "eight across three" while its own table already
  # listed more. Guy's arbitration 6: **ONE story, and §6 rewritten as a METHOD rather than a list**,
  # because two inventories of it were wrong and the list is explicitly NOT asserted complete. Three
  # reasons the draft's method failed: the prescribed `rg` on 24/23/13 misses the sharpest sites
  # (their literals are **11, 39, 50** -- the clean twin being control-free SILENTLY JOINS 5.13's
  # sweep); failures surface in **TWO WAVES**, a failing assertion hiding the later ones in the same
  # test; and **`cargo test --workspace` never runs the `fixtures` sha256 gate**.
  # ⚠️ Also caught: a `.jsonl` can carry NO header comment (`read_records` parses every non-empty
  # line), so §5's DRY label had to move to the trap file's header and the guard's doc; T1's
  # "generate, don't hand-write" is only PARTLY executable (`ControlRecord` is private and
  # `serde_json::json!` emits keys alphabetically, so the one capability line is hand-written);
  # `earliest_legal_as_of(A,2)` returns 00:00:05Z, not the prescribed 00:00:07Z, so a guard rebuilding
  # the derivation from it reds spuriously; and **`read_jsonl` DROPS control records**, so the trap
  # path is PROVABLY BLIND to the capability record -- §9's D36 precondition is a property of the
  # committed BYTES and nothing more, which is narrower than the draft claimed.
  # 🔑 WHAT HELD, measured: AC1, AC4, AC6, AC7 satisfiable and MET (**26 discovered, 15 scored, 0
  # failures, 0 wrong-rule, 11 unanswerable, `passed() == false`**, `scored_in` 8/7/0, recall
  # 1000‰, 28 fixtures); both new traps PASS; §3a's two specific claims, §3b and §3c re-established
  # independently; the baseline exact at 489 with no drift; **no database needed** and none started
  # by either layer; and **M8's prediction was CORRECT** where the draft had flagged it as its
  # likeliest wrong one -- the unearned caution is removed rather than left standing.
  # ⚠️ The gap-hunt's worktree is KEPT at `.claude/worktrees/agent-ab8fdf0c43e3fd320` (branch
  # `worktree-agent-ab8fdf0c43e3fd320`, commit 017694a): it holds the whole build at 492 tests.
  # 5.13b IMPLEMENTED -> `review` (NOT `done`: that is the MERGE's business), 2026-08-11.
  # **489 -> 493 tests** (272 bin + 159 core + 62 xtask), seven gates green, **28 fixtures**,
  # **26 traps across TEN families**, trap gate still RED (26 discovered, 15 scored, 0 failures,
  # 0 wrong-rule, 11 unanswerable, `passed() == false`). `opencmdb-core` untouched, no dependency,
  # `epics.md` not edited. Branch `story-5-13b`. NEXT = `code-review` 5.13b, then PR.
  # ⚠️ THE COUNT SWEEP CAME IN **ONE WAVE, NOT TWO** -- twenty tests red, one round of repairs, green.
  # That is NOT a refutation of the story's warning; it is what the VALIDATION bought. The inventory
  # was pre-computed by two layers, so every site was repaired before any could hide another. Driven
  # from the draft's own `rg '24|23|13'`, the five `fault_injection.rs` sites (literals **11, 39,
  # 50**) would have formed the second wave exactly as predicted.
  # 🔑 THE FINDING THE IMPLEMENTATION ADDED, and it is the story's own subject turned on itself: the
  # twins' TIGHT descriptor (`{Mac, IpV4, Rtt}` rather than `corpus_caps()`'s seven kinds) was
  # justified by a SENTENCE and carried by NOTHING. **M2b** plants a `Hostname` before the capability
  # record: it reds, naming *"the INITIAL descriptor"*. **And the CONTROL is what makes it mean
  # something** -- the same planted fact with the twins declared under `corpus_caps()` leaves the
  # connector test **GREEN**. A justification with no mutation behind it is the shape six reviews
  # have caught; it is now measured.
  # ⚠️ TWO MUTATIONS' CARRIERS WERE NOT WHAT THE ROW PREDICTED, and both are recorded rather than
  # smoothed: **M7's completeness assertion is UNREACHABLE in a full run** -- deleting a pole drops
  # the trap count and `discovered == 26` fires first, story 5.13's assertion-order finding arriving
  # a THIRD time -- so the completeness carrier had to be ISOLATED to a single test
  # (`a_mixed_family_splits_between_the_engine_and_the_bucket`, printing the `IncompleteFamily`).
  # And **M10 is `panic!`-carried, not assertion-carried**. **Fourteen rows: twelve mutations and TWO
  # CONTROLS green by design; carriers MIXED and named row by row** -- the *"every red
  # assertion-carried"* headline is deliberately NOT restated, having been refuted by review in three
  # separate stories.
  # 🔑 M5's CONTROL reproduced the validation's decisive measurement on the real tree: with the guard
  # written as a KEYED lookup -- literally AC2's earlier letter, *"total and injective"*, no position
  # anywhere -- **the twin guard leaves the failure list** under a post-record swap. ⚠️ The SUITE does
  # not go green (the byte pin reds for its OWN reason), and the claim is about the GUARD; an earlier
  # phrasing would have invited reading it as the whole suite, which is story 5.13's own lesson.
  # 🔑 M1 made §3a EXECUTABLE: delete the capability line from the committed faulted twin and the
  # stream **still LOADS** -- the strip is baked into the observations, so removing the record
  # restores nothing. M4 reds **exactly one** test, and it is the twin guard.
  # ⚠️ Also corrected in flight: `docs/project-context.md`'s live test count read **450 after 5.11b**
  # -- **two merged stories out of date, for the SECOND time in that same paragraph**, in the file
  # BMad auto-loads first; and both twins carried *"25 artefacts, 24 traps across nine families"* in
  # the PRESENT tense as Epic 4's closing figure. The dated fact and the living one now sit apart,
  # labelled, in both files.
  # ⚠️ REGISTERED, not fixed (appended to `deferred-work.md`, never rewriting a bullet): the
  # corpus-wide `obs_id` anchor does not cover `fixtures/scenario/wire/` and the compensation is a
  # README reservation rather than a check; a THIRD stream is judged by no trap (the clean twin,
  # deliberately); D36's precondition is supplied and is NARROWER than it reads, `read_jsonl`
  # dropping control records so the trap path is provably blind to the record just committed; and
  # story 5.2b's *"green without the pin"* is STALE since 5.7 and must be re-derived per family.
  5-13b-monotone-honesty-trap-family: done
  # 🔴 5.14 CONTEXTED 2026-08-11 with Guy — **THREE arbitrations**, and the first CHANGES WHAT THE
  # STORY IS. Two clauses of `epics.md` are unimplementable, each established by code:
  #   (a) **the counter would read ZERO in the shipped product.** `main.rs:130-135` spawns the
  #       startup scan and `:249` writes each observation, but **nothing calls `resolver::resolve`**
  #       outside `#[cfg(test)]` — verified over the whole tree. A display alone would be
  #       structurally zero, permanently, and GREEN: decoration in D18's sense. **Guy: 5.14 WIRES
  #       the pass AND displays.** One story, not split.
  #   (b) **`epics.md`'s third AC is unreachable TWICE OVER.** Nothing produces `Verdict::Supports`
  #       or `Opposes` (the only occurrence outside `cascade.rs` is an exhaustive match arm at
  #       `l1.rs:475`), so L1 cannot conclude `Ambiguous` at all — `resolver.rs:654` says so in as
  #       many words; and nothing writes a `link_candidate` row in production, the only call site
  #       being a test. `resolver.rs:669-671` names the owner: *"Whoever produces the first
  #       `Ambiguous` owns filling this slice, and that is **Epic 6**"*. **Guy: re-owned to Epic 6,
  #       with the unreachability ASSERTED** — a test that reds the day one appears, so the
  #       successor is named by a falling test rather than by a sentence.
  # 🔑 THIRD ARBITRATION: **about twenty `deferred-work.md` entries name 5.14**, most of them BY
  # CONDITION (*"the first story with a ranking surface"*, *"the first place a consumer can justify
  # the split"*, *"the FR16 surface"*). **Answer the conditions one by one** — CLOSED where answered,
  # RE-OWNED where not, each with the measurement. Story 5.7's precedent: `Conclusion::rule()` was
  # closed by ANSWERING its condition, not by building the accessor. A divergence from the story's
  # predicted disposition is a FINDING.
  # ⚠️ READ §4 BEFORE ANY UI: **the abstention panel ALREADY EXISTS.** Story 3.8 shipped "Reach"
  # (`page.rs:112` `cause_label` + the `t!()` seam, `_gap_card.html:47-48`) — but for
  # `gap::AbstentionCause` over DECLARED ATTRIBUTES, where this story renders
  # `IdentityAbstentionCause` over INTERFACES. `deferred-work.md` forbids a `From` bridge between
  # them *"appearing silently"*, so: a second section, its own label function, its own locale keys.
  # A shared `cause_label` IS that silent bridge.
  # 🔑 The parse-back is a DESIGN decision, not plumbing: `cause_token` writes and **nothing parses
  # it back**. The display should parse into `IdentityAbstentionCause` and REFUSE an unknown token,
  # so a cause the domain does not have cannot reach a screen — which also gives the enum its first
  # real consumer and makes several registered entries answerable by measurement.
  # ⚠️ Today exactly ONE cause is producible (`AbsenceOfProof`), so the grouped list has one line.
  # Build the grouping anyway (Epic 6 fills it) but **assert what is true** — the line that exists
  # AND the absence of the other — never that "grouping works".
  # ⚠️ THIS IS THE LAST STORY OF EPIC 5, and the epic does NOT close until `bmad-retrospective` has
  # run. Its input is already registered by name: `epics.md`'s AC1 falsified by the code it
  # describes (5.9b), the `TRUNCATE … WHERE` correction (5.10), 5.12's second-session lesson, and
  # 5.13b's four — the reserved-prefix hole no gate covers, *"a promise of non-modification shelters
  # false sentences"*, *"a floor is only a guard while it equals what is there"*, and the
  # mutation-driver family's FOURTH recurrence. Carry them in; do not rediscover them.
  # ⚠️ NEXT = `create-story validate` (MANDATORY: two fresh-context agents).
  # ⚠️ Baseline to RE-MEASURE, not quote: `master` at `6ceb284` — 494 tests, seven gates, 28
  # fixtures, 26 traps across ten families, trap gate RED.
  # 🔴 5.14 SPLIT AT VALIDATION, 2026-08-11 (Guy, arbitration 4). **Epic 5: 19 -> 20 stories.**
  # ⚠️ The KEY was renamed with the file (`5-14-abstention-displayed-by-cause` ->
  # `5-14-wire-the-pass-and-measure-what-it-cannot-say`), because the surface it was named after is
  # no longer in it — story 5.13's precedent.
  # ✅ VALIDATED by two fresh-context agents. **The gap-hunt BUILT the whole story** (read, wiring,
  # display, bans, AC7 assertion — 503 tests, seven gates green) and then **ran the REAL BINARY
  # against a REAL MariaDB**. What it found there is why the story is no longer the story it was.
  # 🔴 **WIRING THE PASS REPLACES ONE STRUCTURAL ZERO WITH ANOTHER.** `arp_ping.rs:177` emits
  # `vec![Fact::IpV4, Fact::Rtt]` and declares `Capabilities { kinds: {IpV4, Rtt} }` — **no MAC,
  # EVER** — and it is the only connector `main.rs` reaches; `join` keys on `(L2DomainId, MacAddr)`.
  # So **every scanned observation abstains**, `interfaces_minted` is 0 by construction, and the
  # shipped page reads `0 evaluated · 1 not evaluated` against the mock's `187 · 113`. The story's
  # §3a proved the counter would read zero WITHOUT the wiring; WITH it the *evaluated* half is a
  # permanent zero for the same structural reason, one line further down.
  # 🔴 **AND THE COUNTER AGES, which falsifies the promise it exists to keep.** Each scan mints
  # fresh `obs_id`s, so each scan writes a NEW current abstention link and **nothing supersedes
  # anything**: five runs of the binary over ONE host gave `current engine links = 1, 2, 3, 4, 5`.
  # ⚠️ The `current_subject IS NOT NULL` filter does NOT stop it — the draft believed it did. At a
  # five-minute scan interval that one unplaceable host reads **~105 000 after a year**. So the
  # story's own AC4 (*"the count renders identically whatever the age of the rows"*) and the UX ban
  # (*"six months of inaction and it still reads 113, in the same grey"*) are falsified BY THE
  # WIRING ITS OWN AC1 MANDATES. **The counter measures uptime, not reach.**
  # 🔑 **Guy: SPLIT. 5.14 wires and MEASURES; 5.14b INSERTED, the display.** The reason for not
  # fixing the denominator here: an abstained observation HAS no interface — that is what abstention
  # means — so collapsing sightings of one unplaceable thing means deciding what makes two sightings
  # the same thing WITHOUT an identity. **That is grouping, and grouping is Epic 6.** Choosing a
  # denominator under the pressure of a screen to ship would settle in a display story what the next
  # epic exists to decide.
  # 🔑 TWO FURTHER ARBITRATIONS: (5) the section is HOISTED out of the declared side's visibility
  # gate — measured, with four abstentions in the database and no declared row the page renders only
  # *"No declared record yet"*, so a scan-only deployment (the DEFAULT at first boot) never sees the
  # counter; the AC forbidding any change to story 3.8's template is amended and the reason written.
  # 5.14b's. (6) The guard seam **IS the helper, decided and WRITTEN**: deleting the ENTIRE wiring
  # from `spawn_startup_scan` leaves all 282 tests GREEN, because that function is a
  # `std::thread::spawn` with no join handle whose body is inseparable from a real ICMP poll. So the
  # last link is carried by NOTHING, and the story SAYS so with the mutation as evidence rather than
  # claiming a guard on the startup path.
  # ⚠️ Also measured and correcting the draft: `InstantRegressed`/`ContradictoryObservation` are
  # **UNREACHABLE from a scan slice** (fresh `Uuid::now_v7()` per observation, one `observed_at` per
  # poll, five binary runs, no refusal) — the draft called them *"the two refusals a real network can
  # produce"*, which is FALSE; and the transaction menu the draft offered **does not exist** — the
  # ingest is already one `transact` PER OBSERVATION, so two units is the only shape the code offers.
  # ⚠️ And the fact-check found the draft's §9 listed **12 of the 22** register entries naming 5.14,
  # the omission including **`:2407` — the entry that registers the WIRING**, i.e. the story's own
  # headline act. §1 said "about twenty" while §9 listed twelve: two sections of one document
  # disagreeing, and §9 is the one the tasks bind to. Corrected to twenty-two, counted.
  # ✅ VALIDATED A SECOND TIME 2026-08-11, on the REWRITTEN story. **Two further arbitrations (7, 8).**
  # 🔴 BOTH LAYERS FOUND THE SAME HEADLINE: **AC3 pinned the DECLARATION while its own mutation muted
  # the EMISSION.** `arp_ping.rs` carries two independent literals forty lines apart with NO
  # cross-check -- `facts: vec![Fact::IpV4, Fact::Rtt]` (:177) and `Capabilities { kinds: {IpV4,
  # Rtt} }` (:183). Adding `Fact::Mac` to the emitted vector left **all 495 tests GREEN, the pin
  # among them** -- and it is the EMISSION that carries the structural zero, because `join` reads
  # FACTS, not descriptors. 🔑 This is story 5.12's own recorded family (*"M5 said 'neuter the gate'
  # and was applied to the MATCHER"*) recurring in the story whose subject is measurements that
  # measure something other than what they name. Narrowed on 5.12's precedent: pin BOTH literals,
  # and STATE that the one which runs in CI is not the one that carries the fact -- every test
  # reaching a live emit is gated on `OPENCMDB_NET_TESTS`, which **CI never sets**.
  # 🔴 **TWO OF THE STORY'S OWN ACs CONTRADICTED EACH OTHER**: AC4 forbade a new read, AC5 required
  # one. `Resolution` is per-PASS and reports `links_written: 1` after two passes, never "2 current
  # links", so the accumulation is visible only through the database. The register disposition built
  # on *"this story adds no read"* fell with it.
  # 🔴 **ARBITRATION 7 -- a DB-refused observation cost EVERY OTHER observation its link**, and AC2
  # could not see it. `identity_link.observation_id` is a foreign key onto `observation_record`, so
  # one un-ingested row fails the whole pass: measured `resolution=None`, **0 current engine links**,
  # while AC2 stayed green throughout (it asserted only that the observations survive, and they do).
  # **Guy: hand the pass only what LANDED, and REPLACE AC2** -- its subject becomes *"the blast
  # radius of a refused ingest is bounded to its own row"*, because the old subject was unwritable:
  # the only two refusals are unreachable from a scan slice.
  # 🔴 **ARBITRATION 8 -- the cheap guard arbitration 6 dismissed EXISTS, in ~40 lines.** A seam
  # GENERIC over `Connector` lets a test drive poll->ingest->resolve with the already-committed
  # `FixtureConnector`; deleting the wiring inside it reds exactly ONE test where the story's M1 reds
  # none. The uncarried region shrinks from the whole wiring to **three lines**. 🔑 The sentence
  # worth keeping: *recording an unavoidable GREEN is honest; recording it without measuring how much
  # it covers is not.* Arbitration 6 is KEPT beside it, not overwritten -- it was right that the last
  # link cannot be carried, and wrong that one extraction was the end of it.
  # ⚠️ Also caught: **every predicate of the read the story must add is carried by nothing** (drop
  # `decided_by`, drop `current_subject IS NOT NULL`, drop both -- all three GREEN), the FIFTH
  # recurrence of that family; the accumulation's honest mutation is a PRODUCTION one (widen the
  # vacate pass -- 7 red) rather than a fixture edit, and it doubles as the argument for NOT fixing
  # the denominator here, since over-vacating **erases a host that missed a single scan**; the
  # story's pin told a future reader to DELETE it on a numeric change, but under the honest mutation
  # the number falls to 0, which is the OPPOSITE defect; and `:2407` was quoted at half its length --
  # it reads *"no page to display them **and no purge to remove them**"*, and the PURGE half is the
  # one the accumulation makes acute (`purge_engine_links` has no production caller), owned by
  # NEITHER story and now registered.
  # ⚠️ And a scope statement the story had left implicit: **all four of `epics.md`'s 5.14 clauses are
  # DISPLAY clauses**, so this story implements none of them and the wiring it does is asked for
  # nowhere in the epic -- defensible after the split, but said plainly rather than implied.
  # ⚠️ NEXT = `dev-story` 5.14.
  # 5.14 IMPLEMENTED -> `review` (NOT `done`: that is the MERGE's business), 2026-08-11.
  # **494 -> 502 tests** (281 bin + 159 core + 62 xtask), seven gates green, 28 fixtures, trap gate
  # still RED. Built and mutated against a live `mariadb:10.11.11` on **13314**.
  # 🔑 The pass RUNS IN THE SHIPPED BINARY for the first time; the seam is GENERIC over `Connector`
  # and the story states the MEASURED size of what stays uncarried: **three lines**. M1 (delete the
  # call to the seam) = 0 red; M1b (delete `resolve` inside it) = 3 red.
  # 🔴 A DECISION THE STORY DID NOT ANTICIPATE: it said *"does not touch the ARP/ping connector"* and
  # it touches it. Both literals were anonymous expressions inside `poll`, which opens an ICMP socket
  # eagerly, and every test reaching a live poll is gated on a variable CI never sets -- so neither
  # could be asserted. Two NAMED functions, **no behaviour change**, and naming them is what let the
  # two halves CROSS-CHECK each other: M4 (Mac in the declaration) now reds BOTH pins.
  # 🔑 THE ARGUMENT FOR DEFERRING THE DENOMINATOR IS NOW A MEASUREMENT: M6, the naive fix, reds four
  # tests -- three of them PRE-EXISTING `resolver` tests -- because over-vacating **erases a host
  # that missed a single scan**.
  # ⚠️ TWO DIVERGENCES FROM PREDICTION, both recorded: M1b reds 3 where 1 was predicted, and **M2 is
  # NOT EXECUTABLE** (no single ingest unit exists to join -- the story's own section established it
  # and its mutation table kept a row for it anyway; story 5.9's family).
  # ⚠️ Registered, not fixed: the PURGE half of `:2407` is owned by NEITHER story
  # (`purge_engine_links` has no production caller while the pass accumulates ~105 000 rows/year for
  # one host); the denominator (5.14b + Epic 6); and the three uncarried lines (whoever makes the
  # scan joinable -- the periodic scheduler, FR6).
  # ✅ CODE-REVIEWED (three layers) and REPAIRED, 2026-08-11. **502 tests, seven gates green.**
  # 🔴 ALL THREE LAYERS FOUND THE SAME PRODUCTION DEFECT and one MEASURED it: extracting the seam
  # removed the ingest loop from `main.rs` and **LEFT THE POLL** -- every startup swept the CIDR
  # TWICE and threw the first sweep away (4.009 s / 2.0075 s / 1.0025 s as the probe timeout moved,
  # so it scaled WITH the timeout). A host answering the first sweep and missing the second was
  # silently lost. `sink` stayed syntactically used, so the compiler said nothing, and deleting the
  # dead block left all 502 tests green. ⚠️ **The story's own measurement had been taken on
  # `127.0.0.1/32`** -- the one case where a double sweep of one host is invisible.
  # 🔴 THE PINS DID NOT HOLD THE PROPERTY THEY CLAIMED: a `Fact::Mac` at the EMIT SITE -- the shape
  # the upgrade must take, a MAC coming from a neighbour lookup keyed on the address, which
  # `emitted_facts(ip, millis)` cannot reach -- leaves 502 green while the real binary places a
  # link. **Guy: narrow the promise (5.12's precedent)** -- a TRIPWIRE, never a barrier.
  # 🔴 M1b WAS MISRECORDED: **6 red, not 3**. The mutation discarded the pass RESULT instead of
  # deleting its CALL; 3 was just the number of tests reading `.resolution`. Re-measured live: all
  # six, every one assertion-carried, four dying on the database count. **A mutation named for one
  # thing and applied to another measures the other thing** -- the same family as the pins, TWICE in
  # one story. And *"two divergences"* was FOUR (M5 7->11, M6 7->4, both predictions copied from a
  # validation worktree as if they were this tree's).
  # 🔑 REGISTERED, not fixed (Guy): **two concurrent passes mint TWO interfaces for ONE MAC**, both
  # reporting success -- not reachable today because the MAC-less connector shields it, so **the
  # connector story that gives it a MAC removes the shield and must carry the race**; and
  # `current_subject IS NOT NULL` is NOT equivalent to `valid_to = OPEN_END`.
  # ⚠️ NEXT = PR.
  5-14-wire-the-pass-and-measure-what-it-cannot-say: done
  # 🔴 5.14b INSERTED at 5.14's validation, 2026-08-11. **The display**, and it inherits a QUESTION
  # rather than a spec: **what does the abstention counter count?** A count over current links
  # measures scan iterations (see 5.14's measurement). Deciding the denominator needs a notion of
  # "the same unplaceable thing seen twice", which is grouping — so 5.14b arrives WITH Epic 6 or
  # states its denominator's limit in the surface itself.
  # It also inherits: `epics.md`'s `Ambiguous`-shows-its-candidates AC, re-owned to Epic 6 with its
  # unreachability ASSERTED (nothing produces `Supports`/`Opposes`, nothing writes a `link_candidate`
  # row in production, and `resolver.rs:672` names Epic 6 as the owner); the HOIST out of the
  # declared side's visibility gate (arbitration 5); the read and the token parse-back — ⚠️ where the
  # gap-hunt measured that a new variant breaks the LABEL and the WRITER but **never**
  # `cause_from_token`, whose `_ => None` is structural, so the parse-back cannot carry exhaustiveness
  # and a variant added with the minimal repair persists a token the reader refuses, losing the WHOLE
  # PAGE to a 500; the UX bans as HTML assertions; and the remaining register entries.
  # 5.14b CONTEXTED -> `ready-for-dev`, 2026-08-12. **Epic 5 stands at 19/20.**
  # 🔴 THE TRAP NAMED FIRST, because a dev agent that opens the template would conclude the story is
  # nearly done: **a section titled "Reach" ALREADY EXISTS on the page** (`_gap_card.html:47-57`) and
  # it shows a DIFFERENT engine's abstentions -- `gap::AbstentionCause` (three variants, declared
  # FIELDS of one perimeter entity, recomputed per request by the pure `reconcile`) where this story
  # adds `identity::cascade::IdentityAbstentionCause` (two variants, OBSERVATIONS across the store,
  # READ from persisted `identity_link` rows). Same word, different type, different population.
  # ✅ THE THREE OPEN ARBITRATIONS WERE TAKEN BY GUY, 2026-08-12 -- each recorded in the story WITH
  # the alternative refused and the reason, because a decision whose alternative is not written down
  # reads as a preference to the next person:
  #   · **10 -- TWO SECTIONS, NEVER SUMMED.** The two counts range over different populations
  #     (declared FIELDS vs OBSERVATIONS) and adding them denotes nothing -- D13's *"an algebra, not
  #     a sum"* in miniature. REFUSED: one fused frame with two sub-groups, which is closer to the
  #     spec's mock; **two populations inside one frame invite the reader to add them, and the
  #     invitation is the defect.** ⚠️ So the story DIVERGES from `ux-design-specification.md`'s
  #     one-panel mock (lines 919-928) deliberately; the spec is NOT edited and the divergence is
  #     REGISTERED, so it is met as a decision rather than as drift. Owner of the revisit: Epic 6.
  #   · **11 -- THE READER NEVER FAILS.** An unrecognised `abstention_cause` token is COUNTED (the
  #     total must not shrink by omission), labelled through the i18n seam, and the page renders.
  #     `page.rs` turns any error into a 500 for the WHOLE page, so one unknown token would take
  #     down the gap display as well. REFUSED: a DDL `CHECK` on the token domain -- and the deciding
  #     reason is not *"DDL in a display story"* but that it **moves the failure from the DISPLAY to
  #     the WRITE**: a variant added by a future story would then be refused at insertion, i.e. the
  #     identity pass starts failing rather than the page showing an unfamiliar label. 🔑 *A display
  #     story may not be the place a write starts failing.* ⚠️ The cost is stated rather than
  #     implied: without the CHECK the column's domain is held only by `cause_token` being the sole
  #     writer -- a property of the CODE, not of the schema -- so the schema-side closure is
  #     REGISTERED (story 5.12's precedent, where voie B's `GRANT` was registered rather than
  #     implied by the tripwire that shipped). Owner: unassigned.
  #   · **12 -- THE GROUPED READ SUBSUMES `counted_current_engine_links`**, whose doc sentence
  #     *"story 5.14b is its production consumer"* -- a PREDICTION about this story shipped as a
  #     statement -- is corrected here. The function stays as the instrument of 5.14's four pins and
  #     keeps its `#[allow(dead_code)]`. REFUSED: keeping it as a SECOND ORACLE on the page (groups
  #     sum to the unfiltered total) -- two reads on the REQUEST path against a live store disagree
  #     on any concurrent write, so the oracle would be **flaky by construction** rather than
  #     load-bearing; the same redundancy stays legitimate inside ONE transaction in a test.
  #     ⚠️ **The correction is GATED ON A MEASUREMENT**: the two `WHERE` clauses must be verified
  #     identical and the grouping verified not to drop a row, and a divergence **re-opens the
  #     arbitration rather than being forced**.
  # ✅ VALIDATED BY BOTH LAYERS, 2026-08-12; every finding applied. Fact-check: 2 HIGH / 7 MED /
  # 5 LOW. Gap-hunt, which BUILT the story to **516 tests** against its own `mariadb:10.11.11` on
  # port 13315 and ran nineteen mutations with controls: 5 HIGH / 7 MED / 4 LOW, plus EIGHT of its
  # own suspicions refuted by running them.
  # 🔑 BOTH LAYERS INDEPENDENTLY FOUND `link_columns` -- a function that exists NOWHERE, cited with
  # a line number. Two agents that never spoke, same finding.
  # 🔴 FOUR GUARDS THIS STORY PRESCRIBED WERE MEASURED **GREEN**, each with the control that shows
  # it -- this is what the mandatory validation bought:
  #   (a) AC3's anti-sum passes on a zero-reconciliation fixture, i.e. the NATURAL fixture for a
  #       story about the identity section. *An anti-sum guard over a zero addend asserts nothing.*
  #   (b) a missing locale key is a **SILENT KEY ECHO** (`t!` returns the literal), so the story's
  #       OWN prescribed remedy -- read the rendered HTML -- is a **tautology**. The remedy was the
  #       defect. It is now `assert_ne!(resolved, key)`.
  #   (c) under M12 `guard_decision` refuses the decision and the pass ROLLS BACK, so AC8's
  #       assertion passes over an EMPTY table: 19 tests red, that one GREEN. Premise assertions
  #       are now mandatory.
  #   (d) `#gap-card .abstentions .cause` beats the colour check on specificity.
  # 🔴 AC5's OLD WORDING DROVE THE IMPLEMENTATION INTO `set_locale`, a PROCESS-GLOBAL: four
  # consecutive full runs gave **2 or 3 red out of 290, varying**, one casualty being the ageing
  # guard itself -- *reddened by a locale, with no clock anywhere*. ⚠️ **This is the SHAPE of issue
  # #38 and NOT its cause**, and the check that settles it was run: `set_locale` is called at
  # `main.rs:95` ONLY, in production; **no test calls it**, every test uses the per-call `locale =`
  # override. The mechanism does not exist in the tree, so it cannot explain the past red runs.
  # Recorded as a warning about what this story WOULD have introduced -- *a cause needs a check,
  # not a plausible story*. AC5 is SPLIT and `set_locale` is FORBIDDEN in tests.
  # ⚠️ Also: M11 as written is NOT EXECUTABLE against arbitration 11's own design; the ageing
  # tripwire is NOT "the only thing carrying that ban" (M10 reds 3, two ordinary count assertions);
  # the fresh-install empty state was covered by nothing; `AssertSqlSafe` is unnecessary and
  # conflicts with `repo.rs`'s idiom; `repo.rs`'s code half is **1088** lines, so it IS the right
  # home; and `no_match` is UNPRODUCIBLE through `resolve`, measured and to be pinned.
  # ✅ ARBITRATION 13 TAKEN (Guy, 2026-08-12) -- opened by the fact-check layer, which found that
  # §8 recorded *"does not age"* as true by construction via the view builder's clock-freedom, **a
  # strictly weaker property wearing the ban's name**, while this same story measures that the
  # displayed number GROWS; and that the UX spec's FIRST hard ban is literally *"No badge, no
  # growing counter"*, unaddressed in a story whose deliverable is a growing counter.
  # **DECISION: ship the number with an HONEST UNIT -- *sightings*, not devices** -- on both sides
  # of the pair, because each scan mints fresh `obs_id`s and that is what the number is true about.
  # 🔑 *A number that rises because the product looked many times is the radar's range, not the
  # operator's debt* -- which is why the unit is the arbitration and not a caption. ⚠️ **The ban
  # does not become MET; it becomes STATED**, and the unit is TEMPORARY: Epic 6 gives the population
  # an identity and the locale keys change with it (registered).
  # 🚀 DEV-STORY STARTED 2026-08-12 against a live `mariadb:10.11.11` on port **13316** (13315 is
  # the gap-hunt's, still up). Baseline re-measured, not quoted: **502 tests** green, bin suite
  # **5.48 s** with the database against 0.05 s without -- the witness the DB tests really ran.
  # 5.14b IMPLEMENTED -> `review` (NOT `done`: that is the MERGE's business), 2026-08-12.
  # **502 -> 519 tests** (298 bin + 159 core + 62 xtask), seven gates green, 28 fixtures, trap gate
  # still RED at 26/15/11, `opencmdb-core` untouched, `epics.md` and the UX spec NOT edited.
  # Built and mutated against a live `mariadb:10.11.11` on **13316**; the witness the DB tests ran
  # is the timing (0.05 s without a database, ~4.2 s with one).
  # 🔴 THE MUTATION PASS'S FINDING, and it is the transferable one: **M6b came back GREEN.** The
  # anti-sum guard tested that `build_view` and `build_identity_view` do not add each other's
  # counts -- and NEITHER OF THEM CAN, since neither sees the other's numbers. The only place a sum
  # can be written is `reconcile_view`, the impure edge that assembles both, and no unit test
  # reached it. 🔑 ***A guard placed where the defect cannot occur reads as coverage and is none.***
  # Closed by a database-backed test through the composition, after which M6b reds. ⚠️ **Reading the
  # guard could not have found this** -- the guard is CORRECT about what it tests. Only running the
  # mutation did. Carried to the retrospective.
  # ⚠️ Also caught by the pass, each against my own work: a FALSE SENTENCE in my own assertion
  # message (it claimed a render assertion would pass here; M8's second red refuted it -- a render
  # assertion is vacuous only when it checks that SOMETHING appeared or checks the key's own text,
  # not when it names a distinctive phrase of the translation); **M11a left the total assertion
  # unmeasured** (it dropped the cause LINE, not the count), so M11a-bis was written to shrink the
  # total itself; and a false premise of mine -- three out-of-perimeter sightings are **4**
  # reconciliation abstentions, not 2.
  # ⚠️ TWELVE ROWS: eleven executable mutations, one GREEN by stated limit (M9c, the id-selector
  # override the CSS check cannot see -- §8 predicted it). **Zero compiler-carried reds; carriers
  # MIXED and named row by row** -- M12 is `.expect()`-carried on AC8's premise and never reaches
  # its own assertion, and M6b reds on a premise too. *"Every red assertion-carried"* is NOT claimed.
  # 🔑 THE VALIDATION PAID FOR ITSELF, and the figure says so: **FOUR guards this story prescribed
  # were measured GREEN before any of it shipped** -- the anti-sum over a zero addend, AC5's own
  # prescribed remedy for a missing locale key, AC8's assertion over a table a rollback left empty,
  # and the colour check under an id selector.
  # ⚠️ REGISTERED, not fixed (7 entries, each re-read after writing): the `abstention_cause` column
  # has no domain in the SCHEMA (the tolerant reader is not that closure); the two UX bans the
  # counter NAMES rather than merely misses; the temporary unit; the deliberate divergence from the
  # UX spec's one-panel mock; and `count_identity_links`, ANSWERED -- all three reads stay, and the
  # word *unfiltered* belongs to it alone.
  # ⚠️ NEXT = `code-review` (a DIFFERENT model than the one that implemented it), then PR.
  # ✅ CODE-REVIEWED (three layers) and REPAIRED, 2026-08-12. **519 -> 523 tests** (302 bin + 159
  # core + 62 xtask), `cargo fmt` clean, clippy clean, seven gates green, 28 fixtures, trap gate
  # still RED at 26/15/11. 2 decisions, 14 patches, 4 deferrals, 0 dismissed. Three findings were
  # reached by TWO layers independently and one by ALL THREE.
  # 🔴 THE HEADLINE FALSIFIED ARBITRATION 13 ON THE HALF ALREADY REPORTED AS SETTLED: `placed` and
  # `not_placed` were in DIFFERENT UNITS, side by side -- `join` writes one row per L1 KEY while an
  # observation abstains once per OBSERVATION, so a three-MAC sighting beside a MAC-less one showed
  # `placed = 3 · not placed = 1` for TWO sightings in. **Guy: count SIGHTINGS on both sides**
  # (`COUNT(DISTINCT observation_id)`). ⚠️ That KILLS arbitration 12's subsumption, so the test that
  # asserted the two reads AGREE now measures their DIVERGENCE (links 3 / sightings 1).
  # 🔴 `no_match` was folded into `placed` by a bare `else` -- found by ALL THREE layers. A row that
  # *"names the interface it EXCLUDED"* was reported as a placement, with *"Every sighting was
  # placed."* rendered over it.
  # 🔑 GUY'S TAXONOMY SETTLES IT AND IS NOW THE TYPE'S DOC: **no ambiguity -> the software decides
  # (`Match` AND `NoMatch`); ambiguity -> the operator LIFTS THE DOUBT; unknown -> the operator
  # CREATES THE ENTITY.** Its real yield is outside this story: **case three's gesture is the
  # documenting gesture the product does not have at all**, and case three is the ONLY case
  # reachable today -- *the abstention section is the entry point of a gesture that does not exist*.
  # ⚠️ NOT announced in the surface, by decision: descriptive until the gesture is there, because
  # announcing an absent gesture is a promise.
  # ⚠️ AND THE APPARATUS DID NOT FIND THAT: nineteen findings and not one asked *"what can the
  # operator DO with this number?"* -- three layers checked conformance to a specification and the
  # specification was the author's. A blind spot of the METHOD. Carried to the retrospective.
  # 🔴 AC10 WAS NOT MET: §11 required NINE register rows and SEVEN landed, and the re-read meant to
  # catch it counted the seven written against the seven written. 🔑 ***A re-read that reads only
  # what you wrote cannot find what you did not write.*** ⚠️ Its replacement first reported 7/9 FOR
  # THE WRONG REASON (needles split by a line break) -- *a check that fails for the wrong reason is
  # worth nothing* -- normalised and re-run: 9/9.
  # ⚠️ Four more of the author's guards measured GREEN (the `ORDER BY` doc's *"a test pins it"*
  # while MariaDB already sorted; the clock guard, whose real carrier is `chrono`'s
  # `default-features = false`; the `all_placed` branch, rendered by nothing -- AC4's own comment
  # records that defect found in validation FOR THE SIBLING KEY; and the pure anti-sum guard's two
  # unfailable `assert_ne!`s), five mutations the ACs cite were missing from the table with **AC6's
  # only recorded mutation being a GREEN control**, and *"643 lines"* was written in flight inside
  # the bullet about numbers written in flight.
  # ⚠️ NEXT = PR, then **Epic 5's RETROSPECTIVE**, which is REQUIRED and is what CLOSES the epic.
  # ✅ 5.14b **DONE** -- PR #84 squash-merged as `015f2a3` after a green CI run (1 min 1 s), branch
  # deleted on `origin`. **Epic 5 is 20/20.**
  5-14b-abstention-displayed-by-cause: done
  # ✅ HELD 2026-08-12 -- `epic-5-retro-2026-08-12.md`. Epic 4's six action items: 5 done, 1 not due,
  # ZERO lapsed. **142 consecutive green CI runs** across epics 4-5 (47 + 95). 247 -> 523 tests,
  # 5 -> 7 gates, corpus 25 -> 28 artefacts / 24 -> 26 traps / 9 -> 10 families, 42 PRs merged.
  # 🔴 THE EPIC'S DOMINANT DEFECT CLASS, counted in at least NINE of the twenty stories and NAMED at
  # its end: ***a guard placed where the defect cannot occur reads as coverage and is none.*** Never
  # found by reading — only by running the mutation, because such a guard is CORRECT about what it
  # tests.
  # 🔴 GUY'S DRIFT QUESTION, ANSWERED WITH NUMBERS: epics 1-3 = 21 stories and everything usable;
  # epics 4-5 = **43 stories and nothing operator-visible**. The documenting gesture does not exist
  # (five routes, all read-only). ⚠️ What is NOT drift: writing the corpus before the engine is D19
  # and defensible. **What drifted is the RATIO of rigour to reach.**
  # 🔴 SIGNIFICANT DISCOVERY: **the missing gesture is already in the plan, positioned LAST.** Epic
  # 6 closes with *"a one-click promote of an observed value into a declared record"* (FR13), and
  # the PRD calls FR13(a) **"the day-one case"**. It is Guy's case three, it is the ONLY case
  # reachable today -- and ⚠️ **it does not depend on grouping**, which is Epic 6's subject.
  # ✅ **ACTION ITEM 1 DECIDED (Guy, 2026-08-12): option (a) -- THE PROMOTE GESTURE MOVES TO THE
  # FRONT OF EPIC 6.** `epics.md:419-421` edited (a retrospective may; a story may not) and recorded
  # in **GitHub issue #85**, not silently. Refused, both defensible: (b) a short slice before Epic 6
  # -- new scope where a reordering suffices; (c) leave the plan -- keeps the product unusable for
  # a whole epic.
  # ⚠️ EPIC 6'S FIRST OBLIGATION: it has **no story breakdown**, exactly as Epic 5 had none at this
  # point. The breakdown puts FR13 first and must decide whether FR13(b) -- field-selective
  # documenting, which the PRD calls **the dominant long-run case** -- joins that first slice.
  # ⚠️ Also registered: the review apparatus asks conformance to a specification the author wrote,
  # so it never asks *"what can the operator DO with this?"* -- a blind spot of the METHOD; and the
  # mutation driver misled FOUR times in one epic and should be fixed once in `xtask`.
  epic-5-retrospective: done

  # ── Epic 6: Ne pas compter deux fois la même boîte (v0.4) ────────────────
  # Epic 6 is IN PROGRESS since 2026-08-12 — its first story is contexted.
  epic-6: in-progress
  # 🔴 EPIC 6 DECOMPOSED 2026-08-12, into NINETEEN stories, after Epic 5's retrospective AND its
  # project review, into NINETEEN stories. **The documenting gesture OPENS the epic** (issue #85, Guy's decision): it is
  # what milestone J3 -- *a real gap detected AND CORRECTED* -- has been missing, and it does not
  # depend on grouping, which is this epic's own subject.
  # ⚠️ FOUR MEASURED CONSTRAINTS the decomposition respects, written into `epics.md` so no story
  # rediscovers them: (1) the corpus already NAMES the five `l2-*` rules and the names are
  # load-bearing; (2) 🔴 **those five rules take the unanswerable bucket from 11 to 3, NOT to 0** --
  # the three `must-abstain` traps name no rule, so story **6.15** is what actually turns the gate
  # green and makes J4 true; (3) the documenting gesture writes `declared_attribute`, which story
  # 5.12's authorship gate guards, and **two of NFR5's three assertions were parked waiting for it**;
  # (4) `Ambiguous` becomes producible here, `guard_decision` refuses one with no candidates, and
  # **story 5.14b's tripwire reds by design** at story 6.13 -- implement 6.14, do not delete it.
  # ⚠️ SLICED FINER THAN EPIC 5 ON PURPOSE (19 against 14): all six of Epic 5's insertions were found
  # while contexting a story that carried two ideas.
  # ⚠️ A TWENTIETH STORY WAS WRITTEN AND REMOVED THE SAME DAY: FR13(b), documenting a re-discovery
  # field by field. **The FR coverage map already assigns it to Epic 7** and Epic 7's description
  # carries *"document (all/field)"*. The error was in the QUESTION -- it asked *"opening slice or
  # later?"* meaning later within this epic, while the plan had placed it in the NEXT one. Recorded
  # rather than silently dropped.
  # 6.1 CONTEXTED -> `ready-for-dev`, 2026-08-12. **Epic 6 opens.**
  # 🔴 CONTEXTING FOUND WHAT THE DECOMPOSITION HAD NOT: **`auth_deny` already refuses the route by
  # construction** -- it is deny-by-default over every route and `is_public` allowlists only
  # `/`, `/gap`, `/healthz`, `/assets/*`. So a new POST answers **401 whatever the switch does**, and
  # the obvious fix -- one line in `is_public` -- **IS the exposure decision**, taken while debugging
  # with nothing recording it. **AC3 exists so that line cannot be added silently.** The switch
  # decides whether the route EXISTS; `is_public` decides whether it is REACHABLE; only the second
  # is an exposure.
  # ⚠️ Also found: the gesture's canonical name is **`document`** (`document-all`/`document-field`,
  # `architecture.md:3818`), NOT the epic line's *"promote"* -- and D65's retired list already
  # carries two spellings of an earlier name for this same act. And 🔴 **story 5.14b's *"never reach
  # for `--accent`"* guard WILL COLLIDE with story 6.4's amber Document button** -- `--accent` is
  # reserved for exactly that action in `app.css:12` -- which is a conflict to RESOLVE, not a defect
  # on either side. Recorded now, because a collision met at implementation time gets settled by
  # whoever is annoyed.
  # ✅ ARBITRATION 2 SETTLED (Guy, 2026-08-12): **a SHARED TOKEN** -- `OPENCMDB_WRITE_TOKEN` +
  # `Authorization: Bearer`, on the twelve-line pattern `auth.rs` already uses for `/metrics`.
  # 🔴 **AND THE CORRECTION IS THE DELIVERABLE, NOT THE CHOICE**: the question had been asked BADLY
  # twice, conflating *"is the feature ENABLED"* (the switch) with *"WHO may call it"* (the token).
  # **They are orthogonal, and the story ships BOTH, each with its own justification** -- letting two
  # mechanisms share one justification is exactly how a switch ends up being called authentication.
  # ⚠️ The token authenticates a CALLER, not a person: no users, no sessions, no revocation, everyone
  # holding it is one principal. **Real, and crude.** Stated at that strength and no higher; Epic 19
  # is the closure, registered rather than implied. It fails CLOSED on an unset OR empty variable,
  # which is `scrape_authorized`'s own secure default.
  # Refused and recorded as defensible: joining the public allowlist (⚠️ its blast radius IS bounded
  # -- NFR5 keeps the observed side untouchable, so a bogus declared value surfaces as a divergence
  # -- and that bound is what made the option arguable, NOT what made it chosen); and real sessions
  # now (Epic 19's scope, seven months out, a whole slice).
  # 🔴🔴 VALIDATED 2026-08-12 -- **AND THE STORY MUST BE REWRITTEN BEFORE ANY DEV.** `ready-for-dev`
  # here means only what the vocabulary says, *"a story file exists"*. **§0 of the story file is the
  # rewrite brief; read it first.** Fact-check: 3 HIGH. Gap-hunt, which BUILT the story: 5 HIGH, and
  # of FOURTEEN mutations **two came back GREEN with two more green in the shape an implementer
  # would naturally write**. 🔑 **Both layers found the same central defect independently.**
  # 🔴 (1) THE CENTRAL MECHANISM DOES NOT WORK IN THE PRESCRIBED SHAPE. `Router::layer` covers only
  # routes registered BEFORE it -- axum's own doc -- and a CONDITIONAL registration lands after.
  # Measured end-to-end: a POST with **no `Authorization` at all** answered 200/400, the handler ran,
  # `auth_deny` never did. **The exposure the story exists to force into the open would happen
  # silently: AC3 stays green and M5 reds nothing.**
  # 🔴 (2) THE TOKEN DOES NOT REACH THE BROWSER. `hx-post` sends no `Authorization` (measured: 401),
  # so story 6.4's button would need the token handed to the page's JS -- and `/` is PUBLIC. That
  # serves the token to any stranger, the exact principal arbitration 2 excludes. **Arbitration 2
  # does not cover the gesture it exists for.** PROPOSAL ON THE TABLE, NOT DECIDED: **HTTP Basic** --
  # the only mechanism where the browser sends the header itself, one code path, no JS, no cookie, no
  # session store; `curl -u` covers machines; CSRF closed while the credential is not a cookie.
  # ⚠️ It requires the page to LEAVE `is_public`, and ⚠️ **its load-bearing mechanism is UNMEASURED**
  # (does an `hx-post` carry the Basic credential?). **MEASURE WITH A REAL BROWSER FIRST** -- two
  # mechanism assertions were already wrong that day.
  # 🔴 (3) *"WRITES NOTHING"* WAS CARRIED BY NOTHING: a handler that writes and ROLLS BACK leaves 532
  # tests and seven gates green -- the `authorship` gate included, since it keys on the SQL's SITE.
  # 🔑 The gap-hunt BUILT the fix and measured it: a read-only `SubjectLookup` seam via `FromRef`,
  # ~35 lines, under which **M4b does not compile**. The carrier moves from nothing to the COMPILER.
  # ⚠️ Also: AC2's refusal reds 0 tests without a database while the Dev Notes push the implementer
  # away from one; AC1's discriminator collides (404 both ways -- one character in the request body
  # turns M1 green while the route IS registered); the `vocabulary` gate is inert AND its prescribed
  # doc wording REDS it; AC5 has no carrier; two `epics.md` divergences unregistered.
  # ⚠️ NEXT = decide the Basic-auth question (measure first), then REWRITE, then re-validate.
  6-1-write-route-writes-nothing: done
  # through `insert_declared_attribute` and nowhere else; `SANCTIONED_SITES` gains one named entry
  6-2-route-writes-a-declared-value: done
  # 🔴 NFR5's assertions (1) and (2), parked since story 5.12 for want of this gesture.
  # This comment read "assertions 2 and 3" until 2026-08-15 — WRONG, and it inherited the error
  # from `epics.md:1724`: assertion 3 is the one story 5.12 covered (`deferred-work.md:2896`).
  # `epics.md` is NOT edited; the divergence is registered with Epic 6's retrospective.
  6-3-nfr5-remaining-assertions: done
  # FR13(a), *the day-one case*; the gesture belongs to the CAUSE, not the count
  6-4-abstention-line-carries-the-gesture: backlog
  # story 5.9's three deferrals, as ONE schema story, with NO producer
  6-5-entity-device-and-state-schema: backlog
  # 5.6's rule: a blocker that consults a rule is that rule's echo; recall in milli-units
  6-6-l2-candidates-and-no-rule: backlog
  # the FIRST producer of `Verdict::Opposes` in this codebase; 3 traps
  6-7-l2-different-hostname: backlog
  # the FIRST producer of `Verdict::Supports`; 2 traps
  6-8-l2-uplink-agrees: backlog
  # and the measured floor: hostname is unusable on nearly half of known clients
  6-9-l2-hostname-agrees: backlog
  # geography as evidence; `Neutral` where a switch is unknown (D20)
  6-10-l2-different-switch: backlog
  # 🔴 a STRUCTURAL FACT read at ingestion. Guy: *there is no rule* -- and the decision still NAMES `l2-virtual-mac-prefix`, so the corpus is not bumped
  6-11-virtual-mac-anchor-is-not-a-rule: backlog
  # 5.9b's precedent; ⚠️ two races registered at Epic 5 lose their shield here
  6-12-resolver-writes-device-groupings: backlog
  # `link_candidate`'s first production writer; ⚠️ story 5.14b's tripwire REDS here by design
  6-13-first-ambiguous-with-its-candidates: backlog
  # FR16 -- `epics.md`'s story-5.14 clause, re-owned here at 5.14b's contexting
  6-14-ambiguity-explains-itself: backlog
  # 🔴 the five rules take the bucket 11 -> 3, NOT 0. This closes NFR4 and milestone J4 -- the gate's first green since story 4.6b
  6-15-must-abstain-traps-are-routed: backlog
  # 300 hosts; the seed's PROVENANCE guarded, per 5.11b's finding
  6-16-seeded-generator-at-reference-scale: backlog
  # exact, not a threshold -- NFR4's reasoning applies here too
  6-17-distributional-diff: backlog
  # FR38b; locally-administered is a STRUCTURAL fact, never an inference
  6-18-ephemeral-interface-dormant: backlog
  # FR37, derived by min/max over observations -- the clock is never read
  6-19-observation-history-per-device: backlog
  epic-6-retrospective: required

  # ── Epic 6b: L'interface de la maquette (v0.2) — INSERTED 2026-08-13 ──────
  # 🔴 INSERTED BY CORRECT-COURSE 2026-08-13 (`sprint-change-proposal-2026-08-13.md`), on Guy's
  # instruction, after the reference mock became the interface reference. TWELVE stories.
  # ⚠️ IT SPLITS EPIC 6 IN SEQUENCE, NOT IN CONTENT — no story of Epic 6 is modified, added or
  # removed. THE ORDER IS: 6.1 · 6.2 · 6.3 → EPIC 6b → 6.4 → release v0.2.0 → 6.5 … 6.19.
  # Stories 6.1-6.3 are backend-only; **story 6.4 is a SCREEN story**, and building it before this
  # epic means building it twice — once in today's card, once in the triage screen.
  # ⚠️ THE RELEASE SITS INSIDE EPIC 6, NOT AT ITS END: holding v0.2.0 for the fifteen L2-grouping
  # stories would buy one example screen turning real. Epic 6 continues UNDER the release.
  # 🔴 SIX MEASURED CONSTRAINTS in `epics.md` so no story rediscovers them — among them: the example
  # data is NEVER written to the database (`docker/seed-example.sql` is already a shipped writer and
  # story 5.12 had to sanction it; a seeded demo row is indistinguishable from a real one at the
  # storage layer), and the release CHANGES THE PRODUCT'S COLOUR (`gap.html` hardcodes
  # `data-theme="dark"`, the mock is light-only) — named in the release notes, never discovered.
  epic-6b: in-progress
  6b-1-design-system-tokens-and-accent: done
  6b-2-shell-navigation-and-ten-routes: done
  6b-3-example-data-marker-and-its-gate: done
  6b-4-triage-screen-on-the-real-gap: ready-for-dev
  # 6b.4b INSERTED 2026-08-19 at story 6b.4's VALIDATION (Epic 6b: 12 -> 13 stories). Guy's
  # arbitration: 6b.4 keeps AC1 (the queue and the two photos with provenance and freshness) and AC3
  # (the age sort); 6b.4b takes AC2 -- the action bar and the GESTURE NATURE it needs. The reason is
  # DEPENDENCY, not size: AC2's mechanism is invented by no document this project has, and it governs
  # every screen after this one, not just triage. Its form and words are ALREADY arbitrated in 6b.4's
  # §0b so that 6b.4b starts designed rather than with a question. `epics.md` NOT edited (a story may
  # not; only a retrospective may) -- the divergence is registered in deferred-work.md.
  6b-4b-action-bar-and-the-gesture-nature: backlog
  6b-5-dashboard-real-reach-beside-examples: backlog
  6b-6-inventory-and-device-record: backlog
  6b-7-applications-and-ipam: backlog
  6b-8-sources-and-alerts: backlog
  6b-9-self-diagnostic-and-commissioning: backlog
  6b-10-copy-fr-and-en: backlog
  6b-11-keyboard-layer-and-focus-contract: backlog
  6b-12-release-0-2-0-and-its-documents: backlog
  epic-6b-retrospective: required

  # ── Epic 7: La boucle se ferme (v0.5) ────────────────────────────────────
  epic-7: backlog
  epic-7-retrospective: required

  # ── Epic 8: Triage fluide (v0.6) ─────────────────────────────────────────
  epic-8: backlog
  epic-8-retrospective: required

  # ── Epic 9: Bootstrap jour-1 (v0.7) ──────────────────────────────────────
  epic-9: backlog
  epic-9-retrospective: required

  # ── Epic 10: Secrets au repos (minimal) (v0.8) ───────────────────────────
  epic-10: backlog
  epic-10-retrospective: required

  # ── Epic 11: Source UniFi complète (v0.9) ────────────────────────────────
  # ⚠️ INHERITS from Epic 4's 2026-07-25 re-scope (issue #34): run the 4.18 wire spec under the
  # REAL parser, and implement 4.19b (mutation generator + ~30 fixtures + expected outcomes).
  epic-11: backlog
  epic-11-retrospective: required

  # ── Epic 12: Scan ARP/ping complet (v0.10) ───────────────────────────────
  epic-12: backlog
  epic-12-retrospective: required

  # ── Epic 13: Ma source devient aveugle (v0.11) ───────────────────────────
  epic-13: backlog
  epic-13-retrospective: required

  # ── Epic 14: IPAM (v0.12) ────────────────────────────────────────────────
  epic-14: backlog
  epic-14-retrospective: required

  # ── Epic 15: Applications & « Hosted here » (v0.13) ──────────────────────
  epic-15: backlog
  epic-15-retrospective: required

  # ── Epic 16: Alertes & notifications (v0.14) ─────────────────────────────
  epic-16: backlog
  epic-16-retrospective: required

  # ── Epic 17: Retour après une absence (v0.15) ────────────────────────────
  epic-17: backlog
  epic-17-retrospective: required

  # ── Epic 18: Bulk steady-state (v0.16) ───────────────────────────────────
  epic-18: backlog
  epic-18-retrospective: required

  # ── Epic 19: Exposer sans risque (v0.17) ─────────────────────────────────
  epic-19: backlog
  epic-19-retrospective: required

  # ── Epic 20: Le réseau visible depuis ailleurs (v0.18) ───────────────────
  epic-20: backlog
  epic-20-retrospective: required

  # ── Epic 21: Maîtriser ses données (v0.19) ───────────────────────────────
  epic-21: backlog
  epic-21-retrospective: required

  # ── Epic 22: First-light soigné & bilingue complet (v0.20) ───────────────
  epic-22: backlog
  epic-22-retrospective: required

  # ── Epic 23: Prêt à installer (v1.0) ─────────────────────────────────────
  epic-23: backlog
  epic-23-retrospective: required
