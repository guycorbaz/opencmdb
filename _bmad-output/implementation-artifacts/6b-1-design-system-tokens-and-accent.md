# Story 6b.1: The design system — tokens, typography, and the accent that stays reserved

Status: ready-for-dev

Epic: 6b — *L'interface de la maquette* (inserted 2026-08-13 by correct-course,
`sprint-change-proposal-2026-08-13.md`). **First story of the epic**, so `epic-6b` moves to
`in-progress` with it.

## Story

As the operator,
I want the product to look like the reference mock,
so that what I was shown is what I run.

## What this story does NOT do

It ships **no screen**. `templates/` gains no file, the router gains no route, and the ten
navigation entries are story 6b.2's business. This story changes what the ONE existing page is made
of — its tokens, its typefaces, its theme — and builds the machinery the next eleven stories rebuild
their screens with.

It also does **not** write the example dataset, the marker, or any copy: 6b.3 and 6b.10 own those.

---

## 1. 🔴 The AC's first word presupposes a toolchain this repository does not have

`epics.md` writes the first criterion as *"**When** the Tailwind theme is rebuilt"*. **There is
nothing to rebuild.** Measured on `master` at `b58088d`:

| Claim | Where it is written | What is actually there |
|---|---|---|
| *"Tailwind standalone CLI via `cargo xtask css`"* | `CLAUDE.md`, in the stack list, as a fact | `xtask` has **one** subcommand. `xtask/src/main.rs:67,71` print `usage: cargo xtask ci` and nothing else |
| *"`assets/app.css` is COMMITTED (D55) — generated by `cargo xtask css`"* | `.gitignore:40` | `app.css` is **hand-authored** — its own header line 1 says *"hand-authored design tokens"* and line 3 says *"No Tailwind build step for the walking skeleton"* |
| `/tools/tailwindcss*` ignored as *"a tool, not a source"* | `.gitignore:44-47` | no `tools/` directory exists |
| `assets/tailwind.css` with its `@source` lines | `architecture.md:3402` (the tree D55 prescribes) | the file does not exist; `assets/` holds `app.css`, `app.js`, `htmx.min.js` and nothing else |

🔑 **So this story's real first task is to BUILD the chain D55 specifies, not to re-run it.** That is
a larger thing than the AC's wording suggests, and §7 puts the consequence in front of Guy rather
than absorbing it silently.

⚠️ **Three documents disagree about where `app.css` comes from, and two of them are wrong.** They are
not corrected here — a story does not edit the planning artifacts — but `.gitignore:40` and
`CLAUDE.md`'s stack line become TRUE the day this story lands, and the third (`app.css`'s own
header) must be rewritten by this story because it is the file being replaced. **Registered in
§Validation obligations.**

### 1b. What D55 actually requires, and the trap it names

`architecture.md:3168-3230`, in full force here:

- **`build.rs` invoking the Tailwind CLI is REFUSED.** Not a preference: a `cargo build` that
  rewrites a git-tracked file is a fault, `cargo install --path .` must work on a machine with no
  Tailwind binary, and two generators for one byte is *"the worst of both"*. `cargo xtask css`
  instead — **`xtask` is nobody's dependency (D56), `build.rs` would be everybody's.**
- **`@source` is relative to the CSS FILE, not to the manifest** — `../templates/`, never
  `./templates/`.
- 🔴 **A wrong `@source` path breaks NOTHING VISIBLY**: Tailwind finds nothing, the build is green,
  `app.css` silently loses half its classes, *"and you discover the colourless status pill in
  production."* This is the same shape as constraint 5 of the epic, and it is why the gate below
  counts classes **in the generated CSS** rather than reading the config.
- ⚠️ **`architecture.md:619` (finding F16) records that the UX spec's Tailwind note describes an API
  that no longer exists**: v4 removed the JS-config `content` and `safelist`. Use `@source` and
  `@source inline()`. *The decision stands; the syntax does not.* Do not implement the spec's letter.

---

## 2. The mock's tokens — extracted, not paraphrased

Source: `~/travail/Projets actuels/opencmdb/documents/opencmdb - maquette.html` (495 068 bytes,
2026-08-13), **outside this repository**. Its CSS lives inside a JSON-escaped string; unescaped, it
carries exactly **one `:root` block**:

```
--color-bg: #f2f2f3      --color-surface: #e9e9ea    --color-text: #1d1f20
--color-accent: #5980a6  --color-accent-2: #728fab
--color-divider: color-mix(in srgb, #1d1f20 16%, transparent)

neutral-100..900   #f5f5f8 #e7e7ea #d4d4d7 #b7b7ba #98989b #7a7a7d #5d5d60 #424244 #2b2b2d
accent-100..900    #eef6ff #d6ebff #b5d9fd #94bce3 #749dc4 #597ea3 #416180 #2c455d #1d2d3d
accent-2-100..900  #eef6ff #d6ebff #bdd8f2 #9ebbd8 #7e9cb8 #627d98 #486077 #314457 #1f2d3a

--font-heading: "Barlow Condensed", system-ui, sans-serif   --font-heading-weight: 600
--font-body: "Barlow", system-ui, sans-serif

--space-1..8: 3.4 6.8 10.2 13.6 20.4 27.2 px
--radius-sm/md/lg: 2px 4px 7px
--shadow-sm/md/lg: ink-tinted, via color-mix
```

Four observations that are decisions, not transcription:

1. 🔴 **The mock has NO amber token.** Its `--color-accent` is the blue. So `--accent-document` is an
   ADDITION this product makes to the mock's system, not a token lifted from it — which is exactly
   what the epic's constraint 3 and AC2 describe, and it means nothing in the mock can be consulted
   to check it.
2. 🔴 **The mock has NO monospace token**, while the UX spec requires one for technical data — *"a
   monospace for technical data (IPs, MACs, ports, hostnames) so addresses align and scan"*
   (`ux-design-specification.md:826`) — and `app.css:36-39` ships `.mono` on a system stack today.
   **The system stack is KEPT and no monospace is embedded** (the AC names two families and only
   two); the divergence is registered rather than resolved by silence.
3. ⚠️ **The mock's radii are 2/4/7px and the UX spec says `3px` everywhere** (`:684`, *"Radius 3px
   everywhere"*, and `--radius: 3px` at `:678`). Guy's decision (3) of 2026-08-13 adopts the mock's
   palette and typography; **radius is neither**. Left as the mock's three-step scale, registered.
4. ⚠️ **`color-mix()` and the shadows.** The mock derives its dividers and elevation through
   `color-mix(in srgb, …)`, while the UX spec bans shadows outright — *"elevation via hairline
   borders, no shadows"* (`:684`). Adopting the mock's `--shadow-*` tokens would contradict a
   specified rule; **the tokens are carried into the sheet, and nothing in this story USES them** —
   the same treatment the dark set gets in AC3, and for the same reason.

---

## 3. 🔴 The fonts are NOT in the mock, and the AC assumes they are somewhere

Measured, because *"lift the tokens and the fonts from the mock"* is the obvious plan and it fails:

- the mock declares **15 `@font-face` rules**, resolving to **five faces**: Barlow 400 / 500 / 700
  and Barlow Condensed 400 / 600;
- every one has `src: url("<uuid>")` — an artifact-host reference. **The bytes are not in the file**:
  `base64` occurs exactly **once** in 495 KB, inside the viewer's own JavaScript, and there is **no**
  `fonts.googleapis` / `fonts.gstatic` reference at all.

**Provenance, verified upstream rather than assumed** (`github.com/jpt/barlow`, the designer's own
repository, which carries `OFL.txt`, 4 377 bytes — SIL Open Font License 1.1):

| File | Bytes |
|---|---|
| `Barlow-Regular.woff2` | 58 964 |
| `Barlow-Medium.woff2` | 59 180 |
| `Barlow-Bold.woff2` | 60 492 |
| `BarlowCondensed-Regular.woff2` | 58 652 |
| `BarlowCondensed-SemiBold.woff2` | 60 508 |
| **total** | **297 796 (~291 KB)** |

🔑 **`rust-embed` needs no wiring for this.** `page.rs:24-26` embeds `#[folder = "assets/"]`, so
files dropped in `assets/fonts/` are embedded by the mechanism that already exists — and they are
served under `/assets/*`, which story 6.1 left as one of the only two PUBLIC prefixes. **The binary
grows by ~291 KB and that is the whole cost**; state it in the story record rather than letting a
release note discover it.

⚠️ **The licence must be RECORDED, which the AC says and which is not the same as respecting it.**
OFL 1.1 requires the copyright notice and licence to travel with the fonts. `OFL.txt` lands beside
them (it is not served — it is not a `.woff2`, and no template links it), and `README.md`'s
attribution is 6b.12's business, registered.

⚠️ **A `sha256` lock is NOT prescribed here.** The `fixtures` gate locks the corpus because a fixture
is an ORACLE and a silent edit falsifies a measurement; a font file is neither. Named so that the
next reader knows it was considered and refused, not forgotten.

---

## 4. The accent: the one deliberate divergence, and the gate it must not retire

`app.css:12` carries `--accent: #d99a4e` with the doctrine in its own comment — *"amber — reserved
for the document action, never decorative"*. The mock makes its **blue** the general primary (nav
selection, primary buttons, occupied IPAM cells).

🔑 **These are two doctrines, not two palettes** — *"this colour means the GESTURE"* against *"this
colour means the PRODUCT"* — and the UX spec says in so many words that a palette swap keeping the
second **silently retires a specified gate** (`:590-592`).

So: the mock's blue becomes structure, and the amber survives the swap **renamed**
`--accent-document`, which is what the UX spec already calls it at `:681`. ⚠️ The rename is not
cosmetic — `--accent` is used by `app.css` for the refresh button's border and the card's focus
outline (`:70`, `:76`), which are **structure, not the gesture**. Those two must move to the blue,
or the reservation is false on the day it is written.

⚠️ **Register row (a) of story 6.1 is the collision this settles**: *"the `--accent` conflict"* was
registered to story 6.4. Story 6.4 now lands AFTER this epic, and this story is where `--accent`
stops being one token. **Row (a) is re-owned here and must be marked so in `deferred-work.md`.**

---

## 5. Dark: the deferral turns off the only rendering that ships

`gap.html:2` is `<html lang="en" data-theme="dark">`, and `app.css:5-25` carries **both** sets with
nothing to switch them. So AC3 is not the postponement of an unbuilt feature — **every existing
deployment changes colour**.

What this story does: the light set renders, `data-theme="dark"` **leaves the template**, and the
dark tokens **stay in the sheet, unreferenced**, so their return is a story rather than an
excavation. What it does not do: write the release note. That is 6b.12, and it is already registered
there — this story must not let it be discovered instead.

---

## 6. The guard: a green diff is not evidence that a pill has a colour

Epic constraint 5 and D55 name the same failure, and it is the one this project has met before under
another name (*a guard placed where the defect cannot occur reads as coverage and is none*, Epic 5's
retrospective).

**The gate must count classes in the GENERATED CSS**, per `architecture.md:3199-3202`: regenerate
into a temporary file, compare against the committed `app.css`, and assert each class built in Rust
is present. 🔑 **`@source inline()` alone is NOT a gate — it is a list someone forgets to update.**

⚠️ **The class list must be derived from the tree, not typed into the gate**: a gate carrying its own
copy of the class list is green on the day a Rust file gains a class nobody added to the gate. Where
the derivation cannot be total, the gate must **fail on an unclassified construction site** rather
than pass — story 6b.3's *"a route added without a declared nature must fail rather than default"*,
one story early.

---

## 7. 🔴 THE QUESTION THIS STORY PUTS TO GUY BEFORE DEV, not after

**Two separable deliverables are inside one AC set:**

| | What it is | Carried by |
|---|---|---|
| **A — the CSS chain** | pin the standalone binary, `cargo xtask css`, `assets/tailwind.css` with correct `@source`, the generated-class gate (a **ninth** gate) | D55, D56, epic constraint 5 |
| **B — the design system** | the mock's tokens, the five embedded faces + OFL, `--accent-document`, the dark removal | epic AC1–AC3, Guy's decisions (3) and (4) |

**A is infrastructure whose acceptance criterion is a gate; B is a visual change whose acceptance
criterion is a rendering.** They share a file and nothing else. Epic 5 inserted **six** stories at
contexting for exactly this shape — a story carrying two ideas — and its retrospective counted the
insertions as the mechanism that worked.

**The decomposition below is written for ONE story covering both.** If Guy splits it, A becomes
6b.1 and B becomes 6b.1b, no criterion changes hands, and §1's measurement is what justifies the
split rather than a preference. **This is not a blocking question**: the dev agent can implement the
whole as written.

---

## Acceptance Criteria

Derived from `epics.md:2110-2130`, each one made measurable. **AC numbering is this story's own**;
the epic's four bullets map to AC2/AC4/AC6/AC7.

**AC1 — the chain exists and is nobody's dependency.**
`cargo xtask css` regenerates `crates/opencmdb-bin/assets/app.css` from `assets/tailwind.css` using
the **pinned standalone Tailwind binary** (v4.3.3, released 2026-07-16 — `tailwindcss-linux-x64`,
and `-musl` for the distroless/static-musl image of story 3.9). **No `build.rs` is added** (D55), the
binary is not committed (`.gitignore:44-47` already ignores it), and `cargo build` on a machine
without it still succeeds. A test or gate asserts `xtask` is in nobody's dependency graph — D56's
existing rule, which must not regress when `xtask` gains a subcommand.

**AC2 — the palette and the typefaces are the mock's.**
The generated sheet carries the mock's light base and the three ramps of §2, and
**Barlow / Barlow Condensed are embedded** (five `.woff2` under `assets/fonts/`, from `jpt/barlow`,
with `OFL.txt` beside them) and referenced by `@font-face` with **no external request** — asserted
by a test that greps the generated CSS and every template for `http`, `//fonts.`, and `@import url(`.

**AC3 — light renders, dark stays unreferenced.**
`data-theme="dark"` is gone from `gap.html`; the dark token set is still present in the sheet and
**referenced by nothing** — asserted in both directions, because *"still present"* and *"unused"*
are two claims and one of them is the one that rots.

**AC4 — the accent means the gesture and nothing else.**
`--accent-document` (`#d99a4e`) is used by the documenting gesture only. The two current `--accent`
consumers that are structure (refresh border, focus outline — `app.css:70,76`) use the blue.
🔴 A test asserts `--accent-document` appears **exactly** where the gesture is and nowhere else;
today the gesture's own surface does not exist yet (story 6.4), so the honest assertion is
**zero non-gesture uses**, and it must be written so that it still means something when 6.4 adds
the first legitimate one.

**AC5 — the classes built in Rust survive the build.**
The gate of §6 regenerates into a temp file, compares against the committed `app.css`, and asserts
every Rust-built class is present in the OUTPUT. It reds when an `@source` path is wrong.
⚠️ **Prove it by breaking the path**, not by trusting the green.

**AC6 — the committed CSS and the generated CSS are the same bytes.**
`cargo xtask ci` reds if `app.css` is stale. This is what makes AC1's *"committed, not built"* safe.

**AC7 — the page still renders and still says the same things.**
The existing gap page's behaviour is unchanged: same routes, same strings, same i18n keys, the
identity-reach pair of story 5.14b intact. **No template gains or loses a sentence.** The one page
this product has must not become collateral damage of its own restyling.

**AC8 — the live test count lives HERE**, in this file, and is not copied into `CLAUDE.md`,
`docs/project-context.md` or `sprint-status.yaml` (story 6.1's AC8 rule, F2).

---

## Tasks / Subtasks

- [ ] **T1 — the chain** (AC1, AC6)
  - [ ] Pin Tailwind v4.3.3 standalone; document the download + checksum step in the xtask itself,
        so a missing binary produces a NAMED refusal rather than a stack trace
  - [ ] `xtask/src/css.rs` — `cargo xtask css` (its own module: `main.rs` is at **1894** code lines
        against the 2000 ceiling — **106 lines of margin**, measured 2026-08-18 with the gate's own
        rule, not copied from story 6.3, whose `1829` predates its own review repairs)
  - [ ] `assets/tailwind.css` — `@import "tailwindcss"`, `@source "../templates/**/*.html"`,
        `@source "../src/**/*.rs"`, `@source inline(...)`. **`../`, not `./`** (D55)
  - [ ] Wire the staleness check into `cargo xtask ci`
- [ ] **T2 — tokens** (AC2, AC3)
  - [ ] Port §2's tokens into the theme; keep the dark set unreferenced
  - [ ] Remove `data-theme="dark"` from `gap.html`
  - [ ] Rewrite `app.css`'s header — it currently describes a hand-authored file
- [ ] **T3 — typefaces** (AC2)
  - [ ] Fetch the five `.woff2` + `OFL.txt` from `jpt/barlow`; place under `assets/fonts/`
  - [ ] `@font-face` for the five faces; `--font-body` / `--font-heading` per the mock
  - [ ] Assert no external font request from any template or the sheet
- [ ] **T4 — the accent** (AC4)
  - [ ] `--accent` → `--accent-document`; move the two structural uses to the blue
  - [ ] Re-own story 6.1's register row (a) in `deferred-work.md`
- [ ] **T5 — the guard** (AC5)
  - [ ] Derive the Rust-built class list from the tree; fail on what it cannot classify
- [ ] **T6 — regression** (AC7)
  - [ ] Existing page tests green, unchanged; visual sanity via `/run` against a live DB
- [ ] **T7 — prove-to-red** (below), predictions written BEFORE execution

---

## Prove-to-red — the mutations this story owes

House rule: predictions first, each red read from **its own panic message**, carriers named per row,
and a mutation named for one thing must be applied to that thing (Epic 5's *"the mutation driver
lies"* class, four recurrences).

| # | Mutation | Prediction |
|---|---|---|
| M1 | `@source "./templates/**/*.html"` (the `./` trap) | AC5's gate reds; **the build stays green** — that contrast IS the finding |
| M2 | Delete one `@source inline(...)` entry | the gate reds naming the missing class |
| M3 | Edit `app.css` by hand without regenerating | AC6's staleness check reds |
| M4 | Add `@import url(https://fonts.googleapis.com/...)` to the sheet | AC2's no-external-request test reds |
| M5 | Use `--accent-document` on a non-gesture element | AC4 reds |
| M6 | Put `data-theme="dark"` back on `gap.html` | AC3 reds |
| M7 | Delete the dark token block entirely | AC3's *"still present"* half reds — **this is the half most likely to be carried by nothing** |
| M8 | Remove one `.woff2` from `assets/fonts/` | AC2 reds; if it does not, the fonts are referenced by a name nothing checks |
| M9 (control) | Add a Rust-built class **and** its `@source inline` entry | green by design |

⚠️ **M1 and M7 are the two that matter.** M1 is D55's named silent failure; M7 is the assertion
whose subject is an ABSENCE of use, and this project has measured four such guards green before
their patch.

---

## Dev Notes

### The tree this story extends (measured 2026-08-18, `master` at `b58088d`)

- `crates/opencmdb-bin/assets/` — `app.css` (5 564 B, 150 lines, hand-authored), `app.js` (482 B),
  `htmx.min.js` (50 917 B). No `tailwind.css`, no `fonts/`.
- `crates/opencmdb-bin/templates/` — `gap.html`, `_gap_card.html`. **Two files, and the whole UI.**
- `page.rs:24` — `#[derive(rust_embed::Embed)] #[folder = "assets/"]`, so `assets/` is the embed
  root and needs no change.
- `auth.rs` — `/assets/*` is one of the two public prefixes (story 6.1 shrank the rest).
- `xtask/src/` — `main.rs` (**1894** code lines, 3 530 total; the `file-size` gate counts up to the
  first top-level `#[cfg(test)]`), `observed_immutable.rs` (230, and it carries **no test module**).
  **Eight gates.** This story makes it nine.

### Traps, each measured on this project

- 🔴 **The mutation driver lies** — `cargo test --workspace A B` passes two filters where cargo
  accepts one, and nothing runs. Four recurrences in Epic 5. Read every red from its own message.
- 🔴 **A guard placed where the defect cannot occur reads as coverage and is none.** AC4 and AC5 are
  both exposed: a class-presence test that reads the `@source` list instead of the OUTPUT proves
  nothing, and that is precisely D55's warning.
- ⚠️ **A doc falsified inside its own commit** — six consecutive stories. §1 lists three documents
  that will become true with this story; check them at the end rather than assuming.
- ⚠️ **`file-size`**: `main.rs` has **106 lines** of margin. The new subcommand goes in its own
  module — *"split, not grown"*. 🔑 This figure was first written as `1829 / 171` by copying story
  6.3's; re-measured with the gate's own rule it is `1894 / 106`. **A count copied from another
  story is a count that has already rotted** — the defect this project's reviews have caught in
  every epic.

### Stack — pinned from measurement, not memory

- **Tailwind v4.3.3**, published 2026-07-16 (GitHub releases API, read 2026-08-18). Standalone
  assets: `tailwindcss-linux-x64` (111 749 248 B), `tailwindcss-linux-x64-musl` (108 529 952 B),
  `tailwindcss-macos-arm64`, `tailwindcss-macos-x64`.
- **Barlow** from `github.com/jpt/barlow`, SIL OFL 1.1 (`OFL.txt`, 4 377 B). The five faces of §3.
- `rust-embed = "8.12.0"` (already in `crates/opencmdb-bin/Cargo.toml:38`).
- ⚠️ **Do not invent a version.** Every figure above was read from the source on 2026-08-18.

### Validation obligations (the two fresh-context layers, MANDATORY here)

1. **Verify §1 independently.** If `cargo xtask css` exists after all, this story's shape is wrong.
2. **Build the chain before judging it** — Epic 5's validation layers that BUILT a prototype found
   what the reading layers did not, five stories running.
3. **Measure M1 and M7 on a prototype**, not on the plan. They are the two most likely to come back
   green.
4. **Decide §7 with Guy** before dev, since it changes what gets built.
5. Confirm the three documents of §1 and the two divergences of §2 (monospace, radius) reach
   `deferred-work.md` with owners — story 5.14b's AC10 failure was *"§11 required NINE register rows
   and SEVEN landed"*, found by a re-read that only read what had been written.

### References

- `_bmad-output/planning-artifacts/epics.md:2076-2130` — Epic 6b's premises, its six measured
  constraints, and story 6b.1's four criteria
- `_bmad-output/planning-artifacts/sprint-change-proposal-2026-08-13.md` — why the epic exists;
  constraints 1 and 2 with their measurements
- `_bmad-output/planning-artifacts/architecture.md:3168-3230` — **D55**, `xtask css` never
  `build.rs`, and the `@source` trap; `:619` — finding **F16**, the spec's dead Tailwind API;
  `:3402-3403` — the prescribed tree
- `_bmad-output/planning-artifacts/ux-design-specification.md:795-850` — Visual Design Foundation;
  `:677-700` — the token decisions and the dark-mode correction of 2026-08-13; `:583-596` — the two
  divergences found on a second read
- `crates/opencmdb-bin/assets/app.css`, `templates/gap.html`, `src/page.rs:24`, `src/auth.rs`
- The mock: `~/travail/Projets actuels/opencmdb/documents/opencmdb - maquette.html` — **outside this
  repository**, 495 068 B, 2026-08-13

---

## Dev Agent Record

### Agent Model Used

_(filled by dev-story)_

### Debug Log References

### Completion Notes List

### File List

### Change Log

| Date | Change |
|---|---|
| 2026-08-18 | Story contexted. §1 measured the absent Tailwind chain; §3 measured that the mock carries no font bytes; §7 puts the A/B split to Guy. |
